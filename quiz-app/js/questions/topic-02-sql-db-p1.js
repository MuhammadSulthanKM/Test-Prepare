// SQL & Database Fundamentals — Part 1 (q421–530)
window.QUIZ_BANK['sql-db'] = (window.QUIZ_BANK['sql-db']||[]).concat([
{id:421,topic:'sql-db',difficulty:'beginner',question:'What does SQL stand for?',options:['Structured Query Language','Standard Query Language','Sequential Query Language','Simple Query Language'],answer:0,explanation:'SQL (Structured Query Language) is the standard language for managing and querying relational database management systems.'},
{id:422,topic:'sql-db',difficulty:'beginner',question:'Which SQL statement is used to retrieve data from a table?',options:['INSERT','UPDATE','SELECT','DELETE'],answer:2,explanation:'SELECT is the Data Query Language (DQL) command used to retrieve rows from one or more database tables.'},
{id:423,topic:'sql-db',difficulty:'beginner',question:'Which SQL clause filters rows BEFORE grouping in a GROUP BY query?',options:['HAVING','WHERE','ORDER BY','LIMIT'],answer:1,explanation:'WHERE filters individual rows before aggregation. HAVING filters the grouped results after GROUP BY.'},
{id:424,topic:'sql-db',difficulty:'beginner',question:'Which SQL clause filters rows AFTER grouping?',options:['WHERE','ORDER BY','HAVING','GROUP BY'],answer:2,explanation:'HAVING applies conditions to aggregated groups (e.g., HAVING COUNT(*) > 5), filtering after GROUP BY has been applied.'},
{id:425,topic:'sql-db',difficulty:'beginner',question:'What does the PRIMARY KEY constraint enforce?',options:['All values must be NULL','Each row has a unique, non-NULL value in the column(s)','The column references another table','Values must be within a specified range'],answer:1,explanation:'A PRIMARY KEY uniquely identifies each row and implicitly adds NOT NULL and UNIQUE constraints on the column(s).'},
{id:426,topic:'sql-db',difficulty:'beginner',question:'What does a FOREIGN KEY constraint enforce?',options:['Unique values in the column','A reference from a column in one table to the PRIMARY KEY of another table, enforcing referential integrity','Non-null values in the column','A default value for the column'],answer:1,explanation:'A FOREIGN KEY creates a referential integrity constraint: values in the FK column must exist in the referenced PRIMARY KEY column of the parent table.'},
{id:427,topic:'sql-db',difficulty:'beginner',question:'Which SQL command adds a new row to a table?',options:['UPDATE','ALTER','INSERT INTO','MERGE'],answer:2,explanation:'INSERT INTO adds new rows (records) to a table, specifying column names and corresponding values.'},
{id:428,topic:'sql-db',difficulty:'beginner',question:'Which SQL command modifies existing rows in a table?',options:['INSERT','UPDATE','REPLACE','ALTER'],answer:1,explanation:'UPDATE modifies existing rows; it must be used with a WHERE clause to avoid updating all rows unintentionally.'},
{id:429,topic:'sql-db',difficulty:'beginner',question:'Which SQL command removes specific rows from a table?',options:['TRUNCATE','DROP','DELETE','REMOVE'],answer:2,explanation:'DELETE removes rows matching a WHERE condition. Without WHERE it deletes all rows but preserves the table structure.'},
{id:430,topic:'sql-db',difficulty:'beginner',question:'What is the difference between DELETE and TRUNCATE?',options:['They are identical','DELETE removes specific rows and can be rolled back; TRUNCATE removes all rows quickly and cannot be rolled back in most databases','TRUNCATE removes specific rows; DELETE removes all rows','DELETE is DDL; TRUNCATE is DML'],answer:1,explanation:'DELETE is DML (can be in a transaction, supports WHERE, fires triggers). TRUNCATE is DDL (typically non-transactional, faster, removes all rows, resets identity counters).'},
{id:431,topic:'sql-db',difficulty:'beginner',question:'What does the DISTINCT keyword do in a SELECT statement?',options:['Sorts the result set','Returns only unique rows, eliminating duplicates','Filters NULL values','Groups rows with the same values'],answer:1,explanation:'SELECT DISTINCT eliminates duplicate rows from the result set, returning only one occurrence of each unique combination of selected column values.'},
{id:432,topic:'sql-db',difficulty:'beginner',question:'Which aggregate function returns the total sum of a numeric column?',options:['COUNT()','MAX()','SUM()','AVG()'],answer:2,explanation:'SUM() calculates and returns the total sum of all non-NULL values in the specified numeric column.'},
{id:433,topic:'sql-db',difficulty:'beginner',question:'Which aggregate function returns the number of rows (or non-NULL values)?',options:['SUM()','AVG()','COUNT()','MAX()'],answer:2,explanation:'COUNT(*) counts all rows including NULLs; COUNT(column) counts only non-NULL values in that column.'},
{id:434,topic:'sql-db',difficulty:'beginner',question:'What is a "NULL" value in SQL?',options:['Zero','An empty string','An unknown or missing value','False'],answer:2,explanation:'NULL represents the absence of a value (unknown, missing, or inapplicable). It is not equivalent to zero, empty string, or false.'},
{id:435,topic:'sql-db',difficulty:'beginner',question:'How should NULL comparisons be performed in SQL?',options:['Using = NULL','Using != NULL','Using IS NULL or IS NOT NULL','Using NULL() function'],answer:2,explanation:'NULL cannot be compared with = or !=; use IS NULL or IS NOT NULL. Any comparison with NULL using = returns NULL (unknown), never TRUE or FALSE.'},
{id:436,topic:'sql-db',difficulty:'beginner',question:'Which SQL command creates a new table?',options:['BUILD TABLE','MAKE TABLE','CREATE TABLE','DEFINE TABLE'],answer:2,explanation:'CREATE TABLE is the DDL command for defining a new table with specified columns, data types, and constraints.'},
{id:437,topic:'sql-db',difficulty:'beginner',question:'Which SQL command removes a table and all its data permanently?',options:['DELETE TABLE','TRUNCATE TABLE','REMOVE TABLE','DROP TABLE'],answer:3,explanation:'DROP TABLE permanently removes the table definition, all rows, indexes, and associated constraints — unlike TRUNCATE which keeps the table structure.'},
{id:438,topic:'sql-db',difficulty:'beginner',question:'What does the LIKE operator do in a WHERE clause?',options:['Checks for exact equality','Performs pattern matching using wildcards % (any characters) and _ (one character)','Compares numeric ranges','Joins two tables on matching values'],answer:1,explanation:'LIKE performs string pattern matching: % matches any sequence of characters; _ matches exactly one character. Example: WHERE name LIKE \'J%\' matches names starting with J.'},
{id:439,topic:'sql-db',difficulty:'beginner',question:'What does an INNER JOIN return?',options:['All rows from the left table','All rows from both tables with NULLs for non-matching rows','Only the rows that have matching values in both joined tables','All rows from the right table'],answer:2,explanation:'INNER JOIN returns only the rows where the join condition is satisfied in both tables — rows without a matching record in the other table are excluded.'},
{id:440,topic:'sql-db',difficulty:'beginner',question:'What does a LEFT (OUTER) JOIN return?',options:['Only matching rows from both tables','All rows from the left table, plus matched rows from the right table; NULLs for unmatched right columns','All rows from the right table with NULLs for unmatched left rows','All rows from both tables with NULLs where there is no match'],answer:1,explanation:'LEFT JOIN returns all rows from the left table, filling right-table columns with NULL for rows in the left table that have no matching row in the right table.'},
{id:441,topic:'sql-db',difficulty:'beginner',question:'What is "normalisation" in relational database design?',options:['Converting data to lowercase','Organising data to reduce redundancy and improve data integrity by structuring tables according to normal forms','Sorting data alphabetically','Encrypting sensitive database columns'],answer:1,explanation:'Normalisation applies a series of normal forms (1NF–5NF) to eliminate data redundancy, update anomalies, and improve referential integrity in relational databases.'},
{id:442,topic:'sql-db',difficulty:'beginner',question:'What is First Normal Form (1NF)?',options:['Each row has a unique primary key and no repeating groups or multi-valued attributes in any column','Each non-key column is fully dependent on the primary key','No transitive dependencies exist between non-key columns','All many-to-many relationships are resolved with junction tables'],answer:0,explanation:'1NF requires: atomic (indivisible) column values, unique column names, and no repeating groups or arrays within a single row.'},
{id:443,topic:'sql-db',difficulty:'beginner',question:'What is Second Normal Form (2NF)?',options:['The table is in 1NF with no repeating groups','The table is in 1NF and every non-key column is fully functionally dependent on the ENTIRE primary key (no partial dependencies)','No transitive dependencies exist','All columns have NOT NULL constraints'],answer:1,explanation:'2NF requires 1NF and full functional dependency of all non-key attributes on the entire composite primary key — eliminating partial dependencies where a non-key column depends on only part of a compound key.'},
{id:444,topic:'sql-db',difficulty:'beginner',question:'What is Third Normal Form (3NF)?',options:['No repeating groups in any column','Table is in 2NF and no transitive dependencies exist — no non-key column depends on another non-key column','All many-to-many relationships are resolved','Table has a single column primary key'],answer:1,explanation:'3NF requires 2NF and removal of transitive dependencies: every non-key column must depend directly on the primary key, not on another non-key column.'},
{id:445,topic:'sql-db',difficulty:'beginner',question:'What is an "index" in a database?',options:['A backup copy of a table','A data structure that improves the speed of data retrieval operations on a table column','A constraint that enforces unique values','A foreign key reference to another table'],answer:1,explanation:'An index is a data structure (B-tree, hash, etc.) that allows the database to find rows with specific column values quickly without scanning every row.'},
{id:446,topic:'sql-db',difficulty:'beginner',question:'What is a "view" in SQL?',options:['A physical copy of a table stored separately','A stored, named SELECT query that can be referenced like a table, providing a virtual table representation of the underlying data','A backup of selected table rows','A type of index for frequently queried columns'],answer:1,explanation:'A view is a virtual table defined by a stored SELECT query; querying a view executes the underlying query dynamically, providing abstraction, simplicity, and security.'},
{id:447,topic:'sql-db',difficulty:'beginner',question:'What does ACID stand for in database transactions?',options:['Accuracy, Consistency, Integrity, Durability','Atomicity, Consistency, Isolation, Durability','Atomicity, Correctness, Independence, Data-safety','Accuracy, Concurrency, Isolation, Durability'],answer:1,explanation:'ACID properties guarantee reliable database transactions: Atomicity (all or nothing), Consistency (valid state), Isolation (concurrent transactions independent), Durability (committed data persists).'},
{id:448,topic:'sql-db',difficulty:'beginner',question:'What does "Atomicity" mean in database transactions?',options:['Transactions process as fast as possible','A transaction either completes entirely or is entirely rolled back — no partial updates','Transactions can be split into atomic units','Each transaction operates on a single row'],answer:1,explanation:'Atomicity ensures that all operations in a transaction succeed together or are all undone together — preventing partial updates from leaving data in an inconsistent state.'},
{id:449,topic:'sql-db',difficulty:'beginner',question:'What does ORDER BY do in SQL?',options:['Filters rows by a condition','Groups rows with the same values','Sorts the result set by specified columns in ascending (ASC) or descending (DESC) order','Joins two tables together'],answer:2,explanation:'ORDER BY sorts the query result set. Default is ASC (ascending); use DESC for descending order. Multiple columns can be specified for multi-level sorting.'},
{id:450,topic:'sql-db',difficulty:'beginner',question:'What is the default sort order in SQL\'s ORDER BY clause?',options:['Descending (Z–A, largest first)','Ascending (A–Z, smallest first)','No sorting is applied','Random order'],answer:1,explanation:'The default ORDER BY sort direction is ASC (ascending): alphabetically A–Z for text, numerically smallest-to-largest for numbers.'},
{id:451,topic:'sql-db',difficulty:'intermediate',question:'What is the difference between UNION and UNION ALL?',options:['There is no difference','UNION removes duplicate rows from the combined result; UNION ALL includes all rows including duplicates (faster)','UNION ALL removes duplicates; UNION keeps them','UNION works on different tables; UNION ALL works on the same table'],answer:1,explanation:'UNION combines results of two SELECTs and removes duplicates (requires sorting/hashing for deduplication). UNION ALL simply concatenates results without deduplication — typically faster.'},
{id:452,topic:'sql-db',difficulty:'intermediate',question:'What is a "stored procedure"?',options:['A table that stores computed values','A precompiled, reusable SQL program stored in the database that can accept parameters, contain logic, and be called by name','A type of index for frequently called queries','A trigger that fires on INSERT operations'],answer:1,explanation:'Stored procedures are compiled SQL code units stored in the database, accepting parameters, containing procedural logic (loops, conditionals), and reducing network round-trips by executing multiple statements server-side.'},
{id:453,topic:'sql-db',difficulty:'intermediate',question:'What is a "trigger" in SQL?',options:['A scheduled job that runs SQL on a timer','An automatic procedure that executes in response to specific DML events (INSERT, UPDATE, DELETE) on a table','A function that triggers an alert when query performance degrades','A constraint that prevents duplicate key violations'],answer:1,explanation:'A trigger is a procedural object that automatically executes (fires) when a specified DML event occurs on a table — used for auditing, enforcing business rules, or maintaining derived data.'},
{id:454,topic:'sql-db',difficulty:'intermediate',question:'What is the difference between a "function" and a "stored procedure" in SQL?',options:['They are identical','A function must return a value and can be used in a SELECT; a stored procedure may or may not return values and is called with EXECUTE/CALL','Functions are faster; stored procedures are slower','Stored procedures can take parameters; functions cannot'],answer:1,explanation:'Functions return a single value (or table) and can be embedded in SQL expressions. Stored procedures are called independently, can return multiple values/result sets via OUT parameters, and can include DML/DDL.'},
{id:455,topic:'sql-db',difficulty:'intermediate',question:'What is a "CTE" (Common Table Expression)?',options:['A type of table constraint','A temporary, named result set defined within a WITH clause that can be referenced in the subsequent SELECT, INSERT, UPDATE, or DELETE statement','A column-level encryption technique','A cross-table expression for multi-table queries'],answer:1,explanation:'A CTE (WITH ... AS ...) defines a named temporary result set for the duration of a single query, improving readability and enabling recursive queries.'},
{id:456,topic:'sql-db',difficulty:'intermediate',question:'What is a "window function" in SQL?',options:['A function that opens a new database connection window','A function that performs calculations across a set of rows related to the current row (a "window"), without collapsing rows like GROUP BY','A function for managing database UI windows','A graphical function for data visualisation'],answer:1,explanation:'Window functions (ROW_NUMBER, RANK, LAG, LEAD, SUM OVER, etc.) perform calculations across a "window" of related rows defined by PARTITION BY and ORDER BY, preserving individual row identity.'},
{id:457,topic:'sql-db',difficulty:'intermediate',question:'What does COALESCE() do in SQL?',options:['Combines multiple tables into one','Returns the first non-NULL value from a list of expressions','Returns the count of non-null values in a column','Converts NULL to zero'],answer:1,explanation:'COALESCE(expr1, expr2, ...) evaluates expressions left-to-right and returns the first non-NULL value — useful for substituting default values when a column may be NULL.'},
{id:458,topic:'sql-db',difficulty:'intermediate',question:'What is a "CROSS JOIN"?',options:['A join on a cross-referencing table','A join that produces the Cartesian product of two tables — every row from table A paired with every row from table B','A join using != (not equal) conditions','A self-join on the same table'],answer:1,explanation:'CROSS JOIN returns the Cartesian product: M rows × N rows = M×N rows. It is rarely used intentionally but occasionally useful for generating combinations.'},
{id:459,topic:'sql-db',difficulty:'intermediate',question:'What is a "self-join"?',options:['A table joining with an identity column','Joining a table to itself, usually with different aliases, to compare rows within the same table','A join with no ON condition','A join where all columns are selected with *'],answer:1,explanation:'A self-join uses the same table twice with different aliases, useful for hierarchical data (employee-manager), comparing rows within one table, or finding duplicates.'},
{id:460,topic:'sql-db',difficulty:'intermediate',question:'What does the GROUP BY clause do?',options:['Sorts the result by one or more columns','Groups rows that have the same values in specified columns into summary rows, typically used with aggregate functions','Filters rows based on a condition','Joins tables based on common column values'],answer:1,explanation:'GROUP BY collapses multiple rows with the same values in the grouped columns into single summary rows, enabling aggregate functions (SUM, COUNT, AVG) to calculate per-group totals.'},
{id:461,topic:'sql-db',difficulty:'intermediate',question:'In which isolation level can a transaction read data that another uncommitted transaction has modified?',options:['SERIALIZABLE','REPEATABLE READ','READ COMMITTED','READ UNCOMMITTED'],answer:3,explanation:'READ UNCOMMITTED is the lowest isolation level; transactions can read "dirty" data from uncommitted transactions — the only level that allows dirty reads.'},
{id:462,topic:'sql-db',difficulty:'intermediate',question:'What is a "dirty read" in database concurrency?',options:['Reading corrupted or encoded data','Reading data that has been modified by another transaction that has not yet committed — potentially reading data that will be rolled back','Reading data from a deleted row','Reading stale cached data'],answer:1,explanation:'A dirty read occurs in READ UNCOMMITTED isolation: transaction A reads transaction B\'s uncommitted changes. If B rolls back, A has read phantom data that never actually existed.'},
{id:463,topic:'sql-db',difficulty:'intermediate',question:'What is a "phantom read"?',options:['Reading rows that do not exist yet','A read within a transaction that returns different row sets on two executions because another committed transaction INSERT/DELETEd rows matching the query\'s WHERE condition between the reads','Reading a row that was deleted after the first read','Reading NULL values in a column'],answer:1,explanation:'Phantom reads occur in REPEATABLE READ isolation: a SELECT within transaction A returns different rows on the second execution because transaction B inserted or deleted matching rows between the two reads.'},
{id:464,topic:'sql-db',difficulty:'intermediate',question:'Which isolation level prevents dirty reads, non-repeatable reads, AND phantom reads?',options:['READ UNCOMMITTED','READ COMMITTED','REPEATABLE READ','SERIALIZABLE'],answer:3,explanation:'SERIALIZABLE is the highest isolation level; transactions execute as if they are serial (not concurrent), eliminating all concurrency anomalies: dirty reads, non-repeatable reads, and phantom reads.'},
{id:465,topic:'sql-db',difficulty:'intermediate',question:'What is the difference between a clustered and a non-clustered index?',options:['There is no difference','A clustered index defines the physical order of data rows in the table (one per table); non-clustered indexes are separate structures with pointers to data rows (multiple allowed per table)','Non-clustered indexes are faster than clustered indexes','Clustered indexes cannot be created on primary keys'],answer:1,explanation:'Clustered: data rows are physically ordered by the index key (one per table, typically on PK). Non-clustered: separate B-tree with row locators pointing to the data pages (multiple per table).'},
{id:466,topic:'sql-db',difficulty:'intermediate',question:'What does an EXPLAIN (or EXPLAIN PLAN) statement reveal?',options:['The SQL syntax errors in a query','The query execution plan — showing how the database engine will execute the query: table scans, index usage, join methods, and estimated costs','A list of all tables in the database','The data types of columns in a query'],answer:1,explanation:'EXPLAIN shows the query execution plan: which indexes are used (or not), join algorithms (nested loop, hash join), order of operations, and estimated row counts and costs — essential for performance tuning.'},
{id:467,topic:'sql-db',difficulty:'intermediate',question:'What is denormalisation and when is it used?',options:['Converting a normalised database to a non-relational format','Intentionally introducing redundancy into a normalised schema to improve read query performance by reducing the number of joins needed','Removing foreign key constraints from a table','Converting VARCHAR columns to TEXT'],answer:1,explanation:'Denormalisation trades write efficiency and data integrity (more redundancy) for read performance (fewer joins), commonly used in data warehouses, reporting tables, or high-read OLAP systems.'},
{id:468,topic:'sql-db',difficulty:'intermediate',question:'What is an "OLTP" system?',options:['Online Transaction Processing — a system optimised for high-volume, fast, concurrent transactional operations (insert/update/delete) with strong ACID guarantees','Online Analytical Processing — a system for complex analytical queries across large datasets','A type of NoSQL database','An object-linked transaction protocol'],answer:0,explanation:'OLTP (Online Transaction Processing) systems handle high volumes of short, fast transactions (e.g., banking, e-commerce) with emphasis on ACID compliance and concurrent user support.'},
{id:469,topic:'sql-db',difficulty:'intermediate',question:'What is an "OLAP" system?',options:['Online Transaction Processing','Online Analytical Processing — optimised for complex analytical queries across large historical datasets, typically in data warehouses, using star/snowflake schemas','A language for querying object databases','A real-time inventory management protocol'],answer:1,explanation:'OLAP (Online Analytical Processing) supports complex analytical queries across large historical datasets; it is optimised for reads, aggregations, and multi-dimensional analysis rather than OLTP transactions.'},
{id:470,topic:'sql-db',difficulty:'intermediate',question:'What is a "subquery" (inner query) in SQL?',options:['A query that runs sub-tables','A SELECT statement nested inside another SQL statement, whose result is used by the outer query','A partial query that retrieves a subset of columns','A query executed in a sub-database schema'],answer:1,explanation:'A subquery is a SELECT embedded within another SQL statement (in WHERE, FROM, or SELECT); it executes first and passes its result to the outer (main) query.'},
{id:471,topic:'sql-db',difficulty:'intermediate',question:'What is a "correlated subquery"?',options:['A subquery that uses the same table as the outer query','A subquery that references a column from the outer query, causing it to execute once for each row processed by the outer query','A subquery inside a WHERE clause','A subquery that returns multiple rows'],answer:1,explanation:'A correlated subquery references columns from the outer query, so it must re-evaluate for each outer row — making it potentially slower than a non-correlated subquery but useful for row-by-row comparisons.'},
{id:472,topic:'sql-db',difficulty:'intermediate',question:'What does the NOT IN operator do in SQL?',options:['Selects rows where a column contains NULL values','Selects rows where the column value is not present in a specified list or subquery result','Negates a BETWEEN condition','Checks for values not matching a LIKE pattern'],answer:1,explanation:'NOT IN filters rows where the column value does not appear in the provided list or subquery result. Note: if the list contains any NULL, NOT IN returns no rows due to three-valued logic.'},
{id:473,topic:'sql-db',difficulty:'intermediate',question:'What does the EXISTS operator check in a subquery?',options:['Whether a table exists in the database','Whether the subquery returns at least one row — returning TRUE if rows exist, FALSE if the subquery returns no rows','Whether a column exists in a table','Whether a value is NOT NULL'],answer:1,explanation:'EXISTS(subquery) returns TRUE if the subquery produces at least one row, regardless of the actual values — typically used with correlated subqueries and often more efficient than IN for large datasets.'},
{id:474,topic:'sql-db',difficulty:'intermediate',question:'What is "referential integrity"?',options:['Database security preventing unauthorised access','The rule that every FOREIGN KEY value must either exist as a PRIMARY KEY value in the referenced table or be NULL','Data backup integrity after transactions','Uniqueness of values in indexed columns'],answer:1,explanation:'Referential integrity ensures that FK columns only contain values that exist in the referenced table\'s PK column, preventing orphaned records.'},
{id:475,topic:'sql-db',difficulty:'intermediate',question:'What is the difference between CHAR and VARCHAR data types?',options:['They are identical','CHAR is fixed-length (padded with spaces); VARCHAR is variable-length (stores only the characters used, up to a maximum)','VARCHAR is faster for all operations','CHAR can only store numbers; VARCHAR stores strings'],answer:1,explanation:'CHAR(n) always stores exactly n characters (space-padded); VARCHAR(n) stores up to n characters using only the space needed. CHAR is efficient for fixed-length data; VARCHAR is better for variable-length strings.'},
{id:476,topic:'sql-db',difficulty:'beginner',question:'Which SQL clause is used to restrict the number of rows returned?',options:['FETCH','LIMIT (MySQL/PostgreSQL) or TOP (SQL Server) or ROWNUM (Oracle)','SKIP','REDUCE'],answer:1,explanation:'Row limiting syntax varies by database: LIMIT n (MySQL, PostgreSQL), TOP n in SELECT (SQL Server), FETCH FIRST n ROWS ONLY (SQL:2008 standard), ROWNUM (Oracle).'},
{id:477,topic:'sql-db',difficulty:'beginner',question:'What does the BETWEEN operator do?',options:['Returns rows where a value is within a specified range (inclusive of both endpoints)','Returns rows where a value is NOT in a list','Compares two column values','Filters rows using a pattern match'],answer:0,explanation:'BETWEEN min AND max is inclusive of both endpoints: equivalent to >= min AND <= max. It works with numbers, dates, and strings.'},
{id:478,topic:'sql-db',difficulty:'beginner',question:'What is a "schema" in a relational database?',options:['A backup of the database','The logical structure/container that defines the database objects: tables, views, indexes, stored procedures, and their relationships','A single row in a database table','The physical storage location of data files'],answer:1,explanation:'A database schema defines the logical organisation: table structures, column definitions, data types, relationships, constraints, and other database objects.'},
{id:479,topic:'sql-db',difficulty:'beginner',question:'What does the ALTER TABLE statement do?',options:['Removes a table from the database','Modifies an existing table structure: adding, dropping, or modifying columns and constraints','Renames the entire database','Moves a table to a different schema'],answer:1,explanation:'ALTER TABLE modifies an existing table: ADD COLUMN, DROP COLUMN, MODIFY COLUMN data type, ADD CONSTRAINT, DROP CONSTRAINT, RENAME COLUMN.'},
{id:480,topic:'sql-db',difficulty:'beginner',question:'Which SQL keyword prevents NULL values in a column?',options:['DEFAULT','UNIQUE','NOT NULL','REQUIRED'],answer:2,explanation:'The NOT NULL constraint ensures that a column cannot contain a NULL value — every row must have a non-null value for that column.'},
{id:481,topic:'sql-db',difficulty:'intermediate',question:'What is a "surrogate key"?',options:['A secondary foreign key','A system-generated, meaningless unique identifier (e.g., auto-increment integer or UUID) used as a primary key instead of a natural key','A key that surrounds all other keys in the table','A composite key made of two foreign keys'],answer:1,explanation:'A surrogate key is an artificial PK with no business meaning (auto-increment ID, UUID), chosen over natural keys to avoid issues with natural key changes, composite keys, or privacy concerns.'},
{id:482,topic:'sql-db',difficulty:'intermediate',question:'What is a "natural key"?',options:['A key automatically generated by the database','A primary key formed from one or more real-world attributes that uniquely identify a row — such as email address, SSN, or ISBN','A key that is naturally indexed','A key that is part of a composite index'],answer:1,explanation:'A natural key uses real-world data attributes that inherently identify an entity (e.g., email, ISBN, Social Security Number), as opposed to a system-generated surrogate key.'},
{id:483,topic:'sql-db',difficulty:'intermediate',question:'What is a "composite primary key"?',options:['A primary key made of two or more columns that together uniquely identify each row','A primary key that is automatically generated','A primary key on a view','A foreign key that references a composite primary key'],answer:0,explanation:'A composite (compound) PK uses two or more columns together to uniquely identify rows, common in junction tables for many-to-many relationships.'},
{id:484,topic:'sql-db',difficulty:'intermediate',question:'What is the "N+1 query problem"?',options:['A bug in SQL syntax','A performance anti-pattern where one query fetches N records, then N additional queries are issued to fetch related data for each record — instead of one efficient JOIN query','An error in normalisation that leaves N tables with 1 column each','A concurrency issue with N transactions writing to 1 table'],answer:1,explanation:'N+1 is a common ORM performance issue: 1 query fetches parent records, then N queries (one per parent) fetch children — use eager loading/JOINs to resolve in 1 or 2 queries.'},
{id:485,topic:'sql-db',difficulty:'intermediate',question:'What is "sharding" in database design?',options:['Splitting a table into separate schemas','Horizontally partitioning a large database across multiple servers (shards), each holding a subset of the data rows, to scale out read/write throughput','Vertically splitting a table into multiple tables with fewer columns','Encrypting individual database rows'],answer:1,explanation:'Sharding distributes rows across multiple database nodes (shards), each owning a partition of the data, to scale horizontally beyond the capacity of a single server.'},
{id:486,topic:'sql-db',difficulty:'intermediate',question:'What is database "replication"?',options:['Creating backup copies of stored procedures','Copying and synchronising data from one database server to one or more replica servers to improve availability, read scalability, and disaster recovery','Duplicating table structures without data','Replicating query plans across database nodes'],answer:1,explanation:'Database replication continuously copies data from a primary (master) server to one or more replicas (slaves/standbys), enabling high availability, read scale-out, and geographic distribution.'},
{id:487,topic:'sql-db',difficulty:'intermediate',question:'What does a "full table scan" mean in query execution?',options:['A query that counts all rows in a table','The database engine reads every row in the table to find matching rows because no usable index exists — typically slow on large tables','A backup of every row in the table','A scan that checks foreign key constraints across all rows'],answer:1,explanation:'A full table scan (seq scan) reads all data pages in the table to evaluate each row, which is efficient for small tables or when fetching a large proportion of rows, but expensive for large tables with selective queries.'},
{id:488,topic:'sql-db',difficulty:'intermediate',question:'Which SQL keyword is used to define a "window" for window functions?',options:['PARTITION','GROUP','OVER','WINDOW'],answer:2,explanation:'The OVER() clause defines the window for a window function: PARTITION BY defines groups, ORDER BY defines row order within the window, and ROWS/RANGE defines frame boundaries.'},
{id:489,topic:'sql-db',difficulty:'intermediate',question:'What is a "B-tree index" and why is it the default index type in most RDBMS?',options:['A binary tree that sorts primary keys','A balanced tree data structure where all leaf nodes are at the same depth, providing O(log n) lookup, range query, and sorted access — suitable for most query patterns','A binary tree that stores row addresses only','An index structure used only for text search'],answer:1,explanation:'B-tree (Balanced tree) indexes support equality and range queries efficiently (O(log n)), are kept balanced for consistent performance, and handle ordered data access — making them the default for most databases.'},
{id:490,topic:'sql-db',difficulty:'intermediate',question:'What is a "hash index" best suited for?',options:['Range queries and ORDER BY operations','Exact equality lookups (WHERE col = value) — hashes are fast for exact match but do not support range queries or sorting','Full-text search operations','Multi-column composite queries'],answer:1,explanation:'Hash indexes use a hash function to map keys to buckets, providing O(1) average-case exact lookups but offering no ordering — unsuitable for BETWEEN, <, >, or LIKE operations.'},
{id:491,topic:'sql-db',difficulty:'advanced',question:'What does the SQL MERGE statement do?',options:['Merges two databases into one','Performs INSERT, UPDATE, or DELETE on a target table based on matching rows from a source table in a single atomic operation (upsert)','Merges two columns into one','Combines the schemas of two tables'],answer:1,explanation:'MERGE (UPSERT) compares source and target rows: UPDATE matched rows, INSERT unmatched source rows, and optionally DELETE unmatched target rows — all in one atomic statement.'},
{id:492,topic:'sql-db',difficulty:'advanced',question:'What is "query optimisation" in a RDBMS?',options:['Manually rewriting SQL queries','The process where the query optimizer generates and evaluates multiple execution plans using cost models, then selects the lowest-cost plan for executing a SQL statement','Optimising the network connection for query results','Removing unnecessary columns from SELECT *'],answer:1,explanation:'The query optimizer is a critical RDBMS component that transforms a SQL query into the most efficient execution plan by considering indexes, statistics, join order, and cost estimates.'},
{id:493,topic:'sql-db',difficulty:'advanced',question:'What is "database partitioning"?',options:['Splitting a database into read and write replicas','Dividing a large table into smaller physical sub-tables (partitions) based on a partition key (range, list, hash), improving query performance and manageability','Creating multiple schemas in one database','Separating stored procedures from tables in the schema'],answer:1,explanation:'Partitioning (range, list, hash, composite) divides large tables into smaller chunks; queries hitting only specific partitions (partition pruning) avoid scanning the entire table.'},
{id:494,topic:'sql-db',difficulty:'advanced',question:'What is "two-phase commit" (2PC) in distributed databases?',options:['Committing a transaction in two database instances','A distributed transaction protocol ensuring all nodes in a distributed transaction either commit or rollback atomically: Prepare phase (all nodes vote yes/no) then Commit/Abort phase','A two-step user authentication process','Committing a transaction using two different isolation levels'],answer:1,explanation:'2PC ensures distributed ACID transactions: Phase 1 (Prepare) — coordinator asks all nodes if they can commit; Phase 2 (Commit/Abort) — if all vote YES, coordinator sends Commit; otherwise Abort.'},
{id:495,topic:'sql-db',difficulty:'advanced',question:'What is "eventual consistency" in distributed databases?',options:['All transactions are consistent eventually with enough retries','A consistency model where updates will propagate to all replicas over time, meaning reads may return stale data temporarily but all nodes will converge to the same value eventually','A guarantee that all reads immediately reflect the latest write','A weaker form of ACID compliance'],answer:1,explanation:'Eventual consistency (BASE model) trades immediate consistency for availability and partition tolerance: reads may return stale data, but the system guarantees convergence to a consistent state without network failures.'},
{id:496,topic:'sql-db',difficulty:'advanced',question:'What does the CAP theorem state for distributed systems?',options:['A system can be Consistent, Available, and Partition-tolerant simultaneously','A distributed system can guarantee at most two of three properties: Consistency, Availability, Partition tolerance — it cannot have all three simultaneously','CAP is only applicable to NoSQL databases','A system must choose between Consistency and Accuracy'],answer:1,explanation:'Brewer\'s CAP theorem: distributed systems cannot simultaneously provide Consistency (every read gets the latest write), Availability (every request gets a response), and Partition tolerance (continues operating despite network partitions).'},
{id:497,topic:'sql-db',difficulty:'advanced',question:'What is a "materialised view"?',options:['A view stored in memory for faster access','A view whose query results are stored physically on disk and can be refreshed periodically or on demand, trading storage/staleness for faster query performance','A view that materialises all joins at query time','A view used in OLTP transactions only'],answer:1,explanation:'Materialised views store the result of a complex query physically, making subsequent reads much faster; the trade-off is storage cost and data freshness (requires periodic refresh).'},
{id:498,topic:'sql-db',difficulty:'advanced',question:'What is "query plan caching" and why does it matter for performance?',options:['Caching the entire query result set','The database caches compiled execution plans for parameterised queries, avoiding repeated parsing and optimisation overhead on subsequent identical query structures','Caching the database schema for faster table creation','Storing frequently executed queries in a faster index'],answer:1,explanation:'Query plan caching allows the database to reuse a compiled execution plan for subsequent executions of the same parameterised query, reducing CPU overhead for plan compilation and optimisation.'},
{id:499,topic:'sql-db',difficulty:'advanced',question:'What is "lock escalation" in SQL Server and similar RDBMS?',options:['Upgrading from READ to WRITE locks','When the database engine promotes many fine-grained locks (row-level) to fewer coarser-grained locks (page or table level) to reduce memory consumption, potentially increasing contention','When a deadlock is escalated to a server administrator','Escalating lock timeout errors to the application layer'],answer:1,explanation:'Lock escalation converts thousands of row or page locks to a single table lock when lock count thresholds are exceeded, reducing lock manager memory at the cost of increased blocking for concurrent transactions.'},
{id:500,topic:'sql-db',difficulty:'advanced',question:'What is a "deadlock" in database transactions?',options:['A query that never finishes executing','A situation where two or more transactions wait indefinitely for each other to release locks each holds — neither can proceed','A constraint preventing duplicate rows','A locking mechanism used in distributed transactions'],answer:1,explanation:'A deadlock (deadly embrace) occurs when Transaction A holds a lock T1 needs, and Transaction B holds a lock A needs. RDBMS deadlock detectors identify and automatically rollback one victim transaction.'},
{id:501,topic:'sql-db',difficulty:'beginner',question:'What is an "entity" in the context of database design?',options:['A single row in a table','A real-world object or concept (person, product, order) that data is stored about, typically represented as a table in a relational database','A column data type','A database transaction'],answer:1,explanation:'In ER modelling, an entity is a distinct real-world object about which data is recorded; each entity type becomes a table in the relational database.'},
{id:502,topic:'sql-db',difficulty:'beginner',question:'What does the AVG() aggregate function return?',options:['The median of a set of values','The total sum of values','The arithmetic mean (average) of non-NULL values in a numeric column','The number of distinct values'],answer:2,explanation:'AVG() computes the arithmetic mean: sum of non-NULL values divided by the count of non-NULL values in the specified column.'},
{id:503,topic:'sql-db',difficulty:'beginner',question:'What does a FULL OUTER JOIN return?',options:['Only the matching rows from both tables','All rows from the left table only','All rows from both tables: matching rows joined, and non-matching rows from both sides padded with NULLs','All rows from the right table only'],answer:2,explanation:'FULL OUTER JOIN returns the union of left and right outer joins: all rows from both tables, with NULLs filling columns where there is no matching row in the other table.'},
{id:504,topic:'sql-db',difficulty:'beginner',question:'What is a "candidate key"?',options:['A column that is not yet assigned as a primary key','Any minimal set of columns that could uniquely identify each row in a table — the primary key is chosen from among the candidate keys','A foreign key that references a candidate table','A temporary key used during table design'],answer:1,explanation:'A candidate key is a minimal superkey — a set of columns that uniquely identifies every row with no redundant columns. One candidate key is chosen as the primary key; the rest are alternate keys.'},
{id:505,topic:'sql-db',difficulty:'beginner',question:'Which SQL data type stores whole numbers?',options:['VARCHAR','DECIMAL','FLOAT','INT (or INTEGER)'],answer:3,explanation:'INT/INTEGER stores whole numbers within the platform\'s integer range (e.g., -2,147,483,648 to 2,147,483,647 for 32-bit INT). BIGINT handles larger ranges.'},
{id:506,topic:'sql-db',difficulty:'intermediate',question:'What is the difference between INNER JOIN and OUTER JOIN?',options:['There is no difference','INNER JOIN returns only matching rows; OUTER JOIN (LEFT, RIGHT, FULL) also returns unmatched rows from one or both tables, padded with NULLs','OUTER JOIN is faster than INNER JOIN','INNER JOIN works on indexed columns; OUTER JOIN works on all columns'],answer:1,explanation:'INNER JOIN: only rows satisfying the join condition. OUTER JOINs (LEFT, RIGHT, FULL) include unmatched rows from the specified side(s), replacing missing values with NULL.'},
{id:507,topic:'sql-db',difficulty:'intermediate',question:'What does "data integrity" mean in database design?',options:['The speed at which data can be retrieved','The accuracy, consistency, and reliability of data over its lifecycle, enforced through constraints (PK, FK, NOT NULL, CHECK, UNIQUE)','The encryption of sensitive data','The backup frequency of the database'],answer:1,explanation:'Data integrity ensures data is accurate, consistent, and free from corruption, enforced through entity integrity (PKs), referential integrity (FKs), domain integrity (CHECK, data types), and user-defined integrity.'},
{id:508,topic:'sql-db',difficulty:'intermediate',question:'What is the purpose of the CHECK constraint?',options:['Verifying foreign key references','Ensuring column values satisfy a specified Boolean condition — e.g., age > 0 or salary BETWEEN 0 AND 1000000','Preventing duplicate values in a column','Requiring a default value when no value is provided'],answer:1,explanation:'CHECK constraints enforce domain integrity by requiring that column values satisfy a specified condition on every INSERT and UPDATE. Rows violating the check are rejected.'},
{id:509,topic:'sql-db',difficulty:'intermediate',question:'What is the purpose of the UNIQUE constraint?',options:['Ensures a column cannot be NULL','Ensures all values in a column (or combination of columns) are distinct across all rows in the table','Creates an index on the column','Defines the column as a foreign key'],answer:1,explanation:'UNIQUE ensures no two rows have the same value(s) in the constrained column(s), but allows NULL values (unlike PRIMARY KEY which also enforces NOT NULL).'},
{id:510,topic:'sql-db',difficulty:'intermediate',question:'What is SQL injection and how is it prevented?',options:['A query optimization technique using injected hints','A security vulnerability where an attacker inserts malicious SQL into user input to manipulate database queries; prevented with parameterised queries/prepared statements','A method for inserting large volumes of data','A database migration technique'],answer:1,explanation:'SQL injection exploits unsanitised user input concatenated into SQL strings. Prevention: use parameterised queries/prepared statements (never string concatenation), input validation, and least-privilege DB accounts.'},
{id:511,topic:'sql-db',difficulty:'advanced',question:'What is a "covering index"?',options:['An index that covers all tables in the database','An index that includes all columns needed to satisfy a query, allowing the database to fulfill the query entirely from the index without accessing the actual table data pages','An index that prevents table scans','An index on a covering column (VARCHAR > 255 chars)'],answer:1,explanation:'A covering index contains all columns referenced in a query (SELECT, WHERE, ORDER BY, JOIN); the database can answer the query from the index alone (index-only scan), dramatically reducing I/O.'},
{id:512,topic:'sql-db',difficulty:'advanced',question:'What is "index selectivity" and why does it matter for query performance?',options:['The number of columns in an index','The ratio of distinct values to total rows: high selectivity (many unique values) makes an index effective for filtering; low selectivity (few unique values) often makes an index-scan less efficient than a table scan','How quickly an index is created','The order of columns in a composite index'],answer:1,explanation:'Selectivity = unique values / total rows. High selectivity (close to 1.0) means the index discriminates well, making it efficient. Low-selectivity columns (e.g., boolean) often make the optimizer prefer a full scan.'},
{id:513,topic:'sql-db',difficulty:'advanced',question:'What is "MVCC" (Multi-Version Concurrency Control)?',options:['A multi-vendor cloud configuration','A concurrency control mechanism that maintains multiple versions of data rows, allowing readers to see a consistent snapshot without blocking writers — used in PostgreSQL, Oracle, and InnoDB','A multi-variable computation constraint','A transaction isolation method requiring two-phase commits'],answer:1,explanation:'MVCC allows reads and writes to proceed concurrently by keeping old row versions for readers while writes create new versions; each transaction sees a consistent snapshot of the data at its start time.'},
{id:514,topic:'sql-db',difficulty:'advanced',question:'What is "row-level locking" and what advantage does it provide over table-level locking?',options:['Row-level locking is slower but more accurate','Row-level locking allows multiple transactions to modify different rows in the same table concurrently, dramatically reducing contention compared to table-level locking (which serialises all modifications to a table)','Table-level locking is more granular','Row-level locking is only available in NoSQL databases'],answer:1,explanation:'Row-level locking improves concurrency: Transaction A can lock row 1 while Transaction B modifies row 2 simultaneously. Table-level locking would block B until A commits.'},
{id:515,topic:'sql-db',difficulty:'advanced',question:'What is the "star schema" in data warehousing?',options:['A database schema shaped like a star','A data warehouse schema with a central fact table containing metrics surrounded by denormalised dimension tables, optimised for OLAP queries and reporting tools','A schema for storing star rating data','A distributed database topology shaped like a star network'],answer:1,explanation:'The star schema has a central fact table (sales amounts, quantities) connected directly to denormalised dimension tables (customer, product, date), enabling simple joins and fast aggregation for analytical queries.'},
{id:516,topic:'sql-db',difficulty:'advanced',question:'What is the difference between a "star schema" and a "snowflake schema"?',options:['They are identical','In a star schema, dimension tables are fully denormalised; in a snowflake schema, dimension tables are normalised into sub-dimensions, reducing redundancy but increasing join complexity','Snowflake schemas are used for OLTP; star schemas for OLAP','Star schemas support more complex queries than snowflake schemas'],answer:1,explanation:'Star schema: flat, denormalised dimensions (faster queries, more storage). Snowflake schema: normalised dimension hierarchies (less storage, more complex queries requiring additional joins).'},
{id:517,topic:'sql-db',difficulty:'advanced',question:'What is "row chaining" and "row migration" in Oracle databases?',options:['Techniques for distributing rows across shards','Row chaining: a row too large to fit in one data block spans multiple blocks. Row migration: when a row grows beyond its original block after an UPDATE, it is moved to a new block leaving a pointer, degrading I/O performance','Methods for archiving old rows to separate tables','Synchronisation techniques for replicated rows'],answer:1,explanation:'Row chaining and migration both degrade I/O performance by requiring extra block reads. Chaining occurs at INSERT for very wide rows; migration occurs after UPDATE increases a row\'s size beyond available space in its block.'},
{id:518,topic:'sql-db',difficulty:'advanced',question:'What is "connection pooling" in database-driven applications?',options:['A type of database replication','Maintaining a pool of pre-established database connections that are reused across application requests, reducing the overhead of repeatedly creating and destroying connections','A technique for distributing queries across replicas','A method for pooling multiple queries into a single round trip'],answer:1,explanation:'Connection pooling keeps N open connections to the database, lending them to application threads on request and returning them to the pool on release, dramatically reducing connection establishment latency.'},
{id:519,topic:'sql-db',difficulty:'advanced',question:'What is a "recursive CTE" and what is its use case?',options:['A CTE that calls stored procedures recursively','A CTE that references itself, enabling queries over hierarchical or tree-structured data (e.g., employee-manager hierarchies, bill of materials, directory trees) using an anchor member and a recursive member','A CTE that runs multiple times to optimise performance','A CTE used in recursive foreign key lookups'],answer:1,explanation:'A recursive CTE has an anchor SELECT (base case) UNIONed with a recursive SELECT that references the CTE by name, iterating until no more rows match — ideal for self-referential hierarchical data.'},
{id:520,topic:'sql-db',difficulty:'advanced',question:'What is the purpose of database "statistics" used by the query optimizer?',options:['Statistical reports on database usage patterns for auditing','Metadata about data distribution (column value histograms, number of distinct values, row counts) that the query optimizer uses to estimate cardinality and choose the most efficient execution plan','Database performance benchmarks published by the vendor','Usage statistics for database licencing purposes'],answer:1,explanation:'Optimizer statistics (column histograms, table cardinalities, index density) let the cost-based optimizer estimate how many rows each operation will process, choosing the lowest-cost join order and access path.'}
]);
