window.QUIZ_BANK['selenium'] = (window.QUIZ_BANK['selenium']||[]).concat([
{id:4081,topic:'selenium',difficulty:'beginner',question:'What is Selenium Grid 4?',options:['A spreadsheet for test results','A distributed test execution framework where a Router distributes WebDriver sessions to registered Nodes, supporting standalone, hub-node, and distributed deployment modes','A cloud-only Selenium service','A Selenium IDE plugin'],answer:1,explanation:'Selenium Grid 4 is a complete rewrite from Grid 3. It introduces new topology modes: Standalone (all components in one process), Hub-Node (classic model), and fully Distributed (each component runs separately). It uses WebSockets for real-time communication and supports W3C WebDriver natively.'},
{id:4082,topic:'selenium',difficulty:'intermediate',question:'What are the main components of Selenium Grid 4 in distributed mode?',options:['Hub and Node only','Router, Distributor, Session Map, Session Queue, Event Bus, and Node — each runs as an independent process for maximum scalability','Hub, Node, and Register','Proxy, Grid, and Driver'],answer:1,explanation:'Grid 4 distributed mode splits responsibilities: Router (entry point), Distributor (matches sessions to nodes), Session Map (tracks active sessions), Session Queue (queues incoming requests), Event Bus (internal messaging), and Nodes (actual browser hosts). This enables independent scaling of each component.'},
{id:4083,topic:'selenium',difficulty:'intermediate',question:'How do you start Selenium Grid 4 in standalone mode?',options:['java -jar selenium.jar grid','java -jar selenium-server.jar standalone','selenium-grid start','java -jar selenium-server.jar hub'],answer:1,explanation:'`java -jar selenium-server-<version>.jar standalone` starts Grid 4 in standalone mode, combining all components in a single JVM process. It is ideal for development and small teams. The default port is 4444 and the Grid UI is accessible at http://localhost:4444.'},
{id:4084,topic:'selenium',difficulty:'intermediate',question:'How do you start a Selenium Grid 4 Hub?',options:['java -jar selenium-server.jar hub','java -jar selenium-server.jar node','java -jar selenium-server.jar distributor','java -jar selenium-server.jar router'],answer:0,explanation:'`java -jar selenium-server-<version>.jar hub` starts the Hub, which internally launches the Router, Distributor, Session Map, Session Queue, and Event Bus together. Nodes register with the Hub using `--grid-url`. The Hub UI is at http://localhost:4444/ui.'},
{id:4085,topic:'selenium',difficulty:'intermediate',question:'How do you register a Node with a Selenium Grid 4 Hub?',options:['java -jar selenium-server.jar node','java -jar selenium-server.jar node --grid-url http://localhost:4444','java -jar selenium-server.jar register --hub localhost','java -jar selenium-server.jar attach'],answer:1,explanation:'`java -jar selenium-server.jar node --grid-url http://localhost:4444` starts a Node and registers it with the Hub at the specified URL. The Node auto-detects installed browsers and their drivers. You can configure max sessions, browser types, and resources via flags or a config TOML file.'},
{id:4086,topic:'selenium',difficulty:'intermediate',question:'What is the key difference between Selenium Grid 3 and Selenium Grid 4?',options:['Grid 3 was faster','Grid 4 uses W3C WebDriver protocol natively, has a new distributed architecture, supports CDP via Node relay, has a built-in UI dashboard, and eliminates DesiredCapabilities in favor of Options classes','Grid 4 only supports Chrome','Grid 3 supports more browsers'],answer:1,explanation:'Grid 3 used JSON Wire Protocol, had a monolithic Hub+Node model, and required DesiredCapabilities. Grid 4 is W3C-compliant, introduces modular distributed architecture, supports CDP proxying through nodes, provides a real-time Grid UI at /ui, and integrates with Selenium Manager for driver management.'},
{id:4087,topic:'selenium',difficulty:'intermediate',question:'What is RemoteWebDriver in Selenium?',options:['A driver that controls browsers on the local machine only','A WebDriver implementation that connects to a remote Selenium Grid or cloud provider by sending HTTP requests to a remote endpoint URL, enabling distributed test execution','A JavaScript-based driver','A driver for headless testing only'],answer:1,explanation:'`new RemoteWebDriver(new URL("http://grid-host:4444"), options)` creates a session on a remote Grid Node. The client sends all WebDriver commands over HTTP to the remote server. This is the foundation for cloud testing with BrowserStack, Sauce Labs, and LambdaTest.'},
{id:4088,topic:'selenium',difficulty:'intermediate',question:'What replaced DesiredCapabilities in Selenium 4?',options:['Nothing, DesiredCapabilities is still the preferred approach','Browser-specific Options classes (ChromeOptions, FirefoxOptions, EdgeOptions, SafariOptions) which extend MutableCapabilities and provide type-safe, browser-specific configuration methods','CapabilityManager class','JSONCapabilities'],answer:1,explanation:'DesiredCapabilities is deprecated in Selenium 4. The type-safe browser Options classes provide the same functionality plus browser-specific features. For example, `ChromeOptions` has `addArguments()`, `setExperimentalOption()`, etc. Options extend MutableCapabilities and work with RemoteWebDriver.'},
{id:4089,topic:'selenium',difficulty:'intermediate',question:'How do you handle a browser alert (accept it) in Selenium?',options:['driver.alert().accept()','driver.switchTo().alert().accept()','driver.getAlert().ok()','driver.manage().alerts().accept()'],answer:1,explanation:'`driver.switchTo().alert()` returns an `Alert` interface representing the native browser dialog. Call `.accept()` to click OK, `.dismiss()` to click Cancel/Close, `.getText()` to read the message, or `.sendKeys("text")` to type into a prompt dialog.'},
{id:4090,topic:'selenium',difficulty:'intermediate',question:'What does `driver.switchTo().alert().dismiss()` do?',options:['Accepts the alert','Clicks Cancel or closes the alert dialog — equivalent to pressing Escape or clicking the Cancel button on a confirm or prompt dialog','Refreshes the page','Throws an exception'],answer:1,explanation:'`dismiss()` clicks the Cancel button on confirm dialogs and closes prompt dialogs. For simple alert dialogs (which have only OK), `dismiss()` also closes the dialog. Use `accept()` when you want to proceed with the action the alert is confirming.'},
{id:4091,topic:'selenium',difficulty:'intermediate',question:'What exception is thrown when `driver.switchTo().alert()` is called but no alert is present?',options:['NoSuchWindowException','NoAlertPresentException','AlertNotFoundException','WebDriverException'],answer:1,explanation:'`NoAlertPresentException` is thrown when `switchTo().alert()` is called but no native browser alert, confirm, or prompt dialog is currently open. Use `ExpectedConditions.alertIsPresent()` with WebDriverWait to wait for the alert before attempting to switch to it.'},
{id:4092,topic:'selenium',difficulty:'intermediate',question:'How do you type text into a JavaScript prompt dialog using Selenium?',options:['driver.type("text")','driver.switchTo().alert().sendKeys("text")','alert.input("text")','driver.prompt().sendKeys("text")'],answer:1,explanation:'`driver.switchTo().alert().sendKeys("value")` sends keystrokes to a JavaScript `prompt()` dialog\'s input field. After calling `sendKeys()`, call `.accept()` to confirm the entry. This method only works on `window.prompt()` alerts — standard alerts and confirms have no input.'},
{id:4093,topic:'selenium',difficulty:'intermediate',question:'How do you switch to an iframe by its index?',options:['driver.frame(0)','driver.switchTo().frame(0)','driver.frames().get(0)','driver.iframe(0)'],answer:1,explanation:'`driver.switchTo().frame(0)` switches the driver context to the iframe at index 0 (zero-based). All subsequent `findElement()` calls will operate within that iframe\'s DOM. Use `driver.switchTo().defaultContent()` to return to the top-level document.'},
{id:4094,topic:'selenium',difficulty:'intermediate',question:'How do you switch to an iframe by its name or id attribute?',options:['driver.switchTo().frame("frameName")','driver.selectFrame("frameName")','driver.navigate().frame("frameName")','driver.iframe("frameName")'],answer:0,explanation:'`driver.switchTo().frame("name-or-id")` locates the iframe by its `name` or `id` attribute and switches context to it. It is more stable than index-based switching because adding new iframes won\'t shift the index. This is preferred over index when the frame has a stable identifier.'},
{id:4095,topic:'selenium',difficulty:'intermediate',question:'How do you switch to an iframe using a WebElement reference?',options:['driver.switchTo().frame(driver.findElement(By.id("myFrame")))','driver.frame(webElement)','driver.switchTo().iframeElement(webElement)','driver.switchToFrame(webElement)'],answer:0,explanation:'You can locate the `<iframe>` element first with `findElement()`, then pass the WebElement to `driver.switchTo().frame(frameElement)`. This approach is most reliable when the frame lacks a stable name/id and the WebElement can be located by other means.'},
{id:4096,topic:'selenium',difficulty:'intermediate',question:'What does `driver.switchTo().defaultContent()` do?',options:['Switches to the first iframe','Exits all frame/iframe contexts and returns focus to the top-level (main document) of the page','Switches to the parent frame','Closes the current frame'],answer:1,explanation:'`defaultContent()` exits all nested frame contexts (even deeply nested ones) and returns to the top-level document. Use `switchTo().parentFrame()` to go up only one level (to the parent frame). `defaultContent()` is the "escape hatch" to the main page.'},
{id:4097,topic:'selenium',difficulty:'intermediate',question:'How do you switch to a newly opened browser window in Selenium?',options:['driver.switchTo().newWindow()','Store original handles, trigger the action that opens a new window, get all handles, find the new handle, call driver.switchTo().window(newHandle)','driver.switchTo().window() automatically detects new windows','driver.manage().getNewWindow()'],answer:1,explanation:'There is no automatic switching. The pattern: `String originalHandle = driver.getWindowHandle(); element.click(); for(String handle : driver.getWindowHandles()) { if(!handle.equals(originalHandle)) { driver.switchTo().window(handle); break; } }` — finds the new handle and switches to it.'},
{id:4098,topic:'selenium',difficulty:'intermediate',question:'What does `JavascriptExecutor` enable in Selenium?',options:['Running Java methods inside the browser','Executing arbitrary JavaScript code in the context of the current browser window, enabling interactions not possible through standard WebDriver methods (scroll, dynamic clicks, value injection)','Parsing JSON responses','Generating test reports'],answer:1,explanation:'Cast the driver: `JavascriptExecutor js = (JavascriptExecutor) driver;`. Then call `js.executeScript("return document.title;")` or `js.executeScript("arguments[0].click();", element)`. JavaScript execution bypasses the standard interaction model, useful for hidden elements, scroll, and DOM manipulation.'},
{id:4099,topic:'selenium',difficulty:'intermediate',question:'How do you scroll to an element using JavascriptExecutor in Selenium?',options:['driver.scrollTo(element)','((JavascriptExecutor) driver).executeScript("arguments[0].scrollIntoView(true);", element)','driver.navigate().scroll(element)','element.scroll()'],answer:1,explanation:'`executeScript("arguments[0].scrollIntoView(true);", element)` calls the native DOM `scrollIntoView()` method with `true` meaning "align to top". `arguments[0]` receives the WebElement passed as the second argument to `executeScript()`.'},
{id:4100,topic:'selenium',difficulty:'intermediate',question:'What does `executeAsyncScript()` do differently from `executeScript()`?',options:['executeAsyncScript runs on a separate thread','executeAsyncScript allows the JavaScript to call back asynchronously — the script must call `arguments[arguments.length-1]()` (the injected callback) to signal completion, and WebDriver waits until that callback is called or scriptTimeout expires','They are identical','executeAsyncScript returns a Promise automatically'],answer:1,explanation:'`executeAsyncScript` is for async operations like AJAX calls or timers. The last argument injected by Selenium is the "done" callback. Example: `executeAsyncScript("setTimeout(arguments[arguments.length-1], 2000);")` waits 2 seconds asynchronously without blocking the driver thread with a Thread.sleep.'},
{id:4101,topic:'selenium',difficulty:'intermediate',question:'How do you use the Actions class to hover over an element in Selenium?',options:['element.hover()','new Actions(driver).moveToElement(element).perform()','driver.hover(element)','Actions.hover(driver, element)'],answer:1,explanation:'`new Actions(driver).moveToElement(element).perform()` moves the synthetic mouse pointer to the center of the element, triggering CSS `:hover` states and JavaScript mouseover/mouseenter events. Always call `.perform()` to execute the built action chain.'},
{id:4102,topic:'selenium',difficulty:'intermediate',question:'How do you perform a drag-and-drop action using the Actions class?',options:['driver.dragAndDrop(source, target)','new Actions(driver).dragAndDrop(sourceElement, targetElement).perform()','Actions.drag(source).to(target)','element.moveTo(target)'],answer:1,explanation:'`new Actions(driver).dragAndDrop(source, target).perform()` simulates pressing the mouse on source, moving to target, and releasing. If the simple form doesn\'t work (common with HTML5 drag-and-drop), use `clickAndHold(source).moveToElement(target).release().perform()` or a JavaScript-based approach.'},
{id:4103,topic:'selenium',difficulty:'intermediate',question:'What does `new Actions(driver).contextClick(element).perform()` do?',options:['Left-clicks the element','Performs a right-click (context menu click) on the specified element, triggering contextmenu events','Double-clicks the element','Performs Ctrl+Click'],answer:1,explanation:'`contextClick()` in the Actions API simulates a right mouse button click on the element, which typically displays the browser\'s context menu or triggers a `contextmenu` JavaScript event. Useful for testing custom right-click context menus in web applications.'},
{id:4104,topic:'selenium',difficulty:'intermediate',question:'What does `new Actions(driver).doubleClick(element).perform()` do?',options:['Clicks twice with a delay','Simulates a native double-click (dblclick event) on the element — useful for triggering inline editing, text selection, or double-click-specific JavaScript handlers','Submits a form twice','Scrolls to the element'],answer:1,explanation:'`doubleClick()` dispatches two rapid click events followed by a `dblclick` event, matching browser behavior for a physical double-click. Some UI frameworks use dblclick for inline editing (e.g., editable table cells). Simple `element.click()` called twice does not fire the dblclick event.'},
{id:4105,topic:'selenium',difficulty:'intermediate',question:'How do you simulate holding down a key modifier (e.g., Shift) while clicking in Selenium?',options:['element.click(Keys.SHIFT)','new Actions(driver).keyDown(Keys.SHIFT).click(element).keyUp(Keys.SHIFT).perform()','driver.holdKey(Keys.SHIFT).click(element)','sendKeys(Keys.SHIFT + click)'],answer:1,explanation:'`keyDown(Keys.SHIFT)` holds the Shift key, `click(element)` performs the click while Shift is held, and `keyUp(Keys.SHIFT)` releases it. This pattern enables multi-select with Shift+Click or Ctrl+Click for checkboxes, list items, and other multi-selection UI patterns.'},
{id:4106,topic:'selenium',difficulty:'intermediate',question:'What class handles dropdown (`<select>`) interactions in Selenium?',options:['DropDown','Select (from org.openqa.selenium.support.ui.Select)','ComboBox','SelectElement'],answer:1,explanation:'`Select select = new Select(driver.findElement(By.id("dropdown")));` wraps a native `<select>` element. It provides `selectByVisibleText()`, `selectByValue()`, `selectByIndex()`, `deselectAll()`, `getOptions()`, and `getAllSelectedOptions()` — covering all standard dropdown interactions.'},
{id:4107,topic:'selenium',difficulty:'intermediate',question:'How do you select a dropdown option by its visible text?',options:['select.selectOption("Text")','select.selectByVisibleText("Option Text")','select.choose("Option Text")','select.setText("Option Text")'],answer:1,explanation:'`selectByVisibleText("Option Text")` finds the `<option>` element whose trimmed visible text equals the given string and selects it. It is the most human-readable selection method. Note that it performs an exact match by default and is case-sensitive.'},
{id:4108,topic:'selenium',difficulty:'intermediate',question:'How do you select a dropdown option by its `value` attribute?',options:['select.selectByAttribute("value","v1")','select.selectByValue("v1")','select.setValue("v1")','select.chooseValue("v1")'],answer:1,explanation:'`selectByValue("v1")` selects the `<option value="v1">` element. It matches the `value` attribute of the option, not the displayed text. This is useful when values are stable programmatic identifiers but displayed text might change due to localization.'},
{id:4109,topic:'selenium',difficulty:'intermediate',question:'How do you select a dropdown option by its zero-based index?',options:['select.selectByIndex(0)','select.selectFirst()','select.choose(0)','select.selectByPosition(0)'],answer:0,explanation:'`selectByIndex(0)` selects the first option (index 0). Indices are zero-based and correspond to the position of `<option>` elements within the `<select>`. Useful when options are dynamically generated and lack stable text/value but have a predictable order.'},
{id:4110,topic:'selenium',difficulty:'intermediate',question:'What does `select.isMultiple()` check?',options:['Whether multiple selects exist on the page','Whether the <select> element has the multiple attribute, allowing multiple options to be selected simultaneously','Whether the first option is selected','Whether the dropdown is visible'],answer:1,explanation:'`select.isMultiple()` returns true if the `<select>` element has the `multiple` attribute. For multi-select dropdowns, you can call `selectByIndex()` multiple times or `selectByVisibleText()` for each option you want selected. Use `deselectAll()` to clear all selections first.'},
{id:4111,topic:'selenium',difficulty:'intermediate',question:'What does `select.getOptions()` return?',options:['The selected option only','A List<WebElement> of all <option> elements within the <select> element','The option values as Strings','The visible text of all options as a List<String>'],answer:1,explanation:'`select.getOptions()` returns all `<option>` elements as a `List<WebElement>`. You can then call `getText()` or `getAttribute("value")` on each. Useful for assertions like verifying the total number of options or that a specific option exists in the dropdown.'},
{id:4112,topic:'selenium',difficulty:'intermediate',question:'What does `select.getFirstSelectedOption()` return?',options:['The first option in the list regardless of selection','The first currently selected <option> WebElement — useful for verifying the default selection or current selection state','The option at index 0 always','All selected options'],answer:1,explanation:'`getFirstSelectedOption()` returns the `WebElement` of the first selected option. For single-select dropdowns it returns the currently selected option. For multi-selects it returns the first of multiple selected options. Call `getText()` on the result to get the displayed text.'},
{id:4113,topic:'selenium',difficulty:'intermediate',question:'What does `((TakesScreenshot) driver).getScreenshotAs(OutputType.FILE)` return?',options:['A File path string','A java.io.File object pointing to a temporary PNG file containing the screenshot of the current browser viewport','A BufferedImage','A byte array'],answer:1,explanation:'`getScreenshotAs(OutputType.FILE)` creates a temporary file containing a PNG screenshot of the current browser viewport. Use `FileUtils.copyFile(screenshot, new File("path/to/save.png"))` to persist it. The temporary file is deleted when the JVM exits unless explicitly copied.'},
{id:4114,topic:'selenium',difficulty:'advanced',question:'How do you capture a screenshot in a TestNG @AfterMethod to capture failures?',options:['Screenshots auto-capture on failure','Inject ITestResult parameter, check if result.getStatus() == ITestResult.FAILURE, then cast driver and call getScreenshotAs() to capture and save the screenshot','Use @Screenshot annotation','Use TestNG listener only'],answer:1,explanation:'In a `@AfterMethod(alwaysRun=true)` with `ITestResult result` parameter: `if(result.getStatus() == ITestResult.FAILURE) { File screenshot = ((TakesScreenshot)driver).getScreenshotAs(OutputType.FILE); FileUtils.copyFile(screenshot, new File("screenshots/" + result.getName() + ".png")); }` — captures failure evidence automatically.'},
{id:4115,topic:'selenium',difficulty:'intermediate',question:'What does `driver.switchTo().window(windowHandle)` do?',options:['Minimizes the window','Switches the WebDriver focus to the window identified by the given handle string — all subsequent WebDriver commands operate in that window','Opens a new window','Closes the specified window'],answer:1,explanation:'`driver.switchTo().window(handle)` directs the WebDriver session to operate on the specified window or tab. After switching, all `findElement()`, `navigate()`, and interaction calls target that window. The handle is obtained from `getWindowHandles()` or `getWindowHandle()`.'},
{id:4116,topic:'selenium',difficulty:'intermediate',question:'What does `new Actions(driver).sendKeys(element, Keys.chord(Keys.CONTROL, "a")).perform()` do?',options:['Types the letter "a"','Focuses the element and sends Ctrl+A (Select All) keyboard shortcut using the Actions chord method','Clears the element','Copies text to clipboard'],answer:1,explanation:'`Keys.chord(Keys.CONTROL, "a")` creates a multi-key sequence. Used with `Actions.sendKeys()`, it simulates pressing Ctrl+A simultaneously — selecting all text in the focused element. This differs from `element.clear()` which directly clears without triggering select-all events.'},
{id:4117,topic:'selenium',difficulty:'advanced',question:'What is the W3C Actions API and how does it differ from the legacy Actions class?',options:['They are identical','The W3C Actions API is the specification that defines input source actions (pointer, keyboard, wheel). Selenium 4\'s Actions class is built on top of this spec, replacing the legacy JsonWireProtocol-based Actions, enabling more precise multi-source input coordination','The W3C API is for mobile only','The legacy Actions class is more powerful'],answer:1,explanation:'W3C defines input sources (pointer, keyboard, wheel) and sequences of actions per source. Selenium 4 Actions class generates W3C-compliant action sequences. This provides better cross-browser consistency, support for scroll wheel actions, and precise multi-finger/pointer input sequences compared to the legacy implementation.'},
{id:4118,topic:'selenium',difficulty:'intermediate',question:'How do you scroll the page using Actions in Selenium 4?',options:['driver.scroll(0,500)','new Actions(driver).scrollByAmount(0, 500).perform()','((JavascriptExecutor)driver).executeScript("window.scrollBy(0,500)")','Both B and C work'],answer:3,explanation:'Selenium 4 introduced `Actions.scrollByAmount(deltaX, deltaY)` and `scrollToElement(element)` using the W3C Wheel input source. JavaScript `window.scrollBy()` also works. Both are valid. The Actions approach is more standardized and doesn\'t require casting to JavascriptExecutor.'},
{id:4119,topic:'selenium',difficulty:'intermediate',question:'When using RemoteWebDriver to connect to Selenium Grid, what is the typical endpoint URL format?',options:['http://hub:4444/grid','http://hub:4444/wd/hub (Grid 3) or http://hub:4444 (Grid 4)','http://hub:4444/remote','http://hub:4444/selenium'],answer:1,explanation:'Grid 3 used `http://hub:4444/wd/hub` as the RemoteWebDriver URL. Grid 4 changed the endpoint to `http://hub:4444` (no `/wd/hub` suffix). Using the wrong endpoint format is a common migration error when upgrading from Grid 3 to Grid 4.'},
{id:4120,topic:'selenium',difficulty:'advanced',question:'What is the purpose of `--grid-url` when starting a Selenium Grid 4 Node?',options:['Sets the Grid dashboard URL','Specifies the URL of the Hub/Router that the Node registers with during startup, enabling the Hub to route sessions to this Node','Sets the Node\'s own public URL','Configures Grid authentication'],answer:1,explanation:'When launching `java -jar selenium-server.jar node --grid-url http://hub:4444`, the Node sends a registration request to the Hub. The Hub adds the Node to its list of available machines. Without `--grid-url`, the Node runs standalone without registering anywhere.'},
{id:4121,topic:'selenium',difficulty:'intermediate',question:'What does `driver.manage().window().getSize()` return?',options:['Screen resolution of the monitor','A Dimension object containing the width and height of the current browser window in pixels','The inner window size (viewport)','The OS screen dimensions'],answer:1,explanation:'`getSize()` returns a `Dimension` with `width` and `height` representing the outer browser window size. `getPosition()` returns a `Point` with x/y of the top-left corner. These are useful for verifying responsive breakpoints and ensuring windows are properly positioned during test setup.'},
{id:4122,topic:'selenium',difficulty:'intermediate',question:'What does `new Actions(driver).moveToElement(element, 10, 20).click().perform()` do?',options:['Moves to a specific element ignoring offsets','Moves the mouse to a position 10px from the left and 20px from the top of the element\'s center, then clicks — offset-based clicking for elements with sub-regions requiring precise coordinates','Scrolls 10 pixels right and 20 pixels down','Moves to coordinates (10, 20) on the screen'],answer:1,explanation:'The three-argument `moveToElement(element, xOffset, yOffset)` moves the mouse to a specific offset relative to the element\'s top-left corner. This is useful for clicking specific hotspots in a canvas, map, or image element where the target is not at the element\'s center.'},
{id:4123,topic:'selenium',difficulty:'advanced',question:'What is the difference between `Actions.click()` and `Actions.click(element)`?',options:['Identical behavior','Actions.click() without an argument clicks at the current mouse pointer position; Actions.click(element) first moves to the element then clicks — always use the element form to ensure correct targeting','Actions.click(element) is deprecated','Actions.click() is not a valid method'],answer:1,explanation:'`actions.click()` clicks wherever the virtual mouse currently is (useful after `moveToElement()`). `actions.click(element)` is a shorthand that implicitly calls `moveToElement(element)` first. The explicit chaining form `moveToElement(e).click()` is clearer and equivalent to `click(element)`.'},
{id:4124,topic:'selenium',difficulty:'intermediate',question:'What is the purpose of `driver.manage().addCookie(new Cookie("auth", "token123"))` in test setup?',options:['Adds a cookie to the test report','Injects an authentication cookie directly into the browser session, bypassing the login UI and saving test execution time by pre-authenticating','Sets a browser preference','Configures the Grid node'],answer:1,explanation:'Pre-seeding authentication cookies is a performance optimization for test setups. Instead of navigating to the login page and submitting credentials for every test, you programmatically set the session/auth cookie. This reduces network overhead and test execution time significantly.'},
{id:4125,topic:'selenium',difficulty:'advanced',question:'What does `new Actions(driver).keyDown(Keys.ALT).sendKeys(Keys.F4).keyUp(Keys.ALT).perform()` simulate?',options:['Opens developer tools','Simulates the Alt+F4 keyboard shortcut (close window) in Windows using the Actions API key sequence','Refreshes the page','Nothing, browser shortcuts are blocked'],answer:1,explanation:'Alt+F4 is the Windows shortcut for closing the active window. Using `keyDown(Keys.ALT)`, `sendKeys(Keys.F4)`, `keyUp(Keys.ALT)` in an Actions chain simulates this. Browser security may block some OS-level shortcuts, but this demonstrates the pattern for sending complex key combinations.'},
{id:4126,topic:'selenium',difficulty:'intermediate',question:'What information is available in a Selenium `Cookie` object?',options:['Name and value only','Name, value, domain, path, expiry date, isSecure flag, isHttpOnly flag — all accessible via getter methods on the Cookie object','Name, value, and domain only','Only the cookie string'],answer:1,explanation:'`Cookie` provides: `getName()`, `getValue()`, `getDomain()`, `getPath()`, `getExpiry()` (Date), `isSecure()`, and `isHttpOnly()`. This rich model enables comprehensive cookie-based assertions in authentication, session management, and privacy-related test scenarios.'},
{id:4127,topic:'selenium',difficulty:'intermediate',question:'How does Selenium interact with file upload input elements?',options:['By simulating drag-and-drop only','By calling sendKeys() with the absolute file path on the <input type="file"> element directly — no clicking of OS dialogs required','By using the Actions class','By using JavascriptExecutor only'],answer:1,explanation:'`driver.findElement(By.cssSelector("input[type=\'file\']")).sendKeys("/absolute/path/to/file.txt")` sets the file path directly on the file input, bypassing the OS file picker dialog. WebDriver does not interact with native OS dialogs by default — this is the standard approach.'},
{id:4128,topic:'selenium',difficulty:'advanced',question:'What strategy is used to handle file download dialogs in Selenium with Chrome?',options:['Use AutoIt or Sikuli for native dialog handling','Configure ChromeOptions experimental preferences to set the download directory and suppress the download prompt, preventing the dialog from appearing','Use Actions to click Save','Accept the dialog with driver.switchTo().alert()'],answer:1,explanation:'`options.setExperimentalOption("prefs", Map.of("download.default_directory", "/path", "download.prompt_for_download", false))` configures Chrome to download files silently to a specified directory. Since Selenium cannot interact with native OS dialogs, this Chrome preference approach is the standard solution.'},
{id:4129,topic:'selenium',difficulty:'intermediate',question:'What does `driver.manage().getCookieNamed("JSESSIONID")` return if no such cookie exists?',options:['An empty Cookie','null — getCookieNamed() returns null when no cookie with the specified name is found','Throws NullPointerException','Throws CookieNotFoundException'],answer:1,explanation:'`getCookieNamed()` returns `null` when the named cookie does not exist. Always perform a null check before calling methods on the returned Cookie object to avoid NullPointerException. Use `assertNotNull(driver.manage().getCookieNamed("JSESSIONID"))` to verify session cookie presence.'},
{id:4130,topic:'selenium',difficulty:'advanced',question:'What is the difference between `switchTo().frame()` and `switchTo().parentFrame()`?',options:['They are the same','switchTo().frame() enters a specific frame context; switchTo().parentFrame() exits only one level up to the immediate parent frame (not all the way to the top document like defaultContent() does)','parentFrame() goes to the top','frame() goes up one level'],answer:1,explanation:'In nested iframes: `defaultContent()` exits ALL frames back to the main document. `parentFrame()` exits only one level — from a nested child frame back to its parent frame. This is crucial in deeply nested iframe structures where you need intermediate frame context.'},
{id:4131,topic:'selenium',difficulty:'intermediate',question:'What does `OutputType.BASE64` return when used with TakesScreenshot?',options:['A base64 encoded file path','A Base64-encoded string of the PNG screenshot, suitable for embedding directly into HTML img tags as data: URIs or attaching to JSON-based reporting frameworks','A binary array','A URL to the screenshot'],answer:1,explanation:'`getScreenshotAs(OutputType.BASE64)` returns a Base64 string like `"iVBORw0KGgo..."`. Embedding it in reports: `"<img src=\'data:image/png;base64," + base64 + "\'>"`. This is used by ExtentReports, Allure, and other reporting frameworks to embed screenshots without saving files.'},
{id:4132,topic:'selenium',difficulty:'intermediate',question:'What is `driver.manage().window().fullscreen()` and when was it added?',options:['Added in Selenium 3 for screenshots','Sets the browser window to true fullscreen mode (hides browser chrome like address bar and tabs) — available in Selenium 4 via W3C fullscreen command','Same as maximize()','Minimizes the window'],answer:1,explanation:'`fullscreen()` sends the W3C fullscreen command, making the browser cover the entire screen while hiding toolbars, tab bars, and OS taskbars. This differs from `maximize()` which just makes the window as large as possible while keeping browser chrome visible.'},
{id:4133,topic:'selenium',difficulty:'advanced',question:'What is a major limitation of using Selenium Grid 3 Node configuration?',options:['It only supports Firefox','Grid 3 Nodes required DesiredCapabilities and explicit capability matching; browser versions had to be manually managed, driver paths explicitly configured, and DesiredCapabilities was fragile — Grid 4 addressed these with auto-detection and Options classes','Grid 3 cannot run tests in parallel','Grid 3 uses HTTP/2 only'],answer:1,explanation:'Grid 3\'s dependency on DesiredCapabilities made configuration verbose and error-prone. Capability matching was inconsistent across browsers. Nodes required manual driver binary setup. Grid 4 auto-detects installed browsers, uses browser Options classes, and supports configuration via TOML files.'},
{id:4134,topic:'selenium',difficulty:'intermediate',question:'How do you perform a right-click followed by selecting a context menu item in Selenium?',options:['element.rightClick()','Use Actions.contextClick(element).perform() to open the context menu, then findElement() and click() the desired menu item','driver.contextMenu(element)','element.contextMenu()'],answer:1,explanation:'`new Actions(driver).contextClick(targetElement).perform()` opens the context menu. Then locate the menu item: `driver.findElement(By.id("menu-item-copy")).click()`. Note: native browser right-click menus cannot be controlled; this works for custom JavaScript-based context menus only.'},
{id:4135,topic:'selenium',difficulty:'intermediate',question:'What is the `Select.deselectByVisibleText(text)` method used for?',options:['Selects an option by text','Deselects a specific option by its visible text in a multi-select dropdown — only valid when the <select> has the multiple attribute','Hides an option','Removes an option from the dropdown'],answer:1,explanation:'`select.deselectByVisibleText("Option")` removes the selection from the named option in a multi-select `<select multiple>` element. Corresponding methods include `deselectByValue()`, `deselectByIndex()`, and `deselectAll()`. These methods throw `NotImplementedError` on non-multi select dropdowns.'},
{id:4136,topic:'selenium',difficulty:'advanced',question:'What happens when `Actions.dragAndDrop()` fails on HTML5 drag-and-drop elements?',options:['It always works','Some HTML5 drag-and-drop implementations use custom JavaScript events that the WebDriver Actions API cannot fully replicate; in these cases, use JavaScript simulation or the `dragAndDropBy()` workaround with `clickAndHold().moveByOffset().release()`','Use sendKeys instead','Use the Select class'],answer:1,explanation:'HTML5 `dragstart`, `drag`, `dragover`, `drop` events are sometimes not properly triggered by WebDriver\'s Actions. Workarounds include: JavaScript injection to fire events manually, the `clickAndHold().moveByOffset(x,y).release()` chain, or third-party libraries like `drag-mock`. This is a known WebDriver limitation.'},
{id:4137,topic:'selenium',difficulty:'intermediate',question:'What does `driver.manage().timeouts().implicitlyWait(Duration.ofSeconds(0))` do?',options:['Sets a 1-second wait','Resets the implicit wait to 0, causing findElement() to fail immediately if the element is not present — effectively disabling implicit wait polling','Pauses execution for 0ms','Throws an exception'],answer:1,explanation:'Setting implicit wait to `Duration.ofSeconds(0)` or `Duration.ZERO` disables polling and reverts to the fail-fast behavior. This is sometimes needed when you expect an element NOT to be present and want an immediate `NoSuchElementException` without waiting the full implicit timeout.'},
{id:4138,topic:'selenium',difficulty:'advanced',question:'What are the risks of mixing implicit and explicit waits in Selenium?',options:['No risks','Mixing them causes unpredictable timeout behavior: an explicit wait\'s condition is checked after each poll, but the WebDriver commands inside the condition are themselves subject to implicit wait — this can cause waits to compound and exceed expected total timeout, leading to slow or flaky tests','Implicit wait overrides explicit wait','Explicit wait disables implicit wait'],answer:1,explanation:'The WebDriver specification warns against mixing waits. If implicit wait is 10s and explicit wait is 5s, a `findElement()` inside the explicit wait condition may itself wait 10s before throwing `NoSuchElementException`, causing the total wait to far exceed the intended 5s timeout.'},
{id:4139,topic:'selenium',difficulty:'intermediate',question:'How do you get the value attribute of a text input field using Selenium?',options:['element.getText()','element.getAttribute("value")','element.getValue()','element.getProperty("value")'],answer:1,explanation:'`element.getText()` returns empty for `<input>` elements. The current user-entered value of a text input is stored in the `value` DOM property/attribute. Use `element.getAttribute("value")` (or in Selenium 4, `element.getDomProperty("value")` for the live property) to retrieve it.'},
{id:4140,topic:'selenium',difficulty:'advanced',question:'What is `element.getDomProperty("value")` vs `element.getAttribute("value")` in Selenium 4?',options:['Identical in all cases','getDomProperty() reads the current live DOM property (what user typed), while getAttribute() reads the HTML attribute (initial/default value) — for input fields after user interaction, getDomProperty("value") reflects the current state','getDomProperty is deprecated','getAttribute reads live values'],answer:1,explanation:'This distinction matters for inputs: `getAttribute("value")` returns the HTML attribute\'s initial value. `getDomProperty("value")` returns the current DOM property reflecting user input after page load. Selenium 4 introduced `getDomProperty()` and `getDomAttribute()` to make this distinction explicit.'},
{id:4141,topic:'selenium',difficulty:'intermediate',question:'How do you verify that an element is NOT present on the page using Selenium?',options:['element.isPresent() == false','driver.findElements(By.id("elem")).isEmpty() — findElements returns empty list (not exception) when no match, making it the correct approach for asserting absence','driver.findElement(By.id("elem")) returns null','Use isDisplayed() == false'],answer:1,explanation:'`driver.findElements(locator).isEmpty()` is the idiomatic way to assert absence: it returns an empty list when nothing matches rather than throwing an exception. Alternatively, a `WebDriverWait` with `invisibilityOfElementLocated()` waits for the element to be gone.'},
{id:4142,topic:'selenium',difficulty:'advanced',question:'What Node configuration option limits how many concurrent sessions a Grid 4 Node can handle?',options:['--max-tests','--max-sessions (default is based on available processors, typically number of CPU cores)','--concurrent-limit','--session-cap'],answer:1,explanation:'`--max-sessions N` controls the maximum number of concurrent browser sessions on a single Grid 4 Node. By default it equals the number of available CPU cores. Each session corresponds to one browser instance. Exceeding this causes new session requests to queue in the Session Queue.'},
{id:4143,topic:'selenium',difficulty:'intermediate',question:'What is the `Alert.getText()` method used for?',options:['Returns the current page title','Returns the message text displayed in the JavaScript alert, confirm, or prompt dialog before interacting with it','Returns the URL of the page','Returns the button labels in the alert'],answer:1,explanation:'`driver.switchTo().alert().getText()` retrieves the message string shown in the browser dialog. For an `alert("Please confirm deletion")`, it returns "Please confirm deletion". Useful for asserting the correct message is displayed before accepting or dismissing the dialog.'},
{id:4144,topic:'selenium',difficulty:'advanced',question:'In Selenium Grid 4, what is the Session Queue and why is it important?',options:['A queue of test scripts to execute','The Session Queue holds incoming session requests that cannot be immediately fulfilled (all nodes busy). The Distributor processes the queue when a Node becomes available, enabling backpressure management and preventing request drops under load','A queue of browser downloads','A log of completed sessions'],answer:1,explanation:'The Session Queue is a core Grid 4 component. When all Nodes are at capacity, new `RemoteWebDriver` session requests are queued rather than immediately rejected. This provides graceful handling of traffic bursts. The Queue has a configurable timeout after which queued requests fail with a `SessionNotCreatedException`.'},
{id:4145,topic:'selenium',difficulty:'intermediate',question:'How do you configure ChromeOptions for headless execution?',options:['new ChromeDriver(new ChromeOptions().headless())','"options.addArguments("--headless=new")" (Chrome 112+) or `options.addArguments("--headless")` for older versions, then pass options to new ChromeDriver(options)','Set headless=true in capabilities','Use FirefoxOptions instead'],answer:1,explanation:'`options.addArguments("--headless=new")` is the recommended form for Chrome 112+. The `--headless=new` mode uses a newer implementation with better rendering fidelity. Older `--headless` (without "=new") uses the legacy mode. Always add `--disable-gpu` on Windows for stability in older versions.'},
{id:4146,topic:'selenium',difficulty:'intermediate',question:'What does `new Actions(driver).moveToElement(element).pause(Duration.ofSeconds(1)).click().perform()` do?',options:['Clicks immediately','Moves to the element, waits 1 second (simulating a hover pause for tooltip activation), then clicks — the pause() method is part of the W3C Actions API for timing control','Pauses the entire test','Waits for a condition'],answer:1,explanation:'`pause(Duration)` inserts a timed delay within an Actions chain. This is used to trigger hover-dependent UI changes (tooltips, dropdown menus) before proceeding with the next action. It is preferable to `Thread.sleep()` because it only applies to the specific interaction sequence.'},
{id:4147,topic:'selenium',difficulty:'intermediate',question:'What is the difference between `driver.close()` leaving multiple windows open and what happens to the Grid session?',options:['close() ends the Grid session','If multiple windows are open and driver.close() closes only the active one, the Grid session remains active. Only driver.quit() terminates the session and frees the Grid node slot back to the pool','close() always ends the session','Grid sessions must be quit() within 10 seconds'],answer:1,explanation:'On Selenium Grid, each `RemoteWebDriver` session occupies a slot on a Node. `close()` only closes a window but keeps the session alive and the slot occupied. `quit()` ends the session and releases the slot. Failing to call `quit()` causes session leaks that exhaust Grid capacity.'},
{id:4148,topic:'selenium',difficulty:'advanced',question:'What is the DesiredCapabilities class and why is it deprecated in Selenium 4?',options:['It is not deprecated','DesiredCapabilities provided a generic Map-based capability configuration that was error-prone (runtime errors on typos, no IDE autocomplete). Selenium 4 replaced it with type-safe browser Options classes (ChromeOptions, FirefoxOptions) which provide compile-time safety and browser-specific methods','It was replaced by a JSON file','It was replaced by annotations'],answer:1,explanation:'`DesiredCapabilities caps = new DesiredCapabilities(); caps.setCapability("browserName", "chrome");` — all capabilities were stringly-typed. Selenium 4\'s `ChromeOptions`, `FirefoxOptions`, etc. provide typed methods with IDE auto-completion, reducing configuration errors and improving code clarity.'},
{id:4149,topic:'selenium',difficulty:'intermediate',question:'What does the `--node-max-sessions` flag do when starting a Selenium Grid 4 Node?',options:['Sets the number of Grid hubs','Limits the maximum number of browser sessions that can run simultaneously on the Node — when reached, new session requests are queued by the Distributor until a slot opens','Sets session timeout','Configures browser version'],answer:1,explanation:'`--max-sessions` (Grid 4 flag) controls the concurrency level per Node. Setting it to 1 means the Node handles only one browser at a time. Setting it higher allows parallel execution on the same machine, subject to CPU/RAM constraints. Default is auto-detected from CPU core count.'},
{id:4150,topic:'selenium',difficulty:'advanced',question:'What is the purpose of `driver.manage().timeouts().getImplicitWaitTimeout()` in Selenium 4?',options:['Sets the implicit wait','Returns the currently configured implicit wait Duration, useful for verifying the wait configuration in test utilities or for temporarily adjusting and restoring implicit wait values','Resets timeouts to default','Returns the page load timeout'],answer:1,explanation:'Selenium 4 added getter methods for all timeout configurations. `getImplicitWaitTimeout()` returns a `Duration` representing the current implicit wait setting. Similarly, `getPageLoadTimeout()` and `getScriptTimeout()` allow reading the configured timeout values for inspection or conditional logic.'},
{id:4151,topic:'selenium',difficulty:'intermediate',question:'What is `driver.manage().window().minimize()` used for in Selenium 4?',options:['Hides the browser from the screen','Minimizes the browser window to the taskbar — a W3C WebDriver command added in Selenium 4 — useful for test environments where window focus affects test behavior','Takes a screenshot','Closes the window'],answer:1,explanation:'`minimize()` is a Selenium 4 addition implementing the W3C minimize window command. It sends the browser to the system taskbar. While tests typically run better maximized, minimize is useful for managing test environment windows during long parallel test runs on display servers.'},
{id:4152,topic:'selenium',difficulty:'advanced',question:'When connecting RemoteWebDriver to Selenium Grid 4, what sets it apart from connecting to Grid 3?',options:['RemoteWebDriver API changed completely','The URL endpoint changed from http://hub:4444/wd/hub to http://hub:4444 in Grid 4. Additionally, Grid 4 accepts W3C-compliant browser Options objects directly rather than DesiredCapabilities, and supports CDP relay for debugging remote browser sessions','RemoteWebDriver is not used in Grid 4','Grid 4 requires a special RemoteDriver class'],answer:1,explanation:'Grid 4 migration: change endpoint URL from `/wd/hub` to just the root, replace `DesiredCapabilities` with browser Options. Grid 4 also enables CDP relay, allowing Chrome DevTools Protocol commands to be executed on Grid-managed remote browser instances — a capability Grid 3 lacked.'},
{id:4153,topic:'selenium',difficulty:'intermediate',question:'What does `driver.manage().logs().get(LogType.BROWSER)` retrieve in Selenium?',options:['Server-side Grid logs','Browser console logs (JavaScript console.log, errors, warnings) from the browser session — available as a LogEntries object containing LogEntry items with level, timestamp, and message','Network request logs','Test execution logs'],answer:1,explanation:'`driver.manage().logs().get(LogType.BROWSER)` returns browser console log entries. Each `LogEntry` has `getLevel()` (SEVERE/WARNING/INFO), `getTimestamp()`, and `getMessage()`. This is useful for detecting JavaScript errors during test execution. Note: support varies by browser (Chrome supports it best).'},
{id:4154,topic:'selenium',difficulty:'advanced',question:'What is the `LogType.PERFORMANCE` logging capability in Selenium?',options:['Measures test execution time','When configured in ChromeOptions (setCapability("goog:loggingPrefs", ...)), captures performance logs including network events, rendering metrics, and timeline data from Chrome\'s DevTools performance timeline — used for performance analysis in functional tests','Logs Grid performance metrics','Logs implicit wait timing'],answer:1,explanation:'`ChromeOptions opts = new ChromeOptions(); LoggingPreferences prefs = new LoggingPreferences(); prefs.enable(LogType.PERFORMANCE, Level.ALL); opts.setCapability("goog:loggingPrefs", prefs);` — enables CDP performance logging. The logs can be parsed for network timing, paint events, and scripting metrics during test execution.'},
{id:4155,topic:'selenium',difficulty:'intermediate',question:'How do you configure a proxy in Selenium WebDriver?',options:['driver.setProxy(proxy)','Create a Proxy object, set its HTTP/HTTPS/SOCKS settings, then add it to browser Options via options.setProxy(proxy) or options.setCapability(CapabilityType.PROXY, proxy)','Add proxy to ChromeOptions arguments only','Use system properties only'],answer:1,explanation:'`Proxy proxy = new Proxy(); proxy.setHttpProxy("localhost:8080"); proxy.setSslProxy("localhost:8080"); ChromeOptions options = new ChromeOptions(); options.setProxy(proxy);` — this routes browser traffic through the specified proxy, enabling tools like BurpSuite or mitmproxy for security testing or network monitoring.'},
{id:4156,topic:'selenium',difficulty:'advanced',question:'What does `driver.manage().logs().getAvailableLogTypes()` return?',options:['A list of all test methods','A Set<String> of log types supported by the current browser/driver combination (e.g., "browser", "driver", "performance", "client") — availability varies by browser and driver version','All log entries','Browser capabilities'],answer:1,explanation:'`getAvailableLogTypes()` returns the log types enabled for the current session. ChromeDriver typically supports "browser", "driver", "performance", and "client". GeckoDriver has limited logging support. Always check availability before calling `logs().get()` to avoid exceptions on unsupported log types.'},
{id:4157,topic:'selenium',difficulty:'intermediate',question:'How do you configure a Grid 4 Node via TOML configuration file?',options:['Grid 4 does not support TOML','Use `java -jar selenium-server.jar node --config node.toml` where the TOML file specifies [node] section with detect-drivers, max-sessions, and session-timeout; [driver-configuration] for specific browser/driver pairs','Use XML configuration','Use JSON configuration only'],answer:1,explanation:'Grid 4 supports TOML configuration: `[node]\\ndetect-drivers = true\\nmax-sessions = 5\\n[driver-configuration]\\n[[driver-configuration.configs]]\\nbrowser-name = "chrome"\\nmax-sessions = 3`. TOML files are version-controlled and more maintainable than long command-line flags for complex Grid setups.'},
{id:4158,topic:'selenium',difficulty:'advanced',question:'What is the Grid 4 Observability feature?',options:['A test result viewer','Grid 4 exposes Prometheus-compatible metrics endpoints for monitoring session counts, queue depth, and node health, plus OpenTelemetry tracing for distributed diagnostics — enabling integration with monitoring stacks like Grafana/Prometheus','A built-in test reporter','A capability for screenshot capture'],answer:1,explanation:'Grid 4 includes built-in observability: metrics at `/metrics` in OpenMetrics format for Prometheus scraping, and distributed tracing via OpenTelemetry. Operators can track active sessions, pending queue size, node availability, and request latency — critical for production Grid monitoring.'},
{id:4159,topic:'selenium',difficulty:'intermediate',question:'What is the purpose of `ExpectedConditions.textToBePresentInElementLocated(by, text)`?',options:['Checks if a cookie has specific text','Waits until the visible text of the element located by the given By contains the specified text substring','Waits for the page title to contain text','Waits for an alert containing text'],answer:1,explanation:'`textToBePresentInElementLocated(By, "expectedText")` is used to wait for dynamic text updates. For example, waiting for a status label to change from "Processing..." to "Complete". It polls until the element\'s `getText()` contains the expected string.'},
{id:4160,topic:'selenium',difficulty:'advanced',question:'What is the key advantage of Selenium Grid 4 over Grid 3 for CDP support?',options:['Grid 4 supports more browsers','Grid 4 nodes act as CDP relay endpoints — enabling `driver.getDevTools()` to work with RemoteWebDriver sessions, allowing Chrome DevTools Protocol commands (network interception, console monitoring) on remote grid-managed browsers, which was impossible in Grid 3','Grid 4 is faster','Grid 4 requires less memory'],answer:1,explanation:'Grid 4 proxies CDP WebSocket connections from the RemoteWebDriver client through the Hub to the Node\'s browser. This means `((HasDevTools) remoteDriver).getDevTools()` works for remote sessions — enabling network interception, console access, and CDP-based testing capabilities on distributed Grid infrastructure.'},
{id:4161,topic:'selenium',difficulty:'intermediate',question:'How do you handle multiple browser windows opened by a test using Selenium?',options:['Windows are handled automatically','Store the original window handle, perform the action that opens new windows, iterate driver.getWindowHandles() to find new handles, switch to each as needed, and switch back to original when done using driver.switchTo().window(originalHandle)','Use driver.getWindowHandles()[1] directly','Windows auto-close after each test'],answer:1,explanation:'Window handle management requires explicit tracking. The original handle from `getWindowHandle()` before the action acts as an anchor. Iterating `getWindowHandles()` after the action reveals new handles. Always switch back to the original handle when finished with the auxiliary window.'},
{id:4162,topic:'selenium',difficulty:'advanced',question:'In Selenium Grid 4, what role does the Event Bus play?',options:['Displays test events in the UI','The Event Bus is an internal async messaging channel used by Grid components (Distributor, Session Map, Nodes) to communicate state changes (node registration, session creation, session release) without tight coupling — enabling the distributed architecture','Sends email notifications on test failures','Logs browser console events'],answer:1,explanation:'The Event Bus (implemented with Netty\'s event loop) decouples Grid components. When a Node registers, it publishes to the Event Bus. The Distributor subscribes and updates its routing table. When a session ends, the event propagates so the Session Map is updated. This pub-sub model enables the fully distributed Grid 4 topology.'},
{id:4163,topic:'selenium',difficulty:'intermediate',question:'What does `driver.manage().deleteAllCookies()` NOT delete?',options:['Session cookies','LocalStorage and SessionStorage data, IndexedDB, and Web Storage — it only deletes HTTP cookies visible to the browser. Clear local/session storage with JavaScript: executeScript("window.localStorage.clear()")','Persistent cookies','Third-party cookies'],answer:1,explanation:'`deleteAllCookies()` only removes cookies — not Web Storage (localStorage/sessionStorage), IndexedDB, or the Cache API. For comprehensive state cleanup between tests, additionally execute `window.localStorage.clear()`, `window.sessionStorage.clear()`, and `indexedDB` clearing scripts as needed.'},
{id:4164,topic:'selenium',difficulty:'intermediate',question:'What does `driver.manage().window().getPosition()` return?',options:['The element position','A Point object with the x and y screen coordinates of the top-left corner of the browser window','The browser viewport size','The mouse cursor position'],answer:1,explanation:'`getPosition()` returns a `Point` with `x` (pixels from left of screen) and `y` (pixels from top of screen) of the browser window\'s top-left corner. `setPosition(new Point(0,0))` moves the window. Used for multi-monitor setups or precise window placement in test environments.'},
{id:4165,topic:'selenium',difficulty:'intermediate',question:'What does `new Select(dropdown).getAllSelectedOptions()` return for a single-select dropdown with one selected option?',options:['An empty list','A List<WebElement> containing only the one currently selected option — same as getFirstSelectedOption() but wrapped in a list, consistent with the multi-select API','All options in the dropdown','null'],answer:1,explanation:'`getAllSelectedOptions()` returns a `List<WebElement>` of all currently selected `<option>` elements. For single-select dropdowns, this list has exactly one element. For multi-select dropdowns, it contains all checked options. This uniform return type simplifies code that works with both types.'},
{id:4166,topic:'selenium',difficulty:'advanced',question:'What is the significance of the `goog:chromeOptions` capability key in Grid 4 remote sessions?',options:['It is outdated','Chrome-specific options (arguments, prefs, extensions) must be sent under the "goog:chromeOptions" key in W3C capabilities when connecting to Grid. ChromeOptions.merge() with W3C capabilities ensures proper serialization, as Grid 4 strictly enforces W3C format and rejects non-standard keys','It configures the Grid node','It is only for mobile Chrome'],answer:1,explanation:'W3C capabilities have a standard section and a browser-specific extension section. Chrome extensions use the `goog:chromeOptions` vendor prefix. When creating ChromeOptions in Selenium 4, the library automatically serializes correctly. But when constructing raw capability maps, using the correct vendor prefix is essential for Grid 4 session creation.'},
{id:4167,topic:'selenium',difficulty:'intermediate',question:'What is the purpose of `driver.manage().timeouts().pageLoadTimeout(Duration.ofSeconds(0))`?',options:['Disables all timeouts','Sets the page load timeout to 0ms, causing WebDriver to return immediately after initiating navigation without waiting for the page load to complete — equivalent to the "none" page load strategy','Makes page load blocking','Throws an error'],answer:1,explanation:'Setting `pageLoadTimeout` to 0 causes `driver.get()` to return as soon as the navigation command is issued, before the page finishes loading. This is similar to the `none` page load strategy. Useful for pages that never fully load (infinite spinner) but have navigated to the expected content.'},
{id:4168,topic:'selenium',difficulty:'advanced',question:'How does Selenium Grid 4\'s Session Queue handle timeouts for waiting sessions?',options:['Waiting sessions never timeout','Session requests that wait in the queue beyond the configured --session-request-timeout (default 300 seconds) are rejected with SessionNotCreatedException, preventing indefinitely hanging test sessions','Sessions timeout after 30 seconds always','The queue has no timeout'],answer:1,explanation:'`--session-request-timeout` (in seconds) configures how long a queued session request waits before being rejected. If no Node becomes available within this window, the requesting client receives a `SessionNotCreatedException`. This prevents test clients from hanging indefinitely when the Grid is overloaded.'},
{id:4169,topic:'selenium',difficulty:'intermediate',question:'In Selenium Grid 4 Hub-Node topology, what component does the client connect to?',options:['Directly to the Node','The Hub\'s Router, which then routes the session request to an available Node via the Distributor, transparent to the client','The Session Queue directly','The Event Bus'],answer:1,explanation:'The client connects to the Hub\'s address (Router). The Router receives the session request, forwards it to the Distributor, which finds an available Node. The session is then created on the Node, and the Router maps subsequent WebDriver commands to the correct Node via the Session Map.'},
{id:4170,topic:'selenium',difficulty:'intermediate',question:'What does `((HasAuthentication) driver).register(credentials)` do in Selenium 4?',options:['Handles CAPTCHA','Registers HTTP Basic Authentication credentials that are automatically sent when the browser encounters authentication challenges — a Selenium 4 feature using CDP, replacing the need for URL-embedded credentials','Handles cookie-based auth','Registers OAuth tokens'],answer:1,explanation:'Selenium 4 added `HasAuthentication` (via CDP) for handling HTTP Basic Auth prompts natively. `driver.register(UsernameAndPassword.of("user","pass"))` makes the browser automatically respond to `401 Unauthorized` challenges without triggering the native authentication dialog that WebDriver cannot interact with.'},
{id:4171,topic:'selenium',difficulty:'intermediate',question:'What is a Selenium Grid 4 standalone mode good for?',options:['Large-scale parallel test execution across dozens of machines','Single-machine development and debugging, small teams, and CI pipelines where simplicity is preferred over scalability — all Grid 4 components run in one process, accessible at http://localhost:4444','Running tests on cloud providers','Load testing websites'],answer:1,explanation:'Standalone mode is ideal for local development, single-machine CI, and simple setups. It launches the Router, Distributor, Session Map, Session Queue, Event Bus, and Node in a single JVM. Scaling to multiple machines requires Hub-Node or distributed mode.'},
{id:4172,topic:'selenium',difficulty:'advanced',question:'What is the `selenium.grid.auth` feature for securing a Grid 4 instance?',options:['Grid 4 cannot be secured','Grid 4 supports Basic Authentication for the Grid UI and session endpoints via `--username` and `--password` flags (or `--config` TOML). Clients must include BasicAuth credentials in their RemoteWebDriver URL or session request headers','Grid uses OAuth only','Grid security requires a reverse proxy always'],answer:1,explanation:'`java -jar selenium-server.jar hub --username admin --password secret` enables Grid authentication. Clients use `new URL("http://admin:secret@hub:4444")` in RemoteWebDriver. This prevents unauthorized use of the Grid. In production, combine with TLS/HTTPS for secure transmission of credentials.'},
{id:4173,topic:'selenium',difficulty:'intermediate',question:'How do you retrieve all `<option>` texts from a Select dropdown using Selenium?',options:['select.getOptions() already returns Strings','select.getOptions().stream().map(WebElement::getText).collect(Collectors.toList()) — getOptions() returns WebElements, so getText() must be called on each','select.getOptionTexts()','select.values()'],answer:1,explanation:'`select.getOptions()` returns `List<WebElement>`. To get the text of each option, use Java streams: `list.stream().map(WebElement::getText).collect(Collectors.toList())`. This pattern collects all visible option texts for assertions like `assertTrue(texts.contains("Expected Option"))`.'},
{id:4174,topic:'selenium',difficulty:'advanced',question:'What is the `--bind-host` flag used for in Selenium Grid 4?',options:['Sets the browser hostname','Specifies the network interface address the Grid component should bind to — important in multi-NIC machines where Grid must listen on a specific IP address (e.g., --bind-host false for container environments)','Configures SSL certificates','Sets the Grid external URL'],answer:1,explanation:'`--bind-host false` is commonly needed in Docker containers where the hostname resolution behaves differently. `--host` specifies the hostname/IP to advertise. `--bind-host` controls the network interface binding. Misconfiguration causes nodes to register with unreachable addresses.'},
{id:4175,topic:'selenium',difficulty:'intermediate',question:'What does `driver.manage().logs().get(LogType.DRIVER)` retrieve?',options:['Browser console logs','Logs produced by the browser driver (e.g., ChromeDriver, GeckoDriver) itself — including driver startup, command processing, and internal driver errors — useful for debugging driver-level communication issues','Grid server logs','Test execution logs'],answer:1,explanation:'`LogType.DRIVER` retrieves logs from the driver binary itself, not the browser. These logs include the raw W3C protocol commands being sent and received, making them invaluable for debugging "why is WebDriver doing X when I expected Y". Verbosity is controlled by driver logging level flags.'},
{id:4176,topic:'selenium',difficulty:'advanced',question:'What is the `--session-retry-interval` configuration in Selenium Grid 4?',options:['How long to wait before retrying a failed test','The interval (in milliseconds) at which the Distributor retries assigning a queued session request to a Node when the previous attempt failed — controls how aggressively the Distributor polls for available slots','The implicit wait equivalent','The interval for Node health checks'],answer:1,explanation:'`--session-retry-interval` (default 5000ms) determines how frequently the Distributor attempts to match queued session requests to available Nodes. Lower values reduce session wait latency when Nodes become available but increase Distributor CPU load. Higher values reduce overhead in large Grids.'},
{id:4177,topic:'selenium',difficulty:'intermediate',question:'What is `driver.manage().window().setPosition(new Point(0,0))` used for?',options:['Sets the viewport scroll position','Moves the top-left corner of the browser window to screen coordinates (0,0) — useful in multi-monitor testing to position the browser on a specific monitor or to ensure the window is not off-screen','Centers the window','Sets mouse position'],answer:1,explanation:'`setPosition(new Point(x, y))` controls where the browser window appears on screen. On multi-monitor setups, negative x/y values or values exceeding primary monitor resolution position windows on secondary monitors. This ensures the test browser appears on the expected display.'},
{id:4178,topic:'selenium',difficulty:'advanced',question:'In Selenium Grid 4, what is the Node\'s role in terms of sessions it can detect automatically?',options:['Nodes require manual browser configuration only','When `--detect-drivers true` (default), the Node scans the system PATH for installed browser binaries and their corresponding drivers, automatically registering the available browser/version/driver combinations without manual configuration','Nodes only support Chrome','Nodes require TOML configuration always'],answer:1,explanation:'Auto-detection simplifies Node setup. If Chrome, Firefox, and their drivers are on the PATH, the Node registers all three browser stereotypes automatically. Manual overrides via `--driver-configuration` or TOML files allow precise control over which browsers are exposed and how many slots each gets.'},
{id:4179,topic:'selenium',difficulty:'intermediate',question:'What is the Selenium Grid 4 UI accessible at by default?',options:['http://localhost:4444/grid/console','http://localhost:4444/ui — the new Grid 4 web UI showing real-time node status, active sessions, and queue depth','http://localhost:4444/status','http://localhost:4444/dashboard'],answer:1,explanation:'Grid 4 provides a real-time UI at `/ui` (e.g., `http://localhost:4444/ui`). It displays registered Nodes with their browser capabilities, currently active sessions with session details, and queue statistics. Grid 3\'s console was at `/grid/console`. This is a commonly asked upgrade difference.'},
{id:4180,topic:'selenium',difficulty:'advanced',question:'What does `((RemoteWebDriver) driver).getCapabilities()` return after session creation?',options:['The requested capabilities','The actual negotiated capabilities of the browser session — reflecting the actual browser version, platform, and options the driver server agreed to, which may differ from the originally requested capabilities','The Grid node capabilities','The driver binary version only'],answer:1,explanation:'After creating a RemoteWebDriver session, `getCapabilities()` returns the `Capabilities` object representing what the driver server actually provided, including `browserName`, `browserVersion`, `platformName`, and vendor-specific values. This is useful for logging the actual browser version used in distributed/cloud test executions.'},
]);
