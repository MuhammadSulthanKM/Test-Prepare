window.QUIZ_BANK['selenium'] = (window.QUIZ_BANK['selenium']||[]).concat([
{id:4081,topic:'selenium',difficulty:'intermediate',question:'In JUnit 5 with Selenium WebDriver, what is the best lifecycle annotation for initializing and quitting a browser instance per test?',options:['@BeforeAll for driver setup and @AfterAll for driver teardown across all tests','@BeforeEach for driver initialization and @AfterEach for driver.quit() to ensure test isolation and a fresh browser per test method','@BeforeEach with driver.close() and @AfterEach with driver.init()','@Test with inline try-finally blocks only'],answer:1,explanation:'Using @BeforeEach to initialize WebDriver and @AfterEach to invoke driver.quit() guarantees each test executes in an isolated browser session without state leakage from previous tests. Placing driver.quit() in @AfterEach also ensures browser cleanup even if test assertions fail.'},
{id:4082,topic:'selenium',difficulty:'intermediate',question:'How can custom lifecycle logic like automatic screenshots on test failure be attached to JUnit 5 Selenium tests?',options:['By extending TestNG\'s TestListenerAdapter','By registering a custom Extension implementing TestExecutionExceptionHandler or AfterTestExecutionCallback via @ExtendWith(SeleniumWatcherExtension.class)','By overriding the Object.finalize() method in BaseTest','By annotating every test method with @Rule'],answer:1,explanation:'JUnit 5 uses the Extension model (@ExtendWith) instead of JUnit 4 @Rule. Implementing AfterTestExecutionCallback allows checking ExtensionContext.getExecutionException() to detect failures and capture screenshots via TakesScreenshot before the driver is terminated in @AfterEach.'},
{id:4083,topic:'selenium',difficulty:'intermediate',question:'How do you implement cross-browser testing using JUnit 5 Parameterized Tests in Selenium?',options:['Using @DataProvider with TestNG','Using @ParameterizedTest with @ValueSource(strings = {"chrome", "firefox", "edge"}) and passing the browser name as a parameter to the test method','Using @RepeatedTest(3) and switching on random browser','Using @EnumSource with only BrowserType enum from Selenium 2'],answer:1,explanation:'@ParameterizedTest combined with @ValueSource or @MethodSource allows running the same Selenium test logic across multiple browser types. The test method accepts a String or Browser enum argument, and the driver factory instantiates ChromeDriver, FirefoxDriver, or EdgeDriver accordingly.'},
{id:4084,topic:'selenium',difficulty:'beginner',question:'What is the primary difference between @BeforeAll and @BeforeEach when managing WebDriver in JUnit 5?',options:['@BeforeAll is non-static and runs before every test; @BeforeEach is static and runs once','@BeforeAll must be static (unless PER_CLASS lifecycle is used) and runs once per class sharing one browser session; @BeforeEach runs before every single test method','@BeforeEach cannot instantiate WebDriver instances','@BeforeAll only works with headless browsers'],answer:1,explanation:'In JUnit 5, @BeforeAll runs once before all tests and requires a static method by default, meaning all test methods share a single browser instance. @BeforeEach runs before every test method, allowing each test to start with a fresh, clean WebDriver instance and independent cookies.'},
{id:4085,topic:'selenium',difficulty:'intermediate',question:'In Cucumber BDD with Selenium, how should the WebDriver instance typically be shared across multiple Step Definition classes?',options:['Using a global public static WebDriver variable in every class','Using Dependency Injection (such as PicoContainer, Spring, or Guice) to inject a shared TestContext or WebDriverManager instance into step definition constructors','By passing WebDriver as a step argument in the Gherkin feature file','By recreating a new ChromeDriver instance inside every Given, When, and Then step'],answer:1,explanation:'Cucumber supports Dependency Injection frameworks like cucumber-picocontainer. PicoContainer instantiates step definition classes per scenario and injects the same instance of a shared state class (or WebDriver instance), avoiding fragile global static variables and supporting parallel test runs safely.'},
{id:4086,topic:'selenium',difficulty:'beginner',question:'In Cucumber BDD with Selenium, where is the ideal place to launch and terminate the WebDriver browser session?',options:['Inside the @Given step and @Then step of every feature file','In Cucumber @Before and @After scenario lifecycle hooks, ensuring proper browser setup and driver.quit() teardown for every scenario','Inside the TestRunner main method','Directly inside the page object constructor'],answer:1,explanation:'Cucumber @Before and @After hooks (from io.cucumber.java) run before and after each scenario. Initializing WebDriver in @Before and invoking driver.quit() in @After guarantees proper browser lifecycle management and cleanup even if a step fails or is skipped.'},
{id:4087,topic:'selenium',difficulty:'intermediate',question:'How does a Cucumber step definition call a Selenium Page Object Model method?',options:['By embedding raw JavaScript directly into the .feature file','The step definition instantiates or receives the Page Object and calls descriptive action or verification methods on it (e.g., loginPage.login(user, pass))','By calling driver.executeScript("gherkin_step")','Cucumber step definitions cannot interact with Page Object classes'],answer:1,explanation:'Step definitions act as the glue between Gherkin sentences and underlying test automation code. A step definition method parses Gherkin arguments, invokes high-level domain methods on Page Objects (e.g., loginPage.enterCredentials(u, p)), and performs assertions on returned page state.'},
{id:4088,topic:'selenium',difficulty:'advanced',question:'How do you embed screenshots directly into Cucumber HTML reports upon scenario failure in Selenium?',options:['By saving a .png file to the desktop','By checking scenario.isFailed() in a Cucumber @After hook and calling scenario.attach(((TakesScreenshot) driver).getScreenshotAs(OutputType.BYTES), "image/png", "Failure Screenshot")','By calling driver.captureScreenshotAsCucumber()','By configuring log4j to log binary image data'],answer:1,explanation:'In Cucumber-JVM, the io.cucumber.java.Scenario object passed to @After hooks provides the attach() method. Checking scenario.isFailed() allows capturing screenshot bytes via TakesScreenshot and attaching them with MIME type "image/png" directly into standard Cucumber HTML/JSON reports.'},
{id:4089,topic:'selenium',difficulty:'intermediate',question:'How is parallel execution configured for Selenium tests using Maven Surefire plugin with JUnit 5 or TestNG?',options:['By setting <jvmArgs>-Xmx2g</jvmArgs> in pom.xml','By configuring <forkCount> and <reuseForks> or setting <parallel>methods</parallel> and <threadCount> in the maven-surefire-plugin configuration in pom.xml','By running multiple instances of IntelliJ IDEA simultaneously','By setting browser.parallel=true in ChromeOptions'],answer:1,explanation:'Maven Surefire plugin enables process-level parallelism using <forkCount>N</forkCount> (running multiple JVM processes) or thread-level parallelism via properties like <parallel>classes</parallel> or <parallel>methods</parallel> with <threadCount>. This allows distributing Selenium test execution across available CPU cores.'},
{id:4090,topic:'selenium',difficulty:'advanced',question:'Which JUnit 5 configuration properties file settings enable parallel test method execution for Selenium suites?',options:['junit.jupiter.execution.parallel.enabled = true and junit.jupiter.execution.parallel.mode.default = concurrent','junit.parallel = on and junit.threads = 4','selenium.threads.parallel = true','jupiter.concurrency.active = true'],answer:0,explanation:'In src/test/resources/junit-platform.properties, configuring junit.jupiter.execution.parallel.enabled=true and junit.jupiter.execution.parallel.mode.default=concurrent allows JUnit 5 to run test classes and methods concurrently. When combined with ThreadLocal<WebDriver>, each thread manages its own isolated browser.'},
{id:4091,topic:'selenium',difficulty:'intermediate',question:'When running Selenium tests in parallel with JUnit 5, what is the best practice for managing the WebDriver instance across threads?',options:['Using a static WebDriver instance shared across all classes','Wrapping WebDriver inside a ThreadLocal<WebDriver> container or using per-method instance variables with JUnit 5 default lifecycle','Using a single singleton WebDriver instance with synchronized methods','Writing test results to a shared text file'],answer:1,explanation:'Parallel test execution requires that each concurrent thread has its own isolated WebDriver instance. ThreadLocal<WebDriver> ensures thread-safe access so driver commands from one test thread do not collide with another browser session running concurrently.'},
{id:4092,topic:'selenium',difficulty:'intermediate',question:'How are test screenshots and execution steps attached to Allure Reports in a Selenium Java framework?',options:['By printing Base64 strings to System.out','By annotating helper methods with @Attachment(value = "Screenshot", type = "image/png") returning byte[] or using Allure.addAttachment()','By creating an allure.xml file manually after each test run','By using @AllureScreenshot on every test class'],answer:1,explanation:'Allure Java library provides the @Attachment annotation and the static Allure.addAttachment(name, inputStream/bytes) API. By returning ((TakesScreenshot)driver).getScreenshotAs(OutputType.BYTES), images or page source strings are seamlessly integrated into the Allure report dashboard.'},
{id:4093,topic:'selenium',difficulty:'intermediate',question:'What is the purpose of the @Step annotation from Allure in a Selenium Page Object framework?',options:['It defines a JUnit test step order','It logs human-readable action descriptions with parameter values in the Allure report timeline whenever the annotated Page Object method is executed','It instructs WebDriver to wait 1 second before executing the step','It runs the method repeatedly until it passes'],answer:1,explanation:'Allure\'s @Step("User logs in with username {0}") annotation marks methods in Page Objects or helper classes. When invoked, Allure records the step name and interpolated argument values into the report tree, producing granular, step-by-step visual documentation of test execution.'},
{id:4094,topic:'selenium',difficulty:'intermediate',question:'How does ExtentReports integrate with Selenium WebDriver to generate interactive HTML execution reports?',options:['By replacing WebDriver with ExtentDriver','By using ExtentReports and ExtentTest objects with test listeners (TestNG/JUnit) to log test status, steps, and base64/file screenshots via MediaEntityBuilder','By exporting browser cookies as HTML tables','By parsing ChromeDriver console logs after test completion'],answer:1,explanation:'ExtentReports uses ExtentSparkReporter to build HTML reports. An ExtentTest instance is created for each test method. In test listeners or step hooks, calls like extentTest.pass("Step passed") or extentTest.fail(MediaEntityBuilder.createScreenCaptureFromBase64String(...).build()) record real-time test outcomes and screenshots.'},
{id:4095,topic:'selenium',difficulty:'intermediate',question:'How do you ensure thread-safety when logging to ExtentReports during parallel Selenium test execution?',options:['ExtentReports is natively thread-safe without any setup','By storing the ExtentTest instance inside a ThreadLocal<ExtentTest> variable to prevent concurrent threads from overwriting each other\'s test logs','By using a single global ExtentTest reference across all threads','By synchronizing the entire test execution method'],answer:1,explanation:'In parallel test execution, multiple threads invoke logging calls simultaneously. Maintaining a ThreadLocal<ExtentTest> ensures each thread writes logs and captures screenshots exclusively to its corresponding test node in the report without concurrency issues or interleaved logs.'},
{id:4096,topic:'selenium',difficulty:'beginner',question:'What is the typical role of a ConfigReader class in a Selenium test automation framework?',options:['It downloads browser drivers from GitHub','It reads key-value pairs (such as base URL, browser type, timeouts, and credentials) from properties or YAML files into memory for centralized configuration','It compiles Java classes at runtime','It parses HTML reports into PDF format'],answer:1,explanation:'A ConfigReader utility loads configuration parameters from external files (e.g., config.properties or config.yaml) using java.util.Properties. This decouples test environment settings (URLs, timeouts, browser choices) from test code, enabling flexible multi-environment execution.'},
{id:4097,topic:'selenium',difficulty:'intermediate',question:'In a Data-Driven Selenium framework, how does a TestNG @DataProvider or JUnit 5 @MethodSource supply test data to a test method?',options:['By hardcoding values inside the test assertion','By reading data from Excel (via Apache POI), CSV, or JSON and returning a 2D Object array (Object[][]) or Stream<Arguments> consumed by the test method parameters','By setting environment variables on the OS before each step','By modifying the DOM directly via JavascriptExecutor'],answer:1,explanation:'Data Providers supply parameter sets to test methods. @DataProvider returns Object[][] where each row corresponds to one test execution iteration. When paired with Apache POI for Excel or Jackson for JSON, tests run iteratively with distinct test dataset inputs.'},
{id:4098,topic:'selenium',difficulty:'intermediate',question:'Why does Selenium\'s standard `Select` class fail when interacting with dynamic dropdowns built with React, Angular, or Vue?',options:['Because modern frameworks only support Python WebDriver bindings','Because modern UI frameworks build custom dropdowns using <div>, <ul>, and <li> elements rather than native HTML <select> tags, so Select throws UnexpectedTagNameException','Because dynamic dropdowns are always hidden in shadow DOM','Because Select only works in headless browser mode'],answer:1,explanation:'The Selenium Select class strictly requires a native HTML <select> element. Modern front-end frameworks render custom dropdown menus using <div>, <span>, and <ul>/<li> elements. Attempting new Select(element) on a non-<select> tag throws an UnexpectedTagNameException.'},
{id:4099,topic:'selenium',difficulty:'intermediate',question:'What is the recommended Selenium pattern to select an item from a custom dynamic <div>-based dropdown?',options:['Use new Select(driver.findElement(By.id("dropdown"))).selectByVisibleText("Option")','Click the dropdown container to expand options, wait for options to be visible, locate matching <li> or <div> by text or CSS, and click the desired option','Use driver.switchTo().alert().sendKeys("Option")','Refresh the browser page until the option is pre-selected'],answer:1,explanation:'To handle custom JS dropdowns: 1) Click the dropdown trigger element to render the options list. 2) Use WebDriverWait with ExpectedConditions.visibilityOfElementLocated for the options container. 3) Find and click the target option element using XPath text matching or CSS selectors.'},
{id:4100,topic:'selenium',difficulty:'intermediate',question:'How should a Selenium test handle dynamic search autocomplete / suggestion dropdown fields?',options:['By setting driver.manage().timeouts().implicitlyWait(Duration.ofMinutes(1))','By typing the search query via sendKeys(), waiting for suggestion elements to become visible, and clicking the matching suggestion item from the list','By using Select.selectByIndex(0) on the input field','By taking a screenshot and verifying the pixel color'],answer:1,explanation:'Autocomplete fields trigger asynchronous AJAX/API requests as you type. The test must sendKeys() with the search term, wait explicitly for the suggestion dropdown list to populate in the DOM and become visible, iterate through or locate the matching element, and click it.'},
{id:4101,topic:'selenium',difficulty:'intermediate',question:'When automating an autocomplete input that opens suggestions only on typing, what issue often occurs if sendKeys() is too fast, and how is it resolved?',options:['The browser crashes immediately','Backend debouncing / keyup listeners might miss fast typing; sending keys character by character with slight delays or using Actions.sendKeys() ensures events fire properly','Selenium throws UnhandledAlertException','The input converts all characters to uppercase'],answer:1,explanation:'Many autocomplete fields use JavaScript debounce timers on keyup/input events. Rapid sendKeys() might send the entire string before the debounce triggers. Sending keystrokes individually or triggering an input event via JavascriptExecutor ensures the suggestion list fetches and renders correctly.'},
{id:4102,topic:'selenium',difficulty:'intermediate',question:'What is the most robust strategy for automating modern dynamic date picker widgets in Selenium?',options:['Clicking next month arrows in an infinite loop without exit conditions','If the date input field is writable, use clear() and sendKeys("YYYY-MM-DD"); otherwise, navigate month/year controls using explicit waits and click the specific target day element','Executing a system shell command to change OS calendar date','Date pickers cannot be automated with Selenium WebDriver'],answer:1,explanation:'Many date pickers allow direct text input into the underlying input element after clicking or clearing. For non-writable calendar popups, automate navigation controls (next/prev month buttons) to reach the target month/year, then locate and click the specific day cell (<td> or <div>) via explicit waits.'},
{id:4103,topic:'selenium',difficulty:'intermediate',question:'If a date picker input field has a `readonly` attribute preventing `sendKeys()`, how can Selenium set the date directly via JavaScript?',options:['driver.navigate().to("date://2025-01-01")','((JavascriptExecutor) driver).executeScript("arguments[0].removeAttribute(\'readonly\'); arguments[0].value=\'2025-01-01\'; arguments[0].dispatchEvent(new Event(\'change\'));", dateInput);','driver.switchTo().frame(dateInput)','dateInput.sendKeys(Keys.CONTROL + "v")'],answer:1,explanation:'Using JavascriptExecutor, you can remove the readonly attribute or assign element.value directly. Dispatching a "change" or "input" event ensures framework state listeners (such as in React or Angular) register the updated value properly.'},
{id:4104,topic:'selenium',difficulty:'intermediate',question:'Why do tests encounter `ElementClickInterceptedException` when interacting with pages that display loading spinners or progress overlays?',options:['Because the element does not exist in the HTML source','Because an active overlay or loading spinner layer sits on top of the target element in the z-index stack, intercepting the physical click event','Because the browser window is minimized','Because Selenium lacks network permissions'],answer:1,explanation:'ElementClickInterceptedException is thrown when the target element is in the DOM, but another element (such as a modal backdrop, semi-transparent overlay, or loading spinner) visually obscures it and receives the click event instead.'},
{id:4105,topic:'selenium',difficulty:'intermediate',question:'What is the correct explicit wait condition to handle loading spinners and backdrop overlays before clicking an underlying element?',options:['WebDriverWait.until(ExpectedConditions.invisibilityOfElementLocated(By.cssSelector(".spinner, .loading-overlay")))','WebDriverWait.until(ExpectedConditions.alertIsPresent())','driver.manage().timeouts().pageLoadTimeout(Duration.ofSeconds(5))','Thread.sleep(10000) inside every method'],answer:0,explanation:'ExpectedConditions.invisibilityOfElementLocated or invisibilityOf waits until the spinner element either disappears from the DOM or has its CSS display set to none / opacity to 0. Once invisible, the underlying clickable element is unobstructed.'},
{id:4106,topic:'selenium',difficulty:'advanced',question:'How can Selenium wait for jQuery AJAX requests to complete on pages utilizing jQuery?',options:['By waiting for document.readyState == "complete"','By executing JavaScript: new WebDriverWait(driver, timeout).until(d -> (Boolean)((JavascriptExecutor)d).executeScript("return window.jQuery != undefined && jQuery.active === 0"))','By calling driver.manage().timeouts().setScriptTimeout()','By verifying that network cookies are empty'],answer:1,explanation:'When jQuery is present, window.jQuery.active keeps count of currently active asynchronous AJAX requests. When jQuery.active === 0, all jQuery AJAX requests have completed. Combining this check with WebDriverWait ensures data has loaded before proceeding.'},
{id:4107,topic:'selenium',difficulty:'advanced',question:'How do you implement an explicit wait for network idle or fetch/XHR completion in modern JavaScript frameworks (vanilla JS/React) in Selenium?',options:['By setting browser cache to false','By injecting a script that overrides window.fetch and XMLHttpRequest to track active request counters, or waiting for specific DOM mutation indicators / API responses via CDP','By checking driver.getWindowHandles().size()','By using Thread.sleep(5000) after every click'],answer:1,explanation:'Modern single-page applications without jQuery require tracking active requests by monkey-patching window.fetch / XMLHttpRequest, listening to Chrome DevTools Protocol network events, or waiting for explicit UI state changes (like disappearance of loading skeletons or appearance of data containers).'},
{id:4108,topic:'selenium',difficulty:'intermediate',question:'What is Shadow DOM in modern web applications, and why do standard Selenium locators (like By.xpath) fail inside it?',options:['A dark mode browser setting','An encapsulated DOM tree attached to a custom web component element; standard Selenium XPath engine cannot pierce across shadow roots because Shadow DOM scopes styles and DOM trees privately','A backup DOM created when JavaScript crashes','A browser rendering cache stored on disk'],answer:1,explanation:'Shadow DOM encapsulates markup and styling within a separate shadow root attached to a host element. The W3C specification explicitly prevents XPath queries from traversing across shadow boundaries, causing standard By.xpath to fail when targeting shadow tree elements.'},
{id:4109,topic:'selenium',difficulty:'intermediate',question:'In Selenium 4 (Java), how do you access elements located inside an open Shadow DOM root directly using native WebDriver methods?',options:['driver.switchTo().shadowRoot()','WebElement host = driver.findElement(By.cssSelector("custom-element")); SearchContext shadowRoot = host.getShadowRoot(); WebElement target = shadowRoot.findElement(By.cssSelector(".inner-btn"));','driver.findElement(By.xpath("//custom-element//shadow//button"))','Select.getShadowRoot(driver)'],answer:1,explanation:'Selenium 4 introduced WebElement.getShadowRoot(), which returns a SearchContext interface. You can then invoke findElement(By.cssSelector(...)) directly on the shadow root SearchContext. Note that By.xpath is not supported on shadow roots per W3C specification.'},
{id:4110,topic:'selenium',difficulty:'advanced',question:'How can you interact with elements inside nested open Shadow DOM trees in Selenium using JavascriptExecutor?',options:['driver.navigate().to("shadow://nested")','((JavascriptExecutor) driver).executeScript("return document.querySelector(\'host-one\').shadowRoot.querySelector(\'host-two\').shadowRoot.querySelector(\'button\');")','driver.switchTo().parentFrame().findElement(By.id("btn"))','By setting ChromeOptions.addArguments("--disable-shadow-dom")'],answer:1,explanation:'For nested shadow roots, you can chain .shadowRoot queries in JavaScript: document.querySelector(\'host-1\').shadowRoot.querySelector(\'host-2\').shadowRoot.querySelector(\'target\'). JavascriptExecutor returns this element as a valid WebElement to Java.'},
{id:4111,topic:'selenium',difficulty:'intermediate',question:'Why cannot Selenium WebDriver directly click sub-elements or shapes drawn inside an HTML5 `<canvas>` element using standard By locators?',options:['Canvas elements are encrypted by browser security','Canvas is rendered as a single bitmap pixel grid without individual DOM child elements for shapes or text drawn inside it','Selenium only supports SVG elements, not HTML5','Canvas elements disable mouse events completely'],answer:1,explanation:'An HTML5 <canvas> element is a single raster DOM node. Shapes, charts, and drawings inside it are rendered as pixels on a 2D/3D context and do not exist as distinct DOM elements, meaning standard By.id or By.xpath locators cannot target individual drawn items.'},
{id:4112,topic:'selenium',difficulty:'advanced',question:'What are the primary techniques to interact with or test elements rendered inside an HTML5 `<canvas>` in Selenium?',options:['Using new Select(canvasElement)','Using Actions class with moveToElement(canvas, offsetX, offsetY).click() based on calculated pixel offsets, or calling custom JavaScript APIs exposed by the canvas application','Using By.tagName("shape")','Switching to canvas using driver.switchTo().frame("canvas")'],answer:1,explanation:'Canvas testing relies on either: 1) Actions class offset clicks: actions.moveToElement(canvas, xOffset, yOffset).click().perform(), 2) JavascriptExecutor querying the underlying JS chart/game model directly, or 3) Visual regression comparison of canvas snapshots.'},
{id:4113,topic:'selenium',difficulty:'intermediate',question:'What does `element.getCssValue("color")` or `element.getCssValue("background-color")` return in Selenium WebDriver?',options:['Hex color code (e.g., "#FF0000")','RGBA color format string (e.g., "rgba(255, 0, 0, 1)" or "rgb(255, 0, 0)") representing the computed CSS style of the element','The CSS class name','A Java Color object instance'],answer:1,explanation:'getCssValue() returns the computed style evaluated by the browser. For colors, browsers standardize computed values to RGBA format (e.g., "rgba(255, 0, 0, 1)"), regardless of whether the source CSS defined the color as hex, named string, or rgb.'},
{id:4114,topic:'selenium',difficulty:'intermediate',question:'How can you convert Selenium\'s `getCssValue("color")` RGBA output string into hex format for assertions in Java?',options:['Using String.toHex(color)','Using org.openqa.selenium.support.Color.fromString(rgbaString).asHex()','By casting to Integer.parseInt() directly','By parsing the browser user-agent header'],answer:1,explanation:'Selenium provides a built-in utility class org.openqa.selenium.support.Color. Calling Color.fromString(element.getCssValue("color")).asHex() converts the "rgba(r, g, b, a)" string into standard hex format (e.g., "#ff0000") for simple assertions.'},
{id:4115,topic:'selenium',difficulty:'beginner',question:'What does `element.getCssValue("font-size")` return in Selenium?',options:['Point size integer (e.g., 12)','The computed font size string with units, typically in pixels such as "16px"','The HTML font tag attribute','The font family name only'],answer:1,explanation:'getCssValue("font-size") returns the resolved computed font size as rendered by the browser layout engine, usually formatted as a string ending in "px" (e.g., "14px", "16px"). You can verify typography and design system compliance with this method.'},
{id:4116,topic:'selenium',difficulty:'intermediate',question:'How is JavaScript used in Selenium frameworks to highlight elements with a colored border during test execution or debugging?',options:['driver.manage().window().highlight(element)','((JavascriptExecutor) driver).executeScript("arguments[0].setAttribute(\'style\', \'border: 3px solid red; background: yellow;\');", element);','By calling element.highlight() on the WebElement','By modifying the operating system cursor settings'],answer:1,explanation:'Applying inline CSS via JavascriptExecutor alters the element\'s border and background style immediately. Test frameworks often wrap element interactions with this script during debugging or test recording to visually demonstrate which element is currently being manipulated.'},
{id:4117,topic:'selenium',difficulty:'intermediate',question:'How can you retrieve browser console JavaScript errors and warnings in Selenium WebDriver (Chrome/Edge)?',options:['driver.getConsoleMessages()','driver.manage().logs().get(LogType.BROWSER).getAll() returning LogEntries containing level, timestamp, and message','By parsing the terminal stdout stream','By checking driver.switchTo().alert().getText()'],answer:1,explanation:'driver.manage().logs().get(LogType.BROWSER) returns LogEntries containing all console logs emitted by the browser, including JavaScript errors (SEVERE), warnings (WARNING), and info logs. Tests can assert that no SEVERE errors were logged during critical user flows.'},
{id:4118,topic:'selenium',difficulty:'advanced',question:'How do you configure ChromeOptions to capture browser console logs in Selenium WebDriver?',options:['options.setHeadless(true)','LoggingPreferences logs = new LoggingPreferences(); logs.enable(LogType.BROWSER, Level.ALL); options.setCapability(ChromeOptions.LOGGING_PREFS, logs);','options.addArguments("--enable-console-file")','options.setBrowserLog(true)'],answer:1,explanation:'To ensure browser logs are collected, create a LoggingPreferences object, enable LogType.BROWSER with desired Level (such as Level.ALL or Level.SEVERE), and attach it to ChromeOptions via capability ChromeOptions.LOGGING_PREFS.'},
{id:4119,topic:'selenium',difficulty:'advanced',question:'How can Selenium measure page load performance metrics (DNS, TCP, TTFB, DOM Complete) using the W3C Navigation Timing API?',options:['By measuring System.currentTimeMillis() around driver.get()','By executing JavaScript: `((JavascriptExecutor) driver).executeScript("return window.performance.getEntriesByType(\'navigation\')[0];")` and parsing timing attributes','By querying the ChromeDriver HTTP port directly','By reading browser history files from disk'],answer:1,explanation:'The Navigation Timing API (window.performance.getEntriesByType("navigation")[0] or window.performance.timing) provides detailed millisecond timestamps for domInteractive, domComplete, loadEventEnd, responseStart, and requestStart for precise frontend performance benchmarking.'},
{id:4120,topic:'selenium',difficulty:'advanced',question:'How do you calculate Time to First Byte (TTFB) in Selenium via `window.performance` JavaScript execution?',options:['performance.now()','((Number) js.executeScript("var nav = performance.getEntriesByType(\'navigation\')[0]; return nav.responseStart - nav.requestStart;")).longValue()','driver.manage().timeouts().getPageLoadTimeout()','responseEnd - navigationStart in CSS'],answer:1,explanation:'TTFB measures the latency from the moment the browser sends the HTTP request to the moment it receives the first byte of the response. Calculating nav.responseStart - nav.requestStart using Navigation Timing Level 2 provides accurate TTFB in milliseconds.'},
{id:4121,topic:'selenium',difficulty:'intermediate',question:'How can automated web accessibility (a11y) testing with axe-core be integrated into Selenium tests?',options:['By installing an axe browser extension manually in every test run','By injecting the axe.min.js script into the page via JavascriptExecutor or using the axe-selenium Java library to analyze the DOM and assert on accessibility violations','Axe-core is incompatible with Selenium WebDriver','By checking if all images have width and height attributes'],answer:1,explanation:'The axe-selenium Java library (by Deque) wraps axe-core. It injects axe.min.js into the browser via JavascriptExecutor, runs axe.run(), and returns structured Results containing WCAG violations, tags, severity levels, and selector nodes.'},
{id:4122,topic:'selenium',difficulty:'advanced',question:'How does a test assert that a page has zero critical WCAG accessibility violations using AxeBuilder with Selenium?',options:['Assert.assertEquals(driver.getTitle(), "Accessible")','Results results = new AxeBuilder().withTags(Arrays.asList("wcag2a", "wcag2aa")).analyze(driver); assertTrue(results.violationFree());','driver.findElement(By.tagName("a11y")).isDisplayed()','Axe.assertPage(driver)'],answer:1,explanation:'Using AxeBuilder from axe-selenium: AxeBuilder builder = new AxeBuilder(); Results results = builder.withTags(List.of("wcag2a", "wcag2aa")).analyze(driver); assertTrue(results.violationFree(), results.getViolations().toString()) validates WCAG 2.0/2.1 Level A and AA compliance.'},
{id:4123,topic:'selenium',difficulty:'advanced',question:'In Selenium 4, how can you emulate network throttling (e.g., Slow 3G or Offline) using Chrome DevTools Protocol (CDP)?',options:['driver.manage().timeouts().setSpeed(Speed.SLOW)','DevTools devTools = ((ChromeDriver) driver).getDevTools(); devTools.createSession(); devTools.send(Network.emulateNetworkConditions(offline, latency, downloadThroughput, uploadThroughput, Optional.of(ConnectionType.CELLULAR3G)));','driver.manage().network().throttle("3G")','By setting ChromeOptions.addArguments("--throttle-network")'],answer:1,explanation:'Selenium 4 introduces native CDP support via driver.getDevTools(). Enabling Network domain and sending Network.emulateNetworkConditions allows configuring offline status, latency (ms), download throughput, upload throughput, and connection type to test slow connections.'},
{id:4124,topic:'selenium',difficulty:'advanced',question:'How can you simulate an offline state in Selenium 4 using the DevTools protocol to test service worker caching?',options:['driver.navigate().offline()','DevTools devTools = ((ChromeDriver) driver).getDevTools(); devTools.createSession(); devTools.send(Network.enable(Optional.empty(), Optional.empty(), Optional.empty())); devTools.send(Network.emulateNetworkConditions(true, 0, 0, 0, Optional.empty()));','driver.manage().deleteAllCookies()','By closing all browser tabs except one'],answer:1,explanation:'Calling Network.emulateNetworkConditions with the first parameter (offline) set to true simulates complete network disconnection in the browser, allowing verification of offline error handling, PWA service workers, or offline banners.'},
{id:4125,topic:'selenium',difficulty:'advanced',question:'How do you override browser Geolocation coordinates in Selenium 4 via Chrome DevTools Protocol (CDP)?',options:['driver.get("geo:37.7749,-122.4194")','DevTools devTools = ((ChromeDriver) driver).getDevTools(); devTools.createSession(); devTools.send(Emulation.setGeolocationOverride(Optional.of(37.7749), Optional.of(-122.4194), Optional.of(1)));','driver.manage().window().setPosition(new Point(37, -122))','ChromeOptions.addArguments("--location=37.7749,-122.4194")'],answer:1,explanation:'Selenium 4 CDP bindings allow invoking Emulation.setGeolocationOverride(latitude, longitude, accuracy). This overrides navigator.geolocation responses in the browser without requiring native OS permission prompts, enabling location-specific UI testing.'},
{id:4126,topic:'selenium',difficulty:'intermediate',question:'How do you configure mobile device emulation (such as iPhone 14 or Pixel 7) in Selenium using `ChromeOptions`?',options:['options.setMobile(true)','Map<String, Object> mobileEmulation = new HashMap<>(); mobileEmulation.put("deviceName", "iPhone 14"); options.setExperimentalOption("mobileEmulation", mobileEmulation);','options.addArguments("--device=iPhone14")','driver.switchTo().mobileDevice("iPhone 14")'],answer:1,explanation:'Chrome provides built-in mobile emulation. Setting the experimental option "mobileEmulation" with a predefined "deviceName" (or custom deviceMetrics with width, height, pixelRatio, and userAgent) launches Chrome with responsive mobile viewport and touch event simulation.'},
{id:4127,topic:'selenium',difficulty:'advanced',question:'How can you configure custom mobile viewport dimensions and user agent in `ChromeOptions` using `deviceMetrics`?',options:['options.setWindowSize(375, 812)','Map<String, Object> metrics = Map.of("width", 375, "height", 812, "pixelRatio", 3.0, "touch", true); Map<String, Object> mobile = Map.of("deviceMetrics", metrics, "userAgent", "CustomMobileAgent"); options.setExperimentalOption("mobileEmulation", mobile);','options.addArguments("--user-agent-mobile")','driver.manage().window().setSize(new Dimension(375, 812))'],answer:1,explanation:'Custom mobile emulation is configured by passing "deviceMetrics" (width, height, pixelRatio, touch) and "userAgent" string inside the "mobileEmulation" experimental option map. This simulates exact hardware dimensions and mobile browser headers.'},
{id:4128,topic:'selenium',difficulty:'intermediate',question:'What is the cookie-based authentication bypass pattern in Selenium, and why is it used?',options:['Bypassing firewalls by stealing cookies','Obtaining an auth session cookie via a fast API call or pre-authenticated state, injecting it using `driver.manage().addCookie(cookie)`, and refreshing the page to bypass slow UI login forms','Deleting all cookies to force login','A method for bypassing CAPTCHAs by altering DOM text'],answer:1,explanation:'Logging in via UI forms for every single test is slow. The cookie injection pattern performs an authentication API request once (or uses pre-generated tokens), injects the session cookie into the browser context with driver.manage().addCookie(), and navigates directly to authenticated pages.'},
{id:4129,topic:'selenium',difficulty:'intermediate',question:'What prerequisite must be satisfied in Selenium before calling `driver.manage().addCookie(new Cookie("name", "value"))`?',options:['The browser must be in headless mode','The browser must first navigate to the target domain (e.g., driver.get("https://example.com/favicon.ico" or base URL)), otherwise InvalidCookieDomainException is thrown','All previous cookies must be deleted','driver.quit() must be invoked'],answer:1,explanation:'WebDriver restricts adding cookies to the currently active domain. Attempting to add a cookie before navigating to the domain throws InvalidCookieDomainException. Navigating to the domain or a lightweight asset (like /favicon.ico) first establishes the required domain context.'},
{id:4130,topic:'selenium',difficulty:'intermediate',question:'How do you verify file downloads in Selenium WebDriver when the browser downloads files to the local file system?',options:['By using driver.switchTo().downloadWindow()','By configuring the browser download directory via browser options, triggering the download, and polling the download directory until the target file exists and is not a partial/temporary file (.crdownload/.part)','By checking driver.getPageSource().contains("Downloaded")','Selenium automatically returns a File object upon clicking a download link'],answer:1,explanation:'Browsers handle downloads natively without DOM changes. Configure Chrome download directory via prefs ("download.default_directory", path). After clicking the download link, poll the local folder using a wait loop checking for file.exists() and verifying temporary extensions like .crdownload or .tmp have finished.'},
{id:4131,topic:'selenium',difficulty:'advanced',question:'How can you configure ChromeOptions to automatically download files to a specific directory without showing download prompt dialogs?',options:['options.addArguments("--auto-download")','Map<String, Object> prefs = new HashMap<>(); prefs.put("download.default_directory", downloadPath); prefs.put("download.prompt_for_download", false); prefs.put("safebrowsing.enabled", true); options.setExperimentalOption("prefs", prefs);','driver.manage().window().setDownloadPath(downloadPath)','options.setCapability("download", true)'],answer:1,explanation:'Setting Chrome preferences "download.default_directory" to the absolute target directory and "download.prompt_for_download" to false disables OS-level save-as dialogs and routes all file downloads silently to the designated test folder.'},
{id:4132,topic:'selenium',difficulty:'advanced',question:'What is `BrowserWebDriverContainer` in TestContainers, and what problem does it solve for Selenium tests?',options:['A tool to package Selenium code as a .jar file','A Testcontainers module that manages disposable Docker containers running Chrome/Firefox browsers with VNC/video recording, eliminating local browser/driver installation issues in CI pipelines','A Java Swing component displaying browser windows','A Maven plugin for driver downloading'],answer:1,explanation:'Testcontainers BrowserWebDriverContainer spins up a fresh Docker container containing Chrome/Firefox and matching WebDriver binaries. It provides remote WebDriver URLs, supports video recording of test runs, and guarantees identical browser environments across all machines and CI runners.'},
{id:4133,topic:'selenium',difficulty:'advanced',question:'How do you instantiate a Selenium RemoteWebDriver connecting to a TestContainers `BrowserWebDriverContainer` in Java?',options:['new ChromeDriver()','BrowserWebDriverContainer<?> container = new BrowserWebDriverContainer<>().withCapabilities(new ChromeOptions()); container.start(); WebDriver driver = new RemoteWebDriver(container.getSeleniumAddress(), new ChromeOptions());','new WebDriverContainer()','container.getDriver() directly'],answer:1,explanation:'After starting BrowserWebDriverContainer, container.getSeleniumAddress() returns the dynamic URL of the Selenium Hub inside the container. Initializing new RemoteWebDriver(container.getSeleniumAddress(), new ChromeOptions()) connects the test to the containerized browser.'},
{id:4134,topic:'selenium',difficulty:'intermediate',question:'In Selenium 4, what is the key difference between `element.getAttribute("attributeName")` and `element.getDomAttribute("attributeName")`?',options:['They are exact aliases with identical implementation','`getDomAttribute()` returns the exact value declared in the HTML markup attribute; `getAttribute()` returns either the HTML attribute or the dynamically computed JavaScript DOM property depending on standard fallbacks','`getDomAttribute()` only works on Firefox','`getAttribute()` is deprecated in Selenium 4'],answer:1,explanation:'In Selenium 4, getDomAttribute() returns the literal HTML attribute value from the original DOM markup (or null if absent). getAttribute() retains legacy behavior of inspecting both HTML attributes and dynamic JS DOM properties (like returning "true" for checked even when omitted in markup).'},
{id:4135,topic:'selenium',difficulty:'intermediate',question:'What does `element.getDomProperty("propertyName")` return in Selenium 4?',options:['The HTML source string','The current runtime value of the specified JavaScript property on the DOM element object (e.g., element.getDomProperty("value") or getDomProperty("checked"))','The CSS class name','The element locator string'],answer:1,explanation:'Selenium 4 introduced getDomProperty() to directly read dynamic JavaScript object properties from the element node (e.g., getDomProperty("value"), getDomProperty("selectedIndex"), getDomProperty("checked")), providing clarity between static markup attributes and runtime JS properties.'},
{id:4136,topic:'selenium',difficulty:'beginner',question:'What are the three `PageLoadStrategy` options supported in Selenium WebDriver via Capabilities/Options?',options:['FAST, MEDIUM, SLOW','NORMAL, EAGER, NONE','IMPLICIT, EXPLICIT, FLUENT','SYNC, ASYNC, DEFERRED'],answer:1,explanation:'PageLoadStrategy specifies how WebDriver waits for pages to load during navigation: NORMAL (waits for document.readyState == "complete"), EAGER (waits for document.readyState == "interactive" / DOMContentLoaded), and NONE (returns control immediately without waiting).'},
{id:4137,topic:'selenium',difficulty:'intermediate',question:'What does `PageLoadStrategy.NORMAL` do in Selenium?',options:['Causes WebDriver to wait until the `load` event fires and `document.readyState` equals `"complete"`, meaning all HTML, stylesheets, scripts, and images have loaded','Returns immediately after HTTP status 200 is received','Waits only for DOMContentLoaded event','Disables all page load timeouts'],answer:0,explanation:'PageLoadStrategy.NORMAL is the default strategy. It blocks driver navigation calls (driver.get() / click()) until the browser fires the window load event and document.readyState transitions to "complete", ensuring all initial resources are downloaded.'},
{id:4138,topic:'selenium',difficulty:'intermediate',question:'What is the behavior of `PageLoadStrategy.EAGER` in Selenium, and when is it beneficial?',options:['It waits for all video streams to finish buffering','It causes WebDriver to return navigation control as soon as DOMContentLoaded fires (`document.readyState === "interactive"`), speeding up tests on pages with heavy third-party tracking scripts or large images','It forces the browser to pre-render the next 5 pages','It disables all explicit waits across the test'],answer:1,explanation:'PageLoadStrategy.EAGER returns control once the DOM tree is built (interactive state), without waiting for external stylesheets, subframes, or heavy tracking/marketing scripts to complete. Combined with explicit waits for specific elements, it significantly accelerates test execution.'},
{id:4139,topic:'selenium',difficulty:'intermediate',question:'What is the behavior of `PageLoadStrategy.NONE` in Selenium?',options:['WebDriver throws an exception on every navigation','WebDriver navigates to the URL and returns control immediately without waiting for any document readiness or resource loading','The browser refuses to load external scripts','No cookies or cache are loaded'],answer:1,explanation:'PageLoadStrategy.NONE returns control instantly after the navigation command is dispatched to the browser. The automation script is entirely responsible for using explicit waits to confirm page elements and state before interacting.'},
{id:4140,topic:'selenium',difficulty:'intermediate',question:'How do you configure the Page Load Strategy to EAGER in ChromeOptions in Selenium (Java)?',options:['options.setPageLoad("EAGER")','options.setPageLoadStrategy(PageLoadStrategy.EAGER);','options.addArguments("--page-load-strategy=eager")','driver.manage().timeouts().setPageLoadStrategy("EAGER")'],answer:1,explanation:'In Selenium Java, calling options.setPageLoadStrategy(PageLoadStrategy.EAGER) sets the desired capability. When ChromeDriver initializes, it applies the eager strategy to all navigation commands.'},
{id:4141,topic:'selenium',difficulty:'intermediate',question:'What is the difference between an HTML element attribute and a JavaScript DOM property for a checkbox element in Selenium?',options:['There is no difference between attributes and properties','The `checked` attribute represents the default initial state defined in HTML markup (`<input checked>`); the `checked` DOM property represents the current dynamic state (true/false) as toggled by the user','Properties are static while attributes are dynamic','Attributes only exist in CSS'],answer:1,explanation:'HTML attributes are static values specified in markup tags. DOM properties are live JavaScript object variables. When a user checks or unchecks a box, the DOM property `checked` updates to true/false, whereas the initial HTML attribute remains unchanged.'},
{id:4142,topic:'selenium',difficulty:'intermediate',question:'How can you verify whether a checkbox is currently selected in Selenium WebDriver?',options:['element.getAttribute("selected")','element.isSelected() — which returns a boolean indicating whether the input element (checkbox, radio button, or option) is currently checked/selected','element.getText().equals("checked")','element.getCssValue("checked")'],answer:1,explanation:'The isSelected() method on WebElement returns true if a checkbox, radio button, or select option is currently selected, and false otherwise. It checks the live DOM state rather than static markup attributes.'},
{id:4143,topic:'selenium',difficulty:'intermediate',question:'How do you verify whether a button is disabled in Selenium WebDriver?',options:['element.isDisabled()','!element.isEnabled() or checking element.getDomProperty("disabled") / element.getDomAttribute("disabled")','element.getAttribute("clickable").equals("false")','element.isDisplayed() == false'],answer:1,explanation:'WebElement.isEnabled() returns false if an element is disabled. Calling !element.isEnabled() or verifying element.getDomProperty("disabled") evaluates whether the element has the disabled property set to true.'},
{id:4144,topic:'selenium',difficulty:'intermediate',question:'In XPath, what does the `following-sibling` axis select in Selenium?',options:['All parent nodes of the context node','All sibling nodes that appear after the context node at the same hierarchical level in the DOM tree: `//td[text()=\'John\']/following-sibling::td`','All child nodes of the context node','All elements anywhere after the current element regardless of hierarchy'],answer:1,explanation:'The `following-sibling::` axis selects siblings sharing the same parent that appear after the context element in document order. For example, `//td[text()=\'User1\']/following-sibling::td[2]` selects the second sibling cell in the same table row.'},
{id:4145,topic:'selenium',difficulty:'intermediate',question:'In XPath, what does the `preceding-sibling` axis select in Selenium?',options:['All child elements before the current element','All sibling nodes that share the same parent and appear before the context node in the DOM: `//td[text()=\'Active\']/preceding-sibling::td[1]`','All ancestors of the element','All preceding siblings in parent tables only'],answer:1,explanation:'The `preceding-sibling::` axis selects siblings that share the same parent element and occur before the context node in document order. Useful for selecting a checkbox or username cell located before a known status cell.'},
{id:4146,topic:'selenium',difficulty:'intermediate',question:'In XPath, what does the `ancestor` axis select in Selenium?',options:['Only the immediate parent node','All ancestor elements (parent, grandparent, great-grandparent, up to root element) of the context node: `//input[@id=\'email\']/ancestor::form`','All previous sibling nodes','All descendant elements'],answer:1,explanation:'The `ancestor::` axis selects all ancestor nodes of the context node. For example, `//button[@id=\'delete\']/ancestor::div[@class=\'card\']` finds the containing card container regardless of how deeply nested the button is.'},
{id:4147,topic:'selenium',difficulty:'intermediate',question:'What is the difference between `ancestor` and `parent` axes in XPath for Selenium locators?',options:['`parent` selects all ancestors; `ancestor` selects only the direct parent','`parent::` selects only the single immediate parent node (`..`); `ancestor::` selects all ancestors in the hierarchy up to the document root','They are completely interchangeable in all XPath engines','`parent` only works with <div> tags'],answer:1,explanation:'`parent::*` (or `..`) targets strictly the direct, immediate parent element of the context node. `ancestor::*` traverses up the entire hierarchy, matching any parent, grandparent, or higher ancestor element that satisfies the predicate.'},
{id:4148,topic:'selenium',difficulty:'intermediate',question:'In XPath, what does the `following` axis select compared to `following-sibling`?',options:['`following` selects only direct child nodes','`following::` selects all nodes in the entire document that appear after the context node\'s closing tag, excluding descendants; `following-sibling::` selects only sibling nodes sharing the same parent','`following` is deprecated in XPath 1.0','They are identical in functionality'],answer:1,explanation:'`following::` selects everything in the document following the closing tag of the context node (excluding descendants, attribute nodes, and namespace nodes). `following-sibling::` is restricted strictly to sibling nodes sharing the exact same parent.'},
{id:4149,topic:'selenium',difficulty:'intermediate',question:'In XPath, what does the `descendant` axis select in Selenium?',options:['Only immediate children nodes','All child, grandchild, and deeply nested descendant nodes of the context node, equivalent to `//`: `//table[@id=\'users\']/descendant::tr`','All sibling nodes below the current element','All parent nodes'],answer:1,explanation:'The `descendant::` axis matches all descendant nodes (children, grandchildren, etc.) of the context node. Writing `//form[@id=\'login\']/descendant::input` matches every input element contained anywhere within that form.'},
{id:4150,topic:'selenium',difficulty:'intermediate',question:'How do you extract all row data from a dynamic HTML `<table>` into a `List<Map<String, String>>` in Selenium?',options:['By copying table text with driver.getClipboard()','By extracting header text from `<th>` elements into keys, iterating over each `<tr>` in `<tbody>`, extracting cell values from `<td>` elements, and storing key-value pairs per row','By casting WebElement to TableModel','By calling driver.getTableData("tableId")'],answer:1,explanation:'To extract table data: 1) Find all <th> elements and map their getText() to header names. 2) Find all <tr> rows. 3) For each row, find its <td> cells and put header-value pairs into a Map<String, String>, collecting them into a List.'},
{id:4151,topic:'selenium',difficulty:'intermediate',question:'How do you locate a specific table cell based on text in another column in the same row using XPath in Selenium?',options:['//table//td[text()="target"]','//table//tr[td[text()=\'John Doe\']]//td[3] — finds the row containing "John Doe" and selects the 3rd column cell in that same row','//td[@column=3 and text()="John Doe"]','driver.findElement(By.tableCell("John Doe", 3))'],answer:1,explanation:'Filtering on the <tr> element using a predicate (`//tr[td[text()=\'John Doe\']]`) isolates the specific row matching the criteria. You can then navigate down to another cell (`//td[3]` or `//button`) within that isolated row.'},
{id:4152,topic:'selenium',difficulty:'intermediate',question:'How do you handle pagination when extracting data from multi-page HTML tables in Selenium?',options:['Increase implicit wait to 100 seconds','Extract data from current page, check if "Next" button is enabled/visible, click "Next", wait for table data to update or stale element to refresh, and repeat until "Next" is disabled','By changing the URL parameter ?page=all on every website','Pagination cannot be automated in Selenium'],answer:1,explanation:'Pagination handling follows a loop: 1) Scrape current table rows. 2) Check if "Next" button is present and enabled. 3) Click "Next". 4) Explicitly wait for old table elements to become stale or new data to load. 5) Repeat until no further pages remain.'},
{id:4153,topic:'selenium',difficulty:'intermediate',question:'What is a Soft Assertion (e.g., `SoftAssert` in TestNG), and why is it useful in Selenium test automation?',options:['An assertion that fails without logging errors','An assertion mechanism that collects all validation failures during test execution without stopping test flow immediately, reporting all failed assertions together at the end when `assertAll()` is called','A wait strategy that ignores timeouts','An assertion that automatically retries failed steps'],answer:1,explanation:'Hard assertions (Assert.assertEquals) terminate test execution immediately upon the first failure, skipping subsequent checks. SoftAssert records assertion failures and allows the test to continue validating other UI elements, aggregating all failures when softAssert.assertAll() is invoked.'},
{id:4154,topic:'selenium',difficulty:'beginner',question:'What critical method MUST be called at the end of a test method when using TestNG `SoftAssert` in Selenium?',options:['softAssert.close()','softAssert.assertAll() — which evaluates all accumulated soft assertions and fails the test if any assertion failed','softAssert.flush()','softAssert.complete()'],answer:1,explanation:'If softAssert.assertAll() is omitted, the test will pass even if multiple soft assertions failed during execution. Invoking assertAll() causes SoftAssert to collate all failure messages and throw an AssertionError if any check failed.'},
{id:4155,topic:'selenium',difficulty:'intermediate',question:'In JUnit 5, how do you perform multiple soft assertions similar to TestNG\'s `SoftAssert` in Selenium?',options:['Using @SoftTest annotation','Using `Assertions.assertAll("Header Check", () -> assertEquals(expected1, actual1), () -> assertEquals(expected2, actual2));`','Using JUnit5.softAssert()','By wrapping assertions in while loops'],answer:1,explanation:'JUnit 5 provides native grouped assertions via org.junit.jupiter.api.Assertions.assertAll(). It accepts an optional heading and multiple Executable lambdas, executing every assertion and reporting all accumulated failures together.'},
{id:4156,topic:'selenium',difficulty:'intermediate',question:'How do you configure a custom download directory in Firefox using `FirefoxOptions` and `FirefoxProfile` in Selenium?',options:['options.setDownloadPath(dir)','options.addPreference("browser.download.dir", dir); options.addPreference("browser.download.folderList", 2); options.addPreference("browser.helperApps.neverAsk.saveToDisk", "application/pdf, text/csv");','options.addArguments("--download-dir=" + dir)','driver.manage().window().setDownloadDir(dir)'],answer:1,explanation:'Firefox uses profile preferences: folderList=2 indicates custom directory, browser.download.dir sets the target path, and helperApps.neverAsk.saveToDisk lists MIME types to download automatically without prompting for confirmation.'},
{id:4157,topic:'selenium',difficulty:'advanced',question:'What does `((HasDevTools) driver).getDevTools()` provide in Selenium 4?',options:['Access to the browser debugger UI window','An active WebSocket connection to Chrome DevTools Protocol (CDP) for Chromium-based browsers, enabling network interception, console log listening, performance metrics, and security override APIs','A method to inspect Java bytecode','A built-in performance profiler for JUnit'],answer:1,explanation:'Selenium 4\'s HasDevTools interface enables direct communication with Chrome DevTools Protocol over WebSockets. This exposes browser internals like network mocking, console log inspection, geolocation overrides, CPU throttling, and security certificate bypasses.'},
{id:4158,topic:'selenium',difficulty:'advanced',question:'How can you mock API HTTP responses or intercept network requests in Selenium 4 using CDP?',options:['driver.interceptRequest("https://api.com", "{data:1}")','DevTools devTools = ((ChromeDriver) driver).getDevTools(); devTools.createSession(); devTools.send(Fetch.enable(Optional.empty(), Optional.empty())); devTools.addListener(Fetch.requestPaused(), req -> devTools.send(Fetch.fulfillRequest(req.getRequestId(), 200, ...)));','driver.manage().network().mockResponse()','By editing the /etc/hosts file via Java'],answer:1,explanation:'CDP Fetch domain allows intercepting requests: Fetch.enable() pauses matching outgoing requests. The Fetch.requestPaused() event listener can inspect request headers and respond with mock data using Fetch.fulfillRequest() or fail requests with Fetch.failRequest().'},
{id:4159,topic:'selenium',difficulty:'advanced',question:'How do you capture HTTP request and response traffic status codes in Selenium 4 using CDP `Network` events?',options:['driver.getNetworkTraffic()','DevTools devTools = ((ChromeDriver) driver).getDevTools(); devTools.createSession(); devTools.send(Network.enable(Optional.empty(), Optional.empty(), Optional.empty())); devTools.addListener(Network.responseReceived(), response -> System.out.println(response.getResponse().getStatus() + " " + response.getResponse().getUrl()));','By casting driver to HttpURLConnection','By configuring a proxy server in ChromeOptions only'],answer:1,explanation:'Enabling Network domain in CDP and attaching a listener to Network.responseReceived() captures all HTTP responses loaded by the browser, including status codes (200, 404, 500), headers, MIME types, and timing data without needing an external proxy.'},
{id:4160,topic:'selenium',difficulty:'intermediate',question:'How can you clear browser `localStorage` and `sessionStorage` in Selenium WebDriver using `JavascriptExecutor`?',options:['driver.manage().deleteStorage()','((JavascriptExecutor) driver).executeScript("window.localStorage.clear(); window.sessionStorage.clear();");','driver.manage().deleteAllCookies() clears web storage automatically','driver.navigate().clearCache()'],answer:1,explanation:'driver.manage().deleteAllCookies() only clears HTTP cookies, not HTML5 Web Storage. To clear localStorage and sessionStorage between tests, use JavascriptExecutor to invoke window.localStorage.clear() and window.sessionStorage.clear().'},
{id:4161,topic:'selenium',difficulty:'intermediate',question:'How can you retrieve an item stored in browser `localStorage` using Selenium `JavascriptExecutor`?',options:['driver.manage().getLocalStorage("token")','(String) ((JavascriptExecutor) driver).executeScript("return window.localStorage.getItem(\'authToken\');")','driver.findElement(By.id("localStorage")).getText()','driver.getStorage().get("authToken")'],answer:1,explanation:'Executing the JavaScript expression return window.localStorage.getItem(\'key\') retrieves the stored string value. This is useful for verifying auth tokens, user preferences, or feature flags stored in HTML5 localStorage.'},
{id:4162,topic:'selenium',difficulty:'intermediate',question:'What is the recommended way to scroll an element into the center of the viewport using `JavascriptExecutor` in Selenium?',options:['driver.manage().window().scroll()','((JavascriptExecutor) driver).executeScript("arguments[0].scrollIntoView({behavior: \'smooth\', block: \'center\'});", element);','element.sendKeys(Keys.PAGE_DOWN)','driver.switchTo().frame(element)'],answer:1,explanation:'Calling element.scrollIntoView({block: "center"}) via JavascriptExecutor scrolls the container so that the specified element appears centered in the viewport, preventing it from being hidden beneath fixed top headers or floating bottom footers.'},
{id:4163,topic:'selenium',difficulty:'advanced',question:'In Selenium 4, how do you emulate user location to test localized content without altering the test machine\'s network location?',options:['By passing ?locale=es in every URL','By calling devTools.send(Emulation.setGeolocationOverride(latitude, longitude, accuracy)) to inject coordinates, and granting geolocation permissions via BrowserContext/CDP','By changing system clock time in BaseTest','By modifying Accept-Language headers in ChromeOptions only'],answer:1,explanation:'Emulation.setGeolocationOverride in CDP overrides navigator.geolocation. Combined with Emulation.setUserAgentOverride() and CDP permission grants (Browser.grantPermissions), the browser behaves as if operating in that target geographic region.'},
{id:4164,topic:'selenium',difficulty:'intermediate',question:'What is the purpose of Selenium 4 `driver.manage().timeouts().getScriptTimeout()` and `setScriptTimeout(Duration)`?',options:['It sets the maximum time driver.get() waits for page loads','It sets the maximum time WebDriver will wait for an asynchronous script executed via `executeAsyncScript()` to finish before throwing ScriptTimeoutException','It sets the execution time of JUnit assertions','It limits the duration of explicit waits'],answer:1,explanation:'Script timeout defines the maximum amount of time WebDriver waits for an asynchronous JavaScript callback to invoke the done() function passed by executeAsyncScript(). If the script does not complete within this duration, ScriptTimeoutException is thrown.'},
{id:4165,topic:'selenium',difficulty:'intermediate',question:'When should `((JavascriptExecutor) driver).executeAsyncScript(...)` be used instead of `executeScript(...)` in Selenium?',options:['For all JavaScript executions in Selenium 4','When executing asynchronous JavaScript code (e.g., waiting for a setTimeout, custom Promise, or async event callback) where the script notifies WebDriver of completion via an injected callback argument','Only when executing scripts inside iframes','When JavaScript code has no return value'],answer:1,explanation:'executeAsyncScript passes a callback function as the last argument (arguments[arguments.length - 1]). WebDriver pauses Java execution until the JavaScript code invokes this callback, making it ideal for awaiting asynchronous Promises or timer-based operations.'},
{id:4166,topic:'selenium',difficulty:'intermediate',question:'How do you verify if an image element on a web page is broken (not loaded properly) using Selenium and JavaScript?',options:['image.isDisplayed() returns false for broken images','((JavascriptExecutor) driver).executeScript("return arguments[0].complete && typeof arguments[0].naturalWidth != \'undefined\' && arguments[0].naturalWidth > 0", imgElement)','driver.findElement(By.cssSelector("img.broken"))','By checking if img.getAttribute("src") contains "http"'],answer:1,explanation:'An image might be present in the DOM (isDisplayed() is true), but failed to load due to a 404 error. Checking HTMLImageElement properties `complete === true` and `naturalWidth > 0` via JavaScript confirms the image binary rendered successfully.'},
{id:4167,topic:'selenium',difficulty:'intermediate',question:'In dynamic Single-Page Applications (SPAs), why does `driver.navigate().refresh()` sometimes reset application state unexpectedly, and how should test state be maintained?',options:['Because refresh() clears all browser cookies permanently','Because SPAs maintain in-memory JavaScript state (Redux/Pinia/Vuex); refreshing the browser reloads the initial bundle and wipes in-memory store state unless persisted to localStorage or session storage','Because Selenium disables local storage on refresh','Because refresh() creates a new WebDriver session'],answer:1,explanation:'In SPAs, client-side application state held in memory is re-initialized when the browser reloads the root HTML page. Test designs should avoid arbitrary browser refreshes, or should explicitly re-seed state via cookies, localStorage, or direct route navigation.'},
{id:4168,topic:'selenium',difficulty:'advanced',question:'How can you bypass basic HTTP Authentication popups in Selenium 4 without embedding credentials in the URL?',options:['driver.switchTo().alert().sendKeys("user:pass")','((HasAuthentication) driver).register(UsernameAndPassword.of("username", "password"));','ChromeOptions.addArguments("--auth-user=username")','driver.manage().httpAuth("user", "pass")'],answer:1,explanation:'Selenium 4 introduced the HasAuthentication interface backed by CDP. Registering credentials via ((HasAuthentication) driver).register(UsernameAndPassword.of("user", "password")) intercepts HTTP 401 challenges and supplies Basic/Digest authentication headers automatically.'},
{id:4169,topic:'selenium',difficulty:'intermediate',question:'How can you verify that a CSS property transition or animation has completed on an element in Selenium?',options:['Thread.sleep(1000) always','Using WebDriverWait with ExpectedConditions checking when element attribute/class updates, or listening to the DOM `transitionend`/`animationend` event via JavaScript','By disabling CSS transitions in ChromeOptions','By checking element.isDisplayed() once'],answer:1,explanation:'Animations can cause element coordinates and opacity to change continuously, leading to click interception. The best practice is to wait for the animated class (e.g., .is-open) to be added/removed, or use JavaScript to await transitionend before proceeding.'},
{id:4170,topic:'selenium',difficulty:'intermediate',question:'What happens if you execute `driver.findElement(By.xpath("..."))` on a parent `WebElement` context instead of `driver`?',options:['It searches the entire document root regardless of locator prefix','If the XPath starts with `./` or `.//`, it scopes the search within the parent element\'s sub-tree; if it starts with `//`, it searches the entire document root','It always throws UnsupportedOperationException','It only searches direct child elements'],answer:1,explanation:'When calling parentElement.findElement(By.xpath("//span")), the leading "//" bypasses the parent context and searches from the document root. To scope the search strictly within the parent element, the XPath MUST begin with a dot: `.//span` or `./span`.'},
{id:4171,topic:'selenium',difficulty:'intermediate',question:'How do you locate an input element using XPath by matching a label text located next to it in Selenium?',options:['//input[label="Username"]','//label[text()=\'Username\']/following::input[1] or //label[text()=\'Username\']/..//input','//input[@aria-label=label()]','By using By.labelText("Username")'],answer:1,explanation:'Form inputs often do not have unique IDs but have adjacent <label> elements. XPath locators like `//label[text()=\'Username\']/following::input[1]` or `//label[text()=\'Username\']/following-sibling::input` robustly bind the input to its corresponding label.'},
{id:4172,topic:'selenium',difficulty:'intermediate',question:'What is the best way to handle toast/snack-bar notification messages in Selenium that automatically disappear after 3 seconds?',options:['Thread.sleep(5000) and check page source','Use WebDriverWait with ExpectedConditions.visibilityOfElementLocated() to assert text while visible, followed optionally by invisibilityOfElementLocated() to confirm dismissal','Set implicit wait to 1 second','Take a screenshot and perform OCR'],answer:1,explanation:'Toast notifications have short lifecycles. Waiting explicitly for visibility of the toast container element immediately after triggering the action allows reading getText() and asserting contents before the notification fades away and is removed from the DOM.'},
{id:4173,topic:'selenium',difficulty:'advanced',question:'How can you capture full-page screenshots in Chrome/Chromium using Selenium 4 DevTools Protocol (CDP)?',options:['((TakesScreenshot) driver).getScreenshotAs(OutputType.FILE) captures full scrollable page natively in all modes','DevTools devTools = ((ChromeDriver) driver).getDevTools(); devTools.createSession(); String base64 = devTools.send(Page.captureScreenshot(Optional.empty(), Optional.empty(), Optional.of(clip), Optional.empty(), Optional.of(true), Optional.empty()));','By scrolling down and merging images with Java AWT','Full page screenshots are impossible in Chrome'],answer:1,explanation:'Standard TakesScreenshot in ChromeDriver captures only the visible viewport. Selenium 4 CDP integration allows sending Page.captureScreenshot with captureBeyondViewport set to true, capturing the entire scrollable height of the document.'},
{id:4174,topic:'selenium',difficulty:'intermediate',question:'How do you verify whether a web page contains console errors during test teardown in a TestNG/JUnit listener?',options:['LogEntries logs = driver.manage().logs().get(LogType.BROWSER); List<LogEntry> severeLogs = logs.filter(Level.SEVERE); assertTrue(severeLogs.isEmpty(), "Console errors found: " + severeLogs);','driver.getConsoleStatus() == 0','driver.getPageSource().contains("Error")','By checking driver.getWindowHandles()'],answer:0,explanation:'Retrieving LogType.BROWSER entries and filtering for Level.SEVERE allows automated assertions against unhandled JavaScript exceptions or failed 4xx/5xx network resource fetches, preventing silent frontend crashes from passing tests.'},
{id:4175,topic:'selenium',difficulty:'intermediate',question:'What is the purpose of `WebDriverWait.withMessage(String message)` in Selenium?',options:['It prints a log to the terminal on every polling cycle','It sets a custom error message that will be included in the TimeoutException if the expected condition is not met within the timeout period','It displays a tooltip on the browser web page','It sends a message to the browser console'],answer:1,explanation:'Using withMessage("Custom timeout message describing what failed") on FluentWait/WebDriverWait improves test diagnostic readability by displaying meaningful domain context inside the thrown TimeoutException instead of generic error traces.'},
{id:4176,topic:'selenium',difficulty:'intermediate',question:'How do you handle SVG elements (like icons or charts) using XPath in Selenium WebDriver?',options:['//svg/path[@id="icon"] directly','//*[local-name()=\'svg\']//*[local-name()=\'path\' and @id=\'icon\'] or //*[name()=\'svg\']','SVG elements cannot be located via XPath','By using By.svg("icon")'],answer:1,explanation:'SVG elements reside in an XML namespace (http://www.w3.org/2000/svg). In XPath 1.0, standard tag name queries like `//svg` often fail in browsers. Using `//*[local-name()=\'svg\']` or `//*[name()=\'svg\']` correctly matches SVG nodes regardless of namespace.'},
{id:4177,topic:'selenium',difficulty:'intermediate',question:'In Selenium 4, what are Relative Locators (formerly Friendly Locators), and what methods are available in the `RelativeLocator` class?',options:['Locators based on file paths','Locators that find elements based on visual proximity to a known element: `with(By).above()`, `below()`, `toLeftOf()`, `toRightOf()`, and `near()`','Locators that search relative to Git commit history','Locators that only search inside iframes'],answer:1,explanation:'Selenium 4 Relative Locators allow locating elements relative to a reference element using visual layout relationships: RelativeLocator.with(By.tagName("input")).below(By.id("email")).toLeftOf(By.id("submit")), making locators more intuitive for dynamic layouts.'},
{id:4178,topic:'selenium',difficulty:'intermediate',question:'How do you upload a file using Selenium WebDriver when the HTML page uses a hidden `<input type="file">` element?',options:['Use the Robot class to handle OS dialogs','Make the file input visible using JavaScript (`arguments[0].style.display=\'block\'`) if needed, and call `sendKeys("/absolute/path/to/file")` directly on the input element','Drag and drop the file with Actions class','File uploads are not supported without third-party plugins'],answer:1,explanation:'WebDriver interacts directly with `<input type="file">` via sendKeys(absolutePath). If the input element is hidden by CSS (display: none or opacity: 0), executing a brief script to make it visible allows sendKeys() to attach the file cleanly without OS dialogs.'},
{id:4179,topic:'selenium',difficulty:'intermediate',question:'How can you simulate pressing keyboard shortcut keys (like Ctrl+A or Cmd+A followed by Backspace) to clear an input in Selenium?',options:['element.clearAll()','element.sendKeys(Keys.chord(Keys.CONTROL, "a"), Keys.BACK_SPACE);','element.clear(Keys.ALL)','driver.keyboard().delete()'],answer:1,explanation:'Keys.chord(Keys.CONTROL, "a") (or Keys.COMMAND on macOS) presses Ctrl and A simultaneously to select all text within the focused input, and passing Keys.BACK_SPACE clears the selection. This is useful when element.clear() fails on custom JS inputs.'},
{id:4180,topic:'selenium',difficulty:'advanced',question:'What is the best architectural pattern for designing a maintainable, thread-safe, multi-browser Selenium framework?',options:['A single monolithic test class with static variables and Thread.sleep() calls','A modular architecture combining Page Object Model, Factory Pattern for WebDriver creation, ThreadLocal<WebDriver> for thread safety, External Config/Data Readers, and Listener-driven Reporting (Allure/Extent)','Hardcoding locators and URLs inside test methods directly','Using record-and-playback tools to generate Selenium scripts'],answer:1,explanation:'A scalable enterprise test framework decouples concerns: Factory Pattern creates browser instances, ThreadLocal isolates drivers across parallel threads, Page Object Model encapsulates UI structure and actions, Config/Data readers manage inputs, and test listeners handle lifecycle events and reporting.'}
]);
