// JavaScript Fundamentals — Part 1 (q1071–1175)
window.QUIZ_BANK['javascript'] = (window.QUIZ_BANK['javascript']||[]).concat([
{id:1071,topic:'javascript',difficulty:'beginner',question:'What are the primitive data types in JavaScript?',options:['string, number, object, array','string, number, bigint, boolean, undefined, null, symbol','int, float, string, boolean','string, integer, boolean, object'],answer:1,explanation:'JavaScript has 7 primitive types: string, number (64-bit float), bigint (arbitrary precision), boolean, undefined (declared but not assigned), null (intentional absence), and symbol (unique identifiers).'},
{id:1072,topic:'javascript',difficulty:'beginner',question:'What is the difference between "null" and "undefined" in JavaScript?',options:['They are identical','undefined: variable declared but not assigned any value; null: intentional assignment of "no value" (programmer explicitly sets it to nothing)','null is a primitive; undefined is not','undefined is false; null is true'],answer:1,explanation:'undefined: automatic state when a variable is declared but not assigned, or when a function has no return value. null: explicit developer-assigned "no value." typeof undefined = "undefined", typeof null = "object" (a historical JS bug).'},
{id:1073,topic:'javascript',difficulty:'beginner',question:'What does "typeof" return for a function?',options:['"object"','"function"','"callable"','"method"'],answer:1,explanation:'typeof function(){} === "function". Functions are objects in JavaScript (first-class objects) but typeof returns "function" specifically, not "object". typeof null === "object" is the infamous bug.'},
{id:1074,topic:'javascript',difficulty:'beginner',question:'What is "hoisting" in JavaScript?',options:['Moving variables between scopes','The JavaScript engine\'s behaviour of moving declarations (var declarations and function declarations) to the top of their containing scope during compilation — var is hoisted and initialised to undefined; function declarations are fully hoisted','Exporting variables to global scope','Moving code to the top of the file'],answer:1,explanation:'Hoisting: var declarations are hoisted (moved to scope top) and initialised to undefined. Function declarations are fully hoisted (can be called before declaration). let/const are hoisted but NOT initialised (Temporal Dead Zone — accessing them before declaration throws ReferenceError).'},
{id:1075,topic:'javascript',difficulty:'beginner',question:'What is the difference between "var", "let", and "const"?',options:['They are identical','var: function-scoped, hoisted, re-declarable; let: block-scoped, not re-declarable, temporal dead zone; const: block-scoped, must be initialised, cannot be reassigned (reference is const, object contents can be mutated)','const is identical to var','let allows hoisting; var does not'],answer:1,explanation:'var: function scope, hoisted to undefined, re-declarable. let: block scope, TDZ, cannot be re-declared. const: block scope, TDZ, binding cannot be reassigned (but object/array contents are mutable).'},
{id:1076,topic:'javascript',difficulty:'beginner',question:'What is "closure" in JavaScript?',options:['Closing a browser window with JavaScript','A function that retains access to variables from its outer lexical scope even after the outer function has returned — capturing the surrounding environment in the function\'s "backpack"','A method for closing network connections','A finaliser for JavaScript objects'],answer:1,explanation:'Closure: inner functions close over their outer scope\'s variables. function counter() { let n=0; return () => ++n; } — the returned function retains access to n even after counter() returns. Enables data privacy, factory functions, and partial application.'},
{id:1077,topic:'javascript',difficulty:'beginner',question:'What is the "this" keyword in JavaScript?',options:['A reference to the current HTML element','The execution context object determined at call time (for regular functions) or lexical context (for arrow functions). In methods, this is the object before the dot; in global scope, this is globalThis (window in browsers)','A reserved word for the current file','A reference to the prototype'],answer:1,explanation:'"this" in regular functions is determined by how the function is called (dynamic binding). In arrow functions, this is lexically inherited from the surrounding scope. call/apply/bind explicitly set this.'},
{id:1078,topic:'javascript',difficulty:'beginner',question:'What is an "arrow function" in JavaScript (ES6)?',options:['A function that runs faster','A concise function syntax: (params) => expression or (params) => { statements }. Key difference: does NOT have its own "this", "arguments", "super", or "new.target" — inherits them from the enclosing lexical scope','A function that handles errors automatically','A type of generator function'],answer:1,explanation:'Arrow functions: () => x (implicit return), (a,b) => a+b. No own this (lexical), cannot be used as constructors (no new), no arguments object. Ideal for callbacks where this should come from the enclosing scope.'},
{id:1079,topic:'javascript',difficulty:'beginner',question:'What is a "Promise" in JavaScript?',options:['A guarantee that code will execute','An object representing the eventual completion or failure of an asynchronous operation. States: pending → fulfilled (resolved) or rejected. Methods: .then(onFulfilled), .catch(onRejected), .finally(cleanup)','A synchronous wrapper for async operations','A JavaScript class for future computation'],answer:1,explanation:'Promises avoid callback hell: fetch(url).then(r => r.json()).then(data => ...).catch(err => ...). Promise.all([p1,p2]) waits for all; Promise.race([p1,p2]) resolves with the first to settle.'},
{id:1080,topic:'javascript',difficulty:'beginner',question:'What is "async/await" in JavaScript?',options:['A new type of loop','Syntactic sugar over Promises: async functions return Promises; await pauses execution until the Promise resolves, making async code read like synchronous code. Error handling uses try/catch','A way to run code in parallel','A type of event listener'],answer:1,explanation:'async function fetchUser() { try { const res = await fetch(url); const data = await res.json(); return data; } catch(err) { ... } } — reads synchronously but executes asynchronously. await can only be used inside async functions (or top-level modules).'},
{id:1081,topic:'javascript',difficulty:'beginner',question:'What is the "event loop" in JavaScript?',options:['A loop for handling HTML events','The mechanism that enables JavaScript\'s single-threaded runtime to perform non-blocking I/O: the call stack executes synchronous code; when it empties, the event loop moves microtasks (Promise callbacks), then macrotasks (setTimeout, I/O) from their queues into the call stack','A loop for iterating DOM events','A type of for loop for DOM elements'],answer:1,explanation:'Event loop: call stack (sync code) → microtask queue (Promise .then, queueMicrotask) — emptied completely between tasks → macrotask queue (setTimeout, setInterval, I/O). Microtasks always run before the next macrotask.'},
{id:1082,topic:'javascript',difficulty:'beginner',question:'What does "typeof null === \'object\'" indicate?',options:['null is an object type in JavaScript','A historical JavaScript bug (present since the first implementation in 1995) — null is a primitive value but typeof null returns "object". Use value === null for null checks, not typeof','null extends Object in JavaScript','typeof is broken for all primitives'],answer:1,explanation:'"typeof null === \'object\'" is a famous JS quirk/bug that cannot be fixed without breaking the web. Always use strict equality (=== null) to test for null.'},
{id:1083,topic:'javascript',difficulty:'beginner',question:'What is the difference between "==" (loose equality) and "===" (strict equality)?',options:['They are always identical','== performs type coercion before comparing (0 == false is true, "" == 0 is true); === compares without coercion — both value AND type must match (0 === false is false). Always prefer ===','=== is for objects; == for primitives','== is deprecated in modern JavaScript'],answer:1,explanation:'Loose equality (==) coerces types: null == undefined is true, 0 == "" is true, false == "0" is true. This leads to bugs. Strict equality (===) never coerces: always checks type first, then value. Use === always.'},
{id:1084,topic:'javascript',difficulty:'beginner',question:'What is "destructuring" in JavaScript (ES6)?',options:['Removing properties from objects','A syntax for unpacking values from arrays or properties from objects into distinct variables: const [a,b] = [1,2]; const {name, age} = person; Supports defaults, renaming, and rest patterns','Breaking down complex data structures','A method for converting objects to arrays'],answer:1,explanation:'Array: const [first, ...rest] = [1,2,3]. Object: const {name: n = "default"} = obj (rename to n, default "default"). Function param: function f({x,y}) — destructure directly in parameters. Eliminates verbose property access.'},
{id:1085,topic:'javascript',difficulty:'beginner',question:'What is the "spread operator" (...) in JavaScript?',options:['A multiplication shorthand','Expands an iterable (array, string, object) into individual elements: Math.max(...arr), [...arr1, ...arr2] merges arrays, {...obj1, ...obj2} merges objects. Also used in rest parameters: function f(...args) collects remaining args into array','A comment style','A way to spread values across multiple variables'],answer:1,explanation:'Spread: [...arr] shallow-copies an array; {...obj} shallow-copies an object; f(...args) spreads array as individual arguments. Rest: function f(a, ...rest) collects remaining args. Not a deep clone — nested objects are still referenced.'},
{id:1086,topic:'javascript',difficulty:'beginner',question:'What is a "template literal" (template string) in JavaScript (ES6)?',options:['An HTML template','A string enclosed in backticks (`) that supports multi-line strings, embedded expressions ${expression}, and tagged templates: `Hello, ${name}! You are ${age} years old.`','A regular expression pattern','A type of string formatting function'],answer:1,explanation:'Template literals with backticks: `${expr}` evaluates expressions; multi-line without \\n; tagged templates: tag`string${expr}` passes parts to a function for custom processing (e.g., styled-components, SQL sanitisation).'},
{id:1087,topic:'javascript',difficulty:'beginner',question:'What is "Array.prototype.map()" in JavaScript?',options:['A Map data structure method','Returns a new array containing the results of calling a provided function on each element of the original array, preserving the array length: [1,2,3].map(x => x*2) = [2,4,6]','Modifies the original array in-place','Maps array elements to HTML elements'],answer:1,explanation:'map(): non-mutating, always returns a new array of the same length. For side effects without transformation, use forEach(). For filtering, use filter(). For aggregation, use reduce().'},
{id:1088,topic:'javascript',difficulty:'beginner',question:'What is "Array.prototype.filter()" in JavaScript?',options:['Removes elements from the original array','Returns a new array containing only the elements that pass a provided test function: [1,2,3,4].filter(x => x > 2) = [3,4]. Does not mutate the original array','Reduces the array to a single value','Sorts the array according to a predicate'],answer:1,explanation:'filter() creates a new array with elements for which the callback returns truthy. If no elements pass, returns []. Preserves the original array.'},
{id:1089,topic:'javascript',difficulty:'beginner',question:'What is "Array.prototype.reduce()" in JavaScript?',options:['Reduces the size of an array','Executes a reducer function on each element to accumulate a single output value: [1,2,3,4].reduce((acc, curr) => acc + curr, 0) = 10. Versatile: can implement map, filter, and complex aggregations','Removes duplicate elements','Reduces memory usage of an array'],answer:1,explanation:'reduce(callback, initialValue): acc starts at initialValue; callback receives (acc, currentValue, index, array). Without initialValue, uses first element as acc — risky for empty arrays. Can produce any type (sum, object, array).'},
{id:1090,topic:'javascript',difficulty:'beginner',question:'What is the "prototype chain" in JavaScript?',options:['A chain of promises','The mechanism for inheritance: every object has a [[Prototype]] link to another object; when accessing a property, JavaScript traverses the chain upward until found or reaches null. Object.create(proto) creates an object with specified prototype','A chain of event listeners','A series of prototype files'],answer:1,explanation:'Prototype chain: obj.property → obj[[Prototype]].property → Object.prototype.property → null. Methods defined on the prototype are shared among all instances. __proto__ (non-standard) and Object.getPrototypeOf() access the prototype.'},
{id:1091,topic:'javascript',difficulty:'intermediate',question:'What is the "call", "apply", and "bind" methods in JavaScript?',options:['Methods for calling external APIs','call(thisArg, arg1, arg2): invokes a function with an explicit this and individual args. apply(thisArg, [argsArray]): invokes with array of args. bind(thisArg, arg1...): returns a new function with this permanently bound (partial application)','Methods for calling DOM elements','Methods for function composition'],answer:1,explanation:'call(thisArg, a, b): immediate invocation with explicit this. apply(thisArg, [a,b]): immediate with array args. bind(thisArg, a): creates a new function, does not invoke. Useful for explicit this context and partial application.'},
{id:1092,topic:'javascript',difficulty:'intermediate',question:'What is "event delegation" in JavaScript?',options:['Delegating events to native browser handlers','Attaching a single event listener to a parent element to handle events from multiple child elements; relies on event bubbling — when a child is clicked, the event bubbles up to the parent listener which checks event.target to determine which child was clicked','Delegating DOM manipulations to web workers','Forwarding events between iframes'],answer:1,explanation:'Event delegation: one listener on the parent handles clicks on all (including dynamically added) children. document.getElementById("list").addEventListener("click", e => { if (e.target.matches("li")) handle(e.target); })'},
{id:1093,topic:'javascript',difficulty:'intermediate',question:'What is the difference between "event.preventDefault()" and "event.stopPropagation()"?',options:['They are identical','preventDefault(): cancels the default browser action (form submission, link navigation); stopPropagation(): stops the event from bubbling up (or capturing down) the DOM tree — the element\'s handler still runs but parent handlers do not','stopPropagation() cancels the event; preventDefault() stops bubbling','preventDefault() is for keyboard events only'],answer:1,explanation:'preventDefault(): stops the browser\'s default action (e.g., prevent form submit to reload page). stopPropagation(): stops event propagation through the DOM tree. stopImmediatePropagation(): also prevents other listeners on the same element.'},
{id:1094,topic:'javascript',difficulty:'intermediate',question:'What is the JavaScript "event bubbling" and "event capturing" model?',options:['How browser events are created','Event phases: Capturing (top-to-bottom from window to target): listener fires on way down. At target. Bubbling (bottom-to-top from target back up): listener fires on way up. addEventListener(type, handler, true) captures; default (false) listens during bubbling','Two types of events in JavaScript','How mouse events are generated'],answer:1,explanation:'DOM event flow: Capture phase (window → document → element), Target phase (at the target), Bubble phase (element → document → window). Most handlers use bubbling (default). Capture phase is useful for intercepting events before they reach the target.'},
{id:1095,topic:'javascript',difficulty:'intermediate',question:'What is "debounce" vs "throttle" in JavaScript?',options:['Two types of JavaScript loops','Debounce: delays function execution until N ms after the last call (e.g., search input — wait until user stops typing). Throttle: limits function to execute at most once per N ms regardless of call frequency (e.g., scroll/resize handlers)','Two methods for cancelling promises','Event cancellation techniques'],answer:1,explanation:'Debounce: each call resets the timer; fires once N ms after the last trigger — ideal for input validation, search. Throttle: guarantees at most one execution per N ms window — ideal for scroll handlers, rate-limited API calls.'},
{id:1096,topic:'javascript',difficulty:'intermediate',question:'What is "memoisation" in JavaScript?',options:['Memorising JavaScript syntax','Caching the return values of a function based on its input arguments — if called again with the same arguments, returns the cached result instead of recomputing. Implemented with a Map or closure','Storing variables in memory','A technique for reducing bundle size'],answer:1,explanation:'Memoization: const memo = fn => { const cache = new Map(); return (...args) => { const key = JSON.stringify(args); return cache.has(key) ? cache.get(key) : cache.set(key, fn(...args)).get(key); }; }'},
{id:1097,topic:'javascript',difficulty:'intermediate',question:'What is "currying" in JavaScript?',options:['Adding curry to JavaScript functions','Transforming a function with multiple arguments into a series of functions each taking one argument: add(2)(3) = 5 from const add = a => b => a + b. Enables partial application and point-free style','A cooking library for JavaScript','Converting function results to strings'],answer:1,explanation:'Currying: f(a,b,c) → f(a)(b)(c). Enables partial application: const add10 = add(10); add10(5) = 15. Lodash _.curry() auto-curries functions. Useful for functional composition and reusable partial functions.'},
{id:1098,topic:'javascript',difficulty:'intermediate',question:'What is a "generator function" in JavaScript (ES6)?',options:['A function that generates random numbers','A function that can be paused and resumed, using the "yield" keyword to produce a sequence of values lazily on demand. Declared with function* and returns an iterator','A factory function for creating objects','A function that generates test data'],answer:1,explanation:'function* gen() { yield 1; yield 2; yield 3; } const g = gen(); g.next() = {value:1,done:false}. Generators produce values lazily, enable infinite sequences, and are the foundation of async/await (under the hood).'},
{id:1099,topic:'javascript',difficulty:'intermediate',question:'What are "Symbol" values used for in JavaScript?',options:['Placeholder symbols in code','Unique, immutable primitive values used as property keys to avoid name collisions. Symbol("desc") creates a symbol; each call produces a unique value. Well-known symbols (Symbol.iterator, Symbol.toPrimitive) customise object behaviour','A type for currency symbols','A replacement for string literals'],answer:1,explanation:'Symbols: const id = Symbol("id"); obj[id] = 1 — property key that won\'t conflict with string keys. Symbol.iterator: customise iteration; Symbol.toPrimitive: customise type coercion; Symbol.for(): global symbol registry.'},
{id:1100,topic:'javascript',difficulty:'intermediate',question:'What is a "WeakMap" and how does it differ from "Map"?',options:['A Map with fewer entries','WeakMap has Object-only keys (no primitives), holds weak references (keys can be garbage collected without explicitly deleting the WeakMap entry), is not iterable, and has no size property. Map has any-type keys and is iterable','WeakMap is faster for all operations','WeakMap stores values weakly'],answer:1,explanation:'WeakMap: keys must be objects; not iterable (no forEach, size, keys()); keys are weakly referenced — when the key object has no other references, the entry is GC\'d automatically. Perfect for associating metadata with DOM nodes or objects without preventing GC.'},
{id:1101,topic:'javascript',difficulty:'intermediate',question:'What is "Set" in JavaScript and how does it differ from Array?',options:['A mathematics set library','A collection of unique values of any type. Unlike Array: no duplicates, no index access, O(1) has/add/delete. Iterable in insertion order. new Set([1,1,2,3]) = Set{1,2,3}','A sorted collection','A fixed-size array'],answer:1,explanation:'Set: O(1) lookup via .has(), automatic deduplication, insertion-order iteration. [...new Set(arr)] removes duplicates from an array. Useful for membership testing, deduplication, and set operations.'},
{id:1102,topic:'javascript',difficulty:'intermediate',question:'What is "Optional chaining" (?.) in JavaScript (ES2020)?',options:['A conditional import statement','Safely accesses nested object properties: obj?.prop?.nested — returns undefined instead of throwing TypeError if any intermediate value is null/undefined. Also works for method calls: obj?.method?.() and array access: arr?.[index]','A way to make properties optional in TypeScript','A nullish coalescing operator'],answer:1,explanation:'Optional chaining: user?.address?.city returns undefined (not an error) if user or address is null/undefined. Eliminates verbose null checks: instead of user && user.address && user.address.city.'},
{id:1103,topic:'javascript',difficulty:'intermediate',question:'What is the "nullish coalescing operator" (??) in JavaScript (ES2020)?',options:['A new null type','Returns the right-hand operand only when the left-hand operand is null or undefined (not when falsy). a ?? b: if a is null/undefined → b, otherwise → a. Unlike ||, it doesn\'t treat 0, "", false as nullish','A type check for null','A shorthand for typeof === null'],answer:1,explanation:'?? vs ||: x || "default" treats 0, "" false as falsy (uses "default"). x ?? "default" only uses "default" for null/undefined — 0 ?? "default" = 0. Use ?? when 0 or "" are valid non-null values.'},
{id:1104,topic:'javascript',difficulty:'intermediate',question:'What is "Promise.allSettled()" in JavaScript (ES2020)?',options:['Settles all promises to null','Returns a promise that resolves when all provided promises have settled (fulfilled or rejected), with an array of result objects {status: "fulfilled"|"rejected", value|reason} — unlike Promise.all() which rejects on first failure','A method that always resolves','A method for cancelling promises'],answer:1,explanation:'Promise.allSettled(): never rejects; waits for all to settle. Each result has status="fulfilled" (with value) or status="rejected" (with reason). Use when you need all results regardless of individual failures.'},
{id:1105,topic:'javascript',difficulty:'intermediate',question:'What is the JavaScript "module system" (ES Modules)?',options:['A system for creating JavaScript modules in Node.js only','ES Modules (ESM): import/export syntax for sharing code between files. import {fn} from \'./module.js\'; export const fn = ...; export default class ...; Native browser support, async loading, live bindings. Different from CommonJS (require/module.exports) used in Node.js','A plugin system for JavaScript','A package management system'],answer:1,explanation:'ESM: named exports (export const fn), default export (export default), namespace import (import * as m). Live bindings: imported values reflect changes to the original (not copies). Tree-shaking works with ESM.'},
{id:1106,topic:'javascript',difficulty:'advanced',question:'What is the JavaScript "Proxy" object?',options:['A network proxy for fetch requests','An object that wraps another object to intercept and customise fundamental operations (get, set, has, deleteProperty, apply, construct) via handler traps — used for validation, logging, computed properties, and reactive systems (Vue 3)','A proxy pattern implementation','A cache for JavaScript objects'],answer:1,explanation:'Proxy: const p = new Proxy(target, handler) intercepts operations. get trap enables computed/defaulted properties; set trap enables validation; has trap customises "in" operator. Vue 3 uses Proxy for reactive data (instead of Vue 2\'s Object.defineProperty).'},
{id:1107,topic:'javascript',difficulty:'advanced',question:'What is the "Reflect" API in JavaScript?',options:['A reflection of the DOM structure','Provides methods that reflect the JavaScript engine\'s built-in operations (Reflect.get, Reflect.set, Reflect.has, Reflect.apply, Reflect.construct) — the default implementation for Proxy traps, enabling forwarding operations to the original target','A tool for introspecting object properties','A debugging API'],answer:1,explanation:'Reflect mirrors Proxy\'s traps as static methods. In a Proxy handler: get(target, prop, receiver) { return Reflect.get(target, prop, receiver); } — forwards to the target\'s normal behaviour. Prefer Reflect over direct property access in Proxy handlers.'},
{id:1108,topic:'javascript',difficulty:'advanced',question:'What are "Iterators" and the "Iteration Protocol" in JavaScript?',options:['Array loop utilities','The iteration protocol: Iterable objects implement [Symbol.iterator]() returning an Iterator; an Iterator has next() returning {value, done}. Enables use with for...of, spread, destructuring, and Array.from(). Custom iterables: implement [Symbol.iterator]','For loop abstractions','Event iteration utilities'],answer:1,explanation:'Iterator protocol: next() returns {value: T, done: boolean}. Iterable protocol: [Symbol.iterator]() returns an iterator. for...of, [...iterable], destructuring all use this protocol. Generators automatically implement both protocols.'},
{id:1109,topic:'javascript',difficulty:'advanced',question:'What is an "Async Iterator" in JavaScript (ES2018)?',options:['An iterator that runs asynchronously','An iterator whose next() returns a Promise resolving to {value, done}, enabling iteration over asynchronous data sources (API pagination, database cursors, file streams) with for await...of: for await (const page of paginator)','An asynchronous version of forEach','A background iteration loop'],answer:1,explanation:'Async iterables implement [Symbol.asyncIterator](). for await (const chunk of readable) iterates async data streams. Node.js Readable streams, Fetch body, and file read APIs are async iterables.'},
{id:1110,topic:'javascript',difficulty:'advanced',question:'What is "JavaScript\'s garbage collection" and when does it matter for web apps?',options:['Cleaning up broken JavaScript code','The automatic memory management that reclaims memory from objects no longer reachable from GC roots (global objects, stack). V8 uses generational GC (Scavenger for young gen, Mark-Compact for old gen). Long-lived closures, DOM references, and event listener leaks prevent GC','A browser feature for clearing cache','A Node.js-only concern'],answer:1,explanation:'JS GC: most objects are short-lived (minor GC is fast). Memory leaks: event listeners referencing large data kept indefinitely, closures capturing DOM nodes, global arrays growing unboundedly. DevTools Memory tab profiles heap usage.'},
{id:1111,topic:'javascript',difficulty:'advanced',question:'What is "tail call optimisation" (TCO) in JavaScript?',options:['Optimising the last function in a file','A spec (ES6) for the JavaScript engine to not create a new stack frame for a function call that is the last operation in the current function (tail position), enabling recursive functions without stack overflow. Widely NOT implemented in V8 (Safari implements it)','Caching the last return value of a function','Optimising function calls at the end of loops'],answer:1,explanation:'TCO: if f() returns g(args) in tail position, the engine reuses f\'s stack frame for g — O(1) stack space for tail-recursive functions. ES6 mandated TCO but V8 team chose not to implement it due to debugging complexity.'},
{id:1112,topic:'javascript',difficulty:'advanced',question:'What is "SharedArrayBuffer" and "Atomics" in JavaScript?',options:['A shared variable between tabs','SharedArrayBuffer enables sharing binary data between the main thread and Web Workers (or between workers) without copying. Atomics provides low-level atomic operations (add, sub, compareExchange, wait, notify) on SharedArrayBuffer for multi-threaded coordination','A shared state pattern for SPAs','A Buffer type in Node.js'],answer:1,explanation:'SharedArrayBuffer + Atomics enables true shared memory multi-threading in JavaScript (Web Workers + main thread sharing a buffer). Atomics.wait() / Atomics.notify() implement mutex/condition variables. Requires Cross-Origin Isolation headers for security.'},
{id:1113,topic:'javascript',difficulty:'advanced',question:'What is "JavaScript temporal semantics" and the new "Temporal API"?',options:['The timing of JavaScript execution','The Temporal API (TC39 Stage 3, 2024) provides a modern, immutable date/time library replacing the broken Date object: Temporal.PlainDate, PlainTime, ZonedDateTime, Instant, Duration — with time zone support, calendar support, and arithmetic','A library for measuring test performance','The order of JavaScript file loading'],answer:1,explanation:'Date is deeply flawed (mutable, no timezone support, month 0-indexed). Temporal: Temporal.Now.instant() (current UTC), PlainDate for date-only, ZonedDateTime for timezone-aware, all immutable with rich arithmetic API.'},
{id:1114,topic:'javascript',difficulty:'beginner',question:'What does "JSON.stringify()" do?',options:['Parses a JSON string into a JavaScript object','Converts a JavaScript value (object, array, primitive) to a JSON string. Handles nested objects and arrays. Functions, undefined, Symbols, and circular references are handled according to a replacer function or simply omitted','Formats code as JSON in the console','Compresses JSON data'],answer:1,explanation:'JSON.stringify(obj, replacer, spaces): converts JS to JSON string. Functions, undefined, and Symbol values are omitted. Circular references throw TypeError. JSON.stringify(obj, null, 2) pretty-prints with 2-space indent.'},
{id:1115,topic:'javascript',difficulty:'beginner',question:'What does "JSON.parse()" do?',options:['Converts a JavaScript object to JSON','Parses a valid JSON string and constructs a JavaScript value or object. JSON.parse(\'{"name":"Bob","age":30}\') returns {name:"Bob",age:30}. Throws SyntaxError for invalid JSON','Stringifies a JavaScript object','Validates a JSON schema'],answer:1,explanation:'JSON.parse(jsonString, reviver): constructs JavaScript from JSON. The reviver function can transform values during parsing. JSON is a strict subset of JS: double quotes only, no trailing commas, no undefined/function values.'},
{id:1116,topic:'javascript',difficulty:'beginner',question:'What is "localStorage" in web browsers?',options:['Local file storage on the server','A synchronous key-value storage API in browsers that persists data across sessions and browser restarts (until cleared). Stores strings only: localStorage.setItem("key","val"), getItem("key"), removeItem("key"), clear(). Domain-scoped, 5-10MB limit','An in-memory cache','A cookie management API'],answer:1,explanation:'localStorage: persistent (survives browser restart), synchronous, strings only (use JSON.stringify/parse for objects), domain-scoped, 5-10MB typical limit. sessionStorage: same API but cleared when the tab/window closes.'},
{id:1117,topic:'javascript',difficulty:'beginner',question:'What is the DOM (Document Object Model)?',options:['A JavaScript data structure for arrays','A tree-structured representation of an HTML/XML document as objects, enabling JavaScript to access, modify, add, and remove elements and attributes dynamically','A database for JavaScript objects','A module system for JavaScript'],answer:1,explanation:'DOM: document.getElementById(), querySelector(), createElement(), appendChild(). The browser parses HTML and creates the DOM tree; JavaScript can manipulate it to dynamically update the page without reloading.'},
{id:1118,topic:'javascript',difficulty:'beginner',question:'What does "document.querySelector()" return?',options:['All elements matching a CSS selector','The first Element matching the specified CSS selector, or null if no match found. document.querySelectorAll() returns a NodeList of all matches','A list of elements','The DOM node at a specific index'],answer:1,explanation:'querySelector(selector): returns the first matching Element (or null). querySelectorAll(selector): returns a static NodeList of all matches. Supports full CSS selector syntax: "#id", ".class", "div > p", "[attribute]".'},
{id:1119,topic:'javascript',difficulty:'intermediate',question:'What is the "Fetch API" and how does it replace XMLHttpRequest?',options:['A tool for fetching NPM packages','A modern Promise-based API for making HTTP requests: fetch(url, options) returns a Promise resolving to a Response object. Cleaner than XHR, supports streaming, integrates with async/await, but does NOT reject on HTTP error status (must check response.ok)','A way to fetch DOM elements','A CSS resource loading API'],answer:1,explanation:'fetch: const res = await fetch(url); if (!res.ok) throw Error(res.status); const data = await res.json(). Unlike XHR, fetch does not reject on 4xx/5xx — check response.ok manually. Use AbortController for cancellation.'},
{id:1120,topic:'javascript',difficulty:'intermediate',question:'What is an "AbortController" in JavaScript?',options:['A controller that aborts JavaScript execution','An API for cancelling ongoing operations like fetch requests: const ctrl = new AbortController(); fetch(url, {signal: ctrl.signal}); ctrl.abort() — causes the fetch Promise to reject with an AbortError','A controller for stopping animation loops','A circuit breaker for Promise chains'],answer:1,explanation:'AbortController: ctrl.abort(reason) signals cancellation to any consumers of the AbortSignal. Used with fetch, EventListeners (addEventListener(type, fn, {signal})), and custom async operations that check signal.aborted.'},
{id:1121,topic:'javascript',difficulty:'intermediate',question:'What is "structuredClone()" in JavaScript?',options:['A clone of the structured programming paradigm','A global function (ES2021) for creating deep clones of objects using the structured clone algorithm, supporting: nested objects, arrays, Maps, Sets, Dates, RegExps, ArrayBuffers, but NOT: functions, DOM nodes, or Symbol-keyed properties','A method for cloning HTML structures','A shallow copy utility'],answer:1,explanation:'structuredClone(obj): true deep clone, replaces the {...obj} shallow copy hack. Supports circular references (unlike JSON.stringify/parse). Does not clone functions or class prototypes — clones the data shape.'},
{id:1122,topic:'javascript',difficulty:'intermediate',question:'What is "Object.freeze()" in JavaScript?',options:['Prevents the object from being used after a certain date','Makes an object immutable: cannot add, remove, or modify properties. Shallow only (nested objects are NOT frozen). Object.isFrozen() checks. For deep immutability, recursively freeze or use a library','Clears all object properties','Prevents the object from being garbage collected'],answer:1,explanation:'Object.freeze(obj): properties become non-writable, non-configurable, non-deletable. Shallow freeze — nested objects can still be mutated. For records and tuples (TC39 proposal), use the upcoming primitive value types.'},
{id:1123,topic:'javascript',difficulty:'intermediate',question:'What is "Object.assign()" vs spread operator for object copying?',options:['They are identical in all scenarios','Object.assign(target, ...sources): copies own enumerable properties from sources to target (mutates target), returns target. Spread {...obj}: creates a new object with copied properties (no mutation). Both are SHALLOW copies — nested objects are referenced, not cloned','Object.assign() makes deep copies','Spread operator only works with arrays'],answer:1,explanation:'Shallow copy: {...obj} or Object.assign({}, obj) — only first-level properties are copied; nested objects still share references. For deep copy: structuredClone(obj) or JSON.parse(JSON.stringify(obj)) (limited).'},
{id:1124,topic:'javascript',difficulty:'intermediate',question:'What is JavaScript "prototype-based inheritance" vs "class-based inheritance"?',options:['JavaScript uses classical class-based inheritance only','JavaScript is prototype-based: objects inherit directly from other objects via the prototype chain. ES6 class syntax is syntactic sugar over prototype-based inheritance — classes do NOT create a separate class/instance type distinction, just constructor functions with prototype setup','class and prototype are completely separate systems','ES6 classes replaced the prototype system'],answer:1,explanation:'ES6 class A extends B {} desugars to: A.prototype = Object.create(B.prototype); — the prototype chain still drives inheritance. class is syntax sugar; typeof A === "function". Understanding prototypes helps debug unexpected "class" behaviour.'},
{id:1125,topic:'javascript',difficulty:'advanced',question:'What is "JavaScript object property descriptors" and Object.defineProperty()?',options:['CSS property definitions for JavaScript','Properties have descriptors with: value, writable (reassignable?), enumerable (shows in for...in/Object.keys?), configurable (can descriptor be changed/deleted?). Object.defineProperty(obj, prop, descriptor) enables precise control over property characteristics','A method for documenting object APIs','The JSON schema for JavaScript objects'],answer:1,explanation:'Object.defineProperty(obj, "x", {value:42, writable:false, enumerable:false, configurable:false}): creates a non-writable, hidden, locked property. Getters/setters: {get() {return this._x}, set(v) {this._x=v}}. The foundation of Vue 2\'s reactivity.'},
{id:1126,topic:'javascript',difficulty:'advanced',question:'What is "Symbol.iterator" and custom iterables?',options:['A symbol for iterating over Symbols','Makes a custom object iterable with for...of, spread, and destructuring by implementing [Symbol.iterator]() returning an iterator with next() → {value, done}. class Range { [Symbol.iterator]() { let n=this.start; return { next: () => n<=this.end ? {value:n++,done:false} : {done:true} } } }','A built-in iterator for arrays','A way to convert objects to arrays'],answer:1,explanation:'Custom iterables: implement [Symbol.iterator]() returning an object with next(). This enables: for (const x of customObj), [...customObj], destructuring [a,b,c] = customObj. Generators automatically implement the iterator protocol.'},
{id:1127,topic:'javascript',difficulty:'advanced',question:'What is "JavaScript module federation" and micro-frontends?',options:['Combining JavaScript files','A Webpack 5 / Vite concept enabling separate JavaScript bundles (micro-frontends) to share code at runtime — one application can dynamically load components from another deployed application, enabling independent deployment of frontend modules','A way to split code between multiple teams','A dependency management strategy'],answer:1,explanation:'Module Federation: expose() makes modules available; remotes() consumes them. Teams deploy independently; consumers load the latest version at runtime. Enables micro-frontend architectures where each team owns its chunk of the UI.'},
{id:1128,topic:'javascript',difficulty:'advanced',question:'What is "Content Security Policy" (CSP) and how does it relate to JavaScript?',options:['A policy for JavaScript coding standards','An HTTP header (Content-Security-Policy) defining which sources the browser may load resources from; restricts inline JavaScript (preventing XSS), eval(), and external script sources — a critical web security control','A security policy for JavaScript variables','A browser setting for JavaScript permissions'],answer:1,explanation:'CSP headers restrict: script-src (which scripts allowed), no inline scripts (unless nonce/hash), no eval(). Prevents XSS by ensuring injected scripts cannot execute. Violations are reported to a reporting endpoint.'},
{id:1129,topic:'javascript',difficulty:'advanced',question:'What is "Web Worker" in JavaScript?',options:['A background Node.js process','A browser API for running JavaScript code in a background thread (separate from the main UI thread) — enabling heavy computation without blocking UI. Workers communicate with the main thread via postMessage(). No DOM access','A server-side worker thread','A service worker for push notifications'],answer:1,explanation:'Web Workers: dedicated worker (one sender) or shared worker (multiple tabs). postMessage(data)/onmessage event for communication. Transferable objects (ArrayBuffer) pass by reference (zero-copy). Use for image processing, encryption, large data parsing.'},
{id:1130,topic:'javascript',difficulty:'advanced',question:'What is a "Service Worker" in JavaScript?',options:['A Web Worker that serves HTML','A browser JavaScript script running in the background, separate from the web page, intercepting and modifying network requests, caching responses (enabling offline functionality), and supporting push notifications and background sync — the foundation of Progressive Web Apps (PWA)','A Node.js server-side worker','A background thread for DOM updates'],answer:1,explanation:'Service Worker: intercepts fetch (to serve from cache), receives push notifications (even when page is closed), background sync. Lifecycle: install → activate → fetch. Required: HTTPS (or localhost).'},
{id:1131,topic:'javascript',difficulty:'beginner',question:'What does "Array.isArray()" check?',options:['Checks if a variable is any iterable','Returns true if the argument is an Array, false otherwise — more reliable than instanceof Array (which fails across frames/realms) or typeof (which returns "object")','Checks if an array is sorted','Checks if all elements are of the same type'],answer:1,explanation:'Array.isArray(value): the reliable way to check for arrays. typeof [] === "object" (not helpful). [] instanceof Array fails across iframes/different realms. Array.isArray is the correct approach.'},
{id:1132,topic:'javascript',difficulty:'beginner',question:'What is "Array.prototype.find()" vs "Array.prototype.findIndex()"?',options:['They are identical','find(callback): returns the first element passing the test, or undefined if none found. findIndex(callback): returns the index of the first passing element, or -1 if none found','find() returns all matches; findIndex() returns the first','find() works on arrays; findIndex() on strings'],answer:1,explanation:'find(): [1,2,3].find(x => x > 1) = 2. findIndex(): [1,2,3].findIndex(x => x > 1) = 1. Use findIndex() when you need to splice or know the position; find() when you just need the value.'},
{id:1133,topic:'javascript',difficulty:'beginner',question:'What does "Array.prototype.some()" return?',options:['The first truthy element','Returns true if at least one element in the array passes the test function: [1,2,3].some(x => x > 2) = true. Short-circuits (stops) at first truthy test. Returns false for an empty array','Returns all truthy elements','Returns the index of a truthy element'],answer:1,explanation:'some(callback): short-circuit OR evaluation across the array. Returns true as soon as one element passes. every(callback): short-circuit AND — returns false as soon as one fails. some([]) = false; every([]) = true (vacuous truth).'},
{id:1134,topic:'javascript',difficulty:'beginner',question:'What does "Array.prototype.every()" return?',options:['Returns true only for empty arrays','Returns true if ALL elements pass the test function: [2,4,6].every(x => x%2 === 0) = true. Short-circuits at the first failing test. every([]) = true (vacuously true)','Returns an array of boolean values','Returns a count of passing elements'],answer:1,explanation:'every(callback): returns true if all elements pass the test (AND across all elements). Returns false immediately on first failure. every([]) = true (empty array has no failing elements).'},
{id:1135,topic:'javascript',difficulty:'intermediate',question:'What is "JavaScript strict mode" ("use strict")?',options:['A strict typing mode like TypeScript','A mode activated with "use strict"; that enforces stricter parsing and error handling: prevents undeclared variables, disallows duplicate parameters, makes "this" undefined in regular function calls (not method calls), and more. ES6 modules are always in strict mode','A performance mode for JavaScript','A debugging mode in Node.js'],answer:1,explanation:'"use strict": cannot use undeclared vars (throws ReferenceError instead of creating globals), this = undefined in non-method function calls (not window), no with statement, no octal literals, duplicate properties/params throw errors.'},
{id:1136,topic:'javascript',difficulty:'intermediate',question:'What is the difference between "for...in" and "for...of" loops?',options:['They are identical','for...in: iterates over all enumerable property KEYS (including inherited prototype keys) of an object. for...of: iterates over VALUES of an iterable (Array, Set, Map, String, generator). Use for...of for arrays (avoid for...in on arrays)','for...in is for arrays; for...of is for objects','for...of requires ES7+'],answer:1,explanation:'for...in on arrays iterates over string index keys ("0","1","2") and may include prototype properties — use for...of for arrays. for...of cannot iterate plain objects (not iterable) — use for...in or Object.entries() for objects.'},
{id:1137,topic:'javascript',difficulty:'intermediate',question:'What is "Object.keys()", "Object.values()", and "Object.entries()"?',options:['Methods inherited from class Object','Static methods returning arrays of the object\'s own enumerable string-keyed property: keys() → key strings, values() → values, entries() → [key,value] pairs. None include prototype properties. ES6+','Methods for modifying object properties','Methods for deep-copying objects'],answer:1,explanation:'Object.keys(obj) → ["a","b"]. Object.values(obj) → [1,2]. Object.entries(obj) → [["a",1],["b",2]]. Useful with destructuring and for building Maps from objects: new Map(Object.entries(obj)).'},
{id:1138,topic:'javascript',difficulty:'intermediate',question:'What is the "Intl" API in JavaScript?',options:['An Intel processor optimisation API','The ECMAScript Internationalisation API providing locale-sensitive string comparison, number formatting, date/time formatting, and more: Intl.NumberFormat, Intl.DateTimeFormat, Intl.RelativeTimeFormat, Intl.ListFormat — without external libraries','An API for internal JavaScript operations','A type for international string encoding'],answer:1,explanation:'Intl: new Intl.NumberFormat("en-US", {style:"currency",currency:"USD"}).format(1234.56) = "$1,234.56". new Intl.DateTimeFormat("fr-FR").format(date) = French-formatted date. Locale-aware without third-party libraries.'},
{id:1139,topic:'javascript',difficulty:'advanced',question:'What is "JavaScript compilation" (V8 pipeline)?',options:['Compiling JS to machine code at installation','V8 pipeline: parse → AST → Ignition (bytecode interpreter, fast startup) → profiling identifies hot functions → TurboFan (JIT compiler, optimised machine code with deoptimisation fallback when assumptions fail)','JavaScript does not compile','Compiling TypeScript to JavaScript'],answer:1,explanation:'V8: Ignition interpreter generates bytecode quickly for startup. TurboFan JIT compiles hot paths using speculative optimisations. Deoptimisation occurs when type assumptions (monomorphic call sites) are violated — a key reason for consistent types in hot paths.'},
{id:1140,topic:'javascript',difficulty:'advanced',question:'What is "JavaScript V8 hidden classes" (object shapes) optimisation?',options:['CSS classes optimised by V8','V8 assigns hidden classes (shapes/maps) to objects based on their property layout. Adding properties in the same order creates the same hidden class, enabling JIT-compiled code to use fixed offsets. Inconsistent property order creates different shapes, degrading performance','A rendering optimisation for CSS classes','Hidden HTML classes used by JavaScript'],answer:1,explanation:'V8 hidden classes: obj.x = 1; obj.y = 2 (shape A) vs obj.y = 2; obj.x = 1 (shape B) — different shapes require separate JIT code paths, missing the "monomorphic" fast path. Always initialise all properties in the constructor in the same order.'}
]);
