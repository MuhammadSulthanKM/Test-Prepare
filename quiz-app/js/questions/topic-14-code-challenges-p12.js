window.QUIZ_BANK['code-challenges'] = (window.QUIZ_BANK['code-challenges']||[]).concat([
{id:9651,topic:'code-challenges',difficulty:'beginner',question:'What does this Selenium code do?\n\nWebDriver driver = new ChromeDriver();\ndriver.get("https://example.com");\nSystem.out.println(driver.getTitle());',options:['Opens Chrome and prints the page title','Opens Firefox','Throws an exception','Does nothing'],answer:0,explanation:'ChromeDriver opens a Chrome browser, navigates to the URL, and getTitle() returns the page title which is printed.'},
{id:9652,topic:'code-challenges',difficulty:'beginner',question:'What does this Selenium locator find?\n\ndriver.findElement(By.id("username"));',options:['First element with class "username"','Element with id attribute equal to "username"','All elements named username','Element with text "username"'],answer:1,explanation:'By.id() locates a single element whose id attribute matches the given string exactly.'},
{id:9653,topic:'code-challenges',difficulty:'beginner',question:'What does this Playwright code do?\n\nawait page.goto("https://example.com");\nawait page.click("#submit");\n',options:['Navigates to the URL then clicks the element with id="submit"','Only clicks','Only navigates','Throws an error'],answer:0,explanation:'page.goto() navigates to the URL. page.click("#submit") clicks the element matching the CSS selector #submit.'},
{id:9654,topic:'code-challenges',difficulty:'beginner',question:'What does this Selenium code return?\n\nWebElement el = driver.findElement(By.name("q"));\nel.sendKeys("hello");\nSystem.out.println(el.getAttribute("value"));',options:['hello','q','null','Error'],answer:0,explanation:'sendKeys types "hello" into the element. getAttribute("value") retrieves the current value of the input field, which is "hello".'},
{id:9655,topic:'code-challenges',difficulty:'beginner',question:'What does this Playwright assertion check?\n\nawait expect(page.locator("#title")).toHaveText("Welcome");',options:['Checks that the element with id="title" has text "Welcome"','Clicks the title','Navigates to Welcome','Fails always'],answer:0,explanation:'expect(...).toHaveText() asserts that the matched element\'s text content equals "Welcome". If it does not, the test fails.'},
{id:9656,topic:'code-challenges',difficulty:'beginner',question:'What does this Selenium explicit wait do?\n\nWebDriverWait wait = new WebDriverWait(driver, Duration.ofSeconds(10));\nWebElement el = wait.until(ExpectedConditions.visibilityOfElementLocated(By.id("msg")));',options:['Waits up to 10 seconds for element to be visible','Sleeps 10 seconds always','Fails immediately if element not found','Clicks the element'],answer:0,explanation:'WebDriverWait polls the condition. visibilityOfElementLocated waits up to 10 seconds for the element to become visible, then returns it.'},
{id:9657,topic:'code-challenges',difficulty:'beginner',question:'What does this Playwright code check?\n\nawait expect(page.locator("button")).toBeEnabled();',options:['Checks button is visible','Checks button is not disabled','Clicks the button','Waits 5 seconds'],answer:1,explanation:'toBeEnabled() asserts that the button element is not disabled (i.e., the disabled attribute is not set).'},
{id:9658,topic:'code-challenges',difficulty:'beginner',question:'What does this Selenium code print?\n\nList<WebElement> items = driver.findElements(By.tagName("li"));\nSystem.out.println(items.size());',options:['The number of <li> elements on the page','Always 0','Always 1','Error'],answer:0,explanation:'findElements returns a List of all matching elements. items.size() prints how many <li> elements were found on the page.'},
{id:9659,topic:'code-challenges',difficulty:'beginner',question:'What does this Playwright screenshot capture?\n\nawait page.screenshot({ path: "screenshot.png" });',options:['Takes a screenshot of the visible viewport','Takes a screenshot of the full page','Records a video','Throws an error'],answer:0,explanation:'By default, page.screenshot() captures the visible viewport. To capture the full page, { fullPage: true } must be passed.'},
{id:9660,topic:'code-challenges',difficulty:'beginner',question:'What does this Selenium code do?\n\ndriver.navigate().back();',options:['Refreshes the page','Goes back to the previous page in history','Closes the browser','Navigates to homepage'],answer:1,explanation:'driver.navigate().back() simulates clicking the browser back button, going to the previous page in history.'},
{id:9661,topic:'code-challenges',difficulty:'beginner',question:'What does this Playwright locator do?\n\nawait page.locator("text=Login").click();',options:['Clicks the element containing the text "Login"','Finds an element by id "Login"','Navigates to Login page','Fills a form'],answer:0,explanation:'The text= selector matches elements whose visible text contains "Login". .click() then clicks that element.'},
{id:9662,topic:'code-challenges',difficulty:'beginner',question:'What does this Selenium code verify?\n\nAssert.assertTrue(driver.findElement(By.id("success")).isDisplayed());',options:['Element exists in DOM','Element is visible on screen','Element has text "success"','Element is clickable'],answer:1,explanation:'isDisplayed() returns true if the element is visible (rendered on screen, not hidden). assertTrue verifies it is visible.'},
{id:9663,topic:'code-challenges',difficulty:'beginner',question:'What does this Playwright fill do?\n\nawait page.fill("#username", "admin");',options:['Clicks the element with id username','Types "admin" into the input field with id="username"','Submits the form','Clears the field'],answer:1,explanation:'page.fill() clears the input and types the given value. It fills the #username input with "admin".'},
{id:9664,topic:'code-challenges',difficulty:'beginner',question:'What does this Selenium code do?\n\ndriver.manage().window().maximize();',options:['Closes the browser','Maximizes the browser window','Resizes to 1024x768','Minimizes the window'],answer:1,explanation:'driver.manage().window().maximize() maximizes the browser window to fill the screen.'},
{id:9665,topic:'code-challenges',difficulty:'beginner',question:'What does this Playwright check do?\n\nawait expect(page.locator("#error")).toBeHidden();',options:['Clicks the hidden element','Asserts that the element is not visible','Waits for element to appear','Deletes the element'],answer:1,explanation:'toBeHidden() asserts the element is either not in the DOM or has display:none/visibility:hidden. The test fails if the element is visible.'},
{id:9666,topic:'code-challenges',difficulty:'beginner',question:'What does this Selenium code output?\n\nWebElement btn = driver.findElement(By.cssSelector(".submit-btn"));\nSystem.out.println(btn.getText());',options:['The CSS class name','The text content of the element','The element tag name','null'],answer:1,explanation:'getText() returns the visible text content of the element, i.e., whatever text is displayed inside the .submit-btn element.'},
{id:9667,topic:'code-challenges',difficulty:'beginner',question:'What does this Playwright waitForSelector do?\n\nawait page.waitForSelector("#result", { state: "visible" });',options:['Waits until #result exists in DOM','Waits until #result is visible','Clicks #result when visible','Throws immediately if not found'],answer:1,explanation:'waitForSelector with state:"visible" waits until the element matching #result becomes visible on the page before continuing.'},
{id:9668,topic:'code-challenges',difficulty:'beginner',question:'What does this Selenium code do?\n\nSelect dropdown = new Select(driver.findElement(By.id("color")));\ndropdown.selectByVisibleText("Blue");',options:['Types "Blue" into a text field','Selects the option with visible text "Blue" in a <select> element','Clicks a button labeled Blue','Asserts Blue is selected'],answer:1,explanation:'The Select class wraps <select> elements. selectByVisibleText("Blue") chooses the <option> whose displayed text is "Blue".'},
{id:9669,topic:'code-challenges',difficulty:'beginner',question:'What does this Playwright evaluate do?\n\nconst title = await page.evaluate(() => document.title);\nconsole.log(title);',options:['Returns the page URL','Returns and prints the page title','Clicks the title element','Throws an error'],answer:1,explanation:'page.evaluate() runs JavaScript in the browser context. document.title returns the page title, which is then logged.'},
{id:9670,topic:'code-challenges',difficulty:'beginner',question:'What does this Selenium implicit wait configure?\n\ndriver.manage().timeouts().implicitlyWait(Duration.ofSeconds(5));',options:['Sleeps for 5 seconds before every action','Waits up to 5 seconds for elements to be found before throwing NoSuchElementException','Sets page load timeout to 5 seconds','Does nothing'],answer:1,explanation:'implicitlyWait sets a global wait time. When findElement is called and the element is not immediately found, Selenium polls for up to 5 seconds before throwing NoSuchElementException.'},
{id:9671,topic:'code-challenges',difficulty:'intermediate',question:'What does this Selenium Page Object do?\n\nclass LoginPage {\n    WebDriver driver;\n    @FindBy(id="username") WebElement usernameField;\n    @FindBy(id="password") WebElement passwordField;\n    @FindBy(css=".login-btn") WebElement loginBtn;\n\n    LoginPage(WebDriver d) {\n        this.driver = d;\n        PageFactory.initElements(d, this);\n    }\n\n    void login(String u, String p) {\n        usernameField.sendKeys(u);\n        passwordField.sendKeys(p);\n        loginBtn.click();\n    }\n}',options:['Defines a reusable login abstraction using Page Object Model','Directly tests the login page','Creates a new browser instance','Throws an error'],answer:0,explanation:'This is the Page Object Model pattern. @FindBy annotations locate elements. PageFactory.initElements initializes them. login() encapsulates the login interaction for reuse in tests.'},
{id:9672,topic:'code-challenges',difficulty:'intermediate',question:'What does this Playwright request interception do?\n\nawait page.route("**/api/users", route => {\n    route.fulfill({\n        status: 200,\n        body: JSON.stringify([{id:1, name:"Alice"}])\n    });\n});',options:['Blocks all API calls','Mocks the /api/users response with fake data','Redirects to a different URL','Logs the request'],answer:1,explanation:'page.route() intercepts network requests matching the pattern. route.fulfill() responds with the provided mock data instead of making a real network call.'},
{id:9673,topic:'code-challenges',difficulty:'intermediate',question:'What does this Selenium Actions chain do?\n\nnew Actions(driver)\n    .moveToElement(menu)\n    .click(submenu)\n    .perform();',options:['Clicks menu then submenu','Hovers over menu then clicks submenu','Double clicks menu','Drags menu to submenu'],answer:1,explanation:'Actions chain: moveToElement hovers over the menu (triggering hover dropdowns), then clicks the submenu. .perform() executes the chain.'},
{id:9674,topic:'code-challenges',difficulty:'intermediate',question:'What does this Playwright dialog handler do?\n\npage.on("dialog", dialog => {\n    console.log(dialog.message());\n    dialog.accept();\n});\nawait page.click("#alert-btn");',options:['Closes the browser on dialog','Logs the alert message and accepts it','Ignores the dialog','Throws on dialog'],answer:1,explanation:'The dialog event fires when an alert/confirm/prompt appears. dialog.message() gets the text, dialog.accept() clicks OK. Both run before the dialog blocks execution.'},
{id:9675,topic:'code-challenges',difficulty:'intermediate',question:'What does this Selenium JavaScript executor do?\n\nJavascriptExecutor js = (JavascriptExecutor) driver;\njs.executeScript("arguments[0].scrollIntoView(true);", element);',options:['Deletes the element','Scrolls the page so the element is visible','Clicks the element via JS','Returns element coordinates'],answer:1,explanation:'executeScript runs JavaScript in the browser. scrollIntoView(true) scrolls the page until the element is visible in the viewport.'},
{id:9676,topic:'code-challenges',difficulty:'intermediate',question:'What does this Playwright locator chain do?\n\nawait page.locator("table").locator("tr").nth(2).locator("td").first().click();',options:['Clicks the first cell of the third table row','Clicks the entire table','Clicks the last row','Error'],answer:0,explanation:'Chains: locates the table, then all tr elements, takes index 2 (3rd row, 0-based), finds td elements, takes the first, and clicks it.'},
{id:9677,topic:'code-challenges',difficulty:'intermediate',question:'What does this Selenium stale element handling do?\n\ntry {\n    element.click();\n} catch (StaleElementReferenceException e) {\n    driver.findElement(By.id("btn")).click();\n}',options:['Retries the click if the element is stale','Ignores the error','Fails the test','Refreshes the page'],answer:0,explanation:'StaleElementReferenceException occurs when the DOM is updated and the old reference is no longer valid. The catch block re-finds the element and retries the click.'},
{id:9678,topic:'code-challenges',difficulty:'intermediate',question:'What does this Playwright network monitoring do?\n\nconst [response] = await Promise.all([\n    page.waitForResponse("**/api/data"),\n    page.click("#load-btn")\n]);\nconsole.log(response.status());',options:['Waits for the click then the response','Clicks the button and simultaneously waits for the API response, then logs status','Only logs status','Error'],answer:1,explanation:'Promise.all starts both concurrently: waitForResponse waits for the matching request, click triggers it. Both resolve together. response.status() logs the HTTP status code.'},
{id:9679,topic:'code-challenges',difficulty:'intermediate',question:'What does this Selenium frame switching do?\n\ndriver.switchTo().frame("iframe-id");\ndriver.findElement(By.id("inner-btn")).click();\ndriver.switchTo().defaultContent();',options:['Opens a new window','Switches into an iframe to interact with its content then returns to main page','Refreshes inside frame','Closes the iframe'],answer:1,explanation:'switchTo().frame() switches driver context into the iframe. Elements inside the frame can be interacted with. switchTo().defaultContent() returns to the main page context.'},
{id:9680,topic:'code-challenges',difficulty:'intermediate',question:'What does this Playwright screenshot comparison do?\n\nawait expect(page).toHaveScreenshot("baseline.png");',options:['Takes a screenshot','Asserts the page matches a previously saved baseline screenshot','Deletes the screenshot','Fails always on first run'],answer:1,explanation:'toHaveScreenshot() performs visual regression testing. It compares the current page against a stored baseline. On first run, it creates the baseline. Subsequent runs compare against it.'},
{id:9681,topic:'code-challenges',difficulty:'intermediate',question:'What does this Selenium cookie handling do?\n\nCookie c = new Cookie("session", "abc123");\ndriver.manage().addCookie(c);\ndriver.navigate().refresh();\nSystem.out.println(driver.manage().getCookieNamed("session").getValue());',options:['null','abc123','session','Error'],answer:1,explanation:'A cookie named "session" with value "abc123" is added. After refresh (cookie is still present), getCookieNamed retrieves it and getValue() returns "abc123".'},
{id:9682,topic:'code-challenges',difficulty:'intermediate',question:'What does this Playwright mobile emulation do?\n\nconst iPhone = devices["iPhone 12"];\nconst context = await browser.newContext({ ...iPhone });\nconst page = await context.newPage();',options:['Opens a real iPhone','Emulates iPhone 12 dimensions and user agent in the browser','Takes a mobile screenshot','Fails without a real device'],answer:1,explanation:'Playwright\'s devices dictionary contains presets for device emulation. Spreading iPhone 12 config into newContext sets viewport size, user agent, and touch capabilities.'},
{id:9683,topic:'code-challenges',difficulty:'intermediate',question:'What does this Selenium window handling do?\n\nString main = driver.getWindowHandle();\ndriver.findElement(By.id("new-tab")).click();\nfor (String w : driver.getWindowHandles()) {\n    if (!w.equals(main)) {\n        driver.switchTo().window(w);\n        break;\n    }\n}',options:['Closes all windows','Switches to the newly opened window/tab','Refreshes all windows','Error'],answer:1,explanation:'getWindowHandle() saves the main window. After clicking the link that opens a new tab, getWindowHandles() returns all open windows. Iterating finds the new handle and switches to it.'},
{id:9684,topic:'code-challenges',difficulty:'intermediate',question:'What does this Playwright trace recording do?\n\nawait context.tracing.start({ screenshots: true, snapshots: true });\nawait page.goto("https://example.com");\nawait context.tracing.stop({ path: "trace.zip" });',options:['Records a video','Records a trace file with screenshots and DOM snapshots for debugging','Takes a single screenshot','Fails'],answer:1,explanation:'Playwright tracing records network requests, screenshots, and DOM snapshots. The trace.zip can be opened in Playwright\'s trace viewer for debugging test failures.'},
{id:9685,topic:'code-challenges',difficulty:'intermediate',question:'What does this Selenium drag and drop do?\n\nnew Actions(driver)\n    .dragAndDrop(source, target)\n    .perform();',options:['Clicks source then target','Moves source element to target position','Scrolls from source to target','Double-clicks source'],answer:1,explanation:'dragAndDrop simulates a drag-and-drop operation. It clicks and holds source, moves to target, and releases, effectively moving the draggable element.'},
{id:9686,topic:'code-challenges',difficulty:'intermediate',question:'What does this Playwright evaluate handle do?\n\nconst handle = await page.evaluateHandle(() => document.body);\nconsole.log(await handle.jsonValue());',options:['Returns the body HTML string','Returns a JS handle to the body element; jsonValue() serializes it','Throws error','Returns null'],answer:1,explanation:'evaluateHandle returns a JSHandle (reference to a browser object). jsonValue() attempts to serialize it — for complex DOM elements this returns a basic representation.'},
{id:9687,topic:'code-challenges',difficulty:'advanced',question:'What does this Selenium FluentWait do?\n\nWait<WebDriver> wait = new FluentWait<>(driver)\n    .withTimeout(Duration.ofSeconds(30))\n    .pollingEvery(Duration.ofSeconds(2))\n    .ignoring(NoSuchElementException.class);\nWebElement el = wait.until(d -> d.findElement(By.id("result")));',options:['Waits exactly 30 seconds','Polls every 2 seconds up to 30 seconds, ignoring NoSuchElementException','Fails immediately if not found','Sleeps then finds'],answer:1,explanation:'FluentWait customizes polling interval (2s) and max timeout (30s). ignoring(NoSuchElementException) prevents the wait from failing when the element is temporarily absent.'},
{id:9688,topic:'code-challenges',difficulty:'advanced',question:'What does this Playwright parallelism config enable?\n\n// playwright.config.ts\nexport default {\n    workers: 4,\n    use: { headless: true }\n};',options:['Runs 4 test files sequentially','Runs up to 4 test workers concurrently','Opens 4 browser windows per test','Error'],answer:1,explanation:'workers: 4 in playwright.config.ts configures Playwright to run up to 4 worker processes simultaneously, each running a subset of tests in parallel.'},
{id:9689,topic:'code-challenges',difficulty:'advanced',question:'What does this Selenium Grid configuration specify?\n\nDriverOptions options = new ChromeOptions();\noptions.setCapability("browserName", "chrome");\noptions.setCapability("browserVersion", "120");\nWebDriver driver = new RemoteWebDriver(new URL("http://hub:4444"), options);',options:['Opens local Chrome','Connects to Selenium Grid hub and requests Chrome 120','Connects to BrowserStack','Fails without a grid'],answer:1,explanation:'RemoteWebDriver with a hub URL connects to a Selenium Grid. The capabilities specify the browser and version to request from available grid nodes.'},
{id:9690,topic:'code-challenges',difficulty:'advanced',question:'What does this Playwright fixture do?\n\nconst test = base.extend({\n    loggedInPage: async ({ page }, use) => {\n        await page.goto("/login");\n        await page.fill("#user", "admin");\n        await page.fill("#pass", "secret");\n        await page.click("#submit");\n        await use(page);\n    }\n});\n\ntest("dashboard", async ({ loggedInPage }) => {\n    await expect(loggedInPage.locator("h1")).toHaveText("Dashboard");\n});',options:['Logs in once for all tests','Creates a reusable fixture that logs in before each test using it','Skips login','Error'],answer:1,explanation:'Playwright fixtures use a setup/use/teardown pattern. loggedInPage logs in and passes the authenticated page to tests that declare it. Each test gets its own logged-in page.'},
{id:9691,topic:'code-challenges',difficulty:'advanced',question:'What does this Selenium CDP integration do?\n\nDevTools devTools = ((ChromeDriver) driver).getDevTools();\ndevTools.createSession();\ndevTools.send(Network.enable(Optional.empty(), Optional.empty(), Optional.empty()));\ndevTools.addListener(Network.requestWillBeSent(), r -> \n    System.out.println(r.getRequest().getUrl()));',options:['Blocks all network requests','Logs every outgoing request URL using Chrome DevTools Protocol','Takes a screenshot','Error'],answer:1,explanation:'Chrome DevTools Protocol (CDP) via Selenium 4 enables low-level browser control. Network.requestWillBeSent event fires for each request; listener prints the URL.'},
{id:9692,topic:'code-challenges',difficulty:'advanced',question:'What does this Playwright global setup do?\n\n// globalSetup.ts\nexport default async function() {\n    const browser = await chromium.launch();\n    const page = await browser.newPage();\n    await page.goto("/login");\n    await page.fill("#user","admin");\n    await page.click("#submit");\n    await page.context().storageState({ path: "auth.json" });\n    await browser.close();\n}',options:['Logs in before every test','Logs in once and saves browser state (cookies/localStorage) for reuse by all tests','Fails because browser closes','Runs after tests'],answer:1,explanation:'globalSetup runs once before the entire test suite. It logs in, saves the authenticated storage state to auth.json. Tests can then load this state to skip login.'},
{id:9693,topic:'code-challenges',difficulty:'advanced',question:'What does this Selenium @FindAll do?\n\n@FindAll({\n    @FindBy(id = "menu1"),\n    @FindBy(id = "menu2")\n})\nList<WebElement> menus;',options:['Finds the element matching both selectors','Finds all elements matching any of the selectors','Finds elements matching the first selector only','Error'],answer:1,explanation:'@FindAll creates a union of all elements found by any of the @FindBy selectors. Both menu1 and menu2 elements are included in the menus list.'},
{id:9694,topic:'code-challenges',difficulty:'advanced',question:'What does this Playwright API test do?\n\nconst response = await request.get("https://api.example.com/users");\nexpect(response.status()).toBe(200);\nconst body = await response.json();\nexpect(body).toHaveLength(3);',options:['Opens a browser and navigates','Makes an HTTP GET request and validates response status and body length','Fails because no browser is open','Mocks the response'],answer:1,explanation:'Playwright\'s APIRequestContext makes direct HTTP requests without a browser. It verifies the response status is 200 and the JSON body array has 3 elements.'},
{id:9695,topic:'code-challenges',difficulty:'advanced',question:'What does this Selenium custom expected condition do?\n\nExpectedCondition<Boolean> ajaxDone = d ->\n    ((JavascriptExecutor)d).executeScript(\n        "return jQuery.active == 0"\n    ).equals(true);\nnew WebDriverWait(driver, Duration.ofSeconds(10))\n    .until(ajaxDone);',options:['Waits for page load','Waits until all jQuery AJAX calls are complete','Clicks when AJAX is done','Throws if jQuery is missing'],answer:1,explanation:'jQuery.active counts active AJAX requests. The custom condition returns true when all requests are complete (active==0). WebDriverWait polls this until true or timeout.'},
{id:9696,topic:'code-challenges',difficulty:'advanced',question:'What does this Playwright multi-page test do?\n\nconst [newPage] = await Promise.all([\n    context.waitForEvent("page"),\n    page.click("#open-tab")\n]);\nawait newPage.waitForLoadState();\nawait expect(newPage.locator("h1")).toHaveText("New Page");',options:['Fails because new page is a different context','Waits for and interacts with a new tab opened by the click','Closes the new tab','Error'],answer:1,explanation:'waitForEvent("page") listens for a new page to open. click triggers it. Promise.all captures both concurrently. newPage is the new tab — assertions run on it.'},
{id:9697,topic:'code-challenges',difficulty:'advanced',question:'What does this Selenium ShadowDOM access do?\n\nWebElement shadowHost = driver.findElement(By.id("host"));\nSearchContext shadowRoot = shadowHost.getShadowRoot();\nWebElement inner = shadowRoot.findElement(By.cssSelector(".inner-btn"));\ninner.click();',options:['Fails because shadow DOM is inaccessible','Accesses the shadow root and finds elements inside it','Finds elements by shadow class','Error'],answer:1,explanation:'getShadowRoot() (Selenium 4+) returns the shadow root of a shadow host element. findElement on the shadow root searches within the shadow DOM tree.'},
{id:9698,topic:'code-challenges',difficulty:'advanced',question:'What does this Playwright component test check?\n\nimport { test, expect } from "@playwright/experimental-ct-react";\nimport Button from "./Button";\n\ntest("renders label", async ({ mount }) => {\n    const component = await mount(<Button label="Click me"/>);\n    await expect(component).toHaveText("Click me");\n});',options:['Tests the full browser page','Mounts the React component in isolation and verifies its text','Fails without a server','Error'],answer:1,explanation:'Playwright Component Testing (CT) mounts individual components in isolation (like a unit test for UI). mount() renders the Button and the assertion checks its rendered text.'},
{id:9699,topic:'code-challenges',difficulty:'advanced',question:'What does this Selenium PageFactory @CacheLookup do?\n\n@FindBy(id="header")\n@CacheLookup\nWebElement header;',options:['Finds the element every time it is used','Finds and caches the element after the first lookup, reusing it','Clears the element cache','Error'],answer:1,explanation:'@CacheLookup prevents PageFactory from re-querying the DOM on each access after the first find. Useful for static elements that will not change, but risky for dynamic ones.'},
{id:9700,topic:'code-challenges',difficulty:'advanced',question:'What does this Playwright auth state fixture do?\n\n// playwright.config.ts\nuse: {\n    storageState: "auth.json"\n}\n\n// test\ntest("needs login", async ({ page }) => {\n    await page.goto("/dashboard");\n    await expect(page).toHaveURL("/dashboard");\n});',options:['Logs in during the test','Loads saved auth state (cookies/storage) so the test starts already authenticated','Fails because auth.json may not exist','Skips login entirely with no auth'],answer:1,explanation:'storageState in config loads cookies and localStorage from auth.json for every test. The test starts authenticated without needing to perform the login flow.'},
{id:9701,topic:'code-challenges',difficulty:'advanced',question:'What does this Selenium @FindBy(how=How.XPATH) locate?\n\n@FindBy(how=How.XPATH, using="//button[contains(text(),\'Submit\')]")\nWebElement submitBtn;',options:['Finds button by id "Submit"','Finds any button element whose text contains "Submit"','Finds button by class Submit','Error'],answer:1,explanation:'XPath //button[contains(text(),\'Submit\')] matches any <button> element whose visible text contains "Submit". The @FindBy annotation initializes this via PageFactory.'},
{id:9702,topic:'code-challenges',difficulty:'advanced',question:'What does this Playwright expect.poll do?\n\nawait expect.poll(async () => {\n    const res = await page.evaluate(() => window.counter);\n    return res;\n}, { timeout: 5000 }).toBe(5);',options:['Checks window.counter once','Polls window.counter until it equals 5 or times out after 5 seconds','Fails if counter is not 5 immediately','Waits exactly 5 seconds'],answer:1,explanation:'expect.poll repeatedly evaluates the function and checks the assertion. It keeps retrying until window.counter === 5 or 5 seconds elapse. Useful for async state changes.'},
{id:9703,topic:'code-challenges',difficulty:'advanced',question:'What does this Selenium test retry with Cucumber glue do?\n\n@Retries(3)\n@Scenario("Login should work")\npublic void loginTest() {\n    loginPage.login("admin","pass");\n    Assert.assertTrue(dashboardPage.isDisplayed());\n}',options:['Runs once and fails','Retries up to 3 times if the test fails','Passes regardless of result','Error'],answer:1,explanation:'@Retries(3) (from Cucumber or test frameworks) retries the scenario up to 3 times on failure. If any retry passes, the test is marked as passed.'},
{id:9704,topic:'code-challenges',difficulty:'advanced',question:'What does this Playwright request mock do?\n\nawait page.route("**/api/**", async route => {\n    const response = await route.fetch();\n    const json = await response.json();\n    json.modified = true;\n    await route.fulfill({ json });\n});',options:['Blocks all API calls','Intercepts API calls, fetches the real response, adds a field, and returns the modified response','Only adds modified field','Error'],answer:1,explanation:'route.fetch() makes the actual request. The response is parsed and modified (adding modified:true), then fulfilled with the altered body. Real + modified response pattern.'},
{id:9705,topic:'code-challenges',difficulty:'advanced',question:'What does this Selenium remote execution with capabilities achieve?\n\nDesiredCapabilities caps = new DesiredCapabilities();\ncaps.setBrowserName("chrome");\ncaps.setCapability("recordVideo", true);\ncaps.setCapability("screenResolution", "1920x1080");\nRemoteWebDriver driver = new RemoteWebDriver(hubUrl, caps);',options:['Opens Chrome locally','Connects to a remote grid requesting Chrome with video recording at 1920x1080','Fails without video support','Opens headless Chrome'],answer:1,explanation:'RemoteWebDriver with capabilities connects to Selenium Grid. recordVideo and screenResolution are cloud/grid provider-specific capabilities for recording and resolution.'},
{id:9706,topic:'code-challenges',difficulty:'advanced',question:'What does this Playwright locator evaluation do?\n\nconst count = await page.locator(".item").count();\nconsole.log(count);',options:['Clicks all items','Returns and logs the number of elements matching .item','Throws if 0 elements','Returns true/false'],answer:1,explanation:'locator(".item").count() returns the number of elements matching the CSS selector .item. It does not throw if zero are found — it returns 0.'},
{id:9707,topic:'code-challenges',difficulty:'advanced',question:'What does this Selenium soft assertions wrapper do?\n\nSoftAssertions softly = new SoftAssertions();\nsoftly.assertThat(title).isEqualTo("Home");\nsoftly.assertThat(url).contains("/home");\nsoftly.assertAll();',options:['Fails on first mismatch','Collects all failures and reports them together at assertAll()','Passes if any assertion passes','Error'],answer:1,explanation:'SoftAssertions (from AssertJ or TestNG) collect all failures. Both assertions run. assertAll() throws at the end with all accumulated failures.'},
{id:9708,topic:'code-challenges',difficulty:'advanced',question:'What does this Playwright codegen-produced test do?\n\nawait page.getByRole("button", { name: "Sign in" }).click();\nawait page.getByLabel("Email").fill("user@example.com");\nawait page.getByLabel("Password").fill("password");\nawait page.getByRole("button", { name: "Login" }).click();',options:['Uses brittle CSS selectors','Uses accessible role-based and label-based locators for robust selection','Fails without ARIA roles','Error'],answer:1,explanation:'getByRole() and getByLabel() are accessibility-based locators. They\'re resilient to CSS/ID changes and align with how screen readers interact with the page.'},
{id:9709,topic:'code-challenges',difficulty:'advanced',question:'What does this Selenium parallel TestNG configuration achieve?\n\n<suite parallel="tests" thread-count="2">\n  <test name="Chrome">\n    <parameter name="browser" value="chrome"/>\n    <classes><class name="LoginTest"/></classes>\n  </test>\n  <test name="Firefox">\n    <parameter name="browser" value="firefox"/>\n    <classes><class name="LoginTest"/></classes>\n  </test>\n</suite>',options:['Runs Chrome tests then Firefox','Runs LoginTest on Chrome and Firefox simultaneously','Opens both browsers in one test','Error'],answer:1,explanation:'parallel="tests" with thread-count="2" runs the two <test> blocks concurrently. LoginTest runs in Chrome in one thread and Firefox in another simultaneously.'},
{id:9710,topic:'code-challenges',difficulty:'advanced',question:'What does this Playwright accessibility test do?\n\nconst results = await new AxeBuilder({ page }).analyze();\nexpect(results.violations).toHaveLength(0);',options:['Tests visual layout','Runs an axe-core accessibility audit and asserts no violations','Passes always','Requires a screen reader'],answer:1,explanation:'AxeBuilder integrates axe-core accessibility engine with Playwright. analyze() scans the page for WCAG/accessibility violations. The assertion fails if any violations are found.'},
]);
