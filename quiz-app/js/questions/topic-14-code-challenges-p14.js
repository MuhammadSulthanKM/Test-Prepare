window.QUIZ_BANK['code-challenges'] = (window.QUIZ_BANK['code-challenges']||[]).concat([
{id:9761,topic:'code-challenges',difficulty:'beginner',question:'What does this Git command do?\n\ngit status',options:['Shows commit history','Shows staged, unstaged, and untracked file changes','Commits all changes','Pushes to remote'],answer:1,explanation:'git status displays the state of the working directory and staging area: which files are modified, staged, or untracked.'},
{id:9762,topic:'code-challenges',difficulty:'beginner',question:'What does this Git command output?\n\ngit log --oneline -3',options:['Shows all commits','Shows the last 3 commits in one-line format','Shows 3 branches','Shows 3 modified files'],answer:1,explanation:'--oneline formats each commit as one line (short hash + message). -3 limits to the 3 most recent commits.'},
{id:9763,topic:'code-challenges',difficulty:'beginner',question:'What does this Appium code do?\n\ndriver.findElement(By.id("com.app:id/btn_login")).click();',options:['Finds a web button by id','Finds and clicks a mobile element by its resource-id','Navigates to login screen','Types text'],answer:1,explanation:'Appium uses resource-ids for Android element identification. By.id() with the full package prefix finds the element, and click() taps it.'},
{id:9764,topic:'code-challenges',difficulty:'beginner',question:'What does this Git command do?\n\ngit add .',options:['Adds one file','Stages all changed files in the current directory','Commits all files','Pushes to remote'],answer:1,explanation:'git add . stages all modified and new files in the current directory and subdirectories for the next commit.'},
{id:9765,topic:'code-challenges',difficulty:'beginner',question:'What does this Appium swipe action do?\n\ndriver.swipe(startX, startY, endX, endY, duration);',options:['Clicks an element','Performs a swipe gesture from start coordinates to end coordinates','Zooms the screen','Rotates the device'],answer:1,explanation:'driver.swipe() performs a touch swipe gesture, simulating a finger drag from (startX,startY) to (endX,endY) over the given duration in milliseconds.'},
{id:9766,topic:'code-challenges',difficulty:'beginner',question:'What does this Git command do?\n\ngit diff HEAD~1 HEAD',options:['Shows uncommitted changes','Shows changes between the last two commits','Shows current branch','Shows remote differences'],answer:1,explanation:'git diff HEAD~1 HEAD compares the current commit (HEAD) with the one before it (HEAD~1), showing what changed in the most recent commit.'},
{id:9767,topic:'code-challenges',difficulty:'beginner',question:'What does this core testing code check?\n\nassertEquals("Expected title", "Home", actualTitle);',options:['Checks actualTitle equals "Expected title"','Checks actualTitle equals "Home" with message "Expected title" on failure','Checks "Home" equals "Expected title"','Prints "Expected title"'],answer:1,explanation:'assertEquals(message, expected, actual) — the first string is the failure message, second is expected, third is actual. Fails with "Expected title" if actualTitle != "Home".'},
{id:9768,topic:'code-challenges',difficulty:'beginner',question:'What does this Git command do?\n\ngit checkout -b feature/login',options:['Switches to existing branch "feature/login"','Creates and switches to a new branch "feature/login"','Merges feature/login','Deletes feature/login'],answer:1,explanation:'git checkout -b creates a new branch with the given name and immediately switches to it. Equivalent to git branch + git checkout.'},
{id:9769,topic:'code-challenges',difficulty:'beginner',question:'What does this Appium code check?\n\nString text = driver.findElement(By.id("com.app:id/tv_title")).getText();\nAssert.assertEquals("Welcome", text);',options:['Clicks the title element','Retrieves the element text and asserts it equals "Welcome"','Types "Welcome"','Fails always'],answer:1,explanation:'getText() retrieves the visible text of a mobile element. Assert.assertEquals verifies it matches "Welcome".'},
{id:9770,topic:'code-challenges',difficulty:'beginner',question:'What does this core testing teardown method do?\n\n@After\npublic void tearDown() {\n    if (driver != null) {\n        driver.quit();\n    }\n}',options:['Runs before each test','Closes the browser after each test if the driver was initialized','Skips if driver is null','Creates the driver'],answer:1,explanation:'@After (JUnit) or @AfterMethod (TestNG) runs after each test. The null check prevents a NullPointerException if setup failed. driver.quit() closes the browser.'},
{id:9771,topic:'code-challenges',difficulty:'beginner',question:'What does this Git command show?\n\ngit branch -a',options:['Shows only local branches','Shows all local and remote tracking branches','Shows deleted branches','Shows branch history'],answer:1,explanation:'git branch -a shows all branches: local ones and remote-tracking branches (prefixed with remotes/origin/).'},
{id:9772,topic:'code-challenges',difficulty:'beginner',question:'What does this Appium touchAction do?\n\nnew TouchAction(driver)\n    .press(PointOption.point(100, 500))\n    .waitAction(WaitOptions.waitOptions(Duration.ofMillis(500)))\n    .moveTo(PointOption.point(100, 100))\n    .release()\n    .perform();',options:['Clicks at (100,500)','Performs a press-hold-drag-release gesture (long press + swipe)','Rotates the screen','Takes a screenshot'],answer:1,explanation:'TouchAction chain: press at (100,500), wait 500ms (long press), move to (100,100), release. This simulates a long-press drag gesture.'},
{id:9773,topic:'code-challenges',difficulty:'beginner',question:'What does this Git command do?\n\ngit stash pop',options:['Saves current changes to stash','Restores the most recently stashed changes and removes them from stash','Lists stash entries','Deletes all stashes'],answer:1,explanation:'git stash pop restores the last stashed changes to the working directory and removes that entry from the stash stack.'},
{id:9774,topic:'code-challenges',difficulty:'beginner',question:'What does this core testing boundary test check?\n\nassertTrue("Value should be in range", value >= 1 && value <= 100);',options:['Checks value equals 1 or 100','Checks value is between 1 and 100 inclusive','Checks value is exactly 50','Fails if value is 0'],answer:1,explanation:'The condition value >= 1 && value <= 100 is a boundary check. assertTrue verifies the condition is true. The string is the failure message.'},
{id:9775,topic:'code-challenges',difficulty:'beginner',question:'What does this Git command do?\n\ngit rebase main',options:['Merges main into current branch with a merge commit','Replays current branch commits on top of main','Deletes main branch','Creates a new branch from main'],answer:1,explanation:'git rebase main moves the current branch\'s commits to start from the tip of main, creating a linear history without a merge commit.'},
{id:9776,topic:'code-challenges',difficulty:'beginner',question:'What does this Appium desired capability set?\n\ncaps.setCapability("noReset", true);',options:['Resets app data before each session','Prevents the app from resetting between sessions','Installs the app fresh','Clears app cache'],answer:1,explanation:'noReset=true tells Appium not to reset the app state (clear data/reinstall) between sessions. Useful for speed when state preservation is acceptable.'},
{id:9777,topic:'code-challenges',difficulty:'beginner',question:'What does this Git command do?\n\ngit cherry-pick abc1234',options:['Deletes commit abc1234','Applies the changes from commit abc1234 to the current branch','Reverts commit abc1234','Merges abc1234 branch'],answer:1,explanation:'git cherry-pick applies a specific commit from any branch to the current branch, creating a new commit with the same changes.'},
{id:9778,topic:'code-challenges',difficulty:'beginner',question:'What does this core testing assertion verify?\n\nassertNotEquals("old value", actualValue);',options:['Checks actualValue equals "old value"','Checks actualValue does NOT equal "old value"','Throws if not equal','Prints the difference'],answer:1,explanation:'assertNotEquals verifies the actual value is different from the given value. The test fails if actualValue equals "old value".'},
{id:9779,topic:'code-challenges',difficulty:'beginner',question:'What does this Appium getContext do?\n\nString context = driver.getContext();\nSystem.out.println(context);',options:['Returns the app version','Returns the current automation context: NATIVE_APP or WEBVIEW_xxx','Returns screen orientation','Returns device name'],answer:1,explanation:'getContext() returns the current Appium context. "NATIVE_APP" for native views, or "WEBVIEW_xxx" when operating within a WebView (hybrid app). Used to switch contexts.'},
{id:9780,topic:'code-challenges',difficulty:'beginner',question:'What does this Git command show?\n\ngit blame src/app.js',options:['Shows who deleted app.js','Shows each line of app.js with the commit and author who last changed it','Shows git log for app.js','Shows uncommitted changes in app.js'],answer:1,explanation:'git blame annotates each line of a file with the commit hash, author, and date of the last modification, helping trace who introduced specific code.'},
{id:9781,topic:'code-challenges',difficulty:'intermediate',question:'What does this Git hook do?\n\n#!/bin/sh\n# .git/hooks/pre-commit\nnpm run lint\nif [ $? -ne 0 ]; then\n    echo "Linting failed. Commit aborted."\n    exit 1\nfi',options:['Runs after commit','Runs lint before every commit and aborts the commit if lint fails','Runs only on push','Does nothing'],answer:1,explanation:'pre-commit hook runs before git commit completes. npm run lint runs the linter. If it exits with non-zero ($? -ne 0), the hook exits 1, aborting the commit.'},
{id:9782,topic:'code-challenges',difficulty:'intermediate',question:'What does this Appium hybrid app context switch do?\n\nSet<String> contexts = driver.getContextHandles();\nfor (String ctx : contexts) {\n    if (ctx.contains("WEBVIEW")) {\n        driver.context(ctx);\n        break;\n    }\n}',options:['Opens a new browser','Switches the Appium driver to the WebView context for web automation','Closes the native app','Error'],answer:1,explanation:'getContextHandles() lists all available contexts. The loop finds the WEBVIEW context and driver.context() switches to it, enabling web-style element interaction within a hybrid app.'},
{id:9783,topic:'code-challenges',difficulty:'intermediate',question:'What does this core testing parameterized boundary test verify?\n\n@ParameterizedTest\n@CsvSource({"0,false","1,true","100,true","101,false"})\nvoid rangeTest(int val, boolean expected) {\n    assertEquals(expected, isInRange(val));\n}\n// isInRange returns true for 1-100 inclusive',options:['Tests only one boundary','Tests both boundaries and edge cases beyond them','Fails all four','Error'],answer:1,explanation:'Four test cases cover the key boundary values: 0 (below min), 1 (min), 100 (max), 101 (above max). All four match the expected boolean from isInRange.'},
{id:9784,topic:'code-challenges',difficulty:'intermediate',question:'What does this GitHub Actions workflow step do?\n\nsteps:\n  - uses: actions/checkout@v4\n  - uses: actions/setup-java@v4\n    with:\n      java-version: "21"\n  - run: mvn test -Dsurefire.failIfNoSpecifiedTests=false',options:['Runs tests locally','Checks out code, sets up Java 21, and runs Maven tests in CI','Only sets up Java','Error'],answer:1,explanation:'GitHub Actions workflow: checkout@v4 gets the code, setup-java configures Java 21, and mvn test runs the test suite. This is a standard CI pipeline for Java projects.'},
{id:9785,topic:'code-challenges',difficulty:'intermediate',question:'What does this Appium image-based element finder do?\n\nImageElement el = (ImageElement) driver.findElement(\n    MobileBy.image(base64EncodedImage)\n);\nel.click();',options:['Finds element by color','Finds a UI element that visually matches the provided base64 image template','Error','Finds any image element'],answer:1,explanation:'Appium image-based matching finds elements by visual comparison against a template image (base64 encoded). Useful for elements with no accessible attributes.'},
{id:9786,topic:'code-challenges',difficulty:'intermediate',question:'What does this Git revert do vs reset?\n\ngit revert abc1234',options:['Deletes the commit from history','Creates a new commit that undoes the changes of abc1234 without altering history','Same as git reset --hard abc1234','Stages the reversal'],answer:1,explanation:'git revert creates a NEW commit that reverses the changes of the specified commit. History is preserved. git reset --hard would move the HEAD pointer and may lose commits.'},
{id:9787,topic:'code-challenges',difficulty:'intermediate',question:'What does this core testing mock assertion do?\n\nMockito.verify(mockEmailService, times(2)).sendEmail(anyString());\nMockito.verify(mockEmailService, never()).sendEmail("admin@test.com");',options:['Checks email was sent once','Verifies sendEmail was called exactly 2 times total, and never with "admin@test.com"','Mocks the email service','Error'],answer:1,explanation:'verify(mock, times(2)) asserts the method was called exactly twice. verify(mock, never()) asserts it was never called with that specific argument.'},
{id:9788,topic:'code-challenges',difficulty:'intermediate',question:'What does this Appium orientation test do?\n\ndriver.rotate(ScreenOrientation.LANDSCAPE);\nAssert.assertEquals(ScreenOrientation.LANDSCAPE, driver.getOrientation());\ndriver.rotate(ScreenOrientation.PORTRAIT);',options:['Takes a landscape screenshot','Rotates to landscape, verifies, then rotates back to portrait','Fails always','Error'],answer:1,explanation:'driver.rotate() changes screen orientation. getOrientation() returns the current orientation. The test verifies it changed to LANDSCAPE, then restores it to PORTRAIT.'},
{id:9789,topic:'code-challenges',difficulty:'intermediate',question:'What does this Git interactive rebase do?\n\ngit rebase -i HEAD~3',options:['Reverts 3 commits','Opens an editor to interactively rewrite/squash/reorder the last 3 commits','Merges 3 branches','Cherry-picks 3 commits'],answer:1,explanation:'git rebase -i HEAD~3 opens an interactive editor showing the last 3 commits. You can squash, reword, reorder, or drop commits before replaying them.'},
{id:9790,topic:'code-challenges',difficulty:'intermediate',question:'What does this core testing mock setup verify?\n\nwhen(userService.findById(1L)).thenReturn(new User("Alice"));\nwhen(userService.findById(2L)).thenThrow(new UserNotFoundException());\n\nassertEquals("Alice", userService.findById(1L).getName());\nassertThrows(UserNotFoundException.class, () -> userService.findById(2L));',options:['Uses real database','Stubs two behaviors and verifies both: successful lookup and exception throw','Error','Mocks are not used'],answer:1,explanation:'Mockito when().thenReturn stubs id=1 to return Alice. when().thenThrow stubs id=2 to throw. Both assertions verify the correct stubbed behaviors.'},
{id:9791,topic:'code-challenges',difficulty:'intermediate',question:'What does this GitHub Actions matrix strategy do?\n\nstrategy:\n  matrix:\n    os: [ubuntu-latest, windows-latest]\n    java: [17, 21]\n  fail-fast: false',options:['Runs one job','Runs 4 jobs (2 OS × 2 Java versions) in parallel','Runs sequentially','Error'],answer:1,explanation:'Matrix strategy creates a job for each combination: ubuntu/17, ubuntu/21, windows/17, windows/21 = 4 jobs. fail-fast: false means all run even if one fails.'},
{id:9792,topic:'code-challenges',difficulty:'intermediate',question:'What does this Appium TouchAction multi-touch do?\n\nMultiTouchAction multi = new MultiTouchAction(driver);\nTouchAction touch1 = new TouchAction(driver).press(PointOption.point(300,500)).moveTo(PointOption.point(200,400)).release();\nTouchAction touch2 = new TouchAction(driver).press(PointOption.point(500,500)).moveTo(PointOption.point(600,400)).release();\nmulti.add(touch1).add(touch2).perform();',options:['Single tap','Two-finger pinch-out gesture (zoom out)','Double tap','Error'],answer:1,explanation:'Two TouchActions moving in opposite directions simultaneously simulate a pinch gesture. touch1 moves left, touch2 moves right — this is a pinch-out (zoom in).'},
{id:9793,topic:'code-challenges',difficulty:'intermediate',question:'What does this Git worktree command do?\n\ngit worktree add ../hotfix-branch hotfix/critical-fix',options:['Creates a new branch','Creates a separate working tree for hotfix/critical-fix without switching the current checkout','Merges the branch','Clones the repo'],answer:1,explanation:'git worktree add creates a linked working tree at the given path, checked out to the specified branch. You can work on multiple branches simultaneously without stashing.'},
{id:9794,topic:'code-challenges',difficulty:'advanced',question:'What does this Git bisect session find?\n\ngit bisect start\ngit bisect bad HEAD\ngit bisect good v1.0\n# Git checks out midpoint\n# You test, then:\ngit bisect good  # or: git bisect bad',options:['Reverts to v1.0','Performs binary search through commit history to find which commit introduced a bug','Merges HEAD to v1.0','Shows all commits between v1.0 and HEAD'],answer:1,explanation:'git bisect automates binary search. It checks out the midpoint between known good and bad commits. You test each and mark good/bad until it isolates the offending commit.'},
{id:9795,topic:'code-challenges',difficulty:'advanced',question:'What does this Appium W3C Actions API gesture do?\n\nSequence scroll = new SequenceBuilder(PointerInput.Kind.TOUCH, "scroll")\n    .pause(Duration.ofMillis(0))\n    .moveToLocation(540, 800)\n    .pointerDown(Button.LEFT)\n    .moveToLocation(540, 200)\n    .pointerUp(Button.LEFT)\n    .build();\ndriver.perform(Arrays.asList(scroll));',options:['Taps at (540,800)','Performs a W3C Actions API scroll gesture from y=800 to y=200','Clicks and holds','Error'],answer:1,explanation:'W3C Actions API: moves to (540,800), presses, moves to (540,200), releases — a vertical swipe/scroll gesture. This is the modern replacement for deprecated TouchAction in Appium 2.'},
{id:9796,topic:'code-challenges',difficulty:'advanced',question:'What does this core testing integration test setup achieve?\n\n@SpringBootTest(webEnvironment = RANDOM_PORT)\n@AutoConfigureMockMvc\nclass UserControllerIntegrationTest {\n    @Autowired MockMvc mvc;\n\n    @Test\n    void createUser() throws Exception {\n        mvc.perform(post("/api/users")\n            .contentType(APPLICATION_JSON)\n            .content("{\"name\":\"Alice\"}")\n        )\n        .andExpect(status().isCreated())\n        .andExpect(jsonPath("$.name").value("Alice"));\n    }\n}',options:['Unit test with mocks','Full Spring context integration test: sends HTTP POST and validates response','Fails without database','Error'],answer:1,explanation:'@SpringBootTest starts the full application context. MockMvc sends HTTP requests without a real server. The test validates the HTTP 201 status and the response JSON body.'},
{id:9797,topic:'code-challenges',difficulty:'advanced',question:'What does this Git reflog command recover?\n\ngit reflog\n# Shows: abc1234 HEAD@{2}: commit: added feature\ngit checkout abc1234',options:['Shows all commits','Shows recently visited HEADs including after resets/deletions — helps recover "lost" commits','Shows stash entries','Deletes old commits'],answer:1,explanation:'git reflog records every movement of HEAD, including after hard resets or deleted branches. The commit abc1234 can be recovered by checking it out or creating a branch from it.'},
{id:9798,topic:'code-challenges',difficulty:'advanced',question:'What does this Appium Desired Capabilities for iOS do?\n\ncaps.setCapability("platformName","iOS");\ncaps.setCapability("platformVersion","17.0");\ncaps.setCapability("deviceName","iPhone 15");\ncaps.setCapability("app","/path/to/app.ipa");\ncaps.setCapability("automationName","XCUITest");',options:['Runs on Android','Configures Appium to automate iOS using XCUITest framework','Connects to a web browser','Error'],answer:1,explanation:'automationName="XCUITest" is Apple\'s native UI testing framework used by Appium for iOS. The capabilities configure the platform, version, device, and app to install and test.'},
{id:9799,topic:'code-challenges',difficulty:'advanced',question:'What does this GitHub Actions conditional step do?\n\n- name: Deploy to production\n  if: github.ref == \'refs/heads/main\' && success()\n  run: ./deploy.sh',options:['Always deploys','Deploys only when the workflow runs on the main branch and all previous steps succeeded','Deploys on failure','Error'],answer:1,explanation:'The if condition evaluates context variables. github.ref == "refs/heads/main" restricts to main branch. success() ensures previous steps passed. Both must be true to deploy.'},
{id:9800,topic:'code-challenges',difficulty:'advanced',question:'What does this core testing contract test verify?\n\n@Pact(consumer="OrderService", provider="InventoryService")\npublic RequestResponsePact createPact(PactDslWithProvider builder) {\n    return builder.given("items exist")\n        .uponReceiving("check stock")\n        .path("/api/stock")\n        .method("GET")\n        .willRespondWith()\n        .status(200)\n        .body(new PactDslJsonBody().integerType("count"))\n        .toPact();\n}',options:['Integration test against real service','Consumer-Driven Contract test defining what the consumer expects from the provider','Mock test','Error'],answer:1,explanation:'Pact CDC testing: the consumer defines the contract (endpoint, request, expected response shape). The provider verifies it can fulfill the contract, enabling decoupled service testing.'},
{id:9801,topic:'code-challenges',difficulty:'advanced',question:'What does this Appium visual regression test do?\n\nBufferedImage screenshot = ImageIO.read(new ByteArrayInputStream(\n    ((TakesScreenshot)driver).getScreenshotAs(OutputType.BYTES)));\nBufferedImage baseline = ImageIO.read(new File("baseline.png"));\ndouble similarity = compareImages(screenshot, baseline);\nAssert.assertTrue("Visual difference detected", similarity > 0.95);',options:['Takes a screenshot only','Compares the current screen against a baseline image and fails if similarity < 95%','Saves a new baseline','Error'],answer:1,explanation:'Visual regression testing captures the current screen and compares pixel-similarity to a baseline. If more than 5% differs (similarity < 0.95), the test fails, detecting unexpected UI changes.'},
{id:9802,topic:'code-challenges',difficulty:'advanced',question:'What does this Git submodule command do?\n\ngit submodule update --init --recursive',options:['Clones all submodules','Initializes and updates all submodules to their tracked commits, including nested submodules','Deletes submodules','Shows submodule status'],answer:1,explanation:'--init initializes any submodules not yet configured. --recursive processes nested submodules. The command ensures all submodules are checked out at the correct commits.'},
{id:9803,topic:'code-challenges',difficulty:'advanced',question:'What does this core testing mutation testing result mean?\n\n// PITest result: Mutation Score 67%\n// 30 mutations generated, 20 killed, 10 survived',options:['67% of tests pass','67% of code mutations were detected by the test suite','67% code coverage','Tests kill 67% of the code'],answer:1,explanation:'Mutation testing introduces code changes (mutations). "Killed" means a test failed (detected the change). 67% mutation score means tests found 20 of 30 mutations. Surviving mutations indicate test gaps.'},
{id:9804,topic:'code-challenges',difficulty:'advanced',question:'What does this Appium deep link test do?\n\ndriver.get("myapp://product/123");\nWebElement title = driver.findElement(By.id("com.app:id/product_title"));\nAssert.assertFalse(title.getText().isEmpty());',options:['Opens a browser URL','Launches the app at the deep link URL and verifies the product screen loaded','Creates a new product','Error'],answer:1,explanation:'Appium can trigger deep links by calling driver.get() with a custom scheme URL. The app handles myapp://product/123 and navigates to the product screen. The assertion verifies the title loaded.'},
{id:9805,topic:'code-challenges',difficulty:'advanced',question:'What does this GitHub Actions artifact upload preserve?\n\n- name: Upload test results\n  uses: actions/upload-artifact@v4\n  if: always()\n  with:\n    name: test-results\n    path: target/surefire-reports/',options:['Emails test results','Uploads Surefire XML reports as downloadable artifacts even if tests fail','Deletes reports after CI','Error'],answer:1,explanation:'upload-artifact stores files from the CI run. if: always() ensures it runs even when tests fail. The reports are then downloadable from the GitHub Actions run summary.'},
{id:9806,topic:'code-challenges',difficulty:'advanced',question:'What does this core testing test double pattern distinguish?\n\n// Stub: returns fixed data\nwhen(repo.findAll()).thenReturn(List.of(new User("Alice")));\n\n// Spy: real object with verification\nUserService spy = Mockito.spy(new UserService(repo));\nspy.processAll();\nverify(spy).processAll();',options:['Stubs and spies are the same','Stub provides canned data; spy wraps a real object and allows verification','Spy replaces behavior; stub verifies','Error'],answer:1,explanation:'Test doubles taxonomy: stubs return predetermined data without behavior. Spies wrap real objects — they call real methods but also record interactions for verification.'},
{id:9807,topic:'code-challenges',difficulty:'advanced',question:'What does this Appium performance metrics capture do?\n\nMap<String, Object> args = new HashMap<>();\nargs.put("packageName", "com.example.app");\nargs.put("metrics", Arrays.asList("cpu","memory","battery"));\nMap<String, List<Object>> result = (Map) driver.executeScript("mobile:getPerformanceData", args);',options:['Crashes the app','Captures CPU, memory, and battery metrics for the app during the test','Sends metrics to server','Error'],answer:1,explanation:'Appium mobile:getPerformanceData collects real-time performance data (CPU, memory, battery, network) during a test session. Useful for performance regression testing.'},
{id:9808,topic:'code-challenges',difficulty:'advanced',question:'What does this Git patch workflow do?\n\ngit format-patch main..feature -o patches/\ngit am patches/*.patch',options:['Merges the branch','Creates email-ready patch files from feature commits; git am applies them to current branch','Exports the repo','Error'],answer:1,explanation:'git format-patch creates .patch files (one per commit) from the feature branch. git am applies those patches to the current branch — useful for code review or applying changes without merge.'},
{id:9809,topic:'code-challenges',difficulty:'advanced',question:'What does this core testing load test assertion check?\n\n// k6 script\ncheck(res, {\n    "status is 200": (r) => r.status === 200,\n    "response time < 500ms": (r) => r.timings.duration < 500\n});',options:['Unit test assertions','Performance test: checks every response is 200 and under 500ms during load','Runs once','Error'],answer:1,explanation:'k6 is a load testing tool. check() validates conditions for each virtual user request. Both status and response time are checked across concurrent users during the load test.'},
{id:9810,topic:'code-challenges',difficulty:'advanced',question:'What does this Appium biometric authentication bypass do?\n\ndriver.executeScript("mobile:enrollBiometric", Map.of("isEnabled", true));\ndriver.executeScript("mobile:sendBiometricMatch", Map.of("match", true));',options:['Disables biometrics','Enrolls biometric authentication on the simulator and simulates a successful fingerprint/face match','Requires a real device','Error'],answer:1,explanation:'These Appium commands work on iOS/Android simulators. enrollBiometric enables biometric enrollment, and sendBiometricMatch:true simulates a successful biometric authentication event.'},
{id:9811,topic:'code-challenges',difficulty:'advanced',question:'What does this Git sparse checkout do?\n\ngit clone --filter=blob:none --sparse https://github.com/org/repo.git\ncd repo\ngit sparse-checkout set src/tests',options:['Clones everything','Performs a blobless clone and checks out only the src/tests directory (partial clone)','Creates a shallow clone','Error'],answer:1,explanation:'Partial clone with --filter=blob:none downloads commit/tree objects but not blobs initially. sparse-checkout set limits the working tree to only src/tests, drastically reducing disk usage for large repos.'},
{id:9812,topic:'code-challenges',difficulty:'advanced',question:'What does this core testing chaos engineering test do?\n\n@Test\nvoid resilientUnderDbOutage() {\n    // Toxiproxy: add network latency + 50% packet loss to DB connection\n    proxy.toxics().latency("db-latency", ToxicDirection.DOWNSTREAM, 2000);\n    proxy.toxics().limitData("db-loss", ToxicDirection.DOWNSTREAM, 0).setToxicity(0.5f);\n    \n    Response r = callService("/api/users");\n    assertEquals(503, r.getStatus());  // expects graceful degradation\n}',options:['Normal integration test','Chaos/resilience test: injects DB failures and verifies the service returns 503 (graceful degradation)','Load test','Error'],answer:1,explanation:'Toxiproxy injects network conditions (latency + packet loss) to simulate DB outages. The test verifies the service handles the failure gracefully with a 503 status rather than crashing.'},
{id:9813,topic:'code-challenges',difficulty:'advanced',question:'What does this Appium network condition test do?\n\ndriver.setNetworkConnection(new NetworkConnectionSetting(false, true, false));\n// false=wifi, true=data, false=airplane\nAssert.assertEquals("Loading...", driver.findElement(By.id("status")).getText());',options:['Turns off the device','Disables WiFi and enables mobile data, then verifies the app shows "Loading..." in degraded network','Crashes the app','Error'],answer:1,explanation:'NetworkConnectionSetting controls network state. WiFi off + data on = mobile data only scenario. The assertion checks the app gracefully shows a loading indicator under degraded connectivity.'},
{id:9814,topic:'code-challenges',difficulty:'advanced',question:'What does this GitHub Actions reusable workflow do?\n\n# .github/workflows/test.yml\non:\n  workflow_call:\n    inputs:\n      environment:\n        type: string\n        required: true\n\njobs:\n  test:\n    runs-on: ubuntu-latest\n    steps:\n      - run: echo "Testing in ${{ inputs.environment }}"',options:['Runs on every push','Defines a reusable workflow callable by other workflows with an environment parameter','Error','Runs manually only'],answer:1,explanation:'workflow_call trigger makes a workflow reusable. Other workflows can call it with inputs (like environment). This promotes DRY principles across CI/CD pipelines.'},
{id:9815,topic:'code-challenges',difficulty:'advanced',question:'What does this core testing architecture fitness function do?\n\n@Test\nvoid noCircularDependencies() {\n    JavaClasses classes = new ClassFileImporter().importPackages("com.app");\n    SliceRule rule = SlicesRuleDefinition.slices()\n        .matching("com.app.(*)..")\n        .should().beFreeOfCycles();\n    rule.check(classes);\n}',options:['Unit test','Architecture test using ArchUnit: verifies no circular package dependencies exist','Code coverage check','Error'],answer:1,explanation:'ArchUnit enforces architecture rules in tests. This SlicesRule checks that packages under com.app have no circular dependencies. It fails the test if cycles are detected.'},
{id:9816,topic:'code-challenges',difficulty:'advanced',question:'What does this Appium deep link accessibility test check?\n\ndriver.get("myapp://settings/accessibility");\nWebElement textSizeSlider = driver.findElement(By.accessibilityId("text_size_slider"));\nAssert.assertTrue(textSizeSlider.isEnabled());\nAssert.assertNotNull(textSizeSlider.getAttribute("value"));',options:['Crashes the app','Navigates to accessibility settings via deep link and verifies the text size slider is enabled and has a value','Takes a screenshot','Error'],answer:1,explanation:'Deep link opens the accessibility settings screen. By.accessibilityId() finds by content-description/accessibility ID. Tests verify the control is enabled and returns a value.'},
{id:9817,topic:'code-challenges',difficulty:'advanced',question:'What does this Git signed commit setup verify?\n\ngit config --global commit.gpgsign true\ngit commit -m "Secure commit"\ngit log --show-signature',options:['Encrypts the commit message','Signs commits with GPG key and allows verification of commit authorship integrity','Error without GPG','Creates a new branch'],answer:1,explanation:'GPG-signed commits prove the commit was authored by the keyholder. git log --show-signature shows the GPG signature verification status for each commit.'},
{id:9818,topic:'code-challenges',difficulty:'advanced',question:'What does this core testing test pyramid enforce?\n\n// 70% unit, 20% integration, 10% E2E\n// Maven profile to enforce counts:\n// surefire unit tests: ~350\n// integration tests: ~100  \n// E2E (Selenium): ~50\n// Verify: more unit tests = faster CI, less brittleness',options:['Requires exactly 350 tests','Enforces proportional test distribution for optimal feedback speed and stability','Error','Runs only E2E tests'],answer:1,explanation:'The test pyramid: more unit tests (fast, cheap) at the base, fewer E2E tests (slow, brittle) at the top. This ratio gives fast CI feedback while maintaining integration confidence.'},
{id:9819,topic:'code-challenges',difficulty:'advanced',question:'What does this Appium element wait strategy do?\n\nWebDriverWait wait = new WebDriverWait(driver, Duration.ofSeconds(20));\nWebElement el = wait.until(ExpectedConditions.presenceOfElementLocated(\n    AppiumBy.accessibilityId("submit_button")\n));\nAssert.assertTrue(el.isDisplayed() && el.isEnabled());',options:['Waits 20 seconds always','Waits up to 20 seconds for the element to be present in DOM, then verifies it is visible and enabled','Fails if element takes more than 1 second','Error'],answer:1,explanation:'WebDriverWait with ExpectedConditions.presenceOfElementLocated polls for up to 20 seconds. Once found, assertions check isDisplayed() and isEnabled() before interaction.'},
{id:9820,topic:'code-challenges',difficulty:'advanced',question:'What does this GitHub Actions test reporting action do?\n\n- name: Publish Test Report\n  uses: dorny/test-reporter@v1\n  if: always()\n  with:\n    name: JUnit Tests\n    path: "**/surefire-reports/*.xml"\n    reporter: java-junit',options:['Emails reports','Parses JUnit XML reports and displays results directly in the GitHub Actions UI as annotations','Uploads artifacts only','Error'],answer:1,explanation:'dorny/test-reporter parses test result XML files and posts inline annotations in GitHub, showing which tests passed/failed directly in the PR or workflow run view.'},
{id:9821,topic:'code-challenges',difficulty:'advanced',question:'What does this Appium cloud execution config do?\n\ncaps.setCapability("cloud:name", "My Test");\ncaps.setCapability("cloud:build", "1.0.0");\ncaps.setCapability("cloud:device", "Samsung Galaxy S23");\nRemoteWebDriver driver = new RemoteWebDriver(\n    new URL("https://hub.browserstack.com/wd/hub"), caps);\n',options:['Runs locally on a Samsung device','Connects to BrowserStack cloud to run on a real Samsung Galaxy S23','Error without a device','Error'],answer:1,explanation:'cloud: prefixed capabilities (or bs: for BrowserStack) configure cloud device farms. The RemoteWebDriver URL points to the cloud provider hub. Tests run on real cloud-hosted devices.'},
{id:9822,topic:'code-challenges',difficulty:'advanced',question:'What does this core testing test data builder pattern do?\n\nUser user = new UserBuilder()\n    .withName("Alice")\n    .withEmail("alice@test.com")\n    .withRole(Role.ADMIN)\n    .build();\nAssert.assertEquals(Role.ADMIN, user.getRole());',options:['Uses reflection','Test Data Builder pattern creates test objects with a fluent API — easy to read and maintain','Error','Mocks the User class'],answer:1,explanation:'Test Data Builder uses the builder pattern for test objects. .withX() methods set fields. build() creates the object. It\'s more readable than long constructors and easier to maintain.'},
{id:9823,topic:'code-challenges',difficulty:'advanced',question:'What does this Appium geolocation test do?\n\nLocation location = new Location(37.7749, -122.4194, 10);\ndriver.setLocation(location);\nString displayed = driver.findElement(By.id("com.app:id/tv_location")).getText();\nAssert.assertTrue(displayed.contains("San Francisco"));',options:['Moves the device physically','Sets the device GPS coordinates to San Francisco and verifies the app displays the correct location','Error on real device','Error'],answer:1,explanation:'Appium\'s setLocation() sets the simulated GPS location. The lat/lon corresponds to San Francisco. The test verifies the app correctly reads and displays the mocked location.'},
{id:9824,topic:'code-challenges',difficulty:'advanced',question:'What does this Git large file handling do?\n\ngit lfs track "*.apk"\ngit add .gitattributes\ngit add app-release.apk\ngit commit -m "Add APK via LFS"',options:['Stores APK in git history directly','Tracks large files with Git LFS — stores them externally, only metadata in git history','Compresses the APK','Error'],answer:1,explanation:'Git LFS (Large File Storage) replaces large files with lightweight text pointers in git history. The actual file content is stored on an LFS server. Prevents bloating git history.'},
{id:9825,topic:'code-challenges',difficulty:'advanced',question:'What does this core testing test isolation verify?\n\n@BeforeEach\nvoid setUp() {\n    database.truncate("users");\n    database.truncate("orders");\n}\n\n@Test\nvoid testUserCreation() {\n    service.createUser("Alice");\n    assertEquals(1, database.count("users"));\n}',options:['Tests fail because DB is wiped','Test isolation: each test starts with an empty DB state, ensuring no inter-test data contamination','Slow approach','Error'],answer:1,explanation:'Truncating tables before each test ensures complete state isolation. testUserCreation starts with 0 users. Creates 1. Asserts count=1. Without truncation, a previous test\'s data could cause false passes/fails.'},
{id:9826,topic:'code-challenges',difficulty:'advanced',question:'What does this Appium AI-enhanced element finder do (pseudo-code)?\n\n// Healer SDK or Applitools Ultrafast\nelement = driver.findElement(By.id("btn_submit"));\n// If element ID changes, AI re-locates by visual/semantic similarity\n// No test failure for minor ID changes',options:['Always fails on ID change','AI self-healing: if the locator fails, the SDK uses visual/semantic analysis to find the element automatically','Error','Requires manual update'],answer:1,explanation:'Self-healing frameworks (Healenium, Applitools) intercept element-not-found exceptions and use AI/ML to locate the element by visual appearance or alternative attributes, reducing test maintenance.'},
{id:9827,topic:'code-challenges',difficulty:'advanced',question:'What does this GitHub Actions secrets usage ensure?\n\n- name: Run API tests\n  run: newman run collection.json\n  env:\n    API_KEY: ${{ secrets.API_KEY }}\n    BASE_URL: ${{ secrets.STAGING_URL }}',options:['Hardcodes credentials','Injects secrets as environment variables securely — never exposed in logs','Fails if secrets are empty','Error'],answer:1,explanation:'GitHub Actions secrets are encrypted and masked in logs. ${{ secrets.X }} injects them as environment variables at runtime. Newman accesses them via process.env. No credentials in source code.'},
{id:9828,topic:'code-challenges',difficulty:'advanced',question:'What does this core testing test coverage gate do?\n\n// jacoco plugin in pom.xml:\n<rule>\n  <element>BUNDLE</element>\n  <limits>\n    <limit>\n      <counter>LINE</counter>\n      <value>COVEREDRATIO</value>\n      <minimum>0.80</minimum>\n    </limit>\n  </limits>\n</rule>',options:['Measures coverage only','Fails the Maven build if line coverage drops below 80%','Warns only','Error'],answer:1,explanation:'JaCoCo coverage gates enforce minimum thresholds. If covered line ratio drops below 0.80 (80%), the Maven build fails at the verify phase, preventing merging under-tested code.'},
{id:9829,topic:'code-challenges',difficulty:'advanced',question:'What does this Appium test report integration do?\n\n@After\npublic void tearDown(Scenario scenario) {\n    if (scenario.isFailed()) {\n        byte[] screen = driver.getScreenshotAs(OutputType.BYTES);\n        scenario.attach(screen, "image/png", "failure-screenshot");\n        // Also attach device logs\n        String logs = driver.manage().logs().get("logcat").getAll()\n            .stream().map(LogEntry::getMessage).collect(Collectors.joining("\\n"));\n        scenario.attach(logs.getBytes(), "text/plain", "device-logs");\n    }\n    driver.quit();\n}',options:['Just takes a screenshot','On failure: attaches screenshot AND device logcat logs to the Cucumber report, then closes driver','Error','Always attaches'],answer:1,explanation:'Comprehensive failure evidence: screenshot captures visual state, logcat captures device logs. Both are attached to the Cucumber scenario report for debugging mobile test failures.'},
{id:9830,topic:'code-challenges',difficulty:'advanced',question:'What does this core testing specification by example test represent?\n\n// Living documentation: code and spec are the same\n@Test\n@DisplayName("A discount of 10% is applied when order total exceeds $100")\nvoid discountAppliedForLargeOrders() {\n    Order order = new Order().addItem("book", 120.00);\n    double discount = discountService.calculate(order);\n    assertEquals(12.0, discount, 0.001);\n}',options:['Just a unit test','Specification by example: test encodes business rules as executable documentation','Error','Performance test'],answer:1,explanation:'@DisplayName states the business rule. The test proves 10% of $120 = $12 discount. This is specification by example — the test IS the spec. Passing tests = verified behaviour documentation.'},
]);
