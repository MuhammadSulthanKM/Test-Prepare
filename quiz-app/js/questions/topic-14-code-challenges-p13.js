window.QUIZ_BANK['code-challenges'] = (window.QUIZ_BANK['code-challenges']||[]).concat([
{id:9711,topic:'code-challenges',difficulty:'beginner',question:'What does this Cucumber step definition match?\n\n@Given("the user is on the login page")\npublic void userOnLoginPage() {\n    driver.get(baseUrl + "/login");\n}',options:['Any step starting with "the"','The exact step text "the user is on the login page"','All Given steps','Steps containing "login"'],answer:1,explanation:'The @Given annotation maps the exact string to the method. When the feature file has "Given the user is on the login page", this method runs.'},
{id:9712,topic:'code-challenges',difficulty:'beginner',question:'What does this Postman test script check?\n\npm.test("Status is 200", function() {\n    pm.response.to.have.status(200);\n});',options:['Checks the response body is 200','Asserts HTTP status code equals 200','Sets the status to 200','Prints 200'],answer:1,explanation:'pm.response.to.have.status(200) is a Chai-based assertion in Postman. If the response status code is not 200, the test fails.'},
{id:9713,topic:'code-challenges',difficulty:'beginner',question:'What does this Cucumber feature file step do?\n\nFeature: Login\n  Scenario: Valid login\n    Given the user is on the login page\n    When the user enters "admin" and "password"\n    Then the user should see the dashboard',options:['Defines a test class','Describes a test scenario in natural language','Runs three methods sequentially','Creates a JUnit test'],answer:1,explanation:'Gherkin syntax describes behaviour in Given/When/Then steps. The feature file is human-readable and maps to step definitions in Java/Python/etc.'},
{id:9714,topic:'code-challenges',difficulty:'beginner',question:'What does this Postman pre-request script do?\n\npm.environment.set("token", "abc123");',options:['Gets the token from the response','Sets an environment variable "token" to "abc123"','Adds a request header','Deletes the token'],answer:1,explanation:'pm.environment.set() stores a value in the current Postman environment. Other requests and tests can reference it as {{token}}.'},
{id:9715,topic:'code-challenges',difficulty:'beginner',question:'What does this Cucumber @When step capture?\n\n@When("the user enters {string} and {string}")\npublic void enterCredentials(String user, String pass) {\n    loginPage.enterUser(user);\n    loginPage.enterPass(pass);\n}',options:['Hardcoded values only','Dynamic string values from the step text','Any two numbers','Error'],answer:1,explanation:'{string} is a Cucumber Expression that captures a quoted string argument from the step text. The method receives both captured strings as parameters.'},
{id:9716,topic:'code-challenges',difficulty:'beginner',question:'What does this Postman script validate?\n\npm.test("Response has user", function() {\n    const body = pm.response.json();\n    pm.expect(body.name).to.eql("Alice");\n});',options:['Checks status code','Parses JSON body and asserts name equals "Alice"','Sends a new request','Logs the body'],answer:1,explanation:'pm.response.json() parses the JSON body. pm.expect(body.name).to.eql("Alice") asserts the name field equals "Alice".'},
{id:9717,topic:'code-challenges',difficulty:'beginner',question:'What does this Cucumber Background do?\n\nFeature: Cart\n  Background:\n    Given the user is logged in\n\n  Scenario: Add item\n    When the user adds "book"\n    Then the cart has 1 item',options:['Background runs once per feature','Background runs before each scenario in the feature','Background is optional setup','Background runs after each scenario'],answer:1,explanation:'Background steps run before each Scenario in the feature file. Every scenario in the Cart feature starts with "the user is logged in".'},
{id:9718,topic:'code-challenges',difficulty:'beginner',question:'What does this Postman environment variable reference do in a request URL?\n\nhttps://{{baseUrl}}/api/users',options:['Sends request to literal "{{baseUrl}}/api/users"','Replaces {{baseUrl}} with the environment variable value','Throws an error','Logs the URL'],answer:1,explanation:'Double curly braces {{}} reference Postman environment/global/collection variables. {{baseUrl}} is substituted with its current value before the request is sent.'},
{id:9719,topic:'code-challenges',difficulty:'beginner',question:'What does this Cucumber @Then step verify?\n\n@Then("the user should see the dashboard")\npublic void verifyDashboard() {\n    Assert.assertTrue(dashboardPage.isDisplayed());\n    System.out.println("dashboard visible");\n}',options:['Navigates to dashboard','Asserts dashboard is visible and prints confirmation','Logs in the user','Fails always'],answer:1,explanation:'The @Then step runs after the When step. Assert.assertTrue verifies the dashboard is visible. On success, "dashboard visible" is printed.'},
{id:9720,topic:'code-challenges',difficulty:'beginner',question:'What does this Postman collection runner variable do?\n\n// In Tests tab\npm.collectionVariables.set("userId", pm.response.json().id);',options:['Sets a global variable','Sets a collection-scoped variable that can be used by later requests','Prints the user ID','Deletes the variable'],answer:1,explanation:'pm.collectionVariables.set() stores a value accessible throughout the collection. Later requests can reference it as {{userId}} to chain requests.'},
{id:9721,topic:'code-challenges',difficulty:'beginner',question:'What does this Cucumber Scenario Outline do?\n\nScenario Outline: Login with multiple users\n  Given I login with "<username>" and "<password>"\n  Then I see the home page\n\n  Examples:\n    | username | password |\n    | alice    | pass1    |\n    | bob      | pass2    |',options:['Runs once','Runs twice — once per row in Examples table','Runs the same test with hardcoded values','Error'],answer:1,explanation:'Scenario Outline with Examples runs the scenario template once for each row in the Examples table. Two rows → two test executions with different data.'},
{id:9722,topic:'code-challenges',difficulty:'beginner',question:'What does this Postman test check about response time?\n\npm.test("Response is fast", function() {\n    pm.expect(pm.response.responseTime).to.be.below(500);\n});',options:['Checks status code','Checks the response took less than 500 milliseconds','Sets a timeout','Logs response time'],answer:1,explanation:'pm.response.responseTime is the time in ms. Chai assertion .to.be.below(500) passes only if the response time is under 500ms.'},
{id:9723,topic:'code-challenges',difficulty:'beginner',question:'What does this Cucumber @After hook do?\n\n@After\npublic void tearDown(Scenario scenario) {\n    if (scenario.isFailed()) {\n        byte[] screenshot = ((TakesScreenshot)driver).getScreenshotAs(OutputType.BYTES);\n        scenario.attach(screenshot, "image/png", "failure");\n    }\n    driver.quit();\n}',options:['Runs before each scenario','Runs after each scenario; takes screenshot on failure and closes browser','Only takes screenshots','Only closes browser'],answer:1,explanation:'@After runs after every scenario. On failure it captures a screenshot and attaches it to the report. driver.quit() always closes the browser.'},
{id:9724,topic:'code-challenges',difficulty:'beginner',question:'What does this Postman script set for chained requests?\n\npm.test("Extract token", function() {\n    const json = pm.response.json();\n    pm.environment.set("authToken", json.access_token);\n});',options:['Validates the token','Extracts the access_token from the login response and saves it for use in subsequent requests','Creates a new token','Prints the token'],answer:1,explanation:'This is the chaining pattern: extract a value (access_token) from a response and store it as an environment variable so subsequent requests can use it as {{authToken}}.'},
{id:9725,topic:'code-challenges',difficulty:'beginner',question:'What does this Cucumber data table step pass?\n\nWhen the user adds items:\n  | item   | qty |\n  | apple  | 2   |\n  | banana | 3   |\n\n@When("the user adds items:")\npublic void addItems(DataTable table) {\n    List<Map<String,String>> rows = table.asMaps();\n}',options:['Passes a string','Passes a DataTable parsed as a list of maps','Passes an array','Error'],answer:1,explanation:'DataTable in Cucumber represents tabular data. asMaps() parses each row as a Map<String,String> with column headers as keys. rows has 2 entries: {item=apple,qty=2} and {item=banana,qty=3}.'},
{id:9726,topic:'code-challenges',difficulty:'intermediate',question:'What does this Cucumber PicoContainer dependency injection do?\n\npublic class SharedContext {\n    WebDriver driver;\n    String authToken;\n}\n\npublic class LoginSteps {\n    SharedContext ctx;\n    LoginSteps(SharedContext ctx) { this.ctx = ctx; }\n    @When("user logs in") void login() {\n        ctx.authToken = "token123";\n    }\n}\n\npublic class DashboardSteps {\n    SharedContext ctx;\n    DashboardSteps(SharedContext ctx) { this.ctx = ctx; }\n    @Then("sees dashboard") void check() {\n        System.out.println(ctx.authToken);\n    }\n}',options:['Each step class has separate context','SharedContext is injected into both step classes — state is shared between them','Context is null','Error'],answer:1,explanation:'PicoContainer creates one SharedContext instance per scenario and injects it into all step classes requesting it. Both LoginSteps and DashboardSteps share the same instance.'},
{id:9727,topic:'code-challenges',difficulty:'intermediate',question:'What does this Postman schema validation do?\n\npm.test("Valid schema", function() {\n    const schema = {\n        type: "object",\n        required: ["id","name"],\n        properties: {\n            id: { type: "integer" },\n            name: { type: "string" }\n        }\n    };\n    pm.response.to.have.jsonSchema(schema);\n});',options:['Checks response is JSON','Validates the JSON body matches the defined schema','Adds schema to the response','Error'],answer:1,explanation:'pm.response.to.have.jsonSchema() validates the response body against a JSON Schema. It checks required fields and their types. Fails if the response does not conform.'},
{id:9728,topic:'code-challenges',difficulty:'intermediate',question:'What does this Cucumber @Tag-based runner do?\n\n@RunWith(Cucumber.class)\n@CucumberOptions(\n    features = "src/test/resources/features",\n    glue = "steps",\n    tags = "@smoke"\n)\npublic class SmokeRunner {}',options:['Runs all scenarios','Runs only scenarios tagged with @smoke','Runs only the first scenario','Error'],answer:1,explanation:'tags = "@smoke" filters execution to only scenarios annotated with @smoke in feature files. Non-smoke scenarios are skipped.'},
{id:9729,topic:'code-challenges',difficulty:'intermediate',question:'What does this Postman Newman CLI command do?\n\nnewman run collection.json -e environment.json --reporters cli,html --reporter-html-export report.html',options:['Opens Postman GUI','Runs the collection with environment variables, generates CLI output and HTML report','Only runs the collection','Only generates the report'],answer:1,explanation:'Newman CLI runs a Postman collection (-e for environment). --reporters specifies cli (terminal) and html outputs. --reporter-html-export saves the HTML report to report.html.'},
{id:9730,topic:'code-challenges',difficulty:'intermediate',question:'What does this Cucumber parallel execution config achieve?\n\n@CucumberOptions(\n    features = "src/test/resources",\n    glue = "steps",\n    plugin = {"com.aventstack.extentreports.cucumber.adapter.ExtentCucumberAdapter:"}\n)\n@RunWith(Cucumber.class)\n// testng.xml: <test parallel="methods" thread-count="4">',options:['Runs scenarios sequentially','Runs Cucumber scenarios in parallel across 4 threads with Extent reporting','Only generates reports','Error'],answer:1,explanation:'TestNG with parallel="methods" and thread-count="4" runs Cucumber scenarios in parallel. The ExtentCucumberAdapter plugin generates rich HTML reports.'},
{id:9731,topic:'code-challenges',difficulty:'intermediate',question:'What does this Postman iteration with CSV data do?\n\n// Collection Runner: Data File = users.csv\n// users.csv:\n// username,password\n// alice,pass1\n// bob,pass2\n\npm.test("Login succeeds", function() {\n    pm.response.to.have.status(200);\n});\n// URL uses {{username}} and {{password}}',options:['Runs once with "alice"','Runs twice — once per CSV row with different credentials','Runs for all users in DB','Error'],answer:1,explanation:'Collection Runner with a CSV data file iterates once per row. Two rows → two iterations. {{username}} and {{password}} are replaced per row. Status 200 is checked each time.'},
{id:9732,topic:'code-challenges',difficulty:'intermediate',question:'What does this Cucumber @Before hook do?\n\n@Before("@web")\npublic void openBrowser() {\n    driver = new ChromeDriver();\n}\n\n@Before("@api")\npublic void setupApiClient() {\n    client = new RestAssured();\n}',options:['Both hooks run before every scenario','Only the matching hook runs based on the scenario tag','Neither hook runs','Error'],answer:1,explanation:'Conditional @Before hooks run only for scenarios with the matching tag. @web scenarios get a browser; @api scenarios get an API client.'},
{id:9733,topic:'code-challenges',difficulty:'intermediate',question:'What does this Postman assertion check about an array?\n\npm.test("Has 3 items", function() {\n    const data = pm.response.json();\n    pm.expect(data.items).to.be.an("array").with.lengthOf(3);\n});',options:['Checks items is an object','Asserts items is an array of length 3','Logs the array','Checks items is not empty'],answer:1,explanation:'Chai chaining: .to.be.an("array") checks the type, .with.lengthOf(3) checks the count. Both must pass for the test to succeed.'},
{id:9734,topic:'code-challenges',difficulty:'intermediate',question:'What does this Cucumber DocString step do?\n\n@Given("the request body is:")\npublic void setBody(String body) {\n    requestBody = body;\n}\n\n// Feature:\nGiven the request body is:\n    """\n    {"name": "Alice", "age": 30}\n    """',options:['Passes the JSON as a DataTable','Passes the multi-line string as a single String parameter','Error','Parses JSON automatically'],answer:1,explanation:'DocStrings (triple quotes) pass multi-line text as a single String argument to the step definition. The method receives the JSON text as a String.'},
{id:9735,topic:'code-challenges',difficulty:'intermediate',question:'What does this Postman pm.sendRequest do?\n\npm.sendRequest("https://api.example.com/token", function(err, res) {\n    pm.environment.set("token", res.json().access_token);\n});',options:['Makes a synchronous request','Makes an async request in pre-request script to fetch and store a token','Blocks until token is received','Error'],answer:1,explanation:'pm.sendRequest makes an asynchronous HTTP request from within Postman scripts. The callback receives the response, and the token is stored for use by the main request.'},
{id:9736,topic:'code-challenges',difficulty:'intermediate',question:'What does this Cucumber step with {int} capture?\n\n@When("the user adds {int} items to cart")\npublic void addItems(int count) {\n    cart.add(count);\n}\n\n// Feature step:\nWhen the user adds 5 items to cart',options:['Captures "5" as a String','Captures 5 as an int','Throws NumberFormatException','Error'],answer:1,explanation:'{int} is a Cucumber Expression that automatically converts the matched number string to an int. The method receives 5 as the integer parameter.'},
{id:9737,topic:'code-challenges',difficulty:'intermediate',question:'What does this Postman test chain verify?\n\npm.test("Creates user and returns ID", function() {\n    const json = pm.response.json();\n    pm.expect(json).to.have.property("id");\n    pm.expect(json.id).to.be.a("number");\n    pm.expect(json.id).to.be.above(0);\n});',options:['Three separate tests','One test checking id exists, is a number, and is positive','Fails if any property is missing','Error'],answer:1,explanation:'pm.test groups assertions. All three pm.expect calls run inside one test. If any fails, the test fails. Checks: id exists, id is a number, id > 0.'},
{id:9738,topic:'code-challenges',difficulty:'intermediate',question:'What does this Cucumber custom World do?\n\npublic class CustomWorld {\n    WebDriver driver;\n    String baseUrl = "https://example.com";\n}\n\n@Before\npublic void before(Scenario scenario) {\n    driver = new ChromeDriver();\n}\n\n// In cucumber-jvm, with ObjectFactory pointing to CustomWorld',options:['Static shared state','Provides per-scenario state isolated from other scenarios','Shared between all scenarios','Error'],answer:1,explanation:'The World object in Cucumber provides per-scenario state. Each scenario gets a fresh instance, ensuring test isolation.'},
{id:9739,topic:'code-challenges',difficulty:'advanced',question:'What does this Cucumber RestAssured API step check?\n\n@Then("the API returns {int} users")\npublic void checkUserCount(int expected) {\n    Response response = RestAssured.get("/api/users");\n    List<Object> users = response.jsonPath().getList("$");\n    Assert.assertEquals(expected, users.size());\n}',options:['Checks status code','Makes an API call and asserts the number of users in the response','Prints the users','Error'],answer:1,explanation:'RestAssured.get() makes a GET request. jsonPath().getList("$") returns the root JSON array. Assert.assertEquals checks the count matches the expected parameter from the Cucumber step.'},
{id:9740,topic:'code-challenges',difficulty:'advanced',question:'What does this Postman workflow test do?\n\n// Test 1 response\npm.environment.set("userId", pm.response.json().id);\npm.setNextRequest("Get User by ID");\n\n// Test 2: "Get User by ID"\n// URL: /users/{{userId}}',options:['Runs tests in default order','Chains requests: creates a user, stores ID, then fetches that user','Loops infinitely','Error'],answer:1,explanation:'pm.setNextRequest("Get User by ID") controls execution flow. After Test 1 stores the userId, execution jumps to "Get User by ID" which uses {{userId}} in its URL.'},
{id:9741,topic:'code-challenges',difficulty:'advanced',question:'What does this Cucumber Serenity BDD report step add?\n\n@Step("User {0} logs in with password {1}")\npublic void login(String user, String pass) {\n    loginPage.enterCredentials(user, pass);\n    loginPage.submit();\n}',options:['Skips reporting','Adds a human-readable step to the Serenity report with parameter values substituted','Error','Does nothing extra'],answer:1,explanation:'@Step from Serenity adds a titled entry in the Serenity report. {0} and {1} are replaced with the actual parameter values (user and pass) in the report output.'},
{id:9742,topic:'code-challenges',difficulty:'advanced',question:'What does this Postman monitor do differently from a manual run?\n\n// Postman Monitor configured to run every 1 hour on Cloud',options:['Runs the collection once','Runs the collection on a schedule in the Postman Cloud and notifies on failures','Requires local Newman install','Runs in a local environment'],answer:1,explanation:'Postman Monitors run collections on a schedule in the cloud. They do not require local infrastructure. Email/Slack notifications can be configured for failures.'},
{id:9743,topic:'code-challenges',difficulty:'advanced',question:'What does this Cucumber Spring integration do?\n\n@SpringBootTest(webEnvironment=RANDOM_PORT)\n@CucumberContextConfiguration\npublic class CucumberSpringConfig {}\n\n// Step definition\n@Autowired\nUserRepository userRepo;\n\n@Given("the database has {int} users")\npublic void seedDb(int count) {\n    for (int i=0; i<count; i++) userRepo.save(new User("u"+i));\n}',options:['Error — Spring not compatible','Runs the full Spring context with real database for Cucumber tests','Mocks the repository','Error'],answer:1,explanation:'@SpringBootTest starts the full application context. @CucumberContextConfiguration wires it into Cucumber. @Autowired injects the real repository for database seeding in steps.'},
{id:9744,topic:'code-challenges',difficulty:'advanced',question:'What does this Postman GraphQL test do?\n\npm.test("GraphQL query succeeds", function() {\n    const body = pm.response.json();\n    pm.expect(body.errors).to.be.undefined;\n    pm.expect(body.data.user.name).to.eql("Alice");\n});',options:['Checks REST API','Verifies no GraphQL errors and the user name in the response data','Checks HTTP status','Error'],answer:1,explanation:'GraphQL errors are returned in the body\'s errors field even with HTTP 200. The test checks errors is absent and validates the response data field.'},
{id:9745,topic:'code-challenges',difficulty:'advanced',question:'What does this Cucumber concurrent feature execution achieve?\n\n// Maven surefire config\n<forkCount>2</forkCount>\n<reuseForks>false</reuseForks>\n// Each fork runs separate feature files',options:['Runs all features in one JVM','Runs feature files in 2 separate JVM forks simultaneously','Slows execution','Error'],answer:1,explanation:'Maven Surefire with forkCount=2 and reuseForks=false creates 2 separate JVM processes. Cucumber feature files are distributed across them for parallel execution.'},
{id:9746,topic:'code-challenges',difficulty:'advanced',question:'What does this Postman Flows integration do?\n\n// Postman Flows: If response.status != 200 → send failure Slack message, Else → continue',options:['Runs a test','Creates a no-code workflow that branches based on API response values','Generates documentation','Error'],answer:1,explanation:'Postman Flows is a visual workflow builder. It can branch (if/else) based on response values, send notifications, and chain multiple requests without writing scripts.'},
{id:9747,topic:'code-challenges',difficulty:'advanced',question:'What does this Cucumber Appium step do?\n\n@Given("the app is launched")\npublic void launchApp() {\n    DesiredCapabilities caps = new DesiredCapabilities();\n    caps.setCapability("app", "/path/to/app.apk");\n    caps.setCapability("platformName", "Android");\n    driver = new AndroidDriver(new URL("http://localhost:4723/wd/hub"), caps);\n}',options:['Launches a web browser','Launches an Android app via Appium for mobile BDD testing','Error','Connects to a real device only'],answer:1,explanation:'This combines Cucumber (BDD) with Appium (mobile automation). The @Given step launches the Android app through the Appium server, enabling mobile BDD testing.'},
{id:9748,topic:'code-challenges',difficulty:'advanced',question:'What does this Postman API contract test do?\n\npm.test("Contract valid", function() {\n    const tv4 = require("tv4");\n    const schema = pm.environment.get("userSchema");\n    pm.expect(tv4.validate(pm.response.json(), JSON.parse(schema))).to.be.true;\n});',options:['Validates response format only','Validates the response against a JSON Schema stored as an environment variable','Error','Checks status only'],answer:1,explanation:'Contract testing validates the API response matches an agreed schema. tv4 validates JSON against JSON Schema. The schema is stored as an environment variable for reuse.'},
{id:9749,topic:'code-challenges',difficulty:'advanced',question:'What does this Cucumber retry plugin do?\n\n@CucumberOptions(\n    plugin = {"io.github.glytching.junit.extension.retry.RetryingTest"}\n)\n// Or with --retry flag in cucumber-jvm 7+:\n// @cucumber --retry 2',options:['Runs passing tests twice','Automatically retries failed scenarios up to 2 times','Skips failed scenarios','Error'],answer:1,explanation:'The --retry flag (Cucumber 7+) retries failed scenarios. --retry 2 means failed scenarios are retried up to 2 times before being marked as permanently failed.'},
{id:9750,topic:'code-challenges',difficulty:'advanced',question:'What does this Postman assertion check about response headers?\n\npm.test("Content-Type is JSON", function() {\n    pm.expect(pm.response.headers.get("Content-Type")).to.include("application/json");\n});',options:['Checks body is JSON','Checks the Content-Type header contains "application/json"','Sets the Content-Type header','Error'],answer:1,explanation:'pm.response.headers.get() retrieves a header value. .to.include() checks it contains the substring "application/json". Fails if Content-Type is text/html or other.'},
{id:9751,topic:'code-challenges',difficulty:'advanced',question:'What does this Cucumber Allure reporting integration add?\n\n@CucumberOptions(\n    plugin = {"io.qameta.allure.cucumber7jvm.AllureCucumber7Jvm"}\n)\n\n@Issue("JIRA-123")\n@Severity(SeverityLevel.CRITICAL)\n@Feature("Login")\n@Story("User can login with valid credentials")\n@Test void loginTest() {}',options:['Just runs the test','Generates an Allure report with JIRA links, severity, feature, and story metadata','Error','Prints metadata to console'],answer:1,explanation:'Allure annotations enrich the report with JIRA issue links, severity levels, and BDD-style feature/story categorization. The Allure plugin collects this metadata during execution.'},
{id:9752,topic:'code-challenges',difficulty:'advanced',question:'What does this Postman mock server response do?\n\n// Postman Mock Server with example:\n// GET /users/1 → 200 { "id": 1, "name": "Alice" }\n\n// Team uses {{mockServer}}/users/1 in tests',options:['Calls the real API','Returns the predefined example response without hitting a real backend','Generates users','Error'],answer:1,explanation:'Postman Mock Servers return saved example responses without a real backend. Teams can develop and test against APIs that don\'t exist yet, using the mock as a stand-in.'},
{id:9753,topic:'code-challenges',difficulty:'advanced',question:'What does this Cucumber Screenplay pattern step do?\n\nActor alice = Actor.named("Alice");\nalice.attemptsTo(\n    Click.on(LoginPage.USERNAME_FIELD),\n    Enter.theValue("alice").into(LoginPage.USERNAME_FIELD),\n    Click.on(LoginPage.LOGIN_BUTTON)\n);\nalice.should(seeThat(the(DashboardPage.TITLE), isDisplayed()));',options:['Standard Page Object pattern','Screenplay pattern: actor performs tasks and makes assertions declaratively','Error','Prints actor name'],answer:1,explanation:'Screenplay pattern: actors perform tasks (Enter, Click) and make assertions (seeThat). More expressive than Page Object Model, better at capturing intent and supporting parallel test execution.'},
{id:9754,topic:'code-challenges',difficulty:'advanced',question:'What does this Postman test check about pagination?\n\npm.test("Pagination metadata correct", function() {\n    const body = pm.response.json();\n    pm.expect(body.page).to.eql(1);\n    pm.expect(body.per_page).to.eql(10);\n    pm.expect(body.total).to.be.a("number");\n    pm.expect(body.data).to.be.an("array").with.lengthOf(10);\n});',options:['Checks status only','Validates pagination structure: page, per_page, total, and data array size','Generates pagination','Error'],answer:1,explanation:'Four assertions validate the pagination response: page=1, per_page=10, total is a number, and data array has 10 items. Common pattern for REST API pagination testing.'},
{id:9755,topic:'code-challenges',difficulty:'advanced',question:'What does this Cucumber Step scope ensure?\n\n// Cucumber 7+ with JUnit 5 platform\n// StepScope: each step definition runs in its own scope\n@StepScope\n@Bean\npublic ItemProcessor processor(@Value("#{jobParameters[\'file\']}") String file) {\n    return new FileProcessor(file);\n}',options:['Global scope','Step-scoped beans are created fresh for each step execution, preventing state leakage','Error','Shares state between steps'],answer:1,explanation:'@StepScope (Spring Batch context) creates a new bean instance per step execution. This is commonly used in Cucumber + Spring Batch tests to inject job parameters per scenario step.'},
{id:9756,topic:'code-challenges',difficulty:'advanced',question:'What does this Postman API versioning test do?\n\npm.test("Correct API version", function() {\n    pm.expect(pm.response.headers.get("X-API-Version")).to.eql("v2");\n    pm.expect(pm.request.url.toString()).to.include("/v2/");\n});',options:['Checks request URL only','Verifies both the response header and request URL indicate API v2','Sets the version','Error'],answer:1,explanation:'Dual verification: confirms the response header X-API-Version is "v2" AND the request URL contains "/v2/". Both must pass for complete version contract validation.'},
{id:9757,topic:'code-challenges',difficulty:'advanced',question:'What does this Cucumber Gherkin table transformer do?\n\n@DataTableType\npublic User userEntry(Map<String,String> entry) {\n    return new User(entry.get("name"), Integer.parseInt(entry.get("age")));\n}\n\n@Then("the users are:")\npublic void checkUsers(List<User> users) {\n    Assert.assertEquals(2, users.size());\n}',options:['Error — tables must be strings','Automatically converts DataTable rows to User objects via the registered transformer','Only converts first row','Error'],answer:1,explanation:'@DataTableType registers a transformer that converts each map row into a User object. Cucumber calls it for each row and passes the fully typed List<User> to the step method.'},
{id:9758,topic:'code-challenges',difficulty:'advanced',question:'What does this Postman CRUD test suite pattern do?\n\n// Request order:\n// 1. POST /users → saves {{userId}}\n// 2. GET /users/{{userId}} → verifies created\n// 3. PUT /users/{{userId}} → updates\n// 4. DELETE /users/{{userId}} → cleans up\n// 5. GET /users/{{userId}} → expects 404',options:['Tests only creation','Full CRUD lifecycle test with chained variables and proper cleanup','Deletes all users','Error'],answer:1,explanation:'The CRUD pattern tests all four operations in sequence. Variables chain the created ID. The final GET expects 404, confirming deletion. This verifies the full API contract.'},
{id:9759,topic:'code-challenges',difficulty:'advanced',question:'What does this Cucumber EventBus listener do?\n\n@Subscribe\npublic void on(TestCaseStarted event) {\n    System.out.println("Started: " + event.getTestCase().getName());\n}\n\n@Subscribe\npublic void on(TestCaseFinished event) {\n    Status status = event.getResult().getStatus();\n    System.out.println("Finished with: " + status);\n}',options:['Error','Subscribes to Cucumber events and logs scenario name on start and status on finish','Skips tests','Runs before @Before hooks'],answer:1,explanation:'Cucumber\'s event bus allows plugins to subscribe to lifecycle events. TestCaseStarted fires at scenario start, TestCaseFinished at end. The plugin logs scenario names and statuses.'},
{id:9760,topic:'code-challenges',difficulty:'advanced',question:'What does this Postman OAuth 2.0 token refresh script do?\n\nif (pm.environment.get("tokenExpiry") < Date.now()) {\n    pm.sendRequest({\n        url: pm.environment.get("tokenUrl"),\n        method: "POST",\n        body: { mode: "urlencoded", urlencoded: [\n            {key:"grant_type",value:"refresh_token"},\n            {key:"refresh_token",value:pm.environment.get("refreshToken")}\n        ]}\n    }, function(err,res) {\n        pm.environment.set("accessToken", res.json().access_token);\n        pm.environment.set("tokenExpiry", Date.now() + res.json().expires_in * 1000);\n    });\n}',options:['Always refreshes token','Refreshes the OAuth 2.0 access token only when expired','Creates new refresh tokens','Error'],answer:1,explanation:'The script checks if the stored token has expired (tokenExpiry < now). If so, it sends a refresh_token grant request, stores the new access_token and updated expiry. Efficient token management.'},
]);
