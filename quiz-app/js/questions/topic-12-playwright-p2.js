window.QUIZ_BANK['playwright'] = (window.QUIZ_BANK['playwright']||[]).concat([
{id:4501,topic:'playwright',difficulty:'beginner',question:'What command starts the Playwright Trace Viewer to inspect a recorded trace file?',options:['npx playwright open-trace trace.zip','npx playwright show-trace trace.zip','npx playwright view trace.zip','npx playwright debug trace.zip'],answer:1,explanation:'`npx playwright show-trace trace.zip` opens the Playwright Trace Viewer in your default browser. The trace viewer shows a timeline of test actions, before/after DOM screenshots, network requests, and console messages for each step.'},
{id:4502,topic:'playwright',difficulty:'beginner',question:'What is the @playwright/test package used for?',options:['Installing browser binaries only','Providing the test runner, test/expect APIs, fixtures, and reporters for Playwright tests','Running Playwright in a Docker container','Generating HTML documentation'],answer:1,explanation:'`@playwright/test` is the full test runner built on top of the Playwright browser automation library. It includes the `test()` and `expect()` APIs, built-in fixtures (`page`, `browser`, `context`), parallel execution, reporters, and configuration via `playwright.config.ts`.'},
{id:4503,topic:'playwright',difficulty:'beginner',question:'How do you define a group of related tests in Playwright Test?',options:['test.suite()','test.group()','test.describe()','test.block()'],answer:2,explanation:'`test.describe(name, fn)` groups related tests into a named block. It works like Jest\'s `describe()`. You can nest `describe` blocks and use `beforeEach`/`afterEach` hooks scoped to each group.'},
{id:4504,topic:'playwright',difficulty:'beginner',question:'What does `test.beforeEach(async ({ page }) => { ... })` do in Playwright?',options:['Runs once before all tests in the suite','Runs before each individual test in the describe block or file, useful for common setup like navigating to a URL','Defines a fixture','Configures the browser before the test file loads'],answer:1,explanation:'`test.beforeEach()` registers a setup function that runs before each test. The fixture parameters (like `page`) are provided automatically. Use it to navigate to a starting URL, log in, or reset application state before every test.'},
{id:4505,topic:'playwright',difficulty:'beginner',question:'What does `test.afterEach(async ({ page }) => { ... })` do?',options:['Runs after all tests complete','Runs after each test, useful for cleanup tasks like clearing cookies or taking a failure screenshot','Marks a test as complete','Closes the browser after each test'],answer:1,explanation:'`test.afterEach()` runs after every test regardless of pass or fail. Common uses include logging test duration, cleaning up test data, or capturing a screenshot for debugging. Playwright automatically closes fixtures after the hook runs.'},
{id:4506,topic:'playwright',difficulty:'intermediate',question:'What is the difference between `test.beforeAll()` and `test.beforeEach()` in Playwright?',options:['They are identical','beforeAll runs once before all tests in the describe scope; beforeEach runs before every individual test','beforeAll creates a new browser; beforeEach creates a new page','beforeAll is not available in Playwright'],answer:1,explanation:'`test.beforeAll()` runs once per worker before any test in the describe group starts. It uses worker-scoped fixtures. `test.beforeEach()` runs before every individual test with test-scoped fixtures. Use `beforeAll` for expensive setup shared across tests, like seeding a database.'},
{id:4507,topic:'playwright',difficulty:'intermediate',question:'What scoped fixtures are available inside `test.beforeAll()` in Playwright?',options:['All fixtures including page','Only worker-scoped fixtures like browser — page and context are test-scoped and not available in beforeAll','Only browser and browserName','Only the request fixture'],answer:1,explanation:'`test.beforeAll()` only has access to worker-scoped fixtures. The `page` and `context` fixtures are test-scoped and are not available inside `beforeAll`. You must use `browser.newPage()` manually or create a context from the `browser` fixture.'},
{id:4508,topic:'playwright',difficulty:'intermediate',question:'What does `test.afterAll(async ({ browser }) => { ... })` do?',options:['Runs after each test','Runs once after all tests in the describe scope on the same worker, useful for worker-level cleanup','Closes all browsers','Generates the test report'],answer:1,explanation:'`test.afterAll()` runs once after all tests in the group complete on that worker. It receives worker-scoped fixtures. Use it to disconnect from databases, stop mock servers, or clean up shared resources created in `beforeAll`.'},
{id:4509,topic:'playwright',difficulty:'intermediate',question:'What does `test.use({ storageState: "auth.json" })` do at the top of a test file?',options:['Loads authentication state for the storage layer only','Overrides the context storageState for all tests in that file, loading pre-saved cookies and localStorage so tests start authenticated','Applies only to the next test','Adds a global fixture'],answer:1,explanation:'`test.use()` at file scope (outside any describe) overrides the specified `use` options for every test in that file. Setting `storageState` here makes all tests in the file start with the saved authenticated session, avoiding repeated login flows.'},
{id:4510,topic:'playwright',difficulty:'intermediate',question:'What are the built-in test-scoped fixtures in @playwright/test?',options:['browser, context, page, request, browserName','page only','browser and page','context and page only'],answer:0,explanation:'The built-in test-scoped fixtures provided by `@playwright/test` are: `page` (a new Page in a new context), `context` (the BrowserContext for that test), `browser` (the Browser instance, worker-scoped), `request` (APIRequestContext), and `browserName` (the current browser name string).'},
{id:4511,topic:'playwright',difficulty:'intermediate',question:'Which built-in Playwright fixture allows making HTTP API requests directly without a browser?',options:['apiClient','fetch','request','httpContext'],answer:2,explanation:'The `request` fixture provides an `APIRequestContext` instance scoped to the test. You can call `request.get()`, `request.post()`, etc. to make HTTP calls. It shares the authentication state (cookies) with the browser context by default.'},
{id:4512,topic:'playwright',difficulty:'advanced',question:'How do you define a custom Playwright fixture with test scope?',options:["Use test.fixture('name', async ({}, use) => {})",'Use test.extend() with an async generator fixture function: `const myTest = test.extend({ myFixture: async ({}, use) => { const val = setup(); await use(val); teardown(); } })`','Add it to playwright.config.ts fixtures array','Create a JS class and import it in tests'],answer:1,explanation:'`test.extend({ fixtureName: async ({ otherFixtures }, use) => { ... await use(value); ... } })` creates a new test object with the fixture. Code before `use(value)` is setup; code after is teardown. The fixture is injected into tests that list it as a parameter.'},
{id:4513,topic:'playwright',difficulty:'advanced',question:'What is a "worker-scoped" fixture in Playwright?',options:['A fixture that is created once per test','A fixture that is created once per worker process and shared across all tests running in that worker','A fixture created per describe block','A fixture shared across all workers'],answer:1,explanation:'Worker-scoped fixtures are created once when a worker starts and destroyed when it exits. They are shared across all tests in that worker. Declare worker scope with `{ scope: "worker" }` in `test.extend()`. The `browser` built-in fixture is worker-scoped.'},
{id:4514,topic:'playwright',difficulty:'intermediate',question:'How does Playwright determine the number of parallel workers by default?',options:['Always 1 worker','Half the number of logical CPU cores on the machine','Equal to the number of test files','Equal to the number of browser projects'],answer:1,explanation:'By default Playwright uses half the number of available CPU cores, capped at a reasonable maximum for CI environments. You can override this with the `workers` option in config or the `--workers` CLI flag.'},
{id:4515,topic:'playwright',difficulty:'intermediate',question:'What does `--workers=1` accomplish when passed to `npx playwright test`?',options:['Runs tests with one retry per test','Forces all tests to run sequentially in a single worker process, disabling parallelism','Limits to one browser','Creates one BrowserContext per test'],answer:1,explanation:'`--workers=1` (or `workers: 1` in config) disables parallel execution and runs all tests sequentially in a single worker. This is useful for debugging order-dependent issues or when tests share server-side state that cannot be parallelized.'},
{id:4516,topic:'playwright',difficulty:'intermediate',question:'What does `fullyParallel: true` do in playwright.config.ts?',options:['Runs each test file in a separate browser instance','Enables individual test-level parallelism so tests within the same file run concurrently across workers','Doubles the number of workers','Runs all projects simultaneously'],answer:1,explanation:'Without `fullyParallel`, tests within the same file run sequentially on one worker while different files run in parallel. With `fullyParallel: true`, all individual tests from all files are distributed across workers, maximizing CPU utilization.'},
{id:4517,topic:'playwright',difficulty:'intermediate',question:'How do you run only tests in a specific file with Playwright?',options:['npx playwright test --filter filename','npx playwright test path/to/file.spec.ts','npx playwright test --only filename','npx playwright test --grep filename'],answer:1,explanation:'Pass the file path directly: `npx playwright test tests/login.spec.ts`. You can also pass a directory to run all tests in it. This is the quickest way to run a single spec file during development without modifying configuration.'},
{id:4518,topic:'playwright',difficulty:'intermediate',question:'What does the `--grep` CLI option do in Playwright?',options:['Searches the source code for a pattern','Filters tests to run only those whose title matches the given regex pattern','Filters by file name','Searches test results'],answer:1,explanation:'`npx playwright test --grep "login"` runs only tests whose full title (including describe names) matches the regex. For example `--grep "@smoke"` runs tests tagged with `@smoke` in their title. `--grep-invert` runs all tests EXCEPT those matching.'},
{id:4519,topic:'playwright',difficulty:'intermediate',question:'What is the purpose of adding a tag like `@smoke` to a Playwright test title?',options:['It modifies test priority in the HTML report','Tags are used with --grep to run subsets of tests, enabling focused runs of smoke, regression, or feature-specific tests','It marks tests as expected to fail','It disables the test in CI'],answer:1,explanation:'Playwright has no native annotation for tags, but adding strings like `@smoke` or `@regression` to test titles enables `--grep "@smoke"` filtering. Playwright v1.42+ also supports `test.tag("@smoke")` for a more structured tagging API.'},
{id:4520,topic:'playwright',difficulty:'intermediate',question:'How do you run Playwright tests and split them across 3 CI machines?',options:['Use --split=3 on each machine','Run npx playwright test --shard=1/3 on machine 1, --shard=2/3 on machine 2, --shard=3/3 on machine 3','Use the parallel: 3 config option','Playwright does not support multi-machine sharding'],answer:1,explanation:'The `--shard=X/Y` flag divides all tests into Y equal parts and runs only shard X. Run `--shard=1/3`, `--shard=2/3`, `--shard=3/3` on three separate CI machines simultaneously to reduce total wall-clock time. Use `npx playwright merge-reports` to combine results.'},
{id:4521,topic:'playwright',difficulty:'intermediate',question:'What does the `list` reporter output in Playwright?',options:['A JSON file','A live scrolling list of test names and statuses to stdout, one line per test','An HTML page','A JUnit XML file'],answer:1,explanation:'The `list` reporter prints each test result to the terminal as it completes, showing its name, duration, and pass/fail status. It is the default reporter for interactive terminal sessions. It is compact and readable for small to medium suites.'},
{id:4522,topic:'playwright',difficulty:'intermediate',question:'Which Playwright reporter is designed for GitHub Actions CI and annotates PR checks with failure details?',options:['json','junit','github','html'],answer:2,explanation:'The `github` reporter formats output using GitHub Actions workflow commands (e.g., `::error`), which annotates the pull request check summary with failure details directly in the GitHub UI. It is recommended when running Playwright in GitHub Actions.'},
{id:4523,topic:'playwright',difficulty:'intermediate',question:'Which Playwright reporter generates a JUnit-compatible XML file for CI systems like Jenkins?',options:['html','json','junit','dot'],answer:2,explanation:'The `junit` reporter generates a JUnit XML report file (default `results.xml`). Jenkins, CircleCI, and other CI systems can ingest this format to display test trends, pass/fail charts, and individual test results in their dashboards.'},
{id:4524,topic:'playwright',difficulty:'intermediate',question:'What does the `dot` reporter do in Playwright?',options:['Creates a file called "dot" with results','Outputs a single character per test (. for pass, F for fail, E for error) — compact for CI logs','Opens a browser with results','Shows only failures'],answer:1,explanation:'The `dot` reporter prints one character per test: `.` for pass, `F` for failure, `E` for error. It is extremely compact and suitable for CI environments with limited log output space or when running thousands of tests.'},
{id:4525,topic:'playwright',difficulty:'intermediate',question:'What does the `json` reporter produce?',options:['A JSON file readable by humans only','A machine-readable JSON file containing full test results, useful for custom reporting or integration with dashboards','A JSON screenshot','A JSON config file'],answer:1,explanation:'The `json` reporter writes a JSON file (configurable path via `outputFile`) containing detailed test results: names, status, duration, errors, attachments, and retry info. This is used for custom reporting pipelines, Slack notifications, or data visualization.'},
{id:4526,topic:'playwright',difficulty:'intermediate',question:'How do you configure multiple reporters in playwright.config.ts?',options:["reporter: 'list' only","reporter: [['list'], ['json', { outputFile: 'results.json' }], ['html']]",'reporter: { list: true, json: true }','reporter: "list,json,html"'],answer:1,explanation:'The `reporter` config accepts an array of `[reporterName, options?]` tuples. Multiple reporters can run simultaneously, for example generating an HTML report AND writing a JSON file in the same test run. Options are passed as a second element.'},
{id:4527,topic:'playwright',difficulty:'intermediate',question:'What is a Playwright soft assertion and how is it declared?',options:["Using expect.soft(locator).toBeVisible() — the test continues even if this assertion fails, collecting the failure for a final report","Using expect.maybe(locator).toBeVisible()",'Using expect(locator).softAssert()','Using test.softAssert()'],answer:0,explanation:'`expect.soft(value)` creates a soft assertion. Unlike regular assertions that throw immediately on failure, soft assertions record the failure but allow the test to continue. At the end of the test, all collected soft failures are reported and the test is marked as failed.'},
{id:4528,topic:'playwright',difficulty:'intermediate',question:'When does a test with only soft assertion failures actually fail in Playwright?',options:['Immediately on the first soft assertion failure','Only at the end of the test when Playwright evaluates all accumulated soft failures','Soft assertion failures never fail a test','Only if more than 3 soft assertions fail'],answer:1,explanation:'Soft assertion failures accumulate silently during the test. When the test function body completes, Playwright checks if any soft failures were recorded and marks the entire test as failed if any exist. This lets you see all failures in one run rather than stopping at the first.'},
{id:4529,topic:'playwright',difficulty:'intermediate',question:'What does `expect.configure({ timeout: 10000 })` return?',options:['Nothing — it modifies the global expect','A new expect function with the specified timeout as its default, without modifying the global expect','An assertion object','A Playwright config object'],answer:1,explanation:'`expect.configure()` returns a NEW expect object with customized defaults. The original global `expect` is not modified. You can store it as `const myExpect = expect.configure({ timeout: 10000 })` and use `myExpect(locator).toBeVisible()` with the custom timeout.'},
{id:4530,topic:'playwright',difficulty:'intermediate',question:'What does `expect.configure({ message: "Login button should be visible" })` do?',options:['Sets the test name in reports','Overrides the default error message shown when the assertion fails, making failures more descriptive in reports','Logs the message to the console during the test','Adds the message as a comment to the assertion'],answer:1,explanation:'The `message` option in `expect.configure()` (or as the second argument to `expect(locator, { message })`) replaces the default Playwright error message with a custom one. This greatly improves debugging by showing meaningful context in failure reports.'},
{id:4531,topic:'playwright',difficulty:'intermediate',question:'What is `APIRequestContext` in Playwright used for?',options:['Defining API routes to intercept','Making direct HTTP requests (GET, POST, PUT, DELETE, PATCH) from the test process without a browser, useful for API testing and data setup','Configuring CORS headers for test APIs','Creating mock REST servers'],answer:1,explanation:'`APIRequestContext` allows direct HTTP calls from the test runner (Node.js process), bypassing the browser. It is available as the `request` fixture. It automatically manages cookies and can share authentication state with the browser context.'},
{id:4532,topic:'playwright',difficulty:'intermediate',question:'How do you make a GET request using Playwright\'s request fixture?',options:["const response = await request.get('https://api.example.com/users')",'request.fetch("GET", url)','page.request.get(url)','fetch(url) in the test'],answer:0,explanation:'`await request.get(url, options?)` sends an HTTP GET request and returns an `APIResponse`. Options include `headers`, `params` (query string), and `timeout`. Use `await response.json()` to parse the JSON body.'},
{id:4533,topic:'playwright',difficulty:'intermediate',question:'How do you send a POST request with a JSON body using the Playwright request fixture?',options:["request.post(url, { data: { key: 'value' } })",'request.post(url, { body: JSON.stringify(data) })','request.send("POST", url, data)','Both A and B are valid'],answer:3,explanation:'`request.post(url, { data: object })` serializes the object as JSON and sets the Content-Type header automatically. `request.post(url, { data: jsonString })` sends a raw string body. Both are valid; use `{ data: object }` for convenience with plain JSON payloads.'},
{id:4534,topic:'playwright',difficulty:'intermediate',question:'How do you assert the HTTP status code of an API response in Playwright?',options:['expect(response.status).toBe(200)','expect(response.status()).toBe(200)','response.assertStatus(200)','await expect(response).toHaveStatus(200)'],answer:1,explanation:'`response.status()` (with parentheses — it is a method, not a property) returns the HTTP status code as a number. Use it with regular `expect`: `expect(response.status()).toBe(200)`. `response.ok()` is a shorthand for status 200–299.'},
{id:4535,topic:'playwright',difficulty:'intermediate',question:'What does `await response.json()` do with a Playwright APIResponse?',options:['Returns the response as a JSON string','Parses the response body as JSON and returns the resulting JavaScript object','Validates the response against a JSON schema','Returns the response headers as JSON'],answer:1,explanation:'`await response.json()` parses the response body as JSON and returns the deserialized JavaScript object. It is equivalent to `JSON.parse(await response.text())`. Use it after `request.get()`, `request.post()`, or any other HTTP method.'},
{id:4536,topic:'playwright',difficulty:'intermediate',question:'How do you dispose of an APIRequestContext when it is not provided by a fixture?',options:['It is automatically disposed','await request.dispose()','request.close()','request.destroy()'],answer:1,explanation:'When you create an `APIRequestContext` manually via `playwright.request.newContext()`, you must call `await request.dispose()` when done to release resources. Fixture-provided `request` objects are automatically disposed after each test.'},
{id:4537,topic:'playwright',difficulty:'intermediate',question:'What does `page.evaluate(() => window.localStorage.getItem("token"))` do?',options:['Sets a localStorage item','Evaluates a function in the browser and returns the current value of "token" from localStorage to the test','Clears localStorage','Checks if the token is expired'],answer:1,explanation:'`page.evaluate()` executes the callback in the browser\'s JavaScript context and returns the serialized result to Node.js. This is how you read browser-side state (localStorage, sessionStorage, DOM properties, JavaScript globals) in Playwright tests.'},
{id:4538,topic:'playwright',difficulty:'intermediate',question:'What does `page.evaluate(([a, b]) => a + b, [1, 2])` return?',options:['undefined','The string "1 + 2"','3','An error because arrays cannot be passed'],answer:2,explanation:'`page.evaluate(fn, arg)` passes `arg` to the function in the browser. Here `[1, 2]` is destructured as `[a, b]`, so `a + b = 3`. The result is serialized and returned as a Promise resolving to `3` in Node.js.'},
{id:4539,topic:'playwright',difficulty:'advanced',question:'What is `page.evaluateHandle()` used for?',options:['Same as page.evaluate() but faster','Returns a JSHandle or ElementHandle referencing the object in the browser without serializing it, allowing further browser-side operations','Returns the element count','Evaluates CSS selectors'],answer:1,explanation:'`page.evaluateHandle()` is used when the return value is a complex browser object (like a DOM element, Map, or custom class instance) that cannot be serialized to JSON. The returned handle can be passed back to `page.evaluate()` for further manipulation.'},
{id:4540,topic:'playwright',difficulty:'advanced',question:'How do you get an ElementHandle from a page.evaluateHandle() result?',options:['It is automatically an ElementHandle','Call jsHandle.asElement() — returns an ElementHandle if the JSHandle references a DOM element, or null otherwise','Call jsHandle.getElement()','ElementHandle cannot be obtained from evaluateHandle'],answer:1,explanation:'`jsHandle.asElement()` converts a `JSHandle` to an `ElementHandle` if it references a DOM element node. If the JSHandle points to a non-element value, `asElement()` returns `null`. ElementHandles provide DOM-specific methods like `screenshot()` and `click()`.'},
{id:4541,topic:'playwright',difficulty:'intermediate',question:'What does `page.keyboard.press("F5")` do?',options:['Opens developer tools','Presses the F5 key, which causes the page to reload in most browsers','Focuses the address bar','Runs a macro'],answer:1,explanation:'`page.keyboard.press("F5")` dispatches a keydown event for the F5 key. In Chromium this triggers a page reload. Key names follow the KeyboardEvent.key specification: `"F1"`–`"F12"`, `"Escape"`, `"ArrowUp"`, `"Backspace"`, etc.'},
{id:4542,topic:'playwright',difficulty:'intermediate',question:'What does `page.keyboard.down("Control")` followed by `page.keyboard.press("C")` simulate?',options:['Ctrl+C keyboard shortcut (copy) with the Ctrl key held between calls','Presses Control then C independently','Types the letter C','Nothing — this combination is not supported'],answer:0,explanation:'`keyboard.down("Control")` holds Control pressed. Subsequent `keyboard.press("C")` fires with the Control modifier active, simulating Ctrl+C. Always follow with `keyboard.up("Control")` to release. Alternatively, use `page.keyboard.press("Control+C")` in one call.'},
{id:4543,topic:'playwright',difficulty:'intermediate',question:'What does `page.keyboard.up("Shift")` do?',options:['Types uppercase letters','Releases the Shift key, dispatching a keyup event for Shift','Moves focus up','Scrolls the page up'],answer:1,explanation:'`page.keyboard.up(key)` dispatches a `keyup` event for the specified key, releasing a key that was held with `keyboard.down()`. Always pair `keyboard.down()` with `keyboard.up()` to avoid leaving modifier keys stuck in the pressed state.'},
{id:4544,topic:'playwright',difficulty:'intermediate',question:'What does `page.mouse.click(x, y)` do?',options:['Clicks the element at those coordinates','Moves the mouse to (x, y) and dispatches mousedown/mouseup/click events at that absolute page position','Takes a screenshot at that position','Asserts a click occurred at those coordinates'],answer:1,explanation:'`page.mouse.click(x, y)` moves the virtual mouse to the given page coordinates and fires the full sequence of mouse events: `mousemove`, `mousedown`, `mouseup`, and `click`. Coordinates are relative to the top-left of the page (including scrolled content).'},
{id:4545,topic:'playwright',difficulty:'intermediate',question:'How do you perform a mouse double-click using page.mouse in Playwright?',options:['page.mouse.click(x, y, { count: 2 })','page.mouse.dblclick(x, y)','page.mouse.click(x, y); page.mouse.click(x, y)','page.mouse.doubleClick(x, y)'],answer:1,explanation:'`page.mouse.dblclick(x, y)` dispatches the double-click event sequence at the given coordinates: two clicks followed by a `dblclick` event. You can also use `page.mouse.click(x, y, { clickCount: 2 })` to achieve the same result.'},
{id:4546,topic:'playwright',difficulty:'intermediate',question:'What does `page.mouse.down()` do in Playwright?',options:['Scrolls the page down','Presses and holds the left mouse button at the current cursor position, dispatching a mousedown event','Moves the mouse to the bottom of the page','Closes the browser'],answer:1,explanation:'`page.mouse.down()` dispatches a `mousedown` event at the current mouse position. It keeps the mouse button pressed until `page.mouse.up()` is called. This is the building block for drag-and-drop operations at the coordinate level.'},
{id:4547,topic:'playwright',difficulty:'intermediate',question:'What does `page.mouse.up()` do?',options:['Scrolls the page to the top','Releases the pressed mouse button, dispatching mouseup and click events','Moves the mouse to the top of the page','Asserts the mouse was pressed'],answer:1,explanation:'`page.mouse.up()` releases the held mouse button by dispatching `mouseup` and `click` events. It is the counterpart to `page.mouse.down()`. Together they form the foundation of manual drag-and-drop simulations.'},
{id:4548,topic:'playwright',difficulty:'intermediate',question:'What types of dialogs can Playwright intercept with the `dialog` event?',options:['Only window.alert()','alert, confirm, and prompt','alert, confirm, prompt, and beforeunload','Only confirm and prompt'],answer:2,explanation:'The `page` `dialog` event fires for four types of dialogs: `alert` (window.alert), `confirm` (window.confirm), `prompt` (window.prompt), and `beforeunload` (the navigation confirmation dialog). The `dialog.type()` method tells you which kind triggered the event.'},
{id:4549,topic:'playwright',difficulty:'intermediate',question:'What happens if no `dialog` event handler is registered when a dialog appears in Playwright?',options:['The test hangs indefinitely','Playwright automatically dismisses the dialog and prints a warning to the console','The browser crashes','The test fails immediately with a timeout error'],answer:1,explanation:'If no handler is registered for the `dialog` event, Playwright automatically dismisses the dialog (equivalent to clicking Cancel or OK for alerts). A warning is printed to the console. This prevents tests from hanging when unexpected dialogs appear.'},
{id:4550,topic:'playwright',difficulty:'intermediate',question:'What does `dialog.defaultValue()` return for a `window.prompt()` dialog?',options:['The user\'s entered text','The default value pre-filled in the prompt input box, as defined by the second argument to window.prompt()','An empty string always','The dialog title'],answer:1,explanation:'`dialog.defaultValue()` returns the default value specified by the page\'s `window.prompt("message", defaultValue)` call. If no default is provided, it returns an empty string. Use this to validate or include in your acceptance logic.'},
{id:4551,topic:'playwright',difficulty:'intermediate',question:'How do you capture a file download in Playwright?',options:["Use page.download() method","Combine Promise.all with page.waitForEvent('download') and the action that triggers the download",'Set download: true in the context options','Call browser.downloadFile(url)'],answer:1,explanation:'`const [download] = await Promise.all([page.waitForEvent("download"), locator.click()])` captures the download event. Using `Promise.all` prevents a race condition where the download starts before the listener is registered.'},
{id:4552,topic:'playwright',difficulty:'intermediate',question:'What does `await download.path()` return in Playwright?',options:['The original download URL','The absolute path to the downloaded file saved in a temporary directory, available after the download completes','The file name without the path','The file size in bytes'],answer:1,explanation:'`download.path()` returns a Promise that resolves to the path of the downloaded file in the system\'s temp directory once the download finishes. Returns `null` if the download failed. Use `download.saveAs(destPath)` to copy it to a persistent location.'},
{id:4553,topic:'playwright',difficulty:'intermediate',question:'What does `download.suggestedFilename()` return?',options:['The absolute file path','The suggested filename from the Content-Disposition header or URL, without directory path','The file MIME type','The URL the file was downloaded from'],answer:1,explanation:'`download.suggestedFilename()` returns the filename as suggested by the server via the `Content-Disposition: attachment; filename="..."` response header, or extracted from the URL if that header is absent. It does not return the full saved path.'},
{id:4554,topic:'playwright',difficulty:'intermediate',question:'What does `await download.saveAs("/tmp/myfile.csv")` do?',options:['Streams the download to the given path in real time','Copies the finished download from its temporary location to the specified destination path','Saves the browser cache','Creates a symbolic link to the temp file'],answer:1,explanation:'`download.saveAs(filePath)` copies the downloaded file from the Playwright-managed temp directory to the given destination. Call this after the download has completed (after awaiting `page.waitForEvent("download")`). The temp file is cleaned up when the download object is garbage collected.'},
{id:4555,topic:'playwright',difficulty:'intermediate',question:'How do you listen for console messages from the page in Playwright?',options:["page.on('console', msg => console.log(msg.text()))",'context.interceptConsole()','browser.captureConsole()','test.captureLog()'],answer:0,explanation:'`page.on("console", msg => ...)` registers a listener for all console messages emitted by the page. The `ConsoleMessage` object provides `type()` (log, warn, error, info, debug) and `text()` (the formatted message text).'},
{id:4556,topic:'playwright',difficulty:'intermediate',question:'What does `consoleMessage.type()` return for a `console.warn()` call on the page?',options:['"warning"','"warn"','"alert"','"console"'],answer:1,explanation:'`consoleMessage.type()` returns the console method name as a string: `"log"`, `"warn"`, `"error"`, `"info"`, `"debug"`, `"dir"`, `"trace"`, etc. For `console.warn()`, it returns `"warn"`.'},
{id:4557,topic:'playwright',difficulty:'intermediate',question:'How do you collect all console errors during a Playwright test for assertion?',options:["const errors=[]; page.on('console', m => { if(m.type()==='error') errors.push(m.text()) }); /* later */ expect(errors).toHaveLength(0)",'page.assertNoConsoleErrors()','expect(page).toHaveNoConsoleErrors()','page.on("error", handler)'],answer:0,explanation:'Collect messages manually by pushing to an array inside the `console` event handler. After the test actions, assert the array is empty or contains expected values. This is a common pattern for detecting unexpected JavaScript errors in the application under test.'},
{id:4558,topic:'playwright',difficulty:'intermediate',question:'What event fires in Playwright when an unhandled JavaScript exception occurs in the page?',options:["'error'","'pageerror'","'exception'","'uncaughtError'"],answer:1,explanation:'`page.on("pageerror", error => ...)` fires whenever an unhandled exception (thrown error that is not caught by any try/catch) occurs in the page\'s JavaScript. The handler receives a standard JavaScript `Error` object with `message`, `name`, and `stack` properties.'},
{id:4559,topic:'playwright',difficulty:'intermediate',question:'How do you fail a Playwright test automatically if the page throws a JavaScript error?',options:["page.on('pageerror', err => { throw err; })",'test.failOnJsError(true)','playwright.config.ts: failOnConsoleError: true','Playwright automatically fails on page errors'],answer:0,explanation:'Register `page.on("pageerror", err => { throw err; })` in a `beforeEach` hook. When any uncaught JS error fires, rethrowing it inside the handler causes the current test to fail with the error details. This turns silent page errors into explicit test failures.'},
{id:4560,topic:'playwright',difficulty:'advanced',question:'What is a Playwright trace file and what does it contain?',options:['A zip file containing test source code and config','A zip file containing a timeline of actions, DOM snapshots before and after each action, network HAR data, console messages, and test metadata for post-mortem debugging','A video recording only','A JSON log of assertion results'],answer:1,explanation:'A Playwright trace file (`.zip`) contains: a timeline of all test actions with timestamps and durations, DOM snapshots (before & after each action), a HAR file of all network activity, console messages, source code locations for each step, and screenshots. Open with `npx playwright show-trace`.'},
{id:4561,topic:'playwright',difficulty:'advanced',question:'How do you record a trace for failing tests only in playwright.config.ts?',options:["trace: 'on'",'trace: "retain-on-failure"','trace: "on-first-retry"','trace: "fail-only"'],answer:1,explanation:'`trace: "retain-on-failure"` records a trace for every test but deletes the trace files of passing tests after the run. Only failing tests keep their trace. This is the recommended CI setting: full debugging info for failures with minimal storage overhead.'},
{id:4562,topic:'playwright',difficulty:'advanced',question:'What does `trace: "on-first-retry"` do in Playwright?',options:['Records trace only on the first test run','Records a trace only when a test is being retried for the first time — useful for diagnosing flaky tests without recording on every run','Records traces for the first N tests','Records a partial trace'],answer:1,explanation:'`"on-first-retry"` activates tracing only when a test is running its first retry (i.e., it failed once already). This is useful for catching intermittent failures: the retry produces a trace while passing tests and initial runs incur no trace overhead.'},
{id:4563,topic:'playwright',difficulty:'advanced',question:'How do you start and stop tracing manually in a Playwright test?',options:["context.tracing.start({ screenshots, snapshots }); /* test actions */; context.tracing.stop({ path: 'trace.zip' })",'test.startTrace(); test.stopTrace()','page.trace.start(); page.trace.stop()','browser.startTracing(); browser.stopTracing()'],answer:0,explanation:'`context.tracing.start({ screenshots: true, snapshots: true, sources: true })` begins recording. `context.tracing.stop({ path: "trace.zip" })` saves the trace. You can also call `context.tracing.startChunk()` and `stopChunk()` to record multiple sub-traces within a single test.'},
{id:4564,topic:'playwright',difficulty:'advanced',question:'What does the `snapshots` option in `context.tracing.start()` enable?',options:['Video recording','DOM snapshots captured before and after each action, enabling the "Before" and "After" views in the Trace Viewer UI','Screenshot attachments','Network snapshots only'],answer:1,explanation:'`snapshots: true` enables DOM serialization before and after each action. This is what powers the "Before" and "After" tabs in the Trace Viewer. Without snapshots, you only get action logs and network data but no DOM state replay.'},
{id:4565,topic:'playwright',difficulty:'advanced',question:'What does `context.tracing.startChunk({ title: "Login flow" })` allow?',options:['Starts a new browser context','Records a named sub-trace within the current tracing session that can be saved independently with stopChunk()', 'Pauses tracing temporarily','Splits trace by network requests'],answer:1,explanation:'`startChunk()` begins a new named chunk within an active tracing session. Calling `context.tracing.stopChunk({ path: "login-trace.zip" })` saves just that chunk as a separate trace file. This allows test suites to save per-scenario traces from a single context.'},
{id:4566,topic:'playwright',difficulty:'intermediate',question:'What is the Playwright `test.step()` method used for?',options:['Skips a portion of a test','Groups test actions into a named step visible in the Trace Viewer and HTML report, improving readability and debugging','Creates a sub-test','Adds a timer to measure performance'],answer:1,explanation:'`await test.step("Step description", async () => { ... })` wraps a block of code in a named step. Steps appear as collapsible sections in the HTML report and as distinct entries in the Trace Viewer timeline, making large tests much easier to debug.'},
{id:4567,topic:'playwright',difficulty:'intermediate',question:'What does `test.only()` do in Playwright?',options:['Marks a test as required to pass','Runs ONLY that test (and any other tests marked .only) in the file, skipping all others — useful for focused debugging','Prevents the test from being skipped','Creates an exclusive worker'],answer:1,explanation:'`test.only(name, fn)` marks a test as exclusive. When at least one `.only` exists in a file, only those tests run. Be careful not to commit `.only` to source control as it will skip all other tests in CI, causing incomplete coverage.'},
{id:4568,topic:'playwright',difficulty:'intermediate',question:'What does `test.skip()` do when called without arguments inside a test body?',options:['Skips all tests in the file','Immediately skips the current test — the rest of the test body is not executed, and the test is marked as skipped in the report','Marks the test as expected to fail','Retries the test'],answer:1,explanation:'`test.skip()` (no arguments) called inside the test body skips the test at runtime. It is equivalent to throwing a skip exception. The test appears as skipped in results. Use the conditional form `test.skip(condition, reason)` to skip based on runtime values like `browserName`.'},
{id:4569,topic:'playwright',difficulty:'intermediate',question:'What does `test.fixme()` do in Playwright?',options:['Fixes a broken test automatically','Marks a test as "fixme" — it runs but is expected to fail; the test runner will not count it as a test failure, and it shows up as fixme in the report','Prevents the test from running','Logs a TODO comment'],answer:1,explanation:'`test.fixme()` marks a test as expected to fail (a known broken test). Unlike `test.fail()`, a fixme test is grayed out in reports. If the test unexpectedly passes, it is flagged. Use it to track broken tests without hiding them from the test suite.'},
{id:4570,topic:'playwright',difficulty:'intermediate',question:'What does `test.fail()` indicate in Playwright?',options:['Forces the test to fail','Marks the test as expected to fail — if it fails, it is reported as expected; if it unexpectedly passes, it is reported as unexpected success','Disables retries for the test','Same as test.skip()'],answer:1,explanation:'`test.fail()` declares that the test is expected to fail. If the test fails, it is marked "expected failure" (green in some reporters). If it passes, it is flagged as an unexpected success. Use it for known bugs that are not yet fixed.'},
{id:4571,topic:'playwright',difficulty:'intermediate',question:'What does `test.setTimeout(60000)` do inside a test?',options:['Sets the global timeout for all tests','Overrides the timeout for that specific test only to 60 seconds','Sets the assertion timeout','Sets the browser launch timeout'],answer:1,explanation:'`test.setTimeout(ms)` changes the timeout for the currently running test. Calling it inside the test body overrides the global timeout set in `playwright.config.ts`. Useful for tests that legitimately require more time, like those involving file uploads or complex UI flows.'},
{id:4572,topic:'playwright',difficulty:'advanced',question:'How does the `request` fixture share cookies with the `page` fixture in Playwright?',options:['They are completely isolated','The request fixture shares the same cookie jar as the browser context by default, so any cookies set by page.goto() are available to request.get() and vice versa','Only session cookies are shared','Sharing requires explicit storageState configuration'],answer:1,explanation:'By default, the `request` fixture in a test uses the same underlying storage state as the test\'s `context` fixture. Cookies obtained via browser-based login are automatically available to API requests and vice versa, enabling hybrid browser + API test patterns.'},
{id:4573,topic:'playwright',difficulty:'advanced',question:'What does `playwright.request.newContext({ baseURL, extraHTTPHeaders })` create?',options:['A new BrowserContext for API testing','An APIRequestContext not tied to any browser context, with its own cookie storage and configurable base URL and headers','A new Page for API testing','A mock HTTP server'],answer:1,explanation:'`playwright.request.newContext()` creates a standalone `APIRequestContext` independent of any browser. It has its own cookie jar and can be configured with `baseURL`, `extraHTTPHeaders`, `httpCredentials`, and `ignoreHTTPSErrors`. Must be explicitly disposed after use.'},
{id:4574,topic:'playwright',difficulty:'advanced',question:'What is the difference between `page.on("response", handler)` and `page.route(url, handler)` in Playwright?',options:['They are identical','page.on("response") is a passive listener that observes responses; page.route() actively intercepts requests and requires calling route.fulfill/continue/abort','response handler is faster','route only works for XHR requests'],answer:1,explanation:'`page.on("response")` is a read-only event listener — you can observe responses but cannot modify or block them. `page.route()` is an interceptor that requires an explicit action (`fulfill`, `continue`, or `abort`) to resolve the request. If you forget to call one, the request hangs.'},
{id:4575,topic:'playwright',difficulty:'advanced',question:'What happens if a `page.route()` handler never calls `fulfill()`, `continue()`, or `abort()`?',options:['The request is automatically forwarded','The request hangs indefinitely until the page timeout fires, causing a navigation timeout error in the test','Playwright throws an error immediately','The browser cancels the request after 5 seconds'],answer:1,explanation:'If none of the resolution methods are called, the network request remains pending. The browser and test will eventually time out. Always ensure every route handler path calls exactly one of `fulfill()`, `continue()`, or `abort()` to avoid hanging requests in tests.'},
{id:4576,topic:'playwright',difficulty:'advanced',question:'How do you use `page.route()` to delay API responses to test loading states?',options:["route.delay(1000)",'await new Promise(r => setTimeout(r, 1000)); route.continue()','route.continue({ delay: 1000 })','route.fulfill({ latency: 1000 })'],answer:1,explanation:'To simulate latency, manually delay before resolving the route: `page.route(url, async route => { await new Promise(r => setTimeout(r, 1000)); await route.continue(); })`. This delays the response and lets you assert loading spinners or skeleton UIs are shown.'},
{id:4577,topic:'playwright',difficulty:'intermediate',question:'What is the `page.waitForEvent()` method used for?',options:['Waits for a DOM event on an element','Returns a Promise that resolves when the specified page-level event fires (like "popup", "download", "filechooser", "dialog", "requestfinished")','Waits for a CSS transition to complete','Waits for a network request'],answer:1,explanation:'`page.waitForEvent(eventName)` creates a Promise that resolves to the event payload when the named page event fires. Common uses: `"popup"` (new window), `"download"` (file download starts), `"dialog"` (alert/confirm/prompt), and `"filechooser"` (file upload dialog).'},
{id:4578,topic:'playwright',difficulty:'intermediate',question:'How do you handle a file chooser dialog (triggered by `<input type="file">` clicks) in Playwright?',options:["const [chooser] = await Promise.all([page.waitForEvent('filechooser'), locator.click()]); chooser.setFiles('file.txt')",'page.on("filechooser", c => c.upload("file.txt"))','locator.setInputFiles() always opens the file chooser','page.waitForFileChooser().setFiles("file.txt")'],answer:0,explanation:'`page.waitForEvent("filechooser")` captures the file chooser before it appears. Combine with the click in `Promise.all` to avoid race conditions. Call `fileChooser.setFiles(paths)` to set the files programmatically without opening the OS picker.'},
{id:4579,topic:'playwright',difficulty:'intermediate',question:'How do you assert that a page has no accessibility violations using Playwright?',options:['expect(page).toBeAccessible()','Playwright does not have built-in accessibility assertions; use @axe-core/playwright package: const results = await checkA11y(page)','page.checkAccessibility()','expect(page).toHaveNoA11yErrors()'],answer:1,explanation:'Playwright does not ship built-in accessibility auditing. The community package `@axe-core/playwright` (or `axe-playwright`) integrates axe-core. After installation: `import { checkA11y } from "axe-playwright"` and call `await checkA11y(page)` to run accessibility checks.'},
{id:4580,topic:'playwright',difficulty:'intermediate',question:'What does `page.pause()` do in Playwright?',options:['Pauses the entire test suite','Suspends test execution at that point, opens Playwright Inspector for interactive debugging, and waits until you click Resume in the Inspector UI','Takes a screenshot and pauses for 5 seconds','Pauses all network requests'],answer:1,explanation:'`page.pause()` halts the test at that line and launches Playwright Inspector (a debugging UI). You can step through actions, inspect selectors, and modify the page while the test is paused. Only works in headed mode or with `PWDEBUG=1`.'},
{id:4581,topic:'playwright',difficulty:'intermediate',question:'What environment variable enables Playwright debugging mode?',options:['PLAYWRIGHT_DEBUG=1','PWDEBUG=1','DEBUG=playwright','NODE_DEBUG=playwright'],answer:1,explanation:'Setting `PWDEBUG=1` before running tests enables the Playwright Inspector for step-by-step debugging. It automatically opens the browser in headed mode and pauses before each test. `PWDEBUG=console` logs Playwright API calls to the terminal instead.'},
{id:4582,topic:'playwright',difficulty:'intermediate',question:'What does `npx playwright codegen https://example.com` do?',options:['Generates performance test scripts','Records browser actions as you interact with the page and generates Playwright test code in real time','Downloads the page HTML','Creates a screenshot of the page'],answer:1,explanation:'`playwright codegen` launches a browser and records your interactions, automatically generating Playwright test code in the side panel. It uses smart locator suggestions (preferring `getByRole`, `getByLabel`, etc.) and outputs valid `@playwright/test` code.'},
{id:4583,topic:'playwright',difficulty:'advanced',question:'How do you emulate a specific device (e.g., iPhone 13) in Playwright?',options:["Use { device: 'iPhone 13' } in test.use()",'Use { ...devices["iPhone 13"] } from playwright in the use block of playwright.config.ts or test.use()','Set viewport to iPhone dimensions manually','devices emulation requires a mobile browser'],answer:1,explanation:'Playwright exports a `devices` map. Use `import { devices } from "@playwright/test"` and spread `devices["iPhone 13"]` in a project\'s `use` block. This sets the correct viewport, userAgent, deviceScaleFactor, hasTouch, and isMobile properties automatically.'},
{id:4584,topic:'playwright',difficulty:'advanced',question:'How do you emulate a slow 3G network connection in Playwright?',options:["Set slow3G: true in use block",'Use context.route() to add artificial delays','Use page.emulateNetworkConditions() in Chrome DevTools Protocol via context via cdpSession','Set network: "slow3g" in playwright.config.ts'],answer:2,explanation:'Playwright does not have a built-in `emulateNetworkConditions` API for non-Chromium browsers. For Chromium, use a CDP session: `const client = await context.newCDPSession(page); await client.send("Network.emulateNetworkConditions", { ...slow3GPreset })`.'},
{id:4585,topic:'playwright',difficulty:'advanced',question:'How do you set HTTP authentication credentials for pages in a BrowserContext?',options:["page.authenticate({ username, password })",'Set `httpCredentials: { username, password }` in the BrowserContext options or in `use` in playwright.config.ts','Use page.route() to add Authorization headers','Call context.setCredentials()'],answer:1,explanation:'`httpCredentials: { username: "user", password: "pass" }` in context options enables HTTP Basic/Digest authentication. Playwright automatically responds to `401` challenges with these credentials. Equivalent to entering credentials in a browser auth dialog.'},
{id:4586,topic:'playwright',difficulty:'advanced',question:'What does `context.setDefaultTimeout(60000)` override in Playwright?',options:['Only the assertion timeout','The default timeout for all actions and assertions within that BrowserContext (page.click, page.fill, expect, etc.)','Only the navigation timeout','Only the waitForSelector timeout'],answer:1,explanation:'`context.setDefaultTimeout(ms)` sets the default timeout for all Playwright operations within that context. It overrides the global `timeout` from config. Use `page.setDefaultTimeout()` for page-level overrides and `expect.configure()` for assertion-level overrides.'},
{id:4587,topic:'playwright',difficulty:'intermediate',question:'What does `page.setDefaultNavigationTimeout(30000)` control?',options:['The time between page navigation events','The maximum time allowed for navigation-related operations like goto(), waitForNavigation(), and waitForURL()','The assertion timeout','The time to load stylesheets'],answer:1,explanation:'`page.setDefaultNavigationTimeout(ms)` sets the timeout for navigation operations specifically: `page.goto()`, `page.waitForNavigation()`, `page.reload()`, `page.goBack()`, `page.goForward()`. It is separate from action timeouts, which are controlled by `setDefaultTimeout()`.'},
{id:4588,topic:'playwright',difficulty:'intermediate',question:'What is the recommended way to reuse authentication state across all tests in Playwright?',options:['Log in at the start of every test','Create a global setup file that logs in once and saves storageState to a file; reference it in playwright.config.ts via storageState in the use block','Use a shared browser instance','Store credentials in a fixture'],answer:1,explanation:'The recommended pattern: a `globalSetup` file logs in once using `browser.newPage()`, saves state with `context.storageState({ path: "auth.json" })`, then sets `use: { storageState: "auth.json" }` in config. All tests start authenticated without repeating login.'},
{id:4589,topic:'playwright',difficulty:'intermediate',question:'What is the `globalSetup` option in playwright.config.ts?',options:['A file path to a module that runs once before the entire test suite, before any worker or browser is started','A setup file that runs before each test','A browser initialization script','A TypeScript configuration file'],answer:0,explanation:'`globalSetup: "./global-setup.ts"` points to a file that exports a default async function run once before any tests start. It receives the Playwright config object. Use it for one-time setup like database seeding, login state generation, or starting a server.'},
{id:4590,topic:'playwright',difficulty:'intermediate',question:'What is the `globalTeardown` option in playwright.config.ts?',options:['Runs after every test','Points to a module whose default export runs once after all tests complete, used for cleanup like stopping servers or deleting test data','Generates the final test report','Closes the browser after all tests'],answer:1,explanation:'`globalTeardown` runs once after all tests in all workers have finished. It is the counterpart to `globalSetup`. Common uses: stopping a mock API server started in `globalSetup`, deleting test database records, or clearing generated files.'},
{id:4591,topic:'playwright',difficulty:'advanced',question:'How do you run a specific Playwright test by its title?',options:['npx playwright test --title "my test name"','npx playwright test --grep "my test name"','npx playwright test --name "my test name"','npx playwright test --filter "my test name"'],answer:1,explanation:'`--grep` accepts a regex string and filters tests by their full title (describe block names + test name concatenated). To run a test named exactly "login with valid credentials", use: `npx playwright test --grep "login with valid credentials"`.'},
{id:4592,topic:'playwright',difficulty:'advanced',question:'What does the Playwright `--update-snapshots` CLI flag do?',options:['Updates the playwright.config.ts','Regenerates all snapshot files (screenshots, aria snapshots) used by toMatchSnapshot() and toHaveScreenshot() assertions, replacing existing baselines','Updates the @playwright/test package version','Deletes all existing snapshots'],answer:1,explanation:'`npx playwright test --update-snapshots` regenerates all baseline snapshot files. Use this after intentional UI changes that make existing visual snapshots outdated. After updating, review the new snapshots in version control before committing.'},
{id:4593,topic:'playwright',difficulty:'advanced',question:'What is `expect(page).toHaveScreenshot("name.png")` used for?',options:['Takes a screenshot and asserts it is not blank','Visual regression testing — compares the current page screenshot against a stored baseline PNG, failing if the pixel difference exceeds the configured threshold','Checks the page has a background image named "name.png"','Saves a screenshot with the given filename only'],answer:1,explanation:'`toHaveScreenshot()` is Playwright\'s built-in visual regression API. On first run it creates the baseline. Subsequent runs compare against it with a configurable `maxDiffPixels` or `threshold` option. Baselines are stored alongside test files as `.png` snapshots.'},
{id:4594,topic:'playwright',difficulty:'advanced',question:'How do you configure the pixel diff threshold for `toHaveScreenshot()` in Playwright?',options:["expect.screenshot.threshold = 0.2",'Set `expect: { toHaveScreenshot: { maxDiffPixelRatio: 0.01 } }` in playwright.config.ts or pass `{ maxDiffPixels, threshold }` per assertion','There is no configuration for thresholds','Use --screenshot-threshold CLI flag'],answer:1,explanation:'Configure globally in `playwright.config.ts` under `expect.toHaveScreenshot`: `{ maxDiffPixelRatio: 0.01 }` (max 1% of pixels differ) or `{ maxDiffPixels: 50 }` (absolute count). Override per assertion: `await expect(page).toHaveScreenshot({ maxDiffPixels: 100 })`.'},
{id:4595,topic:'playwright',difficulty:'advanced',question:'What is the `testInfo` object available in Playwright tests?',options:['The test configuration only','An object injected into tests and hooks containing metadata: test title, file path, retry count, test ID, output directory, attachments array, and annotation helpers like testInfo.attach()','The browser information','A reference to the expect object'],answer:1,explanation:'`testInfo` is available as a parameter in tests and hooks. It provides: `title`, `titlePath`, `file`, `line`, `retry`, `workerIndex`, `parallelIndex`, `outputDir`, `snapshotDir`, `attachments`, and methods like `testInfo.attach()` for adding custom attachments to the HTML report.'},
{id:4596,topic:'playwright',difficulty:'advanced',question:'How do you add a custom attachment (e.g., a log file) to the Playwright HTML report?',options:["testInfo.attach('log', { path: './test.log', contentType: 'text/plain' })",'test.addAttachment("log", "./test.log")','reporter.attach("log")','page.addAttachment("log", "./test.log")'],answer:0,explanation:'`testInfo.attach(name, { path, contentType })` or `testInfo.attach(name, { body, contentType })` adds a file or buffer as an attachment visible in the HTML report. Use it to attach logs, JSON data, or extra screenshots to test results for debugging.'},
{id:4597,topic:'playwright',difficulty:'advanced',question:'What does `test.info().annotations` provide in Playwright?',options:['CSS annotations on page elements','An array of test annotations added via testInfo.annotations.push({ type, description }) that appear in the HTML report','Browser version info','Console log annotations'],answer:1,explanation:'`testInfo.annotations` is an array where you can push `{ type: string, description?: string }` objects. These annotations appear in the HTML test report alongside the test result. Common uses: tagging with JIRA tickets, feature flags, or test categories.'},
{id:4598,topic:'playwright',difficulty:'advanced',question:'What is the purpose of `expect(locator).toHaveAccessibleName("Submit")`?',options:['Checks the element text content is "Submit"','Asserts the computed accessible name of the element (as computed by the accessibility tree) is "Submit", testing the label that screen readers announce','Checks the aria-label attribute value','Checks the title attribute value'],answer:1,explanation:'`toHaveAccessibleName()` checks the computed accessible name — the name that assistive technologies announce for the element. It considers `aria-label`, `aria-labelledby`, associated `<label>`, `title`, and `alt` attributes, making it a comprehensive accessibility assertion.'},
{id:4599,topic:'playwright',difficulty:'advanced',question:'How do you run Playwright tests in debug mode using VS Code?',options:['Add breakpoints and use npx playwright test --debug','Install the Playwright extension for VS Code, then use "Run Tests" with breakpoints in the editor — the extension supports step-by-step debugging natively','Use PWDEBUG=1 in VS Code terminal','VS Code debugging is not supported for Playwright'],answer:1,explanation:'The official Playwright extension for VS Code (by Microsoft) enables running and debugging tests directly in the editor. You can set breakpoints, step through code, inspect locators with the built-in locator picker, and see live browser state — no terminal needed.'},
{id:4600,topic:'playwright',difficulty:'advanced',question:'What does `npx playwright merge-reports ./blob-reports --reporter html` do?',options:['Merges multiple playwright.config.ts files','Combines blob report shards from multiple CI machines into a single unified HTML report, used in sharded test runs','Merges test files into one file','Creates a combined screenshot'],answer:1,explanation:'When using `--shard`, each CI machine produces a `blob` reporter output. `npx playwright merge-reports ./blob-reports --reporter html` combines all shard results into one final HTML report. This is the standard pattern for aggregating sharded Playwright results in CI pipelines.'},
]);
