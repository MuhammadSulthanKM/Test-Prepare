window.QUIZ_BANK['code-challenges'] = (window.QUIZ_BANK['code-challenges']||[]).concat([
{id:9591,topic:'code-challenges',difficulty:'beginner',question:'What does this TestNG test print when it passes?\n\n@Test\npublic void testSum() {\n    int result = 2 + 3;\n    Assert.assertEquals(result, 5);\n    System.out.println("passed: " + result);\n}',options:['passed: 2','passed: 5','Nothing — assertions are silent','Error'],answer:1,explanation:'Assert.assertEquals(5,5) passes silently. The println executes next and prints "passed: 5".'},
{id:9592,topic:'code-challenges',difficulty:'beginner',question:'What happens when this TestNG test runs?\n\n@Test\npublic void testFail() {\n    Assert.assertEquals(1, 2);\n    System.out.println("after assert");\n}',options:['Prints "after assert"','Throws AssertionError and stops','Skips the assertion','Passes silently'],answer:1,explanation:'Assert.assertEquals(1,2) throws AssertionError immediately. The println is never reached.'},
{id:9593,topic:'code-challenges',difficulty:'beginner',question:'What does this JUnit 5 test do?\n\n@Test\nvoid testAddition() {\n    assertEquals(4, 2 + 2);\n}',options:['Fails because 2+2 is not 4','Passes because 2+2 equals 4','Throws an exception','Does nothing'],answer:1,explanation:'2+2=4, and assertEquals(4,4) passes. The test is marked as passed.'},
{id:9594,topic:'code-challenges',difficulty:'beginner',question:'What is the output of this TestNG test?\n\n@Test\npublic void sample() {\n    String s = "hello";\n    Assert.assertNotNull(s);\n    Assert.assertTrue(s.length() > 0);\n    System.out.println("ok");\n}',options:['Nothing','ok','AssertionError','null'],answer:1,explanation:'s is not null and length > 0, so both assertions pass. println prints "ok".'},
{id:9595,topic:'code-challenges',difficulty:'beginner',question:'What does this JUnit 5 code do?\n\n@Test\nvoid failTest() {\n    fail("should not reach here");\n}',options:['Passes','Skips','Fails with message "should not reach here"','Prints the message'],answer:2,explanation:'fail() unconditionally fails the test and uses the provided message as the failure reason.'},
{id:9596,topic:'code-challenges',difficulty:'beginner',question:'What is the result of this TestNG assertion?\n\nAssert.assertTrue(false, "value was false");',options:['Passes','Fails with message "value was false"','Prints "value was false"','Throws NullPointerException'],answer:1,explanation:'assertTrue(false,...) fails because the condition is false. The second argument is the failure message shown.'},
{id:9597,topic:'code-challenges',difficulty:'beginner',question:'How many times does this TestNG @DataProvider test run?\n\n@DataProvider(name="nums")\npublic Object[][] data() {\n    return new Object[][]{{1},{2},{3}};\n}\n\n@Test(dataProvider="nums")\npublic void test(int n) {\n    Assert.assertTrue(n > 0);\n}',options:['1','2','3','Error'],answer:2,explanation:'The DataProvider returns 3 rows. The test method runs once per row, totalling 3 executions.'},
{id:9598,topic:'code-challenges',difficulty:'beginner',question:'What does this JUnit 5 assertion do?\n\nassertThrows(IllegalArgumentException.class, () -> {\n    throw new IllegalArgumentException("bad input");\n});',options:['Fails','Passes','Throws the exception to the test runner','Prints "bad input"'],answer:1,explanation:'assertThrows expects the lambda to throw IllegalArgumentException. It does, so the test passes.'},
{id:9599,topic:'code-challenges',difficulty:'beginner',question:'What is the output of this TestNG code?\n\n@Test\npublic void t() {\n    int[] arr = {1, 2, 3};\n    Assert.assertEquals(arr.length, 3);\n    System.out.println("length=" + arr.length);\n}',options:['length=2','length=3','AssertionError','Nothing'],answer:1,explanation:'arr.length is 3. assertEquals(3,3) passes. println prints "length=3".'},
{id:9600,topic:'code-challenges',difficulty:'beginner',question:'What does this JUnit 5 test verify?\n\n@Test\nvoid testNullCheck() {\n    String s = null;\n    assertNull(s);\n}',options:['Fails because s is null','Passes because s is null','Throws NullPointerException','Skips'],answer:1,explanation:'assertNull(null) passes — it confirms the value is indeed null.'},
{id:9601,topic:'code-challenges',difficulty:'beginner',question:'What happens with this TestNG @Test priority?\n\n@Test(priority=1)\npublic void second() { System.out.println("B"); }\n\n@Test(priority=0)\npublic void first() { System.out.println("A"); }',options:['B then A','A then B','Random order','Error'],answer:1,explanation:'TestNG runs tests in ascending priority order. priority=0 runs before priority=1. Output: A then B.'},
{id:9602,topic:'code-challenges',difficulty:'beginner',question:'What does this JUnit 5 annotation do?\n\n@Disabled("not implemented yet")\n@Test\nvoid futureTest() {\n    fail();\n}',options:['Fails with AssertionError','Passes','Is skipped with reason','Throws an exception'],answer:2,explanation:'@Disabled marks a test to be skipped. The test body never executes. The reason string is reported.'},
{id:9603,topic:'code-challenges',difficulty:'beginner',question:'What is the result of this TestNG assertion?\n\nAssert.assertFalse(10 > 5);',options:['Passes','Fails','Prints false','Throws NullPointerException'],answer:1,explanation:'10 > 5 is true. assertFalse(true) fails because it expects the condition to be false.'},
{id:9604,topic:'code-challenges',difficulty:'beginner',question:'What does this JUnit 5 code check?\n\nassertNotNull("hello");\nassertNotNull(null);',options:['Both pass','First passes, second fails','Both fail','First fails, second passes'],answer:1,explanation:'assertNotNull("hello") passes. assertNotNull(null) fails because null is null.'},
{id:9605,topic:'code-challenges',difficulty:'beginner',question:'What does @BeforeMethod in TestNG do?\n\n@BeforeMethod\npublic void setup() {\n    System.out.println("setup");\n}\n\n@Test\npublic void t1() { System.out.println("t1"); }\n\n@Test\npublic void t2() { System.out.println("t2"); }',options:['setup runs once before all tests','setup runs before each test method','setup runs after each test','setup is ignored'],answer:1,explanation:'@BeforeMethod runs before each @Test method. Output would be: setup, t1, setup, t2 (in test execution order).'},
{id:9606,topic:'code-challenges',difficulty:'beginner',question:'What is the output of this JUnit 5 test when it fails?\n\n@Test\nvoid test() {\n    int actual = 5;\n    assertEquals(10, actual, "Expected 10 but got different value");\n}',options:['Passes','Fails with message "Expected 10 but got different value"','Prints the message','Throws NullPointerException'],answer:1,explanation:'assertEquals(10,5,...) fails. The third argument is the failure message displayed in the test report.'},
{id:9607,topic:'code-challenges',difficulty:'beginner',question:'What does this TestNG code check?\n\nList<String> list = Arrays.asList("a","b","c");\nAssert.assertEquals(list.size(), 3);',options:['Fails because list has 3 items','Passes because list has 3 items','Throws NullPointerException','Prints 3'],answer:1,explanation:'list.size() returns 3. assertEquals(3,3) passes.'},
{id:9608,topic:'code-challenges',difficulty:'beginner',question:'What does this JUnit 5 annotation sequence ensure?\n\n@BeforeEach\nvoid init() { /* setup */ }\n\n@AfterEach\nvoid cleanup() { /* teardown */ }\n\n@Test\nvoid t() { /* test */ }',options:['init runs once, cleanup runs once','init runs before t, cleanup runs after t','cleanup runs before t, init runs after','Both run only if t passes'],answer:1,explanation:'@BeforeEach runs before each test, @AfterEach runs after each test. Order: init → t → cleanup.'},
{id:9609,topic:'code-challenges',difficulty:'beginner',question:'What is the result of this TestNG soft assertion?\n\nSoftAssert sa = new SoftAssert();\nsa.assertEquals(1, 2);\nsa.assertEquals(3, 3);\nsa.assertAll();',options:['Passes because second assertion is correct','Fails because first assertion failed','Throws immediately on first failure','Ignores all failures'],answer:1,explanation:'SoftAssert collects all failures. Both assertions run: first fails (1≠2), second passes. assertAll() reports all failures — the test fails.'},
{id:9610,topic:'code-challenges',difficulty:'beginner',question:'What does this JUnit 5 parameterized test do?\n\n@ParameterizedTest\n@ValueSource(ints = {1, 2, 3})\nvoid isPositive(int n) {\n    assertTrue(n > 0);\n}',options:['Runs once with all values','Runs 3 times, once per value','Fails for all values','Skips'],answer:1,explanation:'@ParameterizedTest with @ValueSource runs the test once per value. 3 values → 3 test executions. All pass since 1,2,3 > 0.'},
{id:9611,topic:'code-challenges',difficulty:'intermediate',question:'What does this TestNG @DataProvider test output when run?\n\n@DataProvider(name="data")\npublic Object[][] provide() {\n    return new Object[][]{\n        {"alice", true},\n        {"bob", false}\n    };\n}\n\n@Test(dataProvider="data")\npublic void test(String name, boolean active) {\n    System.out.println(name + ":" + active);\n}',options:['alice:true\nbob:false','bob:false\nalice:true','alice:false\nbob:true','Error'],answer:0,explanation:'DataProvider provides rows in order. Test runs with ("alice",true) then ("bob",false). Output: "alice:true" then "bob:false".'},
{id:9612,topic:'code-challenges',difficulty:'intermediate',question:'What is the result of this JUnit 5 test?\n\n@Test\nvoid testException() {\n    Exception ex = assertThrows(RuntimeException.class, () -> {\n        throw new RuntimeException("oops");\n    });\n    assertEquals("oops", ex.getMessage());\n}',options:['Fails because exception is thrown','Passes because exception is expected and message matches','Fails because getMessage is wrong','Skips'],answer:1,explanation:'assertThrows catches the RuntimeException and returns it. assertEquals checks its message "oops" == "oops". Both pass.'},
{id:9613,topic:'code-challenges',difficulty:'intermediate',question:'What does this TestNG test group annotation do?\n\n@Test(groups={"smoke"})\npublic void loginTest() { /* ... */ }\n\n@Test(groups={"regression"})\npublic void searchTest() { /* ... */ }\n\n// Run with groups="smoke"\n',options:['Both tests run','Only loginTest runs','Only searchTest runs','Neither runs'],answer:1,explanation:'When configured to run group "smoke", only tests annotated with groups={"smoke"} execute. loginTest runs, searchTest is excluded.'},
{id:9614,topic:'code-challenges',difficulty:'intermediate',question:'What is the result of this JUnit 5 nested test?\n\n@Nested\nclass WhenLoggedIn {\n    @BeforeEach\n    void login() { System.out.println("login"); }\n\n    @Test\n    void canViewProfile() { System.out.println("view"); }\n}',options:['login\nview','view\nlogin','login only','Error'],answer:0,explanation:'@BeforeEach in a @Nested class runs before each test within that class. login runs then view. Output: "login" then "view".'},
{id:9615,topic:'code-challenges',difficulty:'intermediate',question:'What does this TestNG @Factory create?\n\npublic class TestFactory {\n    @Factory\n    public Object[] create() {\n        return new Object[]{\n            new MyTest("a"),\n            new MyTest("b")\n        };\n    }\n}\nclass MyTest {\n    String s;\n    MyTest(String s) { this.s = s; }\n    @Test\n    public void t() { System.out.println(s); }\n}',options:['One test instance','Two test instances, each running t()','An error','Nothing'],answer:1,explanation:'@Factory creates multiple instances of a test class. Two MyTest instances are created, each running t(). Output: "a" and "b".'},
{id:9616,topic:'code-challenges',difficulty:'intermediate',question:'What does this JUnit 5 assumption do?\n\n@Test\nvoid onlyOnLinux() {\n    assumeTrue(System.getProperty("os.name").contains("Linux"));\n    assertTrue(true);\n}',options:['Always passes','Always fails','Is skipped on non-Linux systems','Throws an exception'],answer:2,explanation:'assumeTrue aborts the test (marks as skipped/aborted) if the condition is false. On non-Linux systems the test is not run.'},
{id:9617,topic:'code-challenges',difficulty:'intermediate',question:'What is the TestNG execution order here?\n\n@BeforeSuite\npublic void beforeSuite() { System.out.println("S"); }\n\n@BeforeTest\npublic void beforeTest() { System.out.println("T"); }\n\n@BeforeMethod\npublic void beforeMethod() { System.out.println("M"); }\n\n@Test\npublic void t() { System.out.println("X"); }',options:['M T S X','S T M X','X M T S','S M T X'],answer:1,explanation:'TestNG lifecycle order: @BeforeSuite → @BeforeTest → @BeforeMethod → @Test. Output: S, T, M, X.'},
{id:9618,topic:'code-challenges',difficulty:'intermediate',question:'What does this JUnit 5 test output?\n\n@RepeatedTest(3)\nvoid repeatedTest(RepetitionInfo info) {\n    System.out.println("Run " + info.getCurrentRepetition());\n}',options:['Run 1\nRun 2\nRun 3','Run 0\nRun 1\nRun 2','Run 1 only','Error'],answer:0,explanation:'@RepeatedTest(3) runs the test 3 times. getCurrentRepetition() returns 1, 2, 3 respectively.'},
{id:9619,topic:'code-challenges',difficulty:'intermediate',question:'What is the result of this TestNG dependsOnMethods?\n\n@Test\npublic void login() { System.out.println("login"); }\n\n@Test(dependsOnMethods="login")\npublic void dashboard() { System.out.println("dashboard"); }',options:['dashboard runs first','login runs, then dashboard','dashboard is skipped if login fails','Both options B and C are correct'],answer:3,explanation:'dependsOnMethods ensures login runs first. If login passes, dashboard runs next. If login fails, dashboard is skipped.'},
{id:9620,topic:'code-challenges',difficulty:'intermediate',question:'What does this JUnit 5 @Tag test do?\n\n@Tag("fast")\n@Test\nvoid quickTest() {\n    assertEquals(1, 1);\n}',options:['Runs only when fast tag is selected','Always runs','Fails if tag is not registered','Skips'],answer:0,explanation:'@Tag labels the test. When running with tag filter "fast", only tagged tests execute. Without a filter, all tests run normally.'},
{id:9621,topic:'code-challenges',difficulty:'intermediate',question:'What does this TestNG soft assertion collect?\n\nSoftAssert sa = new SoftAssert();\nsa.assertEquals("a", "b", "msg1");\nsa.assertTrue(false, "msg2");\nsa.assertEquals(1, 1, "msg3");\nsa.assertAll();',options:['Passes because last assertion passes','Fails with msg1 and msg2','Fails with all three messages','Throws on first failure'],answer:1,explanation:'SoftAssert collects all failures. msg1 and msg2 fail (a≠b, false), msg3 passes. assertAll() reports msg1 and msg2 as failures.'},
{id:9622,topic:'code-challenges',difficulty:'intermediate',question:'What is the result of this JUnit 5 @TestMethodOrder?\n\n@TestMethodOrder(MethodOrderer.OrderAnnotation.class)\nclass OrderedTest {\n    @Test @Order(2) void second() { System.out.println("B"); }\n    @Test @Order(1) void first() { System.out.println("A"); }\n}',options:['B then A','A then B','Random','Error'],answer:1,explanation:'@Order(1) runs before @Order(2). Despite declaration order, execution order is A then B.'},
{id:9623,topic:'code-challenges',difficulty:'intermediate',question:'What does this TestNG @Listeners annotation wire up?\n\n@Listeners(MyListener.class)\npublic class MyTest {\n    @Test\n    public void t() { Assert.fail(); }\n}\nclass MyListener implements ITestListener {\n    public void onTestFailure(ITestResult r) {\n        System.out.println("FAILED: " + r.getName());\n    }\n}',options:['Nothing extra','Prints "FAILED: t" when test fails','Prevents the test from failing','Skips the test'],answer:1,explanation:'ITestListener.onTestFailure is called when a test fails. The test t() fails via Assert.fail(), triggering the listener which prints "FAILED: t".'},
{id:9624,topic:'code-challenges',difficulty:'intermediate',question:'What does this JUnit 5 test output?\n\n@ParameterizedTest\n@CsvSource({"1,1", "2,4", "3,9"})\nvoid squareTest(int n, int expected) {\n    assertEquals(expected, n * n);\n}',options:['Fails all three','Passes all three','Passes 1,4 fails 9','Error'],answer:1,explanation:'1*1=1, 2*2=4, 3*3=9. All three assertEquals calls match their expected values. All pass.'},
{id:9625,topic:'code-challenges',difficulty:'intermediate',question:'What is the TestNG test dependency chain result if B fails?\n\n@Test void A() {}\n@Test(dependsOnMethods="A") void B() { Assert.fail(); }\n@Test(dependsOnMethods="B") void C() {}',options:['A passes, B fails, C passes','A passes, B fails, C is skipped','All three fail','A skips, B fails, C skips'],answer:1,explanation:'A passes, B fails (Assert.fail). C depends on B — since B failed, C is skipped by TestNG.'},
{id:9626,topic:'code-challenges',difficulty:'advanced',question:'What does this JUnit 5 extension do?\n\npublic class TimingExtension implements BeforeEachCallback, AfterEachCallback {\n    long start;\n    public void beforeEach(ExtensionContext ctx) { start = System.nanoTime(); }\n    public void afterEach(ExtensionContext ctx) {\n        long elapsed = System.nanoTime() - start;\n        System.out.println(ctx.getDisplayName() + " took " + elapsed + "ns");\n    }\n}\n@ExtendWith(TimingExtension.class)\nclass MyTest {\n    @Test void fast() {}\n}',options:['Nothing','Prints timing for each test','Fails tests that are too slow','Skips all tests'],answer:1,explanation:'TimingExtension records start time before each test and prints elapsed nanoseconds after. Each test gets timed and logged.'},
{id:9627,topic:'code-challenges',difficulty:'advanced',question:'What is the output of this TestNG parallel execution config?\n\n@Test(threadPoolSize=2, invocationCount=4)\npublic void parallelTest() {\n    System.out.println(Thread.currentThread().getName());\n}',options:['Runs once on one thread','Runs 4 times on up to 2 threads','Runs 2 times on 4 threads','Error'],answer:1,explanation:'invocationCount=4 runs the test 4 times total. threadPoolSize=2 uses a pool of 2 threads, so the 4 invocations run concurrently across 2 threads.'},
{id:9628,topic:'code-challenges',difficulty:'advanced',question:'What does this JUnit 5 dynamic test generate?\n\n@TestFactory\nStream<DynamicTest> dynamicTests() {\n    return IntStream.range(1, 4)\n        .mapToObj(n -> dynamicTest(\n            "test " + n,\n            () -> assertTrue(n > 0)\n        ));\n}',options:['One test','Three dynamic tests: test 1, test 2, test 3','An error','Nothing'],answer:1,explanation:'@TestFactory generates tests at runtime. IntStream.range(1,4) produces 1,2,3 — creating 3 dynamic tests, each asserting n>0 (all pass).'},
{id:9629,topic:'code-challenges',difficulty:'advanced',question:'What does this TestNG IRetryAnalyzer do?\n\npublic class Retry implements IRetryAnalyzer {\n    int count = 0;\n    public boolean retry(ITestResult result) {\n        return count++ < 2;\n    }\n}\n@Test(retryAnalyzer=Retry.class)\npublic void flakyTest() {\n    Assert.fail();\n}',options:['Fails immediately','Runs up to 3 times (original + 2 retries) then fails','Passes after retry','Skips'],answer:1,explanation:'IRetryAnalyzer.retry returns true to retry. count<2 allows 2 retries. Total runs: 1 original + 2 retries = 3. Since Assert.fail() always fails, it ultimately fails after 3 attempts.'},
{id:9630,topic:'code-challenges',difficulty:'advanced',question:'What does this JUnit 5 Mockito integration test verify?\n\n@ExtendWith(MockitoExtension.class)\nclass ServiceTest {\n    @Mock UserRepository repo;\n    @InjectMocks UserService service;\n\n    @Test\n    void findUser() {\n        when(repo.findById(1L)).thenReturn(Optional.of(new User("Alice")));\n        String name = service.getUserName(1L);\n        assertEquals("Alice", name);\n        verify(repo).findById(1L);\n    }\n}',options:['Fails because repo is a mock','Passes if service.getUserName calls repo.findById(1L) and returns "Alice"','verify always fails','Skips because of @Mock'],answer:1,explanation:'Mockito stubs repo.findById(1L) to return a User("Alice"). If UserService.getUserName delegates to repo.findById, it returns "Alice". assertEquals and verify both pass.'},
{id:9631,topic:'code-challenges',difficulty:'advanced',question:'What is the result of this TestNG @DataProvider with parallel execution?\n\n@DataProvider(name="d", parallel=true)\npublic Object[][] data() {\n    return new Object[][]{{1},{2},{3},{4}};\n}\n@Test(dataProvider="d")\npublic void test(int n) {\n    System.out.println(n + " on " + Thread.currentThread().getName());\n}',options:['Runs 4 iterations sequentially','Runs 4 iterations in parallel','Runs once','Error'],answer:1,explanation:'parallel=true on @DataProvider runs the test method concurrently for each data row. All 4 iterations run in parallel on different threads.'},
{id:9632,topic:'code-challenges',difficulty:'advanced',question:'What does this JUnit 5 @TempDir test ensure?\n\n@Test\nvoid writesFile(@TempDir Path tempDir) throws Exception {\n    Path file = tempDir.resolve("test.txt");\n    Files.writeString(file, "hello");\n    assertEquals("hello", Files.readString(file));\n}',options:['Fails because TempDir is read-only','Passes and cleans up the temp directory after the test','Fails because file system is not mocked','Throws IOException'],answer:1,explanation:'@TempDir provides a fresh temporary directory per test. Files can be written and read. JUnit 5 deletes the directory automatically after the test completes.'},
{id:9633,topic:'code-challenges',difficulty:'advanced',question:'What does this TestNG @Test(timeOut) do?\n\n@Test(timeOut=100)\npublic void slowTest() throws InterruptedException {\n    Thread.sleep(500);\n    System.out.println("done");\n}',options:['Runs and prints "done"','Fails with timeout after 100ms','Passes because sleep is allowed','Skips'],answer:1,explanation:'timeOut=100ms means if the test does not complete within 100ms it fails. Thread.sleep(500) sleeps for 500ms, exceeding the limit. The test fails with a timeout error.'},
{id:9634,topic:'code-challenges',difficulty:'advanced',question:'What does this JUnit 5 test lifecycle with @TestInstance show?\n\n@TestInstance(TestInstance.Lifecycle.PER_CLASS)\nclass SharedStateTest {\n    int count = 0;\n\n    @Test void t1() { count++; System.out.println(count); }\n    @Test void t2() { count++; System.out.println(count); }\n}',options:['Prints 1 twice (new instance per test)','Prints 1 then 2 (shared instance)','Error','Prints 0 twice'],answer:1,explanation:'PER_CLASS creates one instance for all tests. count is shared. t1 increments to 1 (prints 1), t2 increments to 2 (prints 2).'},
{id:9635,topic:'code-challenges',difficulty:'advanced',question:'What is the output of this TestNG @BeforeClass?\n\npublic class MyTest {\n    @BeforeClass\n    public static void setup() { System.out.println("setup"); }\n\n    @Test public void t1() { System.out.println("t1"); }\n    @Test public void t2() { System.out.println("t2"); }\n\n    @AfterClass\n    public static void teardown() { System.out.println("teardown"); }\n}',options:['setup t1 t2 teardown','t1 setup t2 teardown','setup teardown t1 t2','t1 t2 setup teardown'],answer:0,explanation:'@BeforeClass runs once before all tests in the class, @AfterClass once after. Output: setup, t1, t2, teardown.'},
{id:9636,topic:'code-challenges',difficulty:'advanced',question:'What does this JUnit 5 @ArgumentsSource test do?\n\nclass StringProvider implements ArgumentsProvider {\n    public Stream<? extends Arguments> provideArguments(ExtensionContext ctx) {\n        return Stream.of(Arguments.of("a",1), Arguments.of("bb",2));\n    }\n}\n@ParameterizedTest\n@ArgumentsSource(StringProvider.class)\nvoid lengthTest(String s, int len) {\n    assertEquals(len, s.length());\n}',options:['Runs once','Runs twice, testing string lengths','Fails both','Error'],answer:1,explanation:'ArgumentsSource provides 2 argument sets. Test runs with ("a",1): "a".length()==1 ✓. Then ("bb",2): "bb".length()==2 ✓. Both pass.'},
{id:9637,topic:'code-challenges',difficulty:'advanced',question:'What does this TestNG @Test(expectedExceptions) do?\n\n@Test(expectedExceptions=ArithmeticException.class)\npublic void divideByZero() {\n    int result = 10 / 0;\n}',options:['Fails because exception is thrown','Passes because expected exception occurs','Prints the exception','Skips'],answer:1,explanation:'expectedExceptions tells TestNG this test should throw ArithmeticException. 10/0 throws it. The test passes.'},
{id:9638,topic:'code-challenges',difficulty:'advanced',question:'What is the result of this JUnit 5 nested parameterized test?\n\n@Nested\nclass Multiplication {\n    @ParameterizedTest\n    @CsvSource({"2,3,6","4,5,20"})\n    void multiply(int a, int b, int expected) {\n        assertEquals(expected, a * b);\n    }\n}',options:['Runs once','Fails both','Runs twice, both pass','Error'],answer:2,explanation:'@CsvSource provides 2 rows. 2*3=6 ✓, 4*5=20 ✓. Both pass inside the @Nested class.'},
{id:9639,topic:'code-challenges',difficulty:'advanced',question:'What does this TestNG parallel class configuration do?\n\n<suite parallel="classes" thread-count="3">\n  <test>\n    <classes>\n      <class name="TestA"/>\n      <class name="TestB"/>\n      <class name="TestC"/>\n    </classes>\n  </test>\n</suite>',options:['Runs all classes sequentially','Runs each class in a separate thread (up to 3 at once)','Runs each method in parallel','Runs suites in parallel'],answer:1,explanation:'parallel="classes" with thread-count="3" runs each test class in its own thread concurrently. 3 classes run simultaneously.'},
{id:9640,topic:'code-challenges',difficulty:'advanced',question:'What does this JUnit 5 @RegisterExtension do?\n\nclass MyTest {\n    @RegisterExtension\n    static LoggingExtension log = new LoggingExtension();\n\n    @Test\n    void example() {\n        log.log("running test");\n        assertTrue(true);\n    }\n}',options:['Fails because extension is not registered','Registers the extension programmatically and it applies to all tests','Skips the test','Extension has no effect'],answer:1,explanation:'@RegisterExtension registers extensions programmatically (vs. declaratively with @ExtendWith). Static field = class-level scope. log.log() works within the test.'},
{id:9641,topic:'code-challenges',difficulty:'advanced',question:'What is the output of this TestNG ITestContext usage?\n\n@Test\npublic void printSuiteName(ITestContext ctx) {\n    System.out.println(ctx.getSuite().getName());\n}',options:['null','The suite name from testng.xml','Error','ITestContext is not injectable'],answer:1,explanation:'TestNG injects ITestContext as a method parameter. ctx.getSuite().getName() returns the suite name defined in testng.xml.'},
{id:9642,topic:'code-challenges',difficulty:'advanced',question:'What does this JUnit 5 @Timeout annotation do?\n\n@Test\n@Timeout(value=2, unit=TimeUnit.SECONDS)\nvoid mustFinishQuickly() throws InterruptedException {\n    Thread.sleep(3000);\n}',options:['Passes after 3 seconds','Fails with timeout after 2 seconds','Skips','Prints a warning'],answer:1,explanation:'@Timeout(2, SECONDS) fails the test if it does not complete within 2 seconds. Thread.sleep(3000) sleeps 3 seconds, exceeding the limit.'},
{id:9643,topic:'code-challenges',difficulty:'advanced',question:'What is the TestNG result when alwaysRun=true is used?\n\n@Test\npublic void login() { Assert.fail(); }\n\n@Test(dependsOnMethods="login", alwaysRun=true)\npublic void cleanup() { System.out.println("cleanup"); }',options:['cleanup is skipped','cleanup runs even though login failed','Both fail','Error'],answer:1,explanation:'alwaysRun=true bypasses the dependency failure check. Even though login failed, cleanup still runs and prints "cleanup".'},
{id:9644,topic:'code-challenges',difficulty:'advanced',question:'What does this JUnit 5 condition annotation do?\n\n@Test\n@EnabledOnOs(OS.WINDOWS)\nvoid windowsOnly() {\n    assertTrue(System.getProperty("os.name").contains("Windows"));\n}',options:['Runs on all OS','Runs only on Windows, skipped elsewhere','Fails on non-Windows','Error'],answer:1,explanation:'@EnabledOnOs(OS.WINDOWS) makes the test run only on Windows. On other operating systems, the test is skipped (aborted).'},
{id:9645,topic:'code-challenges',difficulty:'advanced',question:'What is the result of this TestNG @Test(invocationCount)?\n\n@Test(invocationCount=5, successPercentage=80)\npublic void flaky() {\n    Random r = new Random();\n    Assert.assertTrue(r.nextInt(10) < 9);\n}',options:['Runs 5 times; passes if 4 or more succeed','Runs once','Fails if any invocation fails','Error'],answer:0,explanation:'invocationCount=5 runs 5 times. successPercentage=80 means at least 80% (4 of 5) must pass. If ≥4 succeed, the test overall passes.'},
{id:9646,topic:'code-challenges',difficulty:'advanced',question:'What does this JUnit 5 @BeforeAll test do?\n\n@TestInstance(TestInstance.Lifecycle.PER_CLASS)\nclass SetupOnceTest {\n    Database db;\n\n    @BeforeAll\n    void connectDb() {\n        db = new Database();\n        db.connect();\n        System.out.println("connected");\n    }\n\n    @Test void t1() { assertNotNull(db); }\n    @Test void t2() { assertTrue(db.isConnected()); }\n}',options:['db is null in tests','connectDb runs once; db shared across t1 and t2','connectDb runs before each test','Error'],answer:1,explanation:'@BeforeAll with PER_CLASS runs once for the class. db is initialized once and shared. Both t1 (db not null) and t2 (db connected) use the same instance.'},
{id:9647,topic:'code-challenges',difficulty:'advanced',question:'What is the output of this TestNG @Test(enabled=false)?\n\n@Test(enabled=false)\npublic void skippedTest() {\n    System.out.println("this should not print");\n    Assert.fail();\n}',options:['Prints the message and fails','Is skipped entirely','Fails silently','Throws an exception'],answer:1,explanation:'enabled=false disables the test. It is not executed at all. The test is reported as skipped/ignored.'},
{id:9648,topic:'code-challenges',difficulty:'advanced',question:'What does this JUnit 5 conditional execution do?\n\n@Test\n@EnabledIfSystemProperty(named="env", matches="ci")\nvoid ciOnly() {\n    System.out.println("running in CI");\n}',options:['Always runs','Runs only when -Denv=ci is set','Fails if property is missing','Error'],answer:1,explanation:'@EnabledIfSystemProperty runs the test only when the system property "env" matches "ci". Otherwise the test is skipped.'},
{id:9649,topic:'code-challenges',difficulty:'advanced',question:'What is the TestNG parallel method execution result?\n\n<suite parallel="methods" thread-count="4">\n  <test>\n    <classes><class name="TestClass"/></classes>\n  </test>\n</suite>\n// TestClass has 10 @Test methods',options:['10 methods run sequentially','Up to 4 methods run in parallel at a time','All 10 run simultaneously','Error'],answer:1,explanation:'parallel="methods" runs test methods concurrently. thread-count="4" limits concurrency to 4. 10 methods are distributed across 4 threads.'},
{id:9650,topic:'code-challenges',difficulty:'advanced',question:'What does this JUnit 5 @TestMethodOrder(Random) do?\n\n@TestMethodOrder(MethodOrderer.Random.class)\nclass RandomOrderTest {\n    @Test void a() {}\n    @Test void b() {}\n    @Test void c() {}\n}',options:['Runs in alphabetical order','Runs in a random order each time','Always runs a,b,c','Error'],answer:1,explanation:'MethodOrderer.Random randomizes the execution order of test methods on every run. This helps detect order-dependent test bugs.'},
]);
