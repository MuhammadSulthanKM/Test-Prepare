// Java Fundamentals — Part 2 (q911–1020)
window.QUIZ_BANK['java'] = (window.QUIZ_BANK['java']||[]).concat([
{id:911,topic:'java',difficulty:'beginner',question:'What is the "break" statement used for in Java loops?',options:['Pauses the loop temporarily','Immediately exits the enclosing loop or switch statement','Skips the current iteration and continues','Throws an exception from the loop'],answer:1,explanation:'break exits the innermost loop or switch immediately. Labelled break (break label;) exits an outer loop by label name.'},
{id:912,topic:'java',difficulty:'beginner',question:'What does the "continue" statement do in a Java loop?',options:['Terminates the loop','Exits the method','Skips the remainder of the current iteration and proceeds to the next iteration check','Restarts the loop from the beginning'],answer:2,explanation:'continue skips the rest of the current loop body and jumps to the next loop condition evaluation or the increment step in a for loop. Labelled continue works with outer loops.'},
{id:913,topic:'java',difficulty:'beginner',question:'What is a "while" loop in Java?',options:['A loop that always runs exactly once','A loop that evaluates a boolean condition before each iteration; continues as long as the condition is true','A loop that counts down to zero','A loop that runs for each element in a collection'],answer:1,explanation:'while (condition) { body } evaluates the condition first; if false initially, the body never executes. Use for loops when the iteration count is known, while loops for condition-based iteration.'},
{id:914,topic:'java',difficulty:'beginner',question:'What is a "do-while" loop and how does it differ from "while"?',options:['It runs twice as fast','The body executes at least once before the condition is checked — even if the condition is initially false','It always runs infinitely','It is identical to a while loop'],answer:1,explanation:'do { body } while (condition): body executes first, then condition is evaluated. Useful when the loop body must execute at least once (e.g., prompting for user input until valid).'},
{id:915,topic:'java',difficulty:'beginner',question:'What is a "switch" statement in Java?',options:['A method that switches between databases','A multi-way branch statement that evaluates an expression and executes the matching case block; falls through to subsequent cases unless break is used','A toggle for enabling/disabling features','An if-else replacement for boolean conditions'],answer:1,explanation:'switch (expression) { case val: ...; break; default: ... } evaluates the expression and jumps to the matching case. Without break, execution falls through to subsequent cases. Java 14+ switch expressions eliminate fall-through.'},
{id:916,topic:'java',difficulty:'beginner',question:'What is the difference between "==" and "!=" operators?',options:['== assigns a value; != declares a variable','== tests equality (returns true if both operands are equal); != tests inequality (returns true if operands are not equal)','!= is used for objects; == for primitives','They perform arithmetic operations'],answer:1,explanation:'== and != are relational/equality operators: for primitives, they compare values. For objects, they compare reference identity (same memory address), not content — use .equals() for content comparison.'},
{id:917,topic:'java',difficulty:'beginner',question:'What is "casting" in Java?',options:['Throwing objects','Converting a value of one type to another: widening (smaller → larger, automatic), narrowing (larger → smaller, explicit required, may lose data), or object casting (subtype ↔ supertype)','Selecting elements from a collection','Copying a reference'],answer:1,explanation:'Widening cast (int → long, int → double): implicit, safe. Narrowing cast (double → int): explicit: int i = (int) 3.7; // truncates to 3. Object downcast: Circle c = (Circle) shape; — may throw ClassCastException if incorrect.'},
{id:918,topic:'java',difficulty:'beginner',question:'What is the Java "String pool" (string intern pool)?',options:['A database pool for storing strings','A JVM runtime constant pool where String literals are stored and shared; identical string literals refer to the same object, saving memory — created by literals "abc" or String.intern()','A collection class for unique strings','A memory area where all String objects are stored'],answer:1,explanation:'String literals go into the string pool: "hello" == "hello" is true because both reference the same pool object. new String("hello") creates a new heap object not in the pool. String.intern() adds a runtime string to the pool.'},
{id:919,topic:'java',difficulty:'beginner',question:'What is the String "charAt()" method?',options:['Checks if a character appears in the string','Returns the char at the specified zero-based index in the string: "Hello".charAt(1) returns \'e\'','Converts the string to a character array','Returns the character code of the first character'],answer:1,explanation:'charAt(index) returns the char at position index (0-based). Throws StringIndexOutOfBoundsException if index < 0 or >= length().'},
{id:920,topic:'java',difficulty:'beginner',question:'What does String.split() return?',options:['A single String with separators removed','An array of String substrings split by the specified regex delimiter','A List<String> of split parts','A Stream<String> of parts'],answer:1,explanation:'split(regex) returns String[] containing the parts between matches of the regex delimiter: "a,b,c".split(",") → ["a","b","c"]. The limit parameter controls maximum split count.'},
{id:921,topic:'java',difficulty:'intermediate',question:'What is a "functional interface" Consumer in Java?',options:['A consumer thread that processes messages','java.util.function.Consumer<T> has one abstract method accept(T t) → void, used for side-effect operations: list.forEach(consumer), where the consumer processes each element without returning a result','An interface for consuming streams','A class for receiving network data'],answer:1,explanation:'Consumer<T>: accept(T) performs a side effect (print, save, send). forEach(System.out::println) passes a Consumer<String>. Consumers compose with andThen(): consumer1.andThen(consumer2).'},
{id:922,topic:'java',difficulty:'intermediate',question:'What is "Function" functional interface in Java?',options:['A mathematical function representation','java.util.function.Function<T,R> has apply(T t) → R, transforming an input to an output: stream.map(function). Composes with andThen() and compose()','A stored procedure equivalent in Java','A method reference wrapper'],answer:1,explanation:'Function<T,R>: apply(T) → R. stream.map(String::toUpperCase) passes a Function<String,String>. Function.identity() returns its input. compose(f) applies f before, andThen(f) applies f after.'},
{id:923,topic:'java',difficulty:'intermediate',question:'What is the "Supplier" functional interface in Java?',options:['A class that provides resources to the application','java.util.function.Supplier<T> has get() → T, providing a value without any input argument — used for lazy evaluation, default values, and factory methods','A database connection supplier','A thread that supplies tasks to a pool'],answer:1,explanation:'Supplier<T>: get() → T. Used in Optional.orElseGet(supplier), CompletableFuture.supplyAsync(supplier), and anywhere a value-producing lambda is needed without an argument.'},
{id:924,topic:'java',difficulty:'intermediate',question:'What is Java "Stream.collect()" and what does it do?',options:['Collects garbage in the stream','A terminal operation that processes stream elements using a Collector to produce a result (List, Set, Map, String, etc.): stream.collect(Collectors.toList()), stream.collect(Collectors.joining(","))','Collects all elements into an array','A method for filtering elements'],answer:1,explanation:'collect() is the primary terminal operation for materialising stream results: Collectors.toList(), toSet(), toUnmodifiableList(), toMap(), groupingBy(), partitioningBy(), joining() are the most common collectors.'},
{id:925,topic:'java',difficulty:'intermediate',question:'What is "Stream.reduce()" used for?',options:['Reducing the stream to a smaller stream','A terminal operation that combines stream elements into a single value using an associative accumulation function: stream.reduce(0, Integer::sum) sums all elements','Removing elements from the stream','Reducing memory usage of the stream'],answer:1,explanation:'reduce(identity, accumulator) folds elements: sum = stream.reduce(0, (a, b) -> a + b). Without identity: Optional<T> reduce(BinaryOperator<T>). For parallel streams, the function must be associative.'},
{id:926,topic:'java',difficulty:'intermediate',question:'What is "Stream.peek()" used for?',options:['Checking if the stream is empty','An intermediate operation that takes a Consumer and applies it to each element (for debugging/logging) without modifying the stream elements, passing them through unchanged','Checking the first element of the stream','Previewing stream elements without processing'],answer:1,explanation:'peek(consumer) is an intermediate debug operation: stream.filter(x -> x>5).peek(System.out::println).collect(toList()) — logs elements after filter. Since intermediate, it only executes if a terminal operation consumes the stream.'},
{id:927,topic:'java',difficulty:'intermediate',question:'What is the difference between "Stream.findFirst()" and "Stream.findAny()"?',options:['They are identical','findFirst() returns an Optional with the first element of the stream in encounter order; findAny() may return any element (useful for parallel streams where encounter order doesn\'t matter, potentially faster)','findAny() always returns the last element','findFirst() is for sequential; findAny() is for ordered streams only'],answer:1,explanation:'findFirst(): deterministic (encounter order). findAny(): non-deterministic in parallel streams (may return first processed element), which can be faster in parallel. For sequential streams, both behave similarly.'},
{id:928,topic:'java',difficulty:'intermediate',question:'What is "Stream.distinct()" and "Stream.limit()"?',options:['distinct() removes null; limit() removes excess elements','distinct() returns a stream of unique elements (using equals/hashCode to detect duplicates); limit(n) truncates the stream to at most n elements — both are intermediate operations','distinct() sorts elements; limit() adds elements','They are terminal operations'],answer:1,explanation:'distinct(): removes duplicate elements (like SQL DISTINCT). limit(n): takes at most n elements (short-circuit, lazy — useful with infinite streams). Both return intermediate streams that are lazily evaluated.'},
{id:929,topic:'java',difficulty:'intermediate',question:'What does "parallelStream()" do in Java?',options:['Creates a new thread for each element','Returns a parallel Stream that uses the ForkJoinPool.commonPool() to process elements in parallel across multiple CPU cores — beneficial for CPU-intensive operations on large datasets, not for I/O-bound or synchronised operations','Creates an asynchronous stream','Splits the stream into smaller streams'],answer:1,explanation:'parallelStream() uses work-stealing ForkJoinPool for parallel processing. Beneficial for CPU-intensive, independent, stateless operations on large collections. Avoid for small collections (overhead > gain), I/O operations, or when shared state is involved.'},
{id:930,topic:'java',difficulty:'intermediate',question:'What is an "inner class" vs "static nested class" in Java?',options:['They are identical','Inner class (non-static nested class): has an implicit reference to the enclosing instance, can access instance members. Static nested class: no reference to enclosing instance, can only access enclosing class\'s static members — preferred to avoid holding an enclosing instance reference unintentionally','Inner classes are faster','Static nested classes cannot be instantiated'],answer:1,explanation:'Inner class: OuterClass.this.field accessible; instantiated as: outer.new Inner(). Static nested: no enclosing instance needed; instantiated as: new Outer.StaticNested(). Static nested is preferred when the enclosing instance is not needed (avoids memory leaks).'},
{id:931,topic:'java',difficulty:'intermediate',question:'What is an "anonymous class" in Java?',options:['A class without a name that cannot be used','A nameless class defined and instantiated in a single expression, typically to implement an interface or extend a class inline — superseded by lambda expressions for functional interfaces in Java 8+','A class that extends Object only','A private class inside a method'],answer:1,explanation:'Anonymous classes: Runnable r = new Runnable() { public void run() {...} }; — one-time use, no name, can capture final local variables. Largely replaced by lambdas for functional interface implementations in Java 8+.'},
{id:932,topic:'java',difficulty:'intermediate',question:'What is "Java Persistence API" (JPA) used for?',options:['Persisting Java applications to disk','A Java specification for Object-Relational Mapping (ORM), defining annotations (@Entity, @Table, @Id, @Column, @OneToMany) and EntityManager API to map Java objects to relational database tables — implemented by Hibernate, EclipseLink, OpenJPA','A Java security persistence layer','A key-value persistence API'],answer:1,explanation:'JPA maps Java objects (entities) to DB tables declaratively with annotations. EntityManager provides CRUD operations; JPQL enables typed object queries. Hibernate is the most popular JPA provider.'},
{id:933,topic:'java',difficulty:'intermediate',question:'What is "JDBC" in Java?',options:['Java Database Configuration','Java Database Connectivity — the standard Java API for connecting to and querying relational databases: DriverManager.getConnection(), PreparedStatement, ResultSet — the foundation layer underlying JPA/Hibernate','Java Data Binding Configuration','A Java ORM framework'],answer:1,explanation:'JDBC: load driver → getConnection(url, user, pass) → createPreparedStatement(sql) → setParameters → executeQuery() → iterate ResultSet → close resources. Low-level but foundational; JPA/Spring Data builds on top.'},
{id:934,topic:'java',difficulty:'advanced',question:'What is "HotSpot JVM" and what are its compilation tiers?',options:['A JVM for hot (CPU-intensive) applications','Oracle\'s reference JVM implementation using tiered compilation: Tier 0 (interpreted), Tier 1 (C1 simple JIT — fast compile, basic optimisations), Tier 2-3 (C1 + profiling), Tier 4 (C2 server JIT — deep optimisation: inlining, vectorisation, escape analysis)','A JVM optimised for server deployments only','A JVM with garbage collection disabled'],answer:1,explanation:'HotSpot tiered compilation profiles methods and promotes to higher tiers based on invocation count. C2 (server compiler) applies aggressive optimisations after sufficient profiling data, achieving near-native performance for hot paths.'},
{id:935,topic:'java',difficulty:'advanced',question:'What is the "G1 garbage collector" and when was it made the default JVM GC?',options:['A first-generation GC replaced by newer algorithms','G1 (Garbage First) became the default JVM GC in Java 9. It divides the heap into equal-size regions (not fixed generations), prioritises regions with most garbage first, targets configurable pause goals, and handles large objects (Humongous regions)','A GC algorithm for 32-bit JVMs only','A generational GC that replaced CMS'],answer:1,explanation:'G1 GC: region-based heap, incremental concurrent collection, configurable pause time targets (-XX:MaxGCPauseMillis). Replaced the Concurrent Mark Sweep (CMS) GC. ZGC and Shenandoah offer even lower pauses for large heaps.'},
{id:936,topic:'java',difficulty:'advanced',question:'What is "ZGC" (Z Garbage Collector) and its key advantage?',options:['A GC from the Z-series of JVM updates','A low-latency JVM GC (production-ready Java 15+) that performs concurrent marking, compaction, and relocation while the application runs, targeting sub-millisecond GC pauses regardless of heap size (tested to multi-terabyte heaps)','A garbage collector for small heaps only','A replacement for serial GC on single-core machines'],answer:1,explanation:'ZGC: concurrent, scalable, sub-millisecond pauses. It uses coloured pointers and load barriers to perform all work concurrently with the application — pause times don\'t scale with heap size. Ideal for latency-sensitive, large-heap applications.'},
{id:937,topic:'java',difficulty:'advanced',question:'What is "ahead-of-time (AOT) compilation" for Java and how does it compare to JIT?',options:['Compiling Java the day before deployment','Compiling Java bytecode to native machine code before runtime (at build time): faster startup and lower memory footprint at the cost of runtime profile-guided optimisations available only to JIT. Used in GraalVM native-image','A technique for compiling test code ahead of production code','AOT produces smaller binaries than JIT'],answer:1,explanation:'JIT: slow startup but excellent peak throughput (profile-guided optimisations). AOT (GraalVM native-image): instant startup, lower RSS — ideal for serverless, CLI tools. Trade-off: cannot use reflections/dynamic features without configuration.'},
{id:938,topic:'java',difficulty:'advanced',question:'What is "CMS" (Concurrent Mark Sweep) GC and why was it deprecated?',options:['A GC for concurrent database mark operations','A low-pause GC that performed concurrent marking and sweeping phases alongside the application. Deprecated in Java 9 and removed in Java 14 due to: fragmentation (no compaction), promotion failures, concurrent mode failures, and maintenance complexity compared to G1/ZGC','A GC algorithm still used as default in modern JVMs','A non-concurrent GC for single-core systems'],answer:1,explanation:'CMS had inherent weaknesses: no compaction (fragmentation causing promotion failures over time), concurrent mode failures (GC couldn\'t keep up with allocation), and complex tuning. G1 replaced it as a compacting concurrent GC.'},
{id:939,topic:'java',difficulty:'advanced',question:'What is a "phantom reference" in Java?',options:['A null reference to a phantom object','The weakest reference type: enqueued in a ReferenceQueue after an object\'s finalization but before its memory is reclaimed — used to take action after object cleanup without preventing GC, replacing finalizers for post-finalization resource cleanup','A reference that cannot be dereferenced','A reference to a method'],answer:1,explanation:'PhantomReference.get() always returns null; its only utility is notification via ReferenceQueue after GC has collected the object. Used for replacing finalizers with cleaner cleanup logic in resource management.'},
{id:940,topic:'java',difficulty:'advanced',question:'What is "soft reference" in Java and when does the GC collect a softly-reachable object?',options:['A reference to a small soft object','A reference that the GC collects only when memory is low (before throwing OutOfMemoryError) — ideal for memory-sensitive caches where objects should persist while memory is available but be reclaimed under pressure','A reference for objects that expire after a timeout','A deprecated alternative to WeakReference'],answer:1,explanation:'SoftReference: JVM guarantees all softly-reachable objects will be collected before an OutOfMemoryError. Use SoftReference for in-memory caches: cache entries survive normal operation but are reclaimed under memory pressure without explicit cache eviction.'},
{id:941,topic:'java',difficulty:'beginner',question:'What is a "two-dimensional array" in Java?',options:['An array of linked lists','An array of arrays: int[][] grid = new int[3][4] creates a 3-row, 4-column grid. Accessed as grid[row][col]; each row can have different lengths (jagged array)','A List containing another List','A matrix stored in a single 1D array'],answer:1,explanation:'Java 2D arrays are arrays of arrays: int[][] matrix = {{1,2},{3,4},{5,6}}. matrix[0][1] = 2. Rows can have different lengths (jagged). Arrays.deepToString() prints multi-dimensional arrays.'},
{id:942,topic:'java',difficulty:'beginner',question:'What does Arrays.asList() return?',options:['A modifiable ArrayList copy','A fixed-size List backed by the specified array — supports set() but not add() or remove() (throws UnsupportedOperationException); changes to the list reflect in the array','A Set of the array elements','An immutable List'],answer:1,explanation:'Arrays.asList(): fixed-size backed list, supports set() but not structural changes. For a fully modifiable copy: new ArrayList<>(Arrays.asList(arr)) or List.of() for immutable (Java 9+).'},
{id:943,topic:'java',difficulty:'beginner',question:'What does Collections.unmodifiableList() do?',options:['Sorts the list in reverse order','Returns a view of the specified list that throws UnsupportedOperationException for any modification operation (add, remove, set), while reads still work — for truly immutable lists, use List.of() (Java 9+)','Converts a list to a Set','Removes all null elements from the list'],answer:1,explanation:'Collections.unmodifiableList(list) wraps the list in an unmodifiable view; the underlying list can still be modified through the original reference. List.of() (Java 9+) creates a truly immutable list.'},
{id:944,topic:'java',difficulty:'beginner',question:'What is "Map.Entry" in Java?',options:['An entry point method in Map implementations','A nested interface within Map representing a key-value pair; Map.entrySet() returns Set<Map.Entry<K,V>>, enabling iteration over both keys and values simultaneously','A way to add entries to a Map','A type for Map keys only'],answer:1,explanation:'Map.Entry<K,V> exposes getKey() and getValue() methods. for (Map.Entry<String,Integer> entry : map.entrySet()) { entry.getKey(); entry.getValue(); } — the most efficient way to iterate both keys and values.'},
{id:945,topic:'java',difficulty:'beginner',question:'What is the purpose of "hashCode()" in Java?',options:['To encrypt object data','To compute an integer hash code for an object, used by hash-based collections (HashMap, HashSet) to determine the bucket placement for the object — must be consistent with equals(): equal objects must have the same hashCode','To compare object content','To measure object memory size'],answer:1,explanation:'hashCode() contract: (1) consistent multiple calls, (2) equal objects must have equal hash codes. HashMap uses hashCode() to find the bucket, then equals() to confirm the key. Violating the contract causes HashMap to lose entries.'},
{id:946,topic:'java',difficulty:'intermediate',question:'What is the contract between "equals()" and "hashCode()" in Java?',options:['No formal contract exists','If a.equals(b) is true, then a.hashCode() MUST equal b.hashCode(). The converse is not required (hash collisions are allowed). Violating this causes incorrect behaviour in HashMap, HashSet, and other hash-based data structures','equals() and hashCode() must return the same value','hashCode() must never be equal for different objects'],answer:1,explanation:'The equals/hashCode contract: equal objects must have equal hash codes. This ensures hash-based collections find objects correctly. Always override both together (IDE generators, Lombok @EqualsAndHashCode, Java records auto-generate both).'},
{id:947,topic:'java',difficulty:'intermediate',question:'What is "Java Annotation Processing" (APT)?',options:['Processing annotations at runtime via reflection','A compile-time mechanism where annotation processors (implementing javax.annotation.processing.Processor) inspect source code annotations and generate new source code, resources, or warnings — used by Lombok, MapStruct, Dagger, and others to reduce boilerplate','A runtime annotation scanning tool','An IDE plugin for annotation highlighting'],answer:1,explanation:'APT runs during compilation: Lombok generates getters/setters/builders; MapStruct generates mapping code; Dagger generates dependency injection factories — all at compile time with zero runtime overhead.'},
{id:948,topic:'java',difficulty:'intermediate',question:'What is "Lombok" and how does it reduce boilerplate in Java?',options:['A Java logging framework','A library using annotation processing to automatically generate boilerplate Java code: @Getter/@Setter, @ToString, @EqualsAndHashCode, @AllArgsConstructor, @Builder, @Data, @Slf4j — reducing class verbosity without runtime overhead','A Java code formatter','A dependency injection framework'],answer:1,explanation:'Lombok inserts generated code at compile time via APT: @Data generates getters, setters, equals, hashCode, toString. @Builder generates the builder pattern. @Slf4j injects a logger field. The bytecode is indistinguishable from hand-written code.'},
{id:949,topic:'java',difficulty:'intermediate',question:'What is "Java String.format()" and when should "String.formatted()" be preferred?',options:['format() is deprecated; formatted() is modern','String.format(pattern, args) creates a formatted string using printf-style format specifiers. String.formatted() (Java 15+) is a convenience instance method: "%s is %d".formatted("count", 42) — same result, slightly more readable for inline use','formatted() only works with text blocks','format() works on char arrays; formatted() works on strings'],answer:1,explanation:'Both produce the same result; formatted() is a method on String making it easier to inline: "%d items".formatted(count) instead of String.format("%d items", count). Neither replaces StringBuilder for performance-critical loops.'},
{id:950,topic:'java',difficulty:'intermediate',question:'What is "Java Record" canonical constructor?',options:['The only allowed constructor type in a record','The primary constructor with the same parameter list as the record components, automatically generated by the compiler with validation support: record Point(int x, int y) {} — can be customised (compact canonical constructor form or full explicit form) to add validation','A constructor called by the Java runtime','A no-arg constructor for records'],answer:1,explanation:'Records auto-generate: canonical constructor (assigns components to fields), accessors (x(), y()), equals, hashCode, toString. A compact canonical constructor lets you add validation without restating the parameters: record Point(int x, int y) { Point { if (x < 0) throw ...; } }'},
{id:951,topic:'java',difficulty:'advanced',question:'What is "Java virtual thread" vs "platform thread" performance characteristics?',options:['Virtual threads are always slower','Platform threads are expensive OS threads (stack ~1MB, ~1k limit practical); virtual threads are cheap JVM-managed threads (small, dynamic stack, millions possible) — same blocking model but scheduled cooperatively on platform threads, making blocking calls scale','Virtual threads require special APIs','Platform threads can be converted to virtual threads at runtime'],answer:1,explanation:'Virtual threads (Java 21): created in microseconds, ~1KB initial stack, millions concurrent. When a virtual thread blocks (I/O, sleep), it unmounts from the platform thread, which runs other virtual threads — achieving async throughput with synchronous code.'},
{id:952,topic:'java',difficulty:'advanced',question:'What is "Project Leyden" in the Java ecosystem?',options:['A Java web framework project','A JDK project focused on improving startup time and reducing footprint of Java applications through ahead-of-time optimisations, static images, and condensers — building on CDS (Class Data Sharing) and AOT compilation','A Java security project','A distributed systems framework for Java'],answer:1,explanation:'Project Leyden (ongoing) addresses Java startup time and footprint for cloud-native deployments through a spectrum of static images (full AOT to JVM+AOT combinations), complementing GraalVM native image.'},
{id:953,topic:'java',difficulty:'advanced',question:'What is "Class Data Sharing" (CDS) and "AppCDS" in Java?',options:['Sharing class source code between projects','CDS allows the JVM to share a pre-generated archive of JVM startup class data across multiple JVM processes, reducing startup time and memory footprint. AppCDS extends this to application classes — useful for containerised microservice deployments','A mechanism for sharing classpath configuration','A JVM feature for sharing objects between threads'],answer:1,explanation:'CDS/AppCDS: dump shared archive at build time (-Xshare:dump), then reuse memory-mapped class data at startup — reducing startup time by 30-60% and RSS for multiple JVM instances sharing the same archive (important for container scaling).'},
{id:954,topic:'java',difficulty:'advanced',question:'What is the "happens-before" relationship in Java Memory Model?',options:['A temporal ordering of developer tasks','A guarantee that an action A happens-before action B means: all actions performed by A and their effects are visible to B. Established by: program order, volatile writes/reads, synchronised unlock/lock, Thread.start(), Thread.join() — prevents compiler/CPU reordering','A scheduling constraint for threads','A SQL ordering concept'],answer:1,explanation:'Happens-before (JMM): if A happens-before B, A\'s writes are visible to B. synchronized ensures unlock HB lock; volatile write HB volatile read; Thread.start() HB first action of the started thread. Without HB, threads may see stale data.'},
{id:955,topic:'java',difficulty:'advanced',question:'What is "lock-free programming" in Java and what classes support it?',options:['Programming without any synchronization','Using atomic operations (CAS — Compare-And-Swap) provided by hardware to achieve thread safety without acquiring locks — avoiding blocking and potential deadlocks. Java classes: AtomicInteger, AtomicLong, AtomicReference, LongAdder, StampedLock','Programming on systems without lock screen','A pattern for avoiding memory locks in GC'],answer:1,explanation:'Lock-free concurrency uses CPU-level CAS instructions: AtomicInteger.compareAndSet(expected, update) succeeds only if current==expected, making atomic updates without locking. LongAdder (Java 8) uses striping for high-contention counters.'},
{id:956,topic:'java',difficulty:'advanced',question:'What is a "phaser" in Java concurrency?',options:['A phase manager for project schedules','java.util.concurrent.Phaser: a reusable, flexible synchronisation barrier that advances through multiple phases. Multiple parties register; arriveAndAwaitAdvance() waits until all registered parties arrive; then advances to next phase — more flexible than CyclicBarrier','A tool for monitoring thread phases','A type of countdown latch'],answer:1,explanation:'Phaser combines features of CountDownLatch (one-time), CyclicBarrier (reusable), and Semaphore (dynamic party count). Used for multi-phase algorithms where parties arrive asynchronously and synchronise at phase boundaries.'},
{id:957,topic:'java',difficulty:'advanced',question:'What is "java.util.concurrent.Flow" API (Java 9+)?',options:['A flow control mechanism for threads','Java\'s reactive streams specification: Publisher<T>, Subscriber<T>, Subscription, Processor<T,R> interfaces defining the reactive programming protocol for asynchronous, backpressure-aware stream processing — aligned with the Reactive Streams initiative','A logging flow control API','A network byte flow API'],answer:1,explanation:'Flow API is Java\'s standard reactive streams: Publisher emits items, Subscriber requests items via Subscription.request(n) (backpressure), onNext/onError/onComplete lifecycle. Project Reactor and RxJava implement this specification.'},
{id:958,topic:'java',difficulty:'advanced',question:'What is "instrumenting" Java code with "Java agents"?',options:['Adding comments to bytecode','Using the Java Agent API (java.lang.instrument) to transform class bytecode at load time via -javaagent:agent.jar: the agent\'s ClassFileTransformer intercepts class loading, enabling APM tools, profilers, test coverage agents (JaCoCo), and AOP weaving without source changes','Adding monitoring scripts to Java classes','A way to profile Java applications using IDE plugins'],answer:1,explanation:'Java agents intercept class loading (premain/agentmain) and can rewrite bytecode before the class is used. JaCoCo instruments coverage probes; New Relic/Dynatrace agents inject monitoring code; AspectJ weaves aspects at load time.'},
{id:959,topic:'java',difficulty:'advanced',question:'What is "Project Valhalla" in Java?',options:['A Java UI framework project','A JDK project introducing value types (primitive classes): objects with value semantics (no identity, no null, flat heap layout) that eliminate boxing overhead, enabling int-like performance for custom numeric types — the future of Java generics specialisation','A Java distributed computing project','A project for Java gaming applications'],answer:1,explanation:'Valhalla\'s value types are objects without identity (no ==, no null): they can be stored inline in arrays and fields (no reference indirection), eliminating boxing overhead for custom numeric/composite types like Complex, Range, Vector.'},
{id:960,topic:'java',difficulty:'advanced',question:'What is the "ForkJoin framework" and how does it achieve work-stealing?',options:['A framework for forking and joining JVM processes','A framework designed for parallelisable divide-and-conquer tasks: ForkJoinPool contains work-stealing deque threads — idle threads steal tasks from the back of busy threads\' deques, maintaining high CPU utilisation. Used by parallelStream() and CompletableFuture','A network protocol for distributed Java','A test framework for parallel test execution'],answer:1,explanation:'ForkJoinPool: tasks split (fork) into subtasks, processed independently, results joined. Work-stealing: each thread has a deque; idle threads steal from the tail of busy threads\' deques — self-balancing without explicit coordination.'}
]);
