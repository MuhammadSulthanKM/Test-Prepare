// SQL & Database Fundamentals — Part 4 (q721–840)
window.QUIZ_BANK['sql-db'] = (window.QUIZ_BANK['sql-db']||[]).concat([
{id:721,topic:'sql-db',difficulty:'beginner',question:'What is the purpose of a database index on a foreign key column?',options:['To enforce referential integrity','To speed up JOIN operations and DELETE/UPDATE operations on the parent table by allowing the database to quickly find child rows','To prevent NULL values in the FK column','To automatically update FK values when the parent PK changes'],answer:1,explanation:'FK columns should almost always be indexed; without an index, any DELETE or UPDATE on the parent table causes a full table scan of the child table to check for dependent rows.'},
{id:722,topic:'sql-db',difficulty:'beginner',question:'What is the result of: SELECT 5 + NULL?',options:['5','NULL','Error','0'],answer:1,explanation:'Any arithmetic operation involving NULL returns NULL in SQL: 5 + NULL = NULL, NULL * 0 = NULL. NULL propagates through expressions.'},
{id:723,topic:'sql-db',difficulty:'beginner',question:'What is the difference between TRUNCATE and DELETE FROM table_name?',options:['They produce identical results','TRUNCATE is DDL (usually faster, cannot be rolled back in most DBs, resets identity, no triggers); DELETE is DML (transactional, can be rolled back, fires triggers, slower for large tables)','DELETE removes the table structure; TRUNCATE removes only data','TRUNCATE supports WHERE conditions; DELETE does not'],answer:1,explanation:'TRUNCATE: DDL, removes all rows minimally logged (no individual row logging), resets auto-increment, does not fire row-level triggers, usually cannot be rolled back. DELETE: DML, fully logged per row, fires triggers, supports WHERE, is transactional.'},
{id:724,topic:'sql-db',difficulty:'beginner',question:'What is the SQL keyword for inserting multiple rows in a single INSERT statement?',options:['INSERT MULTIPLE','INSERT BULK','INSERT ALL (Oracle) / INSERT INTO table VALUES (...),(...),... (MySQL/PostgreSQL/SQL Server)','MULTI INSERT'],answer:2,explanation:'Multi-row INSERT syntax: INSERT INTO table (cols) VALUES (row1), (row2), ... is supported in MySQL, PostgreSQL, SQL Server. Oracle uses INSERT ALL or multi-row subqueries.'},
{id:725,topic:'sql-db',difficulty:'beginner',question:'What does a "unique constraint" allow that a "primary key" does not?',options:['Unique constraints allow duplicate values','Unique constraints allow NULL values (one or more, depending on the database), while primary keys do not permit NULL','Unique constraints create a clustered index; PK creates non-clustered','There is no difference'],answer:1,explanation:'UNIQUE allows NULLs (SQL Server allows only one NULL per unique index by default; PostgreSQL allows multiple NULLs). PRIMARY KEY enforces both UNIQUE and NOT NULL.'},
{id:726,topic:'sql-db',difficulty:'beginner',question:'What is the result of: SELECT UPPER(\'hello world\')?',options:['hello world','Hello World','HELLO WORLD','hELLO wORLD'],answer:2,explanation:'UPPER() converts all characters to uppercase: UPPER(\'hello world\') = \'HELLO WORLD\'.'},
{id:727,topic:'sql-db',difficulty:'beginner',question:'What does the SQL CASE statement return when no condition matches and there is no ELSE?',options:['An error','Zero','An empty string','NULL'],answer:3,explanation:'If no WHEN condition is true and no ELSE clause is specified, the CASE expression returns NULL.'},
{id:728,topic:'sql-db',difficulty:'beginner',question:'What is "REPLACE INTO" in MySQL?',options:['A synonym for UPDATE','Inserts a new row, or if a duplicate PRIMARY KEY or UNIQUE constraint violation would occur, deletes the existing row and inserts the new one','A synonym for INSERT IGNORE','A non-standard join syntax'],answer:1,explanation:'MySQL\'s REPLACE INTO: if no conflict, inserts normally; if PK/UNIQUE conflict, deletes the old row and inserts the new one (note: all columns must be in the INSERT, otherwise NULL). Different from UPSERT (UPDATE on conflict).'},
{id:729,topic:'sql-db',difficulty:'beginner',question:'What does ON DUPLICATE KEY UPDATE do in MySQL?',options:['Inserts a duplicate row','If an INSERT would violate a PRIMARY KEY or UNIQUE constraint, updates the existing row instead — a true UPSERT operation','Prevents any inserts with duplicate keys','Renames the duplicate key column'],answer:1,explanation:'MySQL\'s ON DUPLICATE KEY UPDATE is a true UPSERT: INSERT the row; if PK/UNIQUE conflict, UPDATE only the specified columns of the existing row — preserving unspecified column values.'},
{id:730,topic:'sql-db',difficulty:'beginner',question:'What is the PostgreSQL syntax for upsert?',options:['REPLACE INTO','INSERT OR UPDATE','INSERT ... ON CONFLICT DO UPDATE SET','MERGE INTO'],answer:2,explanation:'PostgreSQL UPSERT syntax: INSERT INTO table (...) VALUES (...) ON CONFLICT (key) DO UPDATE SET col = EXCLUDED.col; EXCLUDED references the proposed new row values.'},
{id:731,topic:'sql-db',difficulty:'intermediate',question:'What is "string_agg" (PostgreSQL) / "group_concat" (MySQL) used for?',options:['Sorting strings in a column','An aggregate function that concatenates non-NULL string values from multiple rows into a single delimited string — useful for creating comma-separated lists from groups','Splitting a string into rows','Aggregating string lengths'],answer:1,explanation:'STRING_AGG(col, \',\') (PostgreSQL) / GROUP_CONCAT(col SEPARATOR \',\') (MySQL) aggregates multiple row values into a single delimited string per group.'},
{id:732,topic:'sql-db',difficulty:'intermediate',question:'What is "CROSS APPLY" (SQL Server) and how does it differ from a subquery?',options:['A type of CROSS JOIN','A table-valued operator that applies a table-valued function (or query) to each row of the left table, returning rows only when the function returns results — like a correlated subquery in the FROM clause that can reference the outer table','A synonym for FULL OUTER JOIN','A parallel execution hint for SQL Server'],answer:1,explanation:'CROSS APPLY applies a TVF or inline query to each outer row; OUTER APPLY includes outer rows even when the right side returns no rows (like LEFT JOIN). More flexible than correlated subqueries because the right side can be a function.'},
{id:733,topic:'sql-db',difficulty:'intermediate',question:'What does FIRST_VALUE() and LAST_VALUE() window functions return?',options:['The first and last rows in the table','FIRST_VALUE(col) returns the first column value in the window frame ordered by ORDER BY; LAST_VALUE(col) returns the last column value within the current frame (default frame may not be the partition end — often needs ROWS BETWEEN UNBOUNDED PRECEDING AND UNBOUNDED FOLLOWING)','The minimum and maximum values','The first and last rows inserted'],answer:1,explanation:'FIRST_VALUE returns the first value in the ordered window frame. LAST_VALUE requires careful frame specification (default is CURRENT ROW to end of frame, often not partition end) to get the actual last value.'},
{id:734,topic:'sql-db',difficulty:'intermediate',question:'What is the purpose of "database partitioning" for testing?',options:['Partitioning test cases by database type','Using database partitioning in test environments to efficiently test queries that use partition pruning, verify partition-specific logic, and generate realistic test data distributions matching production partition patterns','Partitioning test databases for team isolation','Separating production and test data with partitions'],answer:1,explanation:'Testing partitioned tables requires: verifying partition pruning works (EXPLAIN shows correct partition access), testing data at partition boundaries (switchpoint values), and ensuring queries correctly leverage partitioned index structures.'},
{id:735,topic:'sql-db',difficulty:'intermediate',question:'What is "query decomposition" in SQL optimisation?',options:['Breaking up long stored procedures','Splitting a complex, monolithic query into simpler intermediate queries (using CTEs, temp tables, or view staging) that are each more efficiently optimised by the query optimizer and easier to debug','Decomposing a table into its normal form','A technique for parallel query execution'],answer:1,explanation:'Decomposing complex queries into CTEs or staging tables can help the optimizer: each subquery gets its own statistics-based plan, intermediate results are materialised for reuse, and the overall query is easier to understand and debug.'},
{id:736,topic:'sql-db',difficulty:'intermediate',question:'In SQL, what is "aggregate pushdown"?',options:['Pushing aggregates into application code','Moving aggregate computations (SUM, COUNT) closer to the data source in an execution plan — applying them before data is moved across network or joins, reducing data volume transferred','A technique for pushing GROUP BY after HAVING','Applying aggregates before WHERE filtering'],answer:1,explanation:'Aggregate pushdown is an optimizer technique that applies GROUP BY/aggregates at the earliest possible point in the plan, reducing intermediate result set sizes before expensive operations (network transfer, sort, join).'},
{id:737,topic:'sql-db',difficulty:'intermediate',question:'What is "READ COMMITTED SNAPSHOT ISOLATION" (RCSI) in SQL Server?',options:['READ COMMITTED with a snapshot of the database','A SQL Server implementation of MVCC for READ COMMITTED: readers see the last committed version of data (via a row version store in TempDB) rather than waiting for write locks — improving read concurrency without dirty reads','A combination of READ COMMITTED and SNAPSHOT isolation levels','A read-only snapshot taken at query start'],answer:1,explanation:'RCSI uses row version store (TempDB) to give READ COMMITTED queries a consistent view of committed data without acquiring shared locks, dramatically reducing blocking in OLTP systems.'},
{id:738,topic:'sql-db',difficulty:'intermediate',question:'What is "SNAPSHOT isolation" in SQL Server/PostgreSQL?',options:['A backup snapshot of the database','Readers see the committed state of the database at the start of their transaction, providing a consistent snapshot regardless of concurrent writes — preventing all read anomalies without acquiring read locks','A DDL snapshot of the schema','A backup point-in-time snapshot'],answer:1,explanation:'SNAPSHOT isolation gives each transaction a consistent database view as of its start time using row versioning, preventing dirty reads, non-repeatable reads, and phantom reads without reader-writer blocking.'},
{id:739,topic:'sql-db',difficulty:'intermediate',question:'What is a "partial index" (also called filtered index in SQL Server)?',options:['An index on a partial set of columns','An index built only on rows satisfying a specified WHERE condition, reducing index size and maintenance overhead while providing full performance for queries that include that filter','An index with some columns excluded','An incomplete index being built online'],answer:1,explanation:'Partial/filtered indexes: CREATE INDEX idx ON orders (status) WHERE status = \'PENDING\' — only indexes pending orders, making the index smaller and faster to update, while benefiting queries that filter on pending orders.'},
{id:740,topic:'sql-db',difficulty:'intermediate',question:'What is "hot standby" in database high availability?',options:['A standby server that is overheating','A secondary database replica that is continuously synchronised with the primary and can be promoted to primary immediately on failure — optionally serving read-only queries while in standby mode','A warm standby that must be restarted before serving queries','A hot backup taken while the database is online'],answer:1,explanation:'Hot standby (synchronous replica) is always current with the primary; immediate failover is possible with minimal data loss. "Warm standby" requires some startup time; "cold standby" requires restoration from backup.'},
{id:741,topic:'sql-db',difficulty:'advanced',question:'What is "logical backup" vs "physical backup" in databases?',options:['Logical backups are unreliable; physical are preferred','Logical backup (pg_dump, mysqldump): exports data as SQL statements or data files that are database-version portable but slower to restore. Physical backup (pg_basebackup, file system copy, RMAN): copies actual data files, faster to restore, but less portable across major versions','Physical backups are smaller than logical backups','Logical backups include indexes; physical do not'],answer:1,explanation:'Logical: SQL-level dump, portable across versions, can be selective, slow restore for large DBs. Physical: block-level file copy, fast restore, requires same major version, enables PITR when combined with WAL archiving.'},
{id:742,topic:'sql-db',difficulty:'advanced',question:'What is "VACUUM FULL" in PostgreSQL and when should it be used?',options:['A more thorough version of regular VACUUM that runs automatically','An exclusive-lock, table-rewriting operation that compacts the table to the minimum size by creating a new table file without dead space — should be used infrequently as it requires exclusive lock and additional disk space','A full database vacuum that reclaims all tablespace','A VACUUM that runs on all indexes'],answer:1,explanation:'VACUUM FULL reclaims maximum disk space (returns free space to OS) but requires an exclusive table lock, blocking all access. It should only be used when disk space is critical; regular VACUUM (or autovacuum) suffices otherwise.'},
{id:743,topic:'sql-db',difficulty:'advanced',question:'What is the "ACID vs BASE" trade-off in database design?',options:['ACID databases are always better than BASE','ACID (traditional RDBMS): strong consistency, low availability under partition. BASE (many NoSQL): Basically Available, Soft-state, Eventually consistent — trading immediate consistency for higher availability and partition tolerance per the CAP theorem','BASE databases are more secure than ACID','ACID is for OLTP; BASE is only for analytics'],answer:1,explanation:'ACID databases prioritise consistency (CP in CAP); BASE systems (AP in CAP) prioritise availability and partition tolerance, accepting eventual consistency — the right choice depends on business requirements.'},
{id:744,topic:'sql-db',difficulty:'advanced',question:'What is a "graph database" and when is it preferable to a relational database?',options:['A database that generates graphs','A database optimised for storing entities as nodes and relationships as edges, with native graph traversal algorithms — preferable when data is highly connected and query patterns follow relationship paths (social networks, fraud detection, recommendation engines)','A database for storing chart and graph visualisations','A distributed database with a graph topology'],answer:1,explanation:'Graph databases (Neo4j, Amazon Neptune) excel at variable-depth relationship traversal: "find friends of friends within 3 hops" is expensive in SQL (multiple self-joins) but native O(log n) in a graph database.'},
{id:745,topic:'sql-db',difficulty:'advanced',question:'What is a "document database" and when should it be used over an RDBMS?',options:['A database for storing PDF documents','A NoSQL database storing semi-structured JSON/BSON documents as the primary data unit — preferable when data has variable schemas, nested structures, and hierarchical relationships that would require many joins in a relational model (e.g., product catalogues, user profiles)','A database with document-style query language','A relational database with large text columns'],answer:1,explanation:'Document databases (MongoDB, Couchbase, DynamoDB) suit: hierarchical data models, schema flexibility (documents vary in structure), content-oriented applications, and scenarios where embedding related data into one document eliminates joins.'},
{id:746,topic:'sql-db',difficulty:'advanced',question:'What is "table statistics" used for by the query optimizer?',options:['Counting rows for billing purposes','Stored metadata about table characteristics — row counts, column value histograms, distinct value counts, NULL ratios — used by the cost-based optimizer to estimate row counts for query plan steps and choose optimal access paths','Statistical analysis built into SQL functions','Usage statistics for database monitoring'],answer:1,explanation:'Optimizer statistics drive cardinality estimates; stale statistics cause inaccurate row estimates, leading to poor plan choices (wrong join order, index vs scan decisions). Regular statistics refresh (ANALYZE, UPDATE STATISTICS) is critical.'},
{id:747,topic:'sql-db',difficulty:'advanced',question:'What is "invisible index" and why is it useful for testing?',options:['An index that cannot be seen by users','An index that is maintained by the database but is not visible to the query optimizer — allowing DBAs to test whether making an index visible would improve queries before committing to a permanent index change','An index without a name','A hidden system index'],answer:1,explanation:'Invisible indexes (Oracle, MySQL 8+) allow DBAs to test query behaviour without an index (make it invisible) or with an index (make it visible) without dropping/creating it — testing index impact without data modification.'},
{id:748,topic:'sql-db',difficulty:'advanced',question:'What is "query regression testing" in database development?',options:['Testing queries for SQL injection','Systematically verifying that SQL query performance has not degraded after schema changes, index changes, statistics updates, or database version upgrades — using baseline execution plans and timing benchmarks','Testing that queries produce correct results after code changes','Running regression tests against a database schema'],answer:1,explanation:'Query regression testing captures baseline plans and timings; after changes (new index, schema change, DB upgrade), compares new plans against baselines to detect plan regressions before they impact production.'},
{id:749,topic:'sql-db',difficulty:'advanced',question:'What is "database health monitoring" and what key metrics should be monitored?',options:['Checking if the database server is powered on','Continuously monitoring: query response times, connection pool utilisation, lock wait times, replication lag, buffer cache hit ratio, disk I/O latency, CPU usage, long-running queries, and error rates to detect and diagnose performance issues early','Monitoring database software version','Counting the number of active sessions'],answer:1,explanation:'Database health monitoring tracks: buffer cache hit rate (should be high), I/O latency, connection pool saturation, deadlock frequency, slow query rate, replication lag, and disk space utilisation to proactively identify issues.'},
{id:750,topic:'sql-db',difficulty:'advanced',question:'What is "SQL Server AlwaysOn Availability Groups" minimum required edition?',options:['Standard Edition','Express Edition','Enterprise Edition (for all features including multiple synchronous replicas; basic AGs available in Standard Edition with limitations)','Developer Edition'],answer:2,explanation:'Full AlwaysOn AG features (multiple readable replicas, automatic failover partners, multiple synchronous replicas) require Enterprise Edition. SQL Server 2016+ Standard Edition supports Basic Availability Groups (one database, no readable secondary).'},
{id:751,topic:'sql-db',difficulty:'beginner',question:'What is a "database table" in relational database terms?',options:['A visual chart in the database IDE','A structured collection of related data organised in rows (records) and columns (fields) following a defined schema, the fundamental storage unit in a relational database','A file containing database backups','A type of stored procedure'],answer:1,explanation:'A table is the fundamental relational database object: a set of rows, each with values for the defined columns. It represents one entity type (customers, orders, products).'},
{id:752,topic:'sql-db',difficulty:'beginner',question:'What does "SHOW TABLES" do in MySQL?',options:['Displays the structure of a specific table','Lists all table names in the currently selected database','Shows the data in all tables','Shows all databases on the server'],answer:1,explanation:'SHOW TABLES in MySQL lists all table names in the current database. Equivalent to SELECT table_name FROM information_schema.tables WHERE table_schema = DATABASE().'},
{id:753,topic:'sql-db',difficulty:'beginner',question:'What does "DESCRIBE table_name" or "DESC table_name" do?',options:['Shows the first 10 rows of the table','Displays the column definitions of a table: column names, data types, NULL-ability, key information, and defaults','Shows the table\'s indexes only','Generates a database diagram'],answer:1,explanation:'DESCRIBE (DESC) shows a table\'s schema: column name, type, NULL allowed, key type (PRI/UNI/MUL), default value, and extra attributes (AUTO_INCREMENT). Useful for quick schema inspection.'},
{id:754,topic:'sql-db',difficulty:'beginner',question:'What does "SHOW CREATE TABLE table_name" output in MySQL?',options:['The first row inserted into the table','The complete CREATE TABLE statement that would recreate the table, including all column definitions, indexes, and constraints','A visual representation of the table structure','The table\'s row count and average row size'],answer:1,explanation:'SHOW CREATE TABLE outputs the exact DDL needed to recreate the table including engine, charset, collation, all columns with types and constraints, and all index definitions.'},
{id:755,topic:'sql-db',difficulty:'beginner',question:'What is the "information_schema" in SQL databases?',options:['A user-created schema for metadata storage','A built-in system schema that exposes database metadata (tables, columns, indexes, constraints, privileges, etc.) through standard SQL-queryable views','A schema for storing system performance data','A schema for the database administrator only'],answer:1,explanation:'INFORMATION_SCHEMA (SQL:2003 standard) provides read-only views of database metadata: INFORMATION_SCHEMA.TABLES, COLUMNS, INDEXES, TABLE_CONSTRAINTS, etc. — queryable with standard SQL.'},
{id:756,topic:'sql-db',difficulty:'beginner',question:'What is "referential integrity" and how is it enforced?',options:['The accuracy of referenced documentation','The database constraint ensuring FK values reference existing PK values in the parent table; enforced by FOREIGN KEY constraints that prevent orphaned child records and deletion of referenced parent records','The consistency of column data types','The uniqueness of primary keys'],answer:1,explanation:'Referential integrity is enforced by FK constraints: the RDBMS rejects any INSERT/UPDATE that would create an FK value with no matching PK in the parent, and rejects DELETE of a parent that has dependent children (without CASCADE).'},
{id:757,topic:'sql-db',difficulty:'beginner',question:'In SQL, which aggregate function calculates the average value of a numeric column?',options:['MEAN()','AVERAGE()','AVG()','MEDIAN()'],answer:2,explanation:'AVG(column) calculates the arithmetic mean of non-NULL values. Most databases do not have built-in MEDIAN() — it requires window functions or specific syntax.'},
{id:758,topic:'sql-db',difficulty:'beginner',question:'Which SQL keyword is used to grant all privileges to a user?',options:['GRANT SELECT, INSERT, UPDATE, DELETE','GRANT ALL PRIVILEGES','GRANT EVERYTHING','ALLOW ALL'],answer:1,explanation:'GRANT ALL PRIVILEGES ON database.* TO \'user\'@\'host\'; grants all available privileges on the specified object to the user.'},
{id:759,topic:'sql-db',difficulty:'beginner',question:'What is the purpose of an "alias" for a table in a SQL query?',options:['Permanently renames the table','Provides a shorter, more convenient temporary name for a table within the query, reducing typing and enabling self-joins','Creates a copy of the table','Creates a view from the table'],answer:1,explanation:'Table aliases (FROM employees AS e) provide shorter references within a query, essential for self-joins (FROM employees e1 JOIN employees e2 ON e1.manager_id = e2.id) and long table names.'},
{id:760,topic:'sql-db',difficulty:'beginner',question:'What is the difference between CHAR(10) and NCHAR(10)?',options:['They are identical','CHAR(10) stores 10 single-byte characters; NCHAR(10) stores 10 Unicode characters (2 bytes each in SQL Server) — NCHAR is used for multilingual data','NCHAR is faster for all operations','CHAR supports Unicode; NCHAR does not'],answer:1,explanation:'CHAR stores single-byte character sets (ASCII/extended ASCII). NCHAR/NVARCHAR stores Unicode (UTF-16 in SQL Server, 2 bytes per character), required for multilingual data (Chinese, Arabic, etc.).'},
{id:761,topic:'sql-db',difficulty:'intermediate',question:'What is "query plan caching" and what is "plan pollution" or "plan cache bloat"?',options:['Caching all query results in memory','Plan caching stores compiled execution plans for reuse; plan cache bloat occurs when non-parameterised ad-hoc queries generate a unique plan for each distinct literal value, filling the cache with single-use plans and wasting memory','Saving execution plans to disk for faster restarts','A technique for indexing cached queries'],answer:1,explanation:'Plan cache bloat: ad-hoc queries with literal values (WHERE id = 1, WHERE id = 2...) each get a separate cached plan. Parameterised queries (WHERE id = @id) share one plan. Bloat wastes memory and degrades cache efficiency.'},
{id:762,topic:'sql-db',difficulty:'intermediate',question:'What is "table-valued parameter" (TVP) in SQL Server?',options:['A parameter that accepts a table name as a string','A user-defined table type that can be passed as a parameter to stored procedures or functions, enabling sending multiple rows to a procedure in a single network round-trip instead of multiple INSERT calls','A parameter that returns table data','A parameter that accepts a JSON array'],answer:1,explanation:'TVPs (SQL Server) allow passing a DataTable (or IEnumerable) from C# to a stored procedure as a structured parameter, inserting/processing multiple rows with one network call instead of N individual calls.'},
{id:763,topic:'sql-db',difficulty:'intermediate',question:'What is the difference between "INNER JOIN" and "NATURAL JOIN"?',options:['They are identical','INNER JOIN uses an explicit ON condition to specify join columns; NATURAL JOIN automatically joins on ALL columns with the same name in both tables — NATURAL JOIN is generally discouraged as it creates implicit dependencies','NATURAL JOIN is faster than INNER JOIN','INNER JOIN supports multiple columns; NATURAL JOIN supports only one'],answer:1,explanation:'NATURAL JOIN implicitly matches ALL columns with identical names, creating hidden dependencies on schema structure. Explicit INNER JOIN with ON is preferred for clarity, control, and resistance to schema changes.'},
{id:764,topic:'sql-db',difficulty:'intermediate',question:'What is "dynamic SQL" and what are its risks?',options:['SQL that changes at runtime because of user input — always safe when using string formatting','SQL where the query string is constructed at runtime (using EXEC or sp_executesql), enabling flexible queries. Risk: SQL injection if user input is concatenated without parameterisation','SQL that adapts to data types automatically','An optimised form of SQL that the optimizer generates dynamically'],answer:1,explanation:'Dynamic SQL is required for flexible query structures (dynamic table names, column lists, ORDER BY). Risk: SQL injection if user input is concatenated. Use sp_executesql with parameterisation for safe dynamic SQL in SQL Server.'},
{id:765,topic:'sql-db',difficulty:'intermediate',question:'What is "MERGE statement" testing in QA?',options:['Testing that tables can be merged after a schema migration','Verifying that a MERGE statement correctly handles all three scenarios: matched rows are updated correctly, unmatched source rows are inserted correctly, and unmatched target rows are deleted correctly — testing boundary conditions like empty source/target tables and conflict scenarios','Testing that two databases can be merged','Testing stored procedure merges'],answer:1,explanation:'MERGE testing must cover: WHEN MATCHED → correct UPDATE, WHEN NOT MATCHED BY TARGET → correct INSERT, WHEN NOT MATCHED BY SOURCE → correct DELETE, plus edge cases: empty source (no changes), empty target (all inserts), concurrent execution.'},
{id:766,topic:'sql-db',difficulty:'intermediate',question:'What is "database profiling" in QA and performance testing?',options:['Profiling the database administrator\'s work','Capturing and analysing all SQL queries executed against a database during a test or production run to identify slow queries, missing indexes, N+1 problems, and unexpected query patterns — tools include SQL Server Profiler, MySQL slow query log, pg_stat_statements','Creating user profiles in the database','Monitoring database administrator activity'],answer:1,explanation:'Database profiling tools capture: query text, execution count, avg/max duration, rows read/returned. This data identifies optimisation targets: slow queries, frequently executed queries, and unexpected access patterns.'},
{id:767,topic:'sql-db',difficulty:'intermediate',question:'What is "query caching" in MySQL (deprecated in 8.0)?',options:['Caching of execution plans','MySQL\'s (now-removed) feature that cached the complete result set of SELECT queries; subsequent identical queries returned cached results without execution — removed because cache invalidation overhead exceeded benefits in most workloads','Caching database connections','Caching slow query log entries'],answer:1,explanation:'MySQL\'s query cache cached full result sets; invalidated on any table change. Removed in MySQL 8.0 because under write-heavy workloads, cache invalidation overhead exceeded benefits and it was a global mutex bottleneck.'},
{id:768,topic:'sql-db',difficulty:'intermediate',question:'What is "pg_stat_statements" in PostgreSQL?',options:['A system table for database version history','A PostgreSQL extension that tracks statistics for all executed SQL statements: call count, total/mean execution time, rows, buffer hits/misses — essential for identifying slow and frequently-executed queries','A monitoring extension for PostgreSQL connections','A statement history log for security auditing'],answer:1,explanation:'pg_stat_statements is the primary PostgreSQL query analytics tool: it aggregates statistics per normalised query (parameters replaced), showing hottest queries by total time, call frequency, and I/O usage.'},
{id:769,topic:'sql-db',difficulty:'intermediate',question:'What is "slow query log" in MySQL?',options:['A log of syntax errors in queries','A MySQL feature that logs queries exceeding a configurable execution time threshold (long_query_time), enabling DBA identification and optimisation of queries that regularly degrade performance','A log of all connection attempts','A log of failed transaction commits'],answer:1,explanation:'The MySQL slow query log captures queries exceeding long_query_time (default 10s) or examining more rows than min_examined_row_limit — the primary tool for identifying performance problem queries.'},
{id:770,topic:'sql-db',difficulty:'intermediate',question:'What is "database testing" in QA?',options:['Testing the database server hardware','The systematic validation of database functionality, integrity, performance, and security: testing CRUD operations, constraints, stored procedures, triggers, data migrations, transactions, concurrency, backup/restore, and access controls','Testing only SELECT queries','Testing database UI tools'],answer:1,explanation:'Database testing encompasses: schema validation, constraint testing, CRUD functionality, stored procedure/trigger testing, data integrity checks, performance benchmarking, concurrency/locking testing, and security/access control verification.'},
{id:771,topic:'sql-db',difficulty:'advanced',question:'What is "database schema versioning" and why is it critical for CI/CD?',options:['Versioning database backup files','Treating database schema changes as first-class code artefacts: version-controlled migration scripts applied incrementally and automatically to each environment in order, enabling reproducible environments and safe rollback','Versioning stored procedures with comments','Labelling database snapshots with version numbers'],answer:1,explanation:'Schema versioning (Flyway, Liquibase) applies numbered migration scripts in order; CI/CD pipelines automatically apply pending migrations during deployment, ensuring schema and code are always in sync across all environments.'},
{id:772,topic:'sql-db',difficulty:'advanced',question:'What is "query optimisation" for GROUP BY queries?',options:['Removing GROUP BY from queries','Optimisations include: applying WHERE filters before GROUP BY (predicate pushdown), using indexes to avoid sorting for GROUP BY, covering indexes that include GROUP BY and aggregate columns, and hash aggregation vs sort-based aggregation choice','Replacing GROUP BY with subqueries','Adding ORDER BY after GROUP BY'],answer:1,explanation:'GROUP BY optimisation: filter rows first (WHERE reduces group inputs), avoid sort if an index provides pre-sorted order (index scan for GROUP BY), use hash aggregation for large unsorted groups, covering indexes enable index-only aggregation.'},
{id:773,topic:'sql-db',difficulty:'advanced',question:'What is "cardinality estimation error" and why is it the most common cause of bad SQL plans?',options:['An error in the COUNT() function','When the optimizer estimates significantly fewer or more rows for a plan step than actually occur at runtime, causing wrong join order choices (driving the wrong table), wrong join algorithm selection, and inadequate memory grants','A unique key constraint violation','A type of referential integrity error'],answer:1,explanation:'Cardinality estimation errors (too high → too much memory/resources allocated; too low → nested loop on millions of rows) are the root cause of most bad execution plans. Fixed by refreshing statistics, creating better statistics, or query hints.'},
{id:774,topic:'sql-db',difficulty:'advanced',question:'What is "index key length" limitation in databases and why does it matter?',options:['Indexes must have the same name length as the indexed column','Most databases have maximum index key size limits (e.g., MySQL InnoDB: 767 bytes per key column for utf8mb3, 3072 bytes total for prefix indexes) — exceeding limits prevents index creation or requires prefix indexing, affecting query coverage','Index key length determines index performance','Index keys must be longer than 8 bytes'],answer:1,explanation:'MySQL InnoDB limits: 767 bytes per column (utf8mb3 prefix), 3072 bytes total key size. PostgreSQL: 2712 bytes per key. Exceeding limits prevents index creation; solution is prefix indexes (SUBSTR) or switch to hash-based techniques.'},
{id:775,topic:'sql-db',difficulty:'advanced',question:'What is "functional dependency" in database theory?',options:['A dependency on a function defined in SQL','A relationship where the value of one set of attributes (determinant) uniquely determines the value of another attribute — the foundation of normal form theory. If X → Y, knowing X always tells you Y','A dependency on a stored function','A dependency between two stored procedures'],answer:1,explanation:'Functional dependency X → Y: for any two rows with the same X value, they must have the same Y value. FDs are the basis of normalization: 2NF (no partial FDs), 3NF (no transitive FDs), BCNF (all determinants are superkeys).'},
{id:776,topic:'sql-db',difficulty:'advanced',question:'What is "Armstrong\'s Axioms" used for in database theory?',options:['Named after the programmer who created SQL','A set of inference rules for functional dependencies: Reflexivity (if Y⊆X then X→Y), Augmentation (if X→Y then XZ→YZ), Transitivity (if X→Y and Y→Z then X→Z) — used to derive all implied FDs from a given set','A set of rules for query optimisation','Rules for creating database indexes'],answer:1,explanation:'Armstrong\'s Axioms (reflexivity, augmentation, transitivity) form a sound and complete inference system for functional dependencies — the theoretical tool for computing attribute closures and minimal covers used in normalization.'},
{id:777,topic:'sql-db',difficulty:'advanced',question:'What is "OLAP rollup" operation?',options:['Rolling back OLAP transactions','Moving up the dimensional hierarchy in an OLAP cube to see data at a higher aggregation level — e.g., from monthly to quarterly to annual totals','Restarting an OLAP server','Rolling up individual database rows into a summary'],answer:1,explanation:'Rollup moves up the dimension hierarchy: daily → monthly → quarterly → yearly. The SQL ROLLUP extension generates subtotals at each hierarchy level in a single query using GROUP BY with ROLLUP().'},
{id:778,topic:'sql-db',difficulty:'advanced',question:'What is SQL\'s "ROLLUP" extension to GROUP BY?',options:['Rolling back a transaction','GROUP BY ROLLUP(a, b, c) generates subtotals for each combination from the most specific (a, b, c) up to the grand total (no grouping), creating hierarchical summary rows in a single query','Rolling up the previous day\'s data','Rotating columns into rows'],answer:1,explanation:'GROUP BY ROLLUP(a, b, c) generates N+1 grouping levels: (a,b,c), (a,b), (a), () — producing group subtotals and grand total. GROUPING() or GROUPING_ID() distinguishes actual NULLs from rollup NULLs.'},
{id:779,topic:'sql-db',difficulty:'advanced',question:'What is "GROUPING SETS" in SQL?',options:['Multiple GROUP BY clauses combined','A SQL extension that allows specifying multiple arbitrary grouping combinations in a single query, each producing its own set of aggregate rows — more flexible than ROLLUP or CUBE','Sets of groups created by multiple GROUP BY clauses','A synonym for DISTINCT in aggregation contexts'],answer:1,explanation:'GROUPING SETS ((a, b), (a), ()) is equivalent to three separate GROUP BY queries UNIONed: by (a,b), by (a), and total — but executed in a single pass for efficiency.'},
{id:780,topic:'sql-db',difficulty:'advanced',question:'What is "CUBE" extension to GROUP BY in SQL?',options:['Generating 3D data visualisations','GROUP BY CUBE(a, b, c) generates subtotals for ALL 2^N combinations of the specified dimensions, including grand total — ideal for multi-dimensional analysis across all dimension pairs','A type of OLAP cube stored in SQL','A cross-product join operation'],answer:1,explanation:'CUBE generates subtotals for every possible combination of dimensions: for 3 dimensions, 2^3 = 8 grouping sets. It is the SQL equivalent of an OLAP cube slice-and-dice operation.'},
{id:781,topic:'sql-db',difficulty:'beginner',question:'What does "SELECT INTO" do in SQL Server?',options:['Selects data into a variable','Creates a new table and inserts the SELECT result into it in one statement — useful for creating quick copies or temporary result tables','Selects data from one database into another','Inserts data from a file into a table'],answer:1,explanation:'SELECT ... INTO new_table FROM source creates a new table with the structure and data of the query result. Equivalent to CREATE TABLE + INSERT INTO SELECT in standard SQL.'},
{id:782,topic:'sql-db',difficulty:'beginner',question:'What does INSERT INTO SELECT do?',options:['Creates a new table from a query','Copies rows from a SELECT query result into an existing table','Selects data to preview before inserting','Inserts data from an external file'],answer:1,explanation:'INSERT INTO target_table (cols) SELECT cols FROM source_table [WHERE condition] copies rows from one table (or query) into another existing table — ideal for data migration and staging.'},
{id:783,topic:'sql-db',difficulty:'beginner',question:'What is "schema-on-read" vs "schema-on-write"?',options:['They are identical concepts','Schema-on-write (RDBMS): structure is enforced when data is written. Schema-on-read (data lakes): structure is applied at query time — raw data stored as-is, interpreted when read','Schema-on-read is only for JSON data','Schema-on-write is only for NoSQL'],answer:1,explanation:'RDBMS enforces schema at INSERT (schema-on-write); invalid data is rejected. Data lakes (schema-on-read) store raw data and apply structure at query time, providing flexibility at the cost of early validation.'},
{id:784,topic:'sql-db',difficulty:'beginner',question:'What is "atomicity" in the context of a database transaction?',options:['Each transaction processes one atom of data','A transaction is treated as an all-or-nothing unit: all operations in the transaction must succeed and be committed, or if any operation fails, all changes are rolled back completely','Transactions are atomic because they are small','Atomic transactions cannot be rolled back'],answer:1,explanation:'Atomicity (the A in ACID): partial updates are impossible. Either the entire transaction commits (all changes persist) or it rolls back (no changes persist), ensuring data consistency.'},
{id:785,topic:'sql-db',difficulty:'beginner',question:'What is "durability" in ACID properties?',options:['The database files do not degrade over time','Once a transaction is committed, its changes are permanently recorded and will survive system failures — achieved through write-ahead logging (WAL) and/or storage-level redundancy','The database can handle any load without performance loss','Transactions are durable for 30 days'],answer:1,explanation:'Durability (D in ACID) guarantees that committed data survives crashes. WAL ensures committed changes are on persistent storage before confirming to the application; recovery replays committed log records after restart.'},
{id:786,topic:'sql-db',difficulty:'intermediate',question:'In SQL, what is the "APPLY" operator (SQL Server) used for?',options:['Applying indexes to a table','A table-valued operator that invokes a table-valued function or subquery once for each row of a left-side table: CROSS APPLY (like INNER JOIN), OUTER APPLY (like LEFT JOIN)','Applying a constraint to all rows','Applying a trigger to a table'],answer:1,explanation:'APPLY (SQL Server / MS-specific): CROSS APPLY returns only rows where the right side produces results; OUTER APPLY includes outer rows even if right side returns nothing. Enables row-by-row application of complex table-valued expressions.'},
{id:787,topic:'sql-db',difficulty:'intermediate',question:'What is "query cost threshold for parallelism" in SQL Server?',options:['A threshold that controls when queries are cached','A configuration setting (cost threshold for parallelism, default: 5) that specifies the minimum estimated cost at which SQL Server considers a parallel plan — queries below this threshold always run serially','A threshold for triggering query plan recompilation','The maximum cost before a query is killed'],answer:1,explanation:'Cost Threshold for Parallelism (default 5 cost units) prevents trivial queries from going parallel (overhead > savings). Setting it higher (25-50) for OLTP servers reduces unnecessary parallelism.'},
{id:788,topic:'sql-db',difficulty:'intermediate',question:'What is "MAXDOP" (Maximum Degree of Parallelism) in SQL Server?',options:['Maximum number of database operations per second','A server/query-level setting controlling the maximum number of CPU cores SQL Server can use for a single parallel query — MAXDOP 1 forces serial execution, MAXDOP 0 allows all available CPUs','Maximum number of open connections','Maximum number of locks per query'],answer:1,explanation:'MAXDOP controls CPU usage per query for parallel plans: MAXDOP 1 = serial, MAXDOP N = N threads. Important tuning parameter for balancing single-query speed vs concurrency on multi-core systems.'},
{id:789,topic:'sql-db',difficulty:'intermediate',question:'What is "page life expectancy" (PLE) in SQL Server?',options:['The time a database page exists before deletion','A performance counter measuring the average number of seconds a data page remains in the buffer pool (memory cache); low PLE indicates memory pressure causing excessive disk I/O','The expiry time for database user sessions','The maximum duration of a database page lock'],answer:1,explanation:'PLE (target: >300 seconds for 4GB buffer pool, higher for more RAM) measures buffer cache effectiveness; declining PLE indicates memory pressure, causing more physical reads and performance degradation.'},
{id:790,topic:'sql-db',difficulty:'intermediate',question:'What is "row compression" vs "page compression" in SQL Server?',options:['Row compression: compresses individual rows. Page compression: compresses entire pages.','Row compression: stores NULL and zero-value columns using minimal storage (variable-length storage for fixed-width types, no storage for NULLs). Page compression: includes row compression plus dictionary and prefix compression at the page level — greater compression but more CPU overhead','Page compression only works on data pages; row compression works on index pages only','Row compression is automatic; page compression requires manual enablement'],answer:1,explanation:'Row compression: fixed-width data stored as variable-width where possible, NULLs use 0 bytes. Page compression: adds dictionary encoding and prefix compression across all rows on a page, 2-3x compression ratio at higher CPU cost.'},
{id:791,topic:'sql-db',difficulty:'advanced',question:'What is the "fill factor" setting on a SQL Server index?',options:['The percentage of each index page left empty for future inserts (during index creation/rebuild), reducing page splits for subsequent INSERT/UPDATE operations at the cost of slightly larger index size','The fill level of the transaction log','The percentage of buffer pool used by index pages','The compression ratio of index data'],answer:0,explanation:'Fill factor (0-100%, where 100=full, 0/100=100% full) sets free space on index leaf pages during REBUILD, reducing future page splits for insert-heavy tables at the cost of a larger index and more pages to scan.'},
{id:792,topic:'sql-db',difficulty:'advanced',question:'What is "Change Data Capture" (CDC) vs "Change Tracking" in SQL Server?',options:['They are identical','CDC: captures complete row changes (before and after images) from the transaction log into change tables — for ETL and event streaming. Change Tracking: tracks which rows changed (not what changed) with sync version numbers — for lightweight synchronisation without full CDC overhead','Change Tracking stores before images; CDC does not','CDC is real-time; Change Tracking requires manual triggering'],answer:1,explanation:'CDC provides full change data (old and new values, DML type) in change tables; ideal for ETL pipelines needing the changed values. Change Tracking provides only the primary key and DML type; ideal for synchronisation (just need to know WHAT changed, not the values).'},
{id:793,topic:'sql-db',difficulty:'advanced',question:'What is "lock timeout" in database systems?',options:['The maximum duration a connection can remain open','The maximum time a transaction waits to acquire a lock on a resource before the wait is terminated with a lock timeout error — preventing indefinite blocking in high-contention scenarios','The timeout for releasing deadlocks','The maximum duration of a transaction before automatic rollback'],answer:1,explanation:'Lock timeout (SET LOCK_TIMEOUT in SQL Server, lock_timeout in PostgreSQL) prevents indefinite blocking: if a lock cannot be acquired within the timeout, the waiting statement is cancelled (not the transaction).'},
{id:794,topic:'sql-db',difficulty:'advanced',question:'What is "partition switching" in SQL Server and why is it significant?',options:['Switching between database partitioning schemes','A metadata-only operation that moves data between partitions or between a partitioned table and a staging table near-instantly, enabling efficient bulk loading and archiving without actual data movement — critical for large-table ETL patterns','Switching primary and secondary partitions','Reorganising partitions without downtime'],answer:1,explanation:'Partition switching is a near-instant metadata operation: SWITCH a staging table (same structure) into a partition, or SWITCH OUT an old partition to an archive table. Used for efficient bulk data loading and range-based archiving.'},
{id:795,topic:'sql-db',difficulty:'advanced',question:'What is "SQL Server Resource Governor" used for?',options:['Governing SQL Server licencing compliance','A feature for managing SQL Server workloads by classifying sessions into workload groups and pools with defined CPU, memory, and I/O limits — preventing one workload (e.g., reporting) from starving another (e.g., OLTP transactions)','A tool for governing DBA access to the server','A network resource allocation tool'],answer:1,explanation:'Resource Governor classifies connections into pools (each with CPU % and memory limits), preventing runaway queries or reporting workloads from degrading OLTP performance on the same server.'},
{id:796,topic:'sql-db',difficulty:'advanced',question:'What is "data lineage" in database and data warehouse management?',options:['Tracking which lineage (family tree) of users owns data','The ability to trace the origin, transformation history, and movement of a piece of data across systems — from source system through ETL pipelines to final reports — critical for auditing, compliance, and debugging data quality issues','A feature for tracking database schema inheritance','The history of table structure changes'],answer:1,explanation:'Data lineage tracks data from source to destination: where did this report number come from? Which source table? What transformations were applied? Critical for GDPR compliance, financial auditing, and data quality root cause analysis.'},
{id:797,topic:'sql-db',difficulty:'advanced',question:'What is "database testing anti-pattern" of using production data directly in test environments?',options:['A best practice for realistic testing','A major compliance and security risk: using real customer PII (names, emails, credit cards, SSNs) in test environments exposes it to developers, testers, and CI systems — violating GDPR, PCI-DSS, HIPAA, and other regulations. Solution: data masking or synthetic data generation','An efficient testing strategy','Only problematic for healthcare applications'],answer:1,explanation:'Using real PII in test environments violates GDPR, PCI-DSS, HIPAA, and most data protection regulations. Test environments must use masked, anonymised, or synthetic data that mimics real data characteristics without exposing actual personal information.'},
{id:798,topic:'sql-db',difficulty:'advanced',question:'What is "wait statistics" analysis in SQL Server performance tuning?',options:['Analysing time spent waiting for users to respond','Examining the types and durations of waits SQL Server threads experience (PAGEIOLATCH, LCK_M_X, CXPACKET, ASYNC_NETWORK_IO, etc.) to identify the primary performance bottleneck — I/O, CPU, locking, network, or parallelism','Waiting for statistics to be automatically updated','Monitoring database statistics refresh schedule'],answer:1,explanation:'Wait statistics (sys.dm_os_wait_stats) reveal what SQL Server is waiting for: PAGEIOLATCH_* = disk I/O; LCK_M_* = locking/blocking; CXPACKET = parallelism skew; ASYNC_NETWORK_IO = client processing. They diagnose the root bottleneck.'},
{id:799,topic:'sql-db',difficulty:'advanced',question:'What is "DBCC CHECKDB" in SQL Server?',options:['A command for checking database backup consistency','A database consistency checker that verifies the physical and logical integrity of all objects in a SQL Server database — checking for allocation errors, structural damage, and data corruption. Should be run regularly as part of database maintenance','A command for rebuilding all database indexes','A tool for checking database performance'],answer:1,explanation:'DBCC CHECKDB detects: allocation errors (extent/page chain corruption), structural errors (row, page, or index corruption), cross-object consistency (FK violations in system tables). Critical for data integrity assurance; run at minimum weekly.'},
{id:800,topic:'sql-db',difficulty:'advanced',question:'What is "transparent data encryption" (TDE) in SQL Server and Oracle?',options:['Encryption visible to the user','Real-time I/O encryption/decryption of database data files and transaction log files at the page level, transparent to applications — data is encrypted at rest without application changes, protecting against physical media theft','Encryption applied to specific columns only','A network encryption protocol for database connections'],answer:1,explanation:'TDE encrypts data files at rest using a database encryption key (DEK) protected by a certificate or Extensible Key Management (EKM). Applications work normally (no SQL changes), but stolen MDF/LDF files are unreadable without the key.'}
]);
