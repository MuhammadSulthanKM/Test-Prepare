// Java Fundamentals — Part 4 (q1021–1140)
window.QUIZ_BANK['java'] = (window.QUIZ_BANK['java']||[]).concat([
{id:1021,topic:'java',difficulty:'beginner',question:'What is Java\'s "Math" class used for?',options:['A class for creating Math teachers in Java','A utility class with static methods for mathematical operations: Math.abs(), Math.max(), Math.min(), Math.pow(), Math.sqrt(), Math.random(), Math.floor(), Math.ceil(), Math.round()','A class for complex number arithmetic','A package for financial mathematics'],answer:1,explanation:'Math class provides fundamental mathematical functions. Math.random() returns a double in [0,1). For cryptographic random numbers, use java.security.SecureRandom.'},
{id:1022,topic:'java',difficulty:'beginner',question:'What is the difference between Math.floor() and (int) casting for truncation?',options:['They are identical','Math.floor() returns the largest double ≤ x (rounds toward negative infinity); (int) cast truncates toward zero. Difference: Math.floor(-2.5) = -3.0; (int)-2.5 = -2','Math.floor() returns an int','(int) cast rounds to nearest integer'],answer:1,explanation:'floor() rounds toward negative infinity; cast truncates toward zero. For positive numbers: identical. For negatives: floor(-2.5) = -3, (int)-2.5 = -2. Always consider negative numbers when choosing truncation.'},
{id:1023,topic:'java',difficulty:'beginner',question:'What is a "try block" without a catch clause valid for?',options:['It is always invalid','Valid with a finally clause (try-finally) — the finally always executes, enabling cleanup code even without exception handling; or with a multi-catch clause','Valid for empty try blocks only','Valid only if the method declares throws'],answer:1,explanation:'try-finally (without catch) ensures cleanup code runs regardless of whether an exception occurs, propagating the exception to the caller. try-with-resources is preferred for resource cleanup.'},
{id:1024,topic:'java',difficulty:'beginner',question:'What is a "multi-catch" exception block in Java (Java 7+)?',options:['Catching exceptions from multiple threads','Catching multiple exception types in a single catch block: catch (IOException | SQLException e) { ... } — reduces code duplication, re-thrown exception is effectively final','Catching all exceptions','A catch block inside another catch block'],answer:1,explanation:'Multi-catch (Java 7+): catch (IOException | SQLException e) — handles both types with the same code. The caught exception type is effectively final (cannot be reassigned). Improves readability over duplicate catch blocks.'},
{id:1025,topic:'java',difficulty:'beginner',question:'What is "exception chaining" in Java?',options:['Catching exceptions in a chain of catch blocks','Wrapping a caught exception in a new exception to preserve the original cause: throw new RuntimeException("Message", cause) — the cause is accessible via getCause(). Maintains the stack trace of the original failure','Rethrowing the same exception','Creating a chain of exception handlers'],answer:1,explanation:'Exception chaining (Java 1.4+): new IOException("Failed to connect", originalException) — cause.getCause() retrieves the original. Prevents losing the root cause when translating exceptions across layers.'},
{id:1026,topic:'java',difficulty:'beginner',question:'Can a constructor call another constructor in Java?',options:['No, constructors cannot call other constructors','Yes, using this() to call another constructor in the same class (constructor chaining), or super() to call the parent class constructor — must be the first statement in the constructor body','Yes, using new ClassName()','Yes, using constructor()'],answer:1,explanation:'Constructor chaining: this() calls another constructor in the same class (must be first statement). super() calls the parent constructor. This reduces duplication by having specialised constructors delegate to more complete ones.'},
{id:1027,topic:'java',difficulty:'beginner',question:'What does "final" mean when applied to a method?',options:['The method is called last during object lifecycle','The method cannot be overridden by subclasses — it is the final implementation in the inheritance chain','The method has no return value','The method can only be called once'],answer:1,explanation:'final methods cannot be overridden in subclasses; the implementation in the declaring class is definitive. Useful for template methods that must not be changed, and allows JIT to inline final methods.'},
{id:1028,topic:'java',difficulty:'beginner',question:'What does "abstract" mean when applied to a class?',options:['The class has no fields','The class cannot be instantiated directly with new; it must be subclassed. May contain abstract methods (without body) that subclasses must implement, and concrete methods they inherit','The class is not yet implemented','The class uses abstraction design patterns'],answer:1,explanation:'Abstract class: provides partial implementation. Subclasses must implement all abstract methods. Cannot use new AbstractClass() — must use new ConcreteSubclass(). Used for the Template Method pattern.'},
{id:1029,topic:'java',difficulty:'beginner',question:'What does "abstract" mean when applied to a method?',options:['The method is abstract and has a complex algorithm','Declares a method with no body in an abstract class; subclasses must provide an implementation. Syntax: abstract returnType methodName(params);','The method is asynchronous','The method is only called internally'],answer:1,explanation:'Abstract methods: no body, must be in an abstract class, subclasses must override unless they are also abstract. Forces subclasses to provide a concrete implementation of the declared behaviour.'},
{id:1030,topic:'java',difficulty:'beginner',question:'What is Java\'s "interface default method" and when was it introduced?',options:['A method that is called by default on construction','Introduced in Java 8: a method in an interface with a body (keyword default), providing default behaviour that implementing classes can optionally override — enables interface evolution without breaking existing implementations','A standard implementation in abstract classes','The first method in an interface'],answer:1,explanation:'Default methods allow adding new behaviour to interfaces without breaking all existing implementations. List.sort(Comparator) is a Java 8 default method added to the existing List interface.'},
{id:1031,topic:'java',difficulty:'intermediate',question:'What is "Java NIO" (non-blocking I/O) and when should it be used?',options:['A new I/O library for cloud applications','A Java I/O API (java.nio) supporting non-blocking, scalable I/O via Channels (FileChannel, SocketChannel), Selectors (single thread monitoring many channels), and Buffers (direct memory for zero-copy). Preferred for high-concurrency network servers over traditional blocking I/O','A library for reading very large files','A tool for parallel file processing'],answer:1,explanation:'NIO: Channels replace streams, Buffers replace byte arrays, Selectors enable one thread to monitor multiple channels. Preferred for high-concurrency servers (thousands of connections). Virtual threads (Java 21) make blocking I/O scale as well, reducing the need for raw NIO.'},
{id:1032,topic:'java',difficulty:'intermediate',question:'What is the "volatile" variable vs "AtomicInteger"?',options:['They are identical','volatile: guarantees visibility of the latest value to all threads (prevents stale reads from CPU caches) but does NOT make compound operations (i++) atomic. AtomicInteger: CAS-based atomic operations — getAndIncrement(), compareAndSet() — both visible AND atomic','AtomicInteger is always faster','volatile prevents all concurrency issues'],answer:1,explanation:'volatile i++: read (visible) → increment (non-atomic between read and write) → write (visible) — still a race condition. AtomicInteger.incrementAndGet(): single atomic CAS operation, no race. Use volatile for flags; AtomicInteger for counters.'},
{id:1033,topic:'java',difficulty:'intermediate',question:'What is "ThreadLocal" in Java?',options:['A class for creating threads with local variables','A mechanism providing thread-local storage: each thread has its own independent copy of the variable, preventing shared mutable state — used for context propagation (user session, transaction context) without method parameter passing','A local variable inside a thread\'s run method','A variable that is local to the JVM'],answer:1,explanation:'ThreadLocal.withInitial(Supplier): each thread gets its own instance via get(). Common uses: SimpleDateFormat (not thread-safe), database Connection per thread, request context in web servers. Remove with threadLocal.remove() to prevent memory leaks.'},
{id:1034,topic:'java',difficulty:'intermediate',question:'What is "InheritableThreadLocal"?',options:['A ThreadLocal that is inherited by child threads','A ThreadLocal where child threads created from a parent thread inherit the parent\'s thread-local values at creation time — the child gets a copy of the parent\'s value, enabling context propagation into child threads','A global thread-local variable','A deprecated version of ThreadLocal'],answer:1,explanation:'InheritableThreadLocal: child threads inherit parent values. Limitation: if the parent\'s value changes after child creation, the child does not see the update (it got a copy). Be aware with virtual threads — they inherit from the spawning virtual thread.'},
{id:1035,topic:'java',difficulty:'intermediate',question:'What is "Java memory leak" and common causes?',options:['A JVM bug causing memory corruption','Objects that remain reachable (referenced) but are no longer needed, preventing GC from reclaiming them. Common causes: static collections growing unboundedly, listeners/callbacks not unregistered, ThreadLocal values not removed, unclosed resources, inner class holding outer reference','An out-of-memory error from large arrays','A memory allocation failure'],answer:1,explanation:'Memory leaks in Java: static Map growing forever, EventListeners added but never removed, ThreadLocal not cleared (causing ClassLoader leak in app servers), inner class instances preventing outer class GC, unclosed streams/connections.'},
{id:1036,topic:'java',difficulty:'intermediate',question:'What is a "Java heap dump" and when is it used?',options:['A dump truck for moving Java projects','A snapshot of all objects in the JVM heap at a specific point in time, captured with jmap -dump, -XX:+HeapDumpOnOutOfMemoryError, or JVisualVM — analysed with tools like Eclipse MAT, JProfiler, or VisualVM to diagnose memory leaks and OutOfMemoryErrors','A backup of Java source code','A log of all object creations'],answer:1,explanation:'Heap dumps: used to diagnose OOMErrors and memory leaks. Analysis with Eclipse MAT: find the retention tree (what is holding memory), histograms of object counts, unreachable objects, and leak suspects reports.'},
{id:1037,topic:'java',difficulty:'intermediate',question:'What is a "stack trace" in Java and how is it read?',options:['A debug log of all method calls since JVM startup','The sequence of method calls leading to an exception, shown in a Throwable\'s stack trace. Read bottom-to-top for the call chain; top is the exception throwing point. The most relevant frame is usually at or near the top','A list of all classes loaded by the JVM','A performance profiling report'],answer:1,explanation:'Stack trace format: Exception type + message, followed by "at ClassName.methodName(FileName.java:lineNumber)" frames. The top frame is where the exception was thrown; traverse down to find the root cause in your code (not framework internals).'},
{id:1038,topic:'java',difficulty:'intermediate',question:'What is Java\'s "try-with-resources" statement and the "AutoCloseable" interface?',options:['A try block that tries multiple resources','Java 7+ construct: try (Resource r = open()) { use r } — automatically calls r.close() when the block exits. The resource class must implement AutoCloseable (or Closeable for I/O). Multiple resources can be declared, closed in reverse declaration order','A try block for HTTP resources','A resource pool for try blocks'],answer:1,explanation:'try-with-resources ensures close() is called even if an exception occurs. Multiple resources: try (A a = openA(); B b = openB()) — b is closed first, then a. Suppressed exceptions (from close()) are accessible via getSuppressed().'},
{id:1039,topic:'java',difficulty:'intermediate',question:'What is "Java NIO.2 Path API" and what does it replace?',options:['A network path API for Java','Replaces the legacy java.io.File class with a richer, more consistent file system API: Paths.get() / Path.of() for paths, Files class for operations (read, write, copy, move, walk), better exception handling, and operating system-independent path handling','A class for network URLs','A URL routing library for Java servers'],answer:1,explanation:'NIO.2 Path API: Path p = Paths.get("/home/user/file.txt"); Files.readAllBytes(p); Files.walk(dir) for recursive traversal; Files.list(dir) for directory listing. Better than File for new code: more methods, better exceptions, symbolic link awareness.'},
{id:1040,topic:'java',difficulty:'advanced',question:'What is "ServiceLoader" in Java and what is it used for?',options:['A loader for web services','A runtime service discovery mechanism (java.util.ServiceLoader) loading implementations of a service interface registered in META-INF/services/ files — the foundation for Java SPI (Service Provider Interface) enabling pluggable implementations without compile-time dependency','A Spring bean loader','A tool for loading test doubles'],answer:1,explanation:'ServiceLoader.load(Service.class) discovers and loads registered implementations from classpath META-INF/services/. Used by: JDBC driver loading, Java logging, XML parsers, and any extensible framework. Modules use module-info.java provides/with declarations.'},
{id:1041,topic:'java',difficulty:'beginner',question:'What is a "Java wrapper class"?',options:['A class that wraps another class for security','A class that represents a primitive type as an object: Integer wraps int, Long wraps long, Double wraps double, Character wraps char, Boolean wraps boolean, Byte wraps byte, Short wraps short, Float wraps float','A class that extends all primitive types','A class that decorates an interface'],answer:1,explanation:'Wrapper classes: enable primitives to be used as objects (required for generics, Collections). Provide utility constants (Integer.MAX_VALUE, Integer.MIN_VALUE) and conversion methods (Integer.parseInt, Integer.toBinaryString).'},
{id:1042,topic:'java',difficulty:'beginner',question:'What is "Integer.parseInt()" used for?',options:['Converts an Integer to int','Parses a String containing an integer representation and returns the corresponding int value: Integer.parseInt("42") = 42. Throws NumberFormatException if the string is not a valid integer','Converts an int to Integer','Returns the binary representation of an integer'],answer:1,explanation:'Integer.parseInt(String): String → int. Integer.valueOf(String): String → Integer (cached for common values). parseInt("FF", 16) = 255 (hex parsing with radix). Throws NumberFormatException for non-numeric strings.'},
{id:1043,topic:'java',difficulty:'beginner',question:'What is the "String.toCharArray()" method?',options:['Converts a char array to String','Returns a new char array containing each character of the string in sequence: "hello".toCharArray() = {\'h\',\'e\',\'l\',\'l\',\'o\'}','Converts the string to uppercase chars','Returns an array of character codes'],answer:1,explanation:'toCharArray() returns a char[]; useful when you need to manipulate individual characters in the string (reverse, frequency count). The returned array is a copy — modifying it does not affect the original string.'},
{id:1044,topic:'java',difficulty:'beginner',question:'What is "String.valueOf()" used for?',options:['Evaluates the value of a string expression','Returns the string representation of any primitive or object argument: String.valueOf(42) = "42", String.valueOf(null) = "null", String.valueOf(3.14) = "3.14"','Returns the numeric value of a string','Checks if a string is a valid number'],answer:1,explanation:'String.valueOf(x): the null-safe way to convert any type to String (returns "null" for null objects instead of NPE). Equivalent to x.toString() for non-null objects but safer.'},
{id:1045,topic:'java',difficulty:'intermediate',question:'What is the difference between "String.format" and "MessageFormat"?',options:['They are identical','String.format: uses C-style printf format specifiers (%s, %d, %.2f) for quick formatting. MessageFormat (java.text): uses {0}, {1} pattern with locale-aware formatting including date, number, and plural forms — better for i18n applications','MessageFormat is always faster','String.format handles null; MessageFormat does not'],answer:1,explanation:'String.format("%s has %d items", name, count): printf-style, no locale consideration. MessageFormat.format("{0} has {1} items", name, count): index-based, locale-aware, better for internationalization with date/number/plural formatting.'},
{id:1046,topic:'java',difficulty:'intermediate',question:'What is "Spliterator" in Java (Java 8+)?',options:['A thread that splits work','An iterator with the ability to split itself into two halves for parallel processing, enabling the Streams parallel pipeline to divide work across CPU cores; also provides characteristics (SORTED, SIZED, DISTINCT, IMMUTABLE) for stream optimisation','A splitter for string parsing','An iterator for set collections only'],answer:1,explanation:'Spliterator combines iteration and splitting: trySplit() creates a partition for parallel processing. Characteristics hint to the stream engine (e.g., SIZED enables better work distribution, SORTED avoids redundant sorting). Custom collections provide custom Spliterators.'},
{id:1047,topic:'java',difficulty:'intermediate',question:'What is "NavigableMap" and "NavigableSet" in Java?',options:['Maps that navigate between databases','Extensions of SortedMap/SortedSet providing navigation methods: lower(key), floor(key), ceiling(key), higher(key), headMap(toKey, inclusive), tailMap(fromKey, inclusive), descendingMap() — implemented by TreeMap and TreeSet','Maps with built-in browser navigation','Maps that automatically navigate between shards'],answer:1,explanation:'NavigableMap/Set (TreeMap implements NavigableMap): floorKey(k) returns the greatest key ≤ k; ceilingKey(k) returns least key ≥ k. These enable efficient range queries and order statistics on tree-backed collections.'},
{id:1048,topic:'java',difficulty:'intermediate',question:'What does "Collections.synchronizedList()" do?',options:['Makes a list faster for concurrent use','Returns a thread-safe wrapper around a List where each individual method is synchronised — but compound operations (check-then-act, iteration) still require external synchronisation. For true concurrent lists use CopyOnWriteArrayList or ConcurrentLinkedQueue','Makes a list immutable','Creates a sorted thread-safe list'],answer:1,explanation:'synchronizedList() wraps each method in synchronized, but iteration is NOT atomic: for (T e : syncList) must be in a synchronized(syncList) block. CopyOnWriteArrayList provides safer iteration at the cost of copy-on-write overhead.'},
{id:1049,topic:'java',difficulty:'intermediate',question:'What is "EnumSet" and "EnumMap" and why are they preferred for enum-keyed data?',options:['Ordinary Set/Map implementations that happen to use enums','EnumSet: bit-vector backed Set of enum values — extremely fast (single long operation for sets ≤ 64 enum values); EnumMap: array-backed Map with enum keys — constant time, minimal memory, maintains enum declaration order. Both outperform HashSet/HashMap for enum keys','EnumSet only works for int-backed enums','EnumMap requires enum values to be unique'],answer:1,explanation:'EnumSet uses a bit vector under the hood: set.contains() is a bit check, O(1) with minimal memory. EnumMap uses an array indexed by ordinal: extremely fast, no hashing, no collision. Always use these over HashSet/HashMap when keys are enum values.'},
{id:1050,topic:'java',difficulty:'advanced',question:'What is "Java memory model" volatile write visibility guarantee?',options:['Volatile writes are slower but otherwise identical','A volatile write happens-before every subsequent volatile read of the same variable by any thread. This provides visibility: the writing thread\'s changes (including all prior non-volatile writes) are visible to any thread that subsequently reads the volatile variable','Volatile guarantees write ordering but not visibility','Volatile writes are only visible to the same thread'],answer:1,explanation:'Volatile write-read HB relationship: not just the volatile field — all writes done by the thread before the volatile write become visible to the thread reading the volatile. This is why volatile fields can synchronise more than just the field itself.'},
{id:1051,topic:'java',difficulty:'advanced',question:'What is "safe publication" of an object in Java?',options:['Publishing only public objects','Making a fully initialised object safely visible to multiple threads; methods: via synchronised constructor access, final field assignment (visible after constructor returns), volatile field assignment, or publishing through a concurrent collection. Unsafe publication may expose partially initialised objects','Making an object immutable before sharing','Publishing an object to an API'],answer:1,explanation:'Unsafe publication: storing an object reference in a shared field without synchronisation may expose a partially initialised object to other threads due to JVM instruction reordering. Safe publication: final fields, volatile, synchronised, or concurrent collections guarantee full initialisation visibility.'},
{id:1052,topic:'java',difficulty:'advanced',question:'What is "CompletableFuture.thenCompose()" vs "thenApply()"?',options:['They are identical','thenApply(Function<T,U>): transforms the result synchronously, producing CompletableFuture<U>. thenCompose(Function<T,CompletableFuture<U>>): chains two async operations, flattening the result — like flatMap for streams. Use thenCompose when the transformation itself is async','thenCompose is the parallel version of thenApply','thenApply handles errors; thenCompose does not'],answer:1,explanation:'thenApply: like map — transform T to U synchronously. thenCompose: like flatMap — transform T to CompletableFuture<U> asynchronously, returning CompletableFuture<U> (not CompletableFuture<CompletableFuture<U>>).'},
{id:1053,topic:'java',difficulty:'advanced',question:'What is "CompletableFuture.allOf()" used for?',options:['Combining all CompletableFutures into one result','Returns a CompletableFuture<Void> that completes when ALL provided futures have completed. To collect results: CompletableFuture.allOf(futures).thenApply(v -> Stream.of(futures).map(CompletableFuture::join).collect(Collectors.toList()))','Executing futures sequentially','Completing all futures immediately'],answer:1,explanation:'allOf(): wait for all futures to complete. anyOf(): returns CompletableFuture<Object> completing when the first future completes. Use allOf to fan-out multiple async operations and collect all results.'},
{id:1054,topic:'java',difficulty:'advanced',question:'What is the "Exchanger" synchroniser in Java?',options:['A currency exchange service','A synchronisation point where two threads can exchange data: thread A calls exchange(a), blocks until thread B calls exchange(b), then both receive the other\'s value — used for pipeline stages, genetic algorithms, and pair-based data exchange','A protocol for exchanging class definitions','A tool for exchanging mutable objects safely'],answer:1,explanation:'Exchanger<T>: both threads block at exchange() until both arrive, then swap their objects. A classic use: a producer fills a buffer, exchanges it with a consumer\'s empty buffer simultaneously — zero allocation pipeline.'},
{id:1055,topic:'java',difficulty:'advanced',question:'What is "ScheduledExecutorService" used for?',options:['Scheduling meetings in Java applications','Providing thread pool-based task scheduling: schedule(Runnable, delay, unit) for one-time delayed tasks, scheduleAtFixedRate() and scheduleWithFixedDelay() for periodic tasks — replacing the legacy Timer/TimerTask classes','A timer for profiling code blocks','A scheduler for Java web requests'],answer:1,explanation:'ScheduledExecutorService: schedule() one-shot delay; scheduleAtFixedRate(task, 0, 1, SECONDS) runs every second regardless of task duration (may overlap); scheduleWithFixedDelay runs N seconds after the previous task finishes.'},
{id:1056,topic:'java',difficulty:'advanced',question:'What is the "TransferQueue" interface in Java?',options:['A queue that transfers ownership of elements','Extends BlockingQueue with transfer(e): the producer blocks until a consumer actually takes the element (direct handoff), enabling synchronous hand-off between threads. LinkedTransferQueue is the primary implementation','A queue for file transfer operations','A queue that transfers between threads automatically'],answer:1,explanation:'TransferQueue.transfer(e): blocks until a consumer takes the element directly. tryTransfer(e): returns false if no consumer is waiting. Enables rendezvous communication between producer and consumer threads.'},
{id:1057,topic:'java',difficulty:'beginner',question:'What is the "super" keyword for accessing parent fields?',options:['Used to invoke parent class methods only','Accesses a field or method in the immediate parent class that has been hidden or overridden in the subclass: super.fieldName or super.methodName() — cannot access grandparent class members directly with super','Creates a new parent class instance','Calls all parent class constructors'],answer:1,explanation:'super.field accesses the parent\'s field when it is hidden by a same-named child field (field hiding). super.method() calls the parent\'s overridden method from within the overriding child method.'},
{id:1058,topic:'java',difficulty:'beginner',question:'What is "Array.length" vs "ArrayList.size()"?',options:['They are identical','array.length: a field (not method) returning the fixed size of the array. ArrayList.size(): a method returning the current number of elements in the list (may be less than the backing array\'s capacity)','ArrayList.size() is a field','array.length returns a double'],answer:1,explanation:'Arrays use .length (field, fixed at creation). Collections use .size() (method, dynamic). Important: String.length() (method), arrays have no size() method. Know which to use for each type.'},
{id:1059,topic:'java',difficulty:'beginner',question:'What happens when you access an array with an out-of-bounds index?',options:['Returns null','Returns the default value for the element type','Throws ArrayIndexOutOfBoundsException at runtime','Throws IndexOutOfBoundsException at compile time'],answer:2,explanation:'Accessing int[] arr = new int[5]; arr[5] or arr[-1] throws ArrayIndexOutOfBoundsException at runtime. Java always performs bounds checking. Valid indices: 0 to arr.length-1.'},
{id:1060,topic:'java',difficulty:'beginner',question:'What is "Integer.MAX_VALUE" in Java?',options:['The maximum size of an Integer array','The largest value an int can hold: 2,147,483,647 (2^31 - 1). Adding 1 to this value causes integer overflow, wrapping to Integer.MIN_VALUE (-2,147,483,648)','The maximum Integer cache size','The maximum number of Integer objects allowed'],answer:1,explanation:'Integer.MAX_VALUE = 2^31 - 1 = 2,147,483,647. Integer overflow wraps around: Integer.MAX_VALUE + 1 = Integer.MIN_VALUE. Use long or BigInteger for larger values.'},
{id:1061,topic:'java',difficulty:'intermediate',question:'What is "interface segregation" principle in the context of Java interfaces?',options:['Separating production code from test code','Clients should not be forced to implement interfaces they don\'t use — prefer multiple small, specific interfaces over one large interface. Example: instead of one Animal interface with fly(), swim(), run(), create separate Flyable, Swimmable, Runnable interfaces','Keeping interfaces in separate packages','Segregating public and private interface methods'],answer:1,explanation:'ISP (SOLID): a class should not be forced to implement methods it doesn\'t need. A Penguin class forced to implement fly() from a fat Animal interface is a violation. Smaller interfaces (Flyable, Swimmable) are more composable.'},
{id:1062,topic:'java',difficulty:'intermediate',question:'What is the "Liskov Substitution Principle" in Java?',options:['Substituting null for objects safely','Objects of a supertype should be replaceable by objects of any subtype without altering the correctness of the program. A subclass must honour the superclass\'s contracts (preconditions no stricter, postconditions no weaker, invariants preserved)','Substituting one class for another in tests','Replacing implementations with mocks'],answer:1,explanation:'LSP (SOLID): if Rectangle extends Shape, and code accepts Shape, substituting Rectangle must not break the program. Violation example: Square extending Rectangle where setWidth() breaks the Rectangle contract (width ≠ height).'},
{id:1063,topic:'java',difficulty:'intermediate',question:'What is the "Dependency Inversion Principle" in Java?',options:['High-level modules should depend on low-level modules','High-level modules should not depend on low-level modules; both should depend on abstractions (interfaces). Details (implementations) should depend on abstractions, not the other way around — the foundation of dependency injection','Inverting dependencies with super','Reversing the dependency graph'],answer:1,explanation:'DIP (SOLID): OrderService should depend on PaymentGateway interface, not on CreditCardPaymentImpl directly. This enables swapping implementations (test mocks, different providers) without changing high-level code.'},
{id:1064,topic:'java',difficulty:'intermediate',question:'What is the "Open/Closed Principle" in Java?',options:['All classes should be public (open) but have private fields (closed)','Software entities should be open for extension but closed for modification — add new behaviour by adding new classes (inheritance, composition) or configuration rather than changing existing tested code','Open for reading; closed for writing','Only closing resources that are open'],answer:1,explanation:'OCP (SOLID): adding a new PaymentMethod should not require modifying the PaymentProcessor class. Use Strategy/Command patterns, polymorphism, or dependency injection to extend behaviour without modifying existing, tested code.'},
{id:1065,topic:'java',difficulty:'intermediate',question:'What is "Single Responsibility Principle" in Java?',options:['Each class should perform exactly one SQL query','A class should have only one reason to change — one primary responsibility. Combining user authentication and email sending in one class violates SRP; they should change for different reasons (auth logic vs notification logic)','Each file should contain one method','Classes should have a single constructor'],answer:1,explanation:'SRP (SOLID): a UserService handling authentication, profile management, and email notifications violates SRP. Separate into AuthService, UserProfileService, and NotificationService — each with one responsibility and one reason to change.'},
{id:1066,topic:'java',difficulty:'advanced',question:'What is "inversion of control" (IoC) and how does Spring implement it?',options:['Reversing the order of method calls','IoC transfers control of object creation and lifecycle to a container/framework rather than the application code. Spring implements IoC through its application context: it creates beans, injects dependencies, manages lifecycle, and provides AOP — the application does not call new for managed objects','A design pattern for reversing inheritance','Inverting the control flow of exception handling'],answer:1,explanation:'IoC: the framework (Spring container) calls your code (callbacks, lifecycle methods), not the other way around (DI is a form of IoC). Spring reads @Component, @Bean, XML config; creates objects; injects dependencies; your code uses the wired-up objects.'},
{id:1067,topic:'java',difficulty:'advanced',question:'What is the "Circuit Breaker" pattern in Java and which library provides it?',options:['A hardware circuit breaker in server rooms','A resilience pattern that monitors for failures in a service call; after a threshold is exceeded, the circuit "opens" preventing further calls (fail-fast), allowing the failing service to recover. After a timeout the circuit "half-opens" to test recovery. Resilience4j, Hystrix (Netflix, deprecated)','A Java design pattern for exception handling','A pattern for breaking circular dependencies'],answer:1,explanation:'Circuit Breaker (Resilience4j): @CircuitBreaker(name="paymentService", fallbackMethod="fallback") — after N% failures in a window, opens the circuit, calls fallback immediately. Half-open state probes recovery. Critical for microservice resilience.'},
{id:1068,topic:'java',difficulty:'advanced',question:'What is "Retry pattern" vs "Circuit Breaker pattern" in Java?',options:['They are identical','Retry: attempt the operation again on transient failures (network blip), with exponential backoff and jitter. Circuit Breaker: after too many failures, stop retrying entirely and fail fast — preventing cascade failures in dependent services. Resilience4j provides both','Retry is for client code; Circuit Breaker is for server code','Circuit Breaker retries faster than plain Retry'],answer:1,explanation:'Retry: good for transient failures (temporary network issues). Circuit Breaker: good for persistent failures (service is down). Combined: retry within a closed circuit breaker, but when the breaker opens, stop retrying immediately (don\'t pile up queue).'},
{id:1069,topic:'java',difficulty:'advanced',question:'What is "Bulkhead pattern" in Java microservices?',options:['A pattern for water-tight database transactions','Isolating components so failures in one don\'t cascade: separate thread pools or semaphores per downstream service so a slow service only consumes its own pool without starving calls to other services. Resilience4j @Bulkhead implements this','A pattern for isolating test classes','A network partition tolerance pattern'],answer:1,explanation:'Bulkhead: Service A calls B and C; without bulkhead, B going slow can exhaust all threads, starving C. With bulkhead, B and C each have separate thread pools/semaphores — B\'s failure does not affect C\'s availability.'},
{id:1070,topic:'java',difficulty:'advanced',question:'What is "rate limiting" in Java microservices and how does Resilience4j implement it?',options:['Limiting the data transfer rate of services','Controlling the rate of calls to a resource to prevent overload: RateLimiter limits calls-per-period. Resilience4j @RateLimiter(name="service", limitForPeriod=10, limitRefreshPeriod="1s") allows at most 10 calls per second, queuing or rejecting excess calls','Setting API response time limits','A network bandwidth throttling tool'],answer:1,explanation:'Rate limiting protects downstream services from overload. Resilience4j RateLimiter: fixed window (N calls per period) or leaky bucket. Combined with Circuit Breaker and Retry for a full resilience pattern.'}
]);
